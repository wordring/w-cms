# w-cms 全体構成メモ（一時ファイル・読後に削除してください）

> 前回作業から時間が空いた場合の**復帰用サマリ**です。設計の正本ではありません。
> 正本は `docs/` 配下と `README.md`。このファイルは git 管理外にする意図はないので、
> 読み終わったら削除してください（`_一時_` 接頭辞は削除目印）。
> 作成: 2026-08-01 時点のコードを実地調査して記述。

---

## 1. 30秒で思い出す「w-cms とは」

Go 製の**ファイル統合型 CMS**。Notion 風ブロックエディタで HTML ドキュメントを編集し、
本文中の**カスタムタグ（`<m-tag>` `<m-file>` `<m-item>` `<m-material>`）から業務データを
自動抽出**して SQLite にインデックス同期する。製造業の受注・発注・原価管理が題材。

**最大の設計思想＝「ファイルが正本、DBは派生インデックス」**

```
   正本（ファイル）                          派生（SQLite・いつでも再構築可）
   ─────────────────────────                ──────────────────────────────
   data/master/00/000123/
     ├ 000123.html      ← 本文（内容）  ──[解析・同期]──▶  pages / page_tags
     └ 000123.meta.json ← 属性（所有権・権限・親・日時） ▶  pages.parent_id / page_perms
                                          └─[プラグイン]─▶  client_orders / our_orders /
                                                              estimates / materials ...
```

UNIX に倣い「**ファイルの内容＝HTML**」「**ファイルの属性＝サイドカー .meta.json**」を分離。
DB を全消ししても `data/master/` から完全復元できる（管理画面の「DB再構築」）。

---

## 2. いま動かす（サーバーは起動済み）

```bash
go run cmd/w-cms/main.go
```

- URL: **http://localhost:8080** （`/` は `/000000` へ302リダイレクト＝正規URL統一）
- ログイン: **`a` / `a`**（このworktreeのローカル検証用。本番厳禁）
- テスト: `go test ./...`
- プレビュー設定は `.claude/launch.json`（`WCMS_SECURE_COOKIES=0` 済み＝HTTPでもCookieが付く）

> `data/`（cms.db・auth.db・master/）は **gitignore で環境ごとに独立**。
> 別環境で「ログインできない」時はユーザー未作成が原因。`auth.CreateUser` を呼ぶ小さな
> `package main` をサブディレクトリに作って実行する（**ルートに `list_models.go` という
> 別の `package main` がある**ため、ルート直下では `go run` が衝突する。ここが地味な罠）。

---

## 3. ディレクトリ構成

```
w-cms/
├ cmd/w-cms/main.go      ← 起動・全ルーティング・ミドルウェア合成（117行。ここが地図）
├ internal/
│  ├ database/           ← DB接続とコアテーブル定義（sqlite.go / auth.go）
│  ├ auth/               ← 認証・セッション・ユーザー/グループ・ミドルウェア
│  └ cms/                ← 本体。ページCRUD・同期・権限・ロック・プラグイン
├ assets/                ← フロント（サーバーは静的配信するだけ・SPAではない素のJS）
│  ├ index.html          ← エディタ本体（2366行。HTML+CSS+JS が同居）
│  ├ admin.html          ← 管理コンソール
│  ├ web-components.js   ← <m-*> カスタム要素の定義（560行）
│  └ templates/*.html    ← カスタム要素の描画テンプレート（12ファイル）
├ docs/                  ← 設計文書（日本語ファイル名・【種別】接頭辞ルール）
├ data/                  ← [gitignore] cms.db / auth.db / master/（本文＋サイドカー）
└ list_models.go         ← Gemini のモデル一覧を出す単発ツール（package main・本体と無関係）
```

Go は全体で約 5,700 行。`internal/cms/handler.go`（609行）と `perms.go`（396行）が中心。

---

## 4. リクエストの流れ（`cmd/w-cms/main.go` を読めば全体が分かる）

```
             ┌─ CSPProtect ─┐  ← 今回追加。全レスポンスにCSPヘッダ
   Request ──┤              ├──┐
             └─ CSRFProtect ┘  │  ← 状態変更系のOrigin/Referer検証
                                ▼
                          root (ServeMux)
                                │
        ┌───────────────────────┼───────────────────────────┐
        ▼                       ▼                           ▼
   認証不要                 OptionalAuth               RequireAuth
   /assets/                 （匿名でも可・各ハンドラが個別認可）  /api/ 配下すべて
   /login                   /api/load    本文             /api/save
   /api/login               /data/       添付PDF等        /api/new-page
                            /api/me      認証状態         /api/lock, /api/unlock
                            /api/children 子ページ一覧     /api/page-perms
                            /api/page-meta 親ID等          /api/admin/*（admin限定）
                            /{id}        ページ本体        + プラグイン提供API
```

**ポイント**: `/{id}` は **サーバーが本文を埋め込んだ完成HTML** を返す（合成方式）。
`RootHandler` がその場で認可し、認証済みで read 不可なら403・匿名で非公開なら `/login` へ302・
不存在なら404を返す。埋め込む本文は必ずサニタイズを通す。
初期表示で `/api/load` は叩かない（同APIは編集ロック起点の載せ替え専用）。
属性（親・権限など）は従来どおり JS が `/api/page-meta` 等で個別に取得する。

---

## 5. 認証・認可（`internal/auth` ＋ `internal/cms/perms.go`）

- セッションCookie（`wcms_session`・HttpOnly・SameSite=Lax・Secure切替可）
- パスワードは **argon2id**（`golang.org/x/crypto`）
- **UNIX 風パーミッション**: `mode` は3桁文字列 `<owner><group><other>`
  各桁 `read=2` / `write=1` / `rw=3` / `なし=0`。新規ページ既定 **`"330"`**（owner rw・group rw・other なし）
- **匿名公開**: `public` フラグ。ただし**実効公開 = 自分と全祖先の public が AND**
  （親が非公開なら子も見えない＝パスゲート）。owner/admin のみ切替可・既定非公開。
- 権限・所有者・親・公開状態はすべて**サイドカー `.meta.json` が正本**。
  `page_perms` テーブルはそこから再生成される検索用インデックス。

---

## 6. 同時編集ロック（`internal/cms/lock.go`・SSE）

悲観ロック。last-write-wins による lost update は解消済み。

- ロックは**プロセス内メモリ**（mutex付き map）。再起動で消えるのは許容（単一プロセス前提）
- 在席判定は**ポーリングせず SSE 接続の有無**で行う
- 無競合なら**無期限保持**。別の編集者が来たらそこから **2分の猶予**で明け渡し
- 取得直後は **10秒**の接続猶予（SSE接続前に奪われないため）
- 本文保存だけでなく**権限変更・所有者変更・親付け替え・公開切替**も同じロックで直列化
  （サーバー側 `RequireEditLock` ／ フロント側 `lockedFetch` を通すだけで乗る）

---

## 7. プラグイン機構（`internal/cms/plugin.go`）

1ユースケース＝1プラグイン。`init()` で `Register()` に自己登録し、コアは登録済みを走査するだけ。

| 実装 | Name | 対応タグ |
|---|---|---|
| `plugin_client_order.go` | `client_order` | `<m-file tag="顧客の発注書">` ＋ 明細 |
| `plugin_our_order.go` | `our_order` | `<m-file tag="弊社の発注書">` ＋ 明細 |
| `plugin_estimates.go` | `estimates` | `<m-file tag="弊社の見積もり">` |
| `plugin_materials.go` | `materials` | `<m-material>`（部材。集計APIも提供） |

インターフェースは4つだけ: `Name()` / `Schema()` / `Tables()` / `Sync(tx, pageID, root)`。
`Sync` は**そのページ分だけ DELETE→INSERT で洗い替え**する。集計APIを生やしたいときは
`RouteProvider`（`Routes()`）を追加実装すると `main.go` が自動登録する。
新規追加手順は `docs/【ガイド】プラグイン開発.md`。

---

## 8. フロントエンド（`assets/`）

ビルド工程なし・フレームワークなしの**素のHTML/CSS/JS**。`index.html` に HTML・CSS・JS が同居。

**画面は3ペイン構成**（`index.html` の `<main class="container">` 配下）:

```
┌─ header ─────────────────────────────────────────────┐
│ w-cms Wiki Editor   👤ユーザー  ⚙管理  ログアウト  [編集モード切替] │
├──────┬──────┬──────────────────────┬──────────────────┤
│ rail │ rail │   doc-column         │   rail-right     │
│ left │ toc  │   #editor-content    │   ページ情報      │
│ページ │ 目次 │   （ブロック編集）     │   属性・権限・公開 │
│階層  │      │   + HTMLライブ表示    │                  │
└──────┴──────┴──────────────────────┴──────────────────┘
```

- 左2レール（ページ階層／目次）は**同時に開かない**（片方開くと他方が畳まれる）
- レスポンシブは2段階: **1100px以下**で右レールがドロワー化、**760px以下**で左2枚もドロワー化
- 目次はサーバー非依存（`#editor-content` の h1〜h6 を走査して生成）
- 開閉状態は localStorage `wcms.ui` に保存。**FOUC 防止のため `<head>` のインライン
  script が body 描画前にクラスを付ける**（← CSP strict 化で nonce が必要になる箇所）
- `<m-*>` カスタム要素は `web-components.js` が `templates/*.html` を fetch →
  `${変数}` 置換 → `innerHTML` で描画する方式

---

## 9. 今回（前回セッション）入れた変更＝CSP

**中間版 CSP を全レスポンスに付与**し、`main` にマージ済み（`c5853c9` `6b87774` `b450644`）。

```
default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline';
object-src 'self'; base-uri 'self'; frame-ancestors 'self'
```

- 実装: `internal/auth/middleware.go` の `CSPProtect`／`cspPolicy`、`main.go` のチェーン最外周
- `default-src` `object-src` `base-uri` `frame-ancestors` は**目標strictと同値**
  → 外部送信の遮断・クリックジャッキング防止の実利は**もう効いている**
- `script-src`/`style-src` のみ暫定で `'unsafe-inline'`（既存インラインを壊さないため）

**次の一手（strict 化）**は前提リファクタが必要:
`index.html` のインライン script 2つ・`<style>`・`on*=` 11個・`style=` 12個、
`web-components.js` の innerHTML＋動的style、`templates/*.html` 12ファイルの `on*=`/`style=`
を全て外部化・addEventListener化・CSSOM化する。FOUC の head script は per-request nonce で残す方針。
**チェックリストは `docs/【考察】CSP強化.md` §4 に用意済み**。

## 9.5 その後に入れた変更＝サーバー合成＋サニタイズ（2026-08-02）

`GET /{id}` を**サーバー合成方式**へ変更（§4 参照）。これに伴い、本文が初期HTMLの一部として
パースされるようになったため、**許可リスト方式のサニタイザ**を新設し
**保存時と描画時の二層**で通すようにした。正本は `docs/本文サニタイズ設計.md`。

- 保存時は結果をエディタへ返し、除去が起きたブロックだけ差し替えて通知する（エコーバック方式）。
  編集は必ずロック取得時の生HTMLから始まるので、サニタイズ由来の欠損が保存されることはない。
- 無検査で正本へ書き込めた未使用の `POST /upload` は削除（`/api/upload-pdf` は別物で現存）。
- `page_tags` の主キーを廃止し、**同名の `<m-tag>` を複数置けるように**した
  （以前は同名タグ2つで保存が500になっていた）。

> CSP とサニタイズは役割が違う多層防御。中間版CSPが `'unsafe-inline'` を許している現状では、
> **サニタイズが実質の主防御**で、CSPは漏れや別経路に対する網。strict 化が済めば
> インラインスクリプトはCSPでも止まるようになる。

---

## 10. ドキュメント地図（どれを開けばいいか）

命名ルール: 日本語ファイル名＋`【種別】`接頭辞。`【考察】`＝検討中/未完了マーカー（`docs/開発方針.md` §2）。

| 目的 | 読む文書 |
|---|---|
| **まず全体像** | `docs/コンセプト.md` |
| **DB・API・アーキ詳細** | `docs/アーキテクチャとDBスキーマ.md` |
| **権限まわり** | `docs/認証認可設計.md` |
| **エディタUIの仕様** | `docs/エディタ仕様.md`（9.1 に3ペインの用語表） |
| **前回どこまでやったか** | `docs/作業引き継ぎ.md` ★セッション冒頭に必読 |
| **AIエージェントの行動規範** | `docs/開発方針.md` ★`CLAUDE.md` から自動読込 |
| **プラグイン追加手順** | `docs/【ガイド】プラグイン開発.md` |
| **CSP の続き** | `docs/【考察】CSP強化.md` |
| **本文の許可リスト・サニタイズ** | `docs/本文サニタイズ設計.md` |
| **タグの一覧** | `docs/【一覧】カスタムタグ.md`（※許可リストと同期が必要） |

---

## 11. 開発ルール（守るべき掟・`docs/開発方針.md`）

1. **標準ライブラリ優先・外部依存は原則禁止**
   承認済みの直接依存は5つだけ: `modernc.org/sqlite`（Pure Go SQLite）/ `x/crypto`（argon2id）/
   `x/net`（HTMLパース）/ `generative-ai-go`＋`google.golang.org/api`（PDF明細抽出）。
   新規追加時は go.mod と開発方針の一覧を**同時に**更新する。
2. **日本語でコミュニケーション**（応答・コミットメッセージ・PR・報告すべて）
3. **目的が違う変更はコミットを分ける**
4. **変更後は必ず `go test ./...`**
5. **新規コードでインライン `script`/`style`/`on*=` を書かない**（前回追加。CSP strict化の後戻り防止）

---

## 12. いまの残課題（`docs/作業引き継ぎ.md` より抜粋）

- ★ **通信記録の取り込み**（`.eml` 取り込み・`<m-message>`）— 設計のみ。`docs/【考察】通信記録処理.md` §11 に未決多数
- **CSP strict 化** — 上記 §9 の前提リファクタ待ち
- **画面構成の用語を画像つきで可視化**（ユーザー要望・未着手）
- サイドパネルのレイアウト磨き込み、CSS変数のさらなる移行、フッターのリンク追加
- 匿名公開の磨き込み（公開専用の軽量レンダリング・SEO/OGP対応など）。
  ※ 本文のサーバー合成と `<title>` の埋め込みは実装済み。OGP/`og:*` は未着手。

---

**再掲: このファイルは一時メモです。読み終わったら削除してください。**
