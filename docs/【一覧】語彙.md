# 【一覧】語彙

w-cms のページ本文（HTML）で使える**語彙**の実装済みリファレンス。何が書けるのか、どの形式がどのDBテーブルへ同期されるのかをまとめる。

> **分担**——**「いま何があるか」＝本ファイル**（形式の一覧・列・同期先。定義の実体は
> [internal/cms/vocab.go](../internal/cms/vocab.go) の `vocabRegistry`）。
> **「なぜその形か」＝[【考察】語彙モデル.md](【考察】語彙モデル.md)**（3層モデル・設計判断・決定ログ）。
> 安全性の規則は [本文サニタイズ設計.md](本文サニタイズ設計.md) §5、計算プラグインの作り方は
> [【ガイド】プラグイン開発.md](【ガイド】プラグイン開発.md)、ユースケースは
> [【ユースケース】受注から利益まで.md](【ユースケース】受注から利益まで.md)。

> **このファイルは現行実装の鏡（手書き）**。`vocabRegistry` を増減したら本ファイルも更新すること。
> **カスタム要素（`<m-*>`）は全廃済み**（2026-08-20）。新しい語彙はレジストリへ宣言1件を足す（§5）。
> 設計中の未実装形式（納品レコード等）は本ファイルには載せない
> （[【考察】通信記録処理.md](【考察】通信記録処理.md) を参照）。通信記録ページと参照は新しい形式では
> なく**可変タグ**で表す（§4。参照の値は `ページID-ブロックID`・D-4）。

> **`data-type` の位置づけは 2026-08-25 に変わった**——役割は階層と名前が主で、`data-type` は
> **機械が数えるための副**（[コンセプト.md](コンセプト.md) §1②）。さらに形式名を `data-type` 値から
> **セクションの見出しの言葉**へ移す**機能見出し**が 2026-08-31 に実装された（D-2・
> [【考察】語彙モデル.md](【考察】語彙モデル.md) §11）。`<section><h2>表示名</h2>…</section>` と書けば
> `data-type` 無しでその形式になり（解決は `vocabTypeOf`。属性があれば属性が勝つ）、エディタの
> スラッシュメニューもこの形で挿す。本ファイルの `data-type` 表記は属性形の名前で、機能見出し形の
> 実例は §2.3。

---

## 1. 本文の語彙は3つの層でできている

**①語彙レジストリ／②汎用索引／③計算プラグインの3層モデルは
[【考察】語彙モデル.md](【考察】語彙モデル.md) §4 が正本**。本ファイルが写すのは①の中身（§2）と、
それが②③のどこへ流れるか（§3）。運用上おさえるのは次の2点。

- **安全性の門はサニタイザだけで、レジストリは関与しない**。`data-type` の**値は検査されない**ので、
  レジストリに無い値も保存を通り、②汎用索引にも載る（保存応答の `unknown_types` で告知するだけ。
  [【一覧】API.md](【一覧】API.md)）。レジストリが担うのは編集支援（スラッシュメニュー・挿入骨格・
  型検証）と、鍵・型を解決するための語彙だけ。
- **エディタは語彙を手書きで持たない**。要素リストも形式リストも `GET /api/tag-schema` から受け取る
  （応答は `elements` / `void` / `block_id` / `vocab` / `vocabulary` / `column_types` / `extensions` の7キー。
  `extensions` は載っている拡張の名簿で、画面が拡張のボタンを出し分けるのに使う——2026-09-15 追加。
  最後の `column_types` は `th[data-type]` に書ける列型の一覧で、**2026-09-14 に足した**
  ——それまでエディタが手書きで持っており、`datetime`・`ref`・`email` を足した日から
  **3つ古いまま**だった）。
  `assets/index.html` に手書きで残るスラッシュメニュー項目は `h1`（見出し）・`p`（段落）・画像（レジストリの
  形式ではなく添付＋`<img>` の挿入操作）の3つだけで、残りは `populateSlashMenuVocab` がレジストリから生成する。

### 1.1 `data-*` マーカーの許可範囲

**許可範囲（どの属性をどの要素へ許すか・値を検査するか）の正本は
[本文サニタイズ設計.md](本文サニタイズ設計.md) §5.4**（実装は
[internal/cms/htmldoc/sanitize.go](../internal/cms/htmldoc/sanitize.go)）。本文の語彙マーカーは
**`data-type`（型名）・`data-src`（出所アドレス）・`data-ref`（ファイル表示の配線・
2026-09-15 に追加。`section` 限定）の3つだけ**で、`data-id`（保存の単位）と
`id`（リンクの宛先）は全要素共通の別枠（この2つを分ける理由は
[【考察】語彙モデル.md](【考察】語彙モデル.md) §9）。語彙側の固有事情だけをここに書く。

- **`th[data-type]` だけは意味が違う**——形式名ではなく**列型**を明示する。使えるのは
  **9つ**（`validColumnTypes`・[vocab.go](../internal/cms/vocab.go)）: `text` / `code` /
  `number` / `date` / `datetime` / `enum` / `image` / `ref` / `email`。
  あとから足した3つ——`datetime`（時刻を含む日時。`date` は日付だけ）・`ref`（他ページや
  その中のブロックへの参照）・`email`（メールの相手。値は `名前 <アドレス>` で、
  畳んだ値がアドレスだけ）——は、**タグの型としても名前から決まります**（§4）。
- **列型の決定順序は `th` の `data-type` 明示 > レジストリ宣言 > 見出し語の辞書 > `text`**
  （サーバーは `vocab_index.go` の `resolveColumnType`、エディタは同じ辞書を `/api/tag-schema` の
  `vocabulary` で受け取るので、索引と画面の判定が食い違わない）。
  **辞書の正本は `config/settings.json` の `vocabulary`**（1語につき `{"type": …, "values": [...]}` の
  1件で、型と選択肢を一緒に持つ——2026-09-14 に `type_inference` と `tag_enums` の2つを畳んだ。
  **Goコードに既定値は無く、ファイルが唯一の正本**（2026-09-07）。運用者がファイルへ足して
  DB再構築すれば効く——2026-08-30）。
- **鍵（フィールド名）は見出しの表示文字**（`th` / `dt` のテキスト）ただ1つ。「見える文字がすべて」。
  ③計算形式では、その表示文字がレジストリ宣言の**ラベル**に一致することで機械キー
  （`Field`。§2.2）へ解決される。機械キーを本文へ書き出す `data-field` は 2026-08-20 に撤去した。

> **見出しを改名すると、その列は③計算プラグインに読まれなくなる**（部材手配計算などの
> 型付きテーブルへ入らない）。黙って止まらないよう、解決できなくなった列は保存応答の
> `unresolved_fields` で告知する（拒否はしない。[【一覧】API.md](【一覧】API.md)）。

---

## 2. 語彙レジストリ（既定のビルドで21形式）

`data-type` は kebab-case でレジストリ全体で一意。内訳は**コアが5つ**
（`internal/cms/vocab.go` の `vocabRegistry` に `tags`・`file`・`inspection-record`・`child-list` の4つ＋
`file_view.go` の `init` が `file-view` の1つ）と、**拡張が `RegisterVocab` で足す16**——
`ext/subcon` 14（明細表2・部品の表5・改訂履歴2・ビュー5）・`ext/comm` 1（`unhandled-intake`）・
`ext/comm/contacts` 1（`unknown-contacts`）。`go build -tags minimal` では後者16が丸ごと外れ、
**コアの5つだけが残ります**。

> **2026-09-22 に、下の §2.1／§2.2 の表をレジストリから生成し直しました**（使い捨ての `go run` で
> 4つの拡張を blank import して `cms.VocabDefs()` を印字——21形式・列の宣言をそのまま写す）。
> それまでの表は 09-14 時点の手書きで、**ヘッダだけの5形式**（`client-order`・`our-order`・
> `our-estimate`・`supplier-estimate`・`drawing`）を載せたままでした——**2026-09-18 に
> レジストリから消えています**（「タグと表だけがDBに入る」＝1文書1ページで、ヘッダはページの
> 可変タグへ。ユーザー決定）。合計21が変わらなかった（同日にビューが5つ増えた）ので、
> 内訳だけが静かに嘘になっていました。**手書きの鏡は追いつきません**——次に形式を増減したら、
> 同じ手で生成し直すこと（§5）。

> ⚠ **かつてここは「コアが7つ（`vocabRegistry` に6つ）」でした。** 2026-09-15 の組み替えで
> `unhandled-intake` が `ext/comm` へ、`unknown-contacts` が `ext/comm/contacts` へ出たぶんです。

### 2.1 形式の一覧

| `data-type` | 表示名 | 分類 | 要素 | 同期先テーブル | 備考 |
|---|---|---|---|---|---|
| `tags` | 🏷️ 可変タグ | メタ | `dl` | **`page_tags`**（2026-09-13 に `vocab_index` から分離） | 鍵は `dt` の表示文字（自由語）。**タグ1つ＝名前：値の1対**（2026-08-26 決定。1つの `dt` に複数 `dd` は形式外・同名は対の繰り返しで）。見た目はチップ（左に名前・右に値の角丸）。骨格は1項目だけ生成し、項目操作UIで増やす。**2026-09-18 から業務文書のヘッダ（発注書番号・発注元・発注日・納期・小計…）もここ**（§4） |
| `file` | 📎 ファイル（PDF） | 業務 | `section` | —（配線のみ・索引にも載らない） | `data-src` にPDFの相対パス。**属性は本文に残るが索引はしない**（D-1・2026-08-31）。受発注を包む容器としては同日に廃止（既存の容器と単独のPDF添付ブロックは互換で動く）。いま原本を開くのは `file-view` |
| `inspection-record` | 📋 検査記録 | 記録 | `table` | `vocab_index` のみ | サンプル語彙（縦切り第1段の実証用）。**計算プラグインを持たない①だけの形式** |
| `child-list` | 📂 子ページ一覧 | ビュー | `section` | —（表示専用） | **View**。本文には空のマーカーだけが保存され、中身はサーバー事前描画 |
| `file-view` | 📄 ファイル表示 | 共通 | `section` | —（配線は属性だけ。中は空なので索引に載る行も無い） | **View ではなく鏡型**（`RegisterMirror`・[file_view.go](../internal/cms/file_view.go)）。**配線は属性1つ**——`data-ref="ページID-添付ID"`（定数 `FileRefAttr`・2026-09-15 に中の参照タグから移した）。その指すファイルを、その場に開く枠を**下へ足す**——人が書いた中身は消さない（計算ビューは全消しするので使えない）。開ける形式は `fileViewKinds`（pdf→`<embed>`／png・jpg・jpeg・webp・gif・svg→`<img>`／mp4→`<video>`／m4a・mp3→`<audio>`。**表は配信側 `setAttachmentHeaders` の帰結**——inline で配らない形式を足しても描けない。09-15 に足した txt・csv・log・md・webm・wav は同日のコードレビューで外した）。**表に無い拡張子でも開く口（リンク）を出す**（`kindOther`。DXF・Excel・ZIP など。「見つかりません」と言うのは嘘だった・2026-09-15）。**中に書いた参照タグは見ない**（`TestFileViewIgnoresInnerTags`） |
| `client-order-items` | 📩 受注明細 | 業務 | `table` | `vocab_index` | **Hidden**（単独では挿入しない——🤖 解析が受注ページに書く。`<caption>受注明細</caption>` で名乗る）。`弊社品番` は加工製品ページのページ番号（**結ぶのは人**）、`品番` は顧客の言葉で `code` 型（図番と揃える・2026-09-20） |
| `our-order-items` | 📤 発注明細 | 業務 | `table` | `vocab_index` | **Hidden**（未手配の一覧から発注書を作るときに書く・2026-09-21。置き場は `発注／年／月`） |
| `part-materials` | 🔩 材料 | 業務 | `table` | `vocab_index` | 加工製品ページの材料表。**RequiresTag**=`部品番号`（鍵はページの可変タグにある。§4）。⚠ **`品名` の列は無い**（材質・形状・寸法の焼き直しになる）。単価も書かない——最新単価は右に**鏡の列**で出る（[【考察】材料の単価と価格の履歴.md](【考察】材料の単価と価格の履歴.md)） |
| `part-outsourcing` | 🏭 外注加工 | 業務 | `table` | `vocab_index` | `推奨業者` の列に入力候補（`Suggest`=`subcon.partners`・`/api/suggest`） |
| `part-purchased` | 📦 購入部品 | 業務 | `table` | `vocab_index` | **品名が同一性そのもの**なので品名を持つ（材料と逆） |
| `part-supplied` | 🎁 支給部品 | 業務 | `table` | `vocab_index` | |
| `part-estimate` | 🧮 見積もり | 業務 | `table` | `vocab_index` | 工程ごとの数（原価の側）。客先向けの見積書を作る日は**別の名前**にすること（同じ言葉にすると原価が客先向けの集計に混ざる） |
| `drawing-revisions` | 🕐 改訂履歴 | 業務 | `section` | `vocab_index` | **Items**=`drawing-revision-items`。改定図面の合流（`mergeAsRevision`）が行を積む——⚠ `InsertRevisionRow` は `data-type` を文字列で探す（caption 化の前に直す） |
| `drawing-revision-items` | 🕐 改訂明細 | 業務 | `table` | `vocab_index` | **Hidden** |
| `required-materials` | 📊 手配状況リスト | ビュー | `section` | —（表示専用） | **View**（`ext/subcon`）。受注ページに**加工製品 × 購入品**の行（必要数・どの発注書で手配したか・2026-09-21） |
| `order-backlog` | 📋 受注残 | ビュー | `section` | —（表示専用） | **View**。受注の置き場に全社の受注残——`状態＝完了` の行は出さない（`納品済` とは別・残数があれば出る） |
| `unordered-items` | 🧾 未手配の一覧 | ビュー | `section` | —（表示専用） | **View**。全社の受注明細を横断し、ここから発注書を1枚作る（`/api/our-order/new`） |
| `material-search` | 🔎 材料を探す | ビュー | `section` | —（表示専用） | **View**。材質・形状・寸法の欄（最初は結果を出さない）→ `/api/material-search` |
| `unlinked-customers` | 🔗 連絡帳と未接続の相手 | ビュー | `section` | —（表示専用） | **View**。メールは来ているのに `連絡帳` の参照が無い取引先（FAXだけの相手は出ない） |
| `unhandled-intake` | 📥 未処理の受信 | ビュー | `section` | —（表示専用） | **View**（`ext/comm`）。通信箱の作業面 |
| `unknown-contacts` | 📇 未登録の連絡先 | ビュー | `section` | —（表示専用） | **View**（`ext/comm/contacts`）。連絡帳の作業面 |

> **2026-09-18 に消えた5形式**——`client-order`（顧客の発注書）・`our-order`（自社の発注書）・
> `our-estimate`・`supplier-estimate`・`drawing`。いずれも**ヘッダ（素の `dl`）だけ**の形式で、
> ヘッダはページの可変タグへ移りました（発注書番号・発注元・発注日・図面番号・図面名称…は §4）。
> 明細表は `<caption>` で自分の形式を名乗るので、包む `section` が要らなくなっています。

> **②汎用索引（`vocab_index`）が載せるのは `table[data-type]`・`table` に `<caption>` で名乗った形式と、
> 形式を持つ `section` の素の `table`**（`vocab_index.go` の `OnElement`／`syncVocabSection`）。
> `dl[data-type="tags"]` は `page_tags` へ。⚠ **`section` の素の `dl` は載りません**（2026-09-18
> ユーザー決定「素の定義リストはDBから外しましょう」——見た目がタグと同じで振る舞いが違った）。
> 素の `<table>` は `Items` の形式名（`client-order-items` 等）で載る。ファイル容器・
> 計算ビューは索引に無い。保存時の未知形式の告知（`UnknownVocabTypes`）は `table`／`dl`／`section` の
> `data-type` を見る（見出し語は未登録でも告知しない——普通の見出しへの誤検知を避けるため）。

### 2.2 列（`table` では見出し行の列、`dl` では項目。`section` は列を持たず `Items` の表が持つ）

**機械キー**はレジストリ内部の識別子で、本文には現れない（③計算プラグインが読む鍵）。
欄が「—」の形式は機械キーを持たず、見出しの表示文字がそのまま鍵になる。

| 形式 | 機械キー（`Field`） | ラベル（見出しの表示文字＝**本文の鍵**） | 型 | enum の選択肢 |
|---|---|---|---|---|
| `tags` | — | 新規タグ | text | |
| `file` | （列なし） | | | |
| `inspection-record` | — | 品番 | text | |
| 〃 | — | 判定 | enum | 合格 / 不合格 |
| 〃 | — | 検査写真 | image | |
| 〃 | — | 検査日 | date | |
| `client-order-items` | `our-item-id` | 弊社品番 | ref | |
| 〃 | `item-id` | 品番 | code | |
| 〃 | `item-name` | 品名 | text | |
| 〃 | `quantity` | 数量 | number | |
| 〃 | `unit` | 単位 | enum | 個 / セット |
| 〃 | `price` | 単価 | number | |
| 〃 | `due` | 納期 | date | |
| 〃 | `shipped` | 出荷済み | number | |
| 〃 | `note` | 備考 | text | |
| 〃 | `status` | 状態 | enum | 未着手 / 加工中 / 検査中 / 納品済 / 完了 |
| 〃 | `material-ordered` | 材料発注 | enum | 済 |
| 〃 | `delivery-note` | 納品書発行 | enum | 済 |
| 〃 | `invoice` | 請求書発行 | enum | 済 |
| `our-order-items` | `our-item-id` | 弊社品番 | ref | |
| 〃 | `item-id` | 品番 | code | |
| 〃 | `item-name` | 品名 | text | |
| 〃 | `material` | 材質 | text | |
| 〃 | `shape` | 形状 | text | |
| 〃 | `size` | 寸法 | text | |
| 〃 | `color` | 色 | text | |
| 〃 | `quantity` | 数量 | number | |
| 〃 | `unit` | 単位 | enum | 個 / セット / 本 / 枚 / kg / m |
| 〃 | `cost` | 単価 | number | |
| 〃 | `note` | 備考 | text | |
| 〃 | `status` | 状態 | enum | 未納品 / 納品済 |
| `part-materials` | `material` | 材質 | text | |
| 〃 | `shape` | 形状 | text | |
| 〃 | `size` | 寸法 | text | |
| 〃 | `quantity` | 個数 | number | |
| 〃 | `note` | 備考 | text | |
| 〃 | `status` | 区分 | enum | 現行 / 廃版 |
| `part-outsourcing` | `work` | 加工内容 | text | |
| 〃 | `supplied` | 支給 | text | |
| 〃 | `quantity` | 個数 | number | |
| 〃 | `vendor` | 推奨業者 | text | 候補=`subcon.partners` |
| 〃 | `doc` | 資料 | ref | |
| 〃 | `note` | 備考 | text | |
| 〃 | `status` | 区分 | enum | 現行 / 廃版 |
| `part-purchased` | `item-name` | 品名 | text | |
| 〃 | `spec` | 仕様 | text | |
| 〃 | `quantity` | 個数 | number | |
| 〃 | `note` | 備考 | text | |
| 〃 | `status` | 区分 | enum | 現行 / 廃版 |
| `part-supplied` | `item-name` | 品名 | text | |
| 〃 | `spec` | 仕様 | text | |
| 〃 | `quantity` | 個数 | number | |
| 〃 | `note` | 備考 | text | |
| 〃 | `status` | 区分 | enum | 現行 / 廃版 |
| `part-estimate` | `step` | 工程 | text | |
| 〃 | `amount` | 数 | number | |
| 〃 | `unit` | 単位 | text | |
| 〃 | `note` | 備考 | text | |
| `drawing-revisions` | （列なし） | | | |
| `drawing-revision-items` | `revision` | 版 | text | |
| 〃 | `drawing-no` | 図面番号 | text | |
| 〃 | `received-at` | 受領日 | date | |
| `child-list`・`file-view`・`required-materials`・`order-backlog`・`unordered-items`・`material-search`・`unlinked-customers`・`unhandled-intake`・`unknown-contacts` | （列なし） | | | |

> ⚠ **`備考` の列だけが画面で折り返します**（`app.js` の `WRAP_COLUMNS`・2026-09-21）。
> ほかの列は1行に保ち、表は自分の中で横スクロールします。
> ⚠ **`状態` の `完了` だけは「もう受注残に出さない」の宣言**です（`納品済` は出したという事実で、
> 残数があればまだ出る）。`材料発注`・`納品書発行`・`請求書発行` は人が付ける `済` の印
> （⚠ 「材料発注が終わった」の事実は手配状況リストの残要手配数 0 が答えで、印と事実を二重に持たない
> ——[【考察】製造という軸.md](【考察】製造という軸.md)）。

補足：

- **数値は正規化して読む**。`¥8,000` や全角の `８０００` は `8000` として解釈される
  （`NormalizeValue`）。正規化値は**併記**であり、生テキストが常に正本。
- **`quantity` の空セルは 1** として扱う（手で書く表で「1個」をいちいち書かせないため）。
- **型の不一致は拒否せず赤印で通知するだけ**。「自由な記述をシステムが認識する」というコンセプトを
  壊さないため（[【考察】語彙モデル.md](【考察】語彙モデル.md) §5.1）。
- `date` 型は `YYYY-MM-DD` へ正規化する（`YYYY/M/D`・`YYYY年M月D日` も受ける）。
- **`text` と `code` は「畳む強さ」だけが違う**（2026-09-04・案2）。見た目も入力補助も同じで、
  索引の併記値（`norm_value`）の作り方が変わる:
  - `text` …… 軽く畳む（全角英数→半角・半角カナ→全角・濁点の合成・前後の空白）。
    **長音 `ー` には触らない**——`ひかりレーザー` を壊さないため。
  - `code` …… 強く畳む（軽い正規化＋空白の除去・ハイフン類/長音/`_`→`-`・英字は大文字）。
    図面番号・発注書番号のような**符牒**に付ける。**区切りの有無は畳まない**
    （`R520-M070` と `R520M070` は別扱い——別の部品を1つにしないため）。

  引くときは `PagesByTag`（生の完全一致）と `PagesByTagLoose`（畳んだ一致）を**呼ぶ側が選ぶ**。
  正本は [【考察】テキストの正規化.md](【考察】テキストの正規化.md)。
- **`enum` は `text` と同じに畳む**（2026-09-14）。それまでの不変条件は「enum は正規化しない」
  だったが、`在籍` のように「選択肢だけあって型が無い」ため text 扱いだった語が enum になると、
  **畳んだ値が黙って空になる**（索引の後退）ので改めた。中の空白は畳まない（それは `code` だけ）。
  正規化しない列型は `image` だけ。
- **`image` 列の入力補助**は、セルにキャレットを置くと出る「🖼 画像を選ぶ」ボタンと、セルへの画像ドロップ
  （どちらも `insertImageIntoCell`。`/api/upload-image` へ送って `<img>` を入れる。[【要求】添付.md](【要求】添付.md)）。

### 2.3 形式の外形（HTMLの実例）

**いまの形（2026-09-18 以降・1文書＝1ページ）**。ヘッダは**ページの可変タグ**、原本は `<details>` で
畳んだ**ファイル表示**と「読んだまま」の写し、明細は **`<caption>` で形式を名乗る表**。
🤖 解析が受注ページに書く形がこれです（[ext/subcon/analyze_pdf.go](../ext/subcon/analyze_pdf.go) の
`buildOrderPageHTML`）。並びは**原本（PDF）→ 読んだまま → 弊社の明細**＝確かさの順。

```html
<h1>250715-304 みなと商店</h1>
<dl data-type="tags">
  <dt>発注書番号</dt><dd>250715-304</dd>
  <dt>発注元</dt><dd>みなと商店</dd>
  <dt>発注日</dt><dd>2026-07-15</dd>
  <dt>納期</dt><dd>2026-08-20</dd>
  <dt>小計</dt><dd>80000</dd>
  <dt>消費税</dt><dd>8000</dd>
  <dt>合計金額</dt><dd>88000</dd>
  <dt>受信元</dt><dd>000042-c3p7</dd>
</dl>
<details><summary>顧客の発注書（PDF）</summary>
  <section data-type="file-view" data-ref="000042-c3p7"></section></details>
<details><summary>顧客の発注書（読んだまま）</summary><table>…</table></details>
<table data-type="client-order-items">
  <caption>受注明細</caption>
  <tbody>
    <tr><th>弊社品番</th><th>品番</th><th>品名</th><th>数量</th><th>単位</th><th>単価</th><th>納期</th><th>出荷済み</th><th>備考</th><th>状態</th></tr>
    <tr><td>000077</td><td>A100-B01-0051</td><td>留めブラケット</td><td>10</td><td>個</td><td>8000</td><td>2026-08-20</td><td></td><td></td><td>未着手</td></tr>
  </tbody>
</table>
```

- 形式の解決は ①`data-type` ②節の機能見出し ③表の `<caption>` の順で**1回だけ**（⚠ 節の見出しと
  caption が重なると索引が二重に入っていた・2026-09-21 修正）。
- 検算（`数量 × 単価 = 金額`／`Σ金額 = 小計`／`小計 + 消費税 = 合計`）の結果は明細の `<tfoot>` に
  **鏡**で出ます——本文には書きません。
- **登録された語彙だけが索引に入ります**（2026-09-20）。名乗らない表は `vocab_index` に載りません。

**旧形の業務文書ブロック**（`section` がヘッダ `dl` と明細 `table` を包む。論点A・案1）。
⚠ **素の `dl`（ヘッダ）は 2026-09-18 から索引しません**（`eachPlainVocabTable` は節の**素の表だけ**を
観察係へ渡す・[vocab_index.go](../internal/cms/vocab_index.go)）。この形で残っている本文は、
発注元・発注日が索引に入らないので**タグへ書き直すこと**。素の表は拾い続けます
（`■材料` のような見出しの下に表が並ぶワンノート移行の受け皿。ただし**登録されていない見出しの
節は、中の素の表も索引しない**・2026-09-20）。下はさらに古い形（file 容器で包む。2026-08-31 までの挿入形）。

```html
<section data-type="file" data-src="PO-A100.pdf">
  <p>📎 <a href="/data/master/00/000012/PO-A100.pdf">顧客発注書.pdf</a></p>
  <section data-type="client-order">
    <dl>
      <dt>発注書番号</dt><dd>PO-A100</dd>
      <dt>発注元</dt><dd>南北</dd>
      <dt>発注日</dt><dd>2026-06-18</dd>
    </dl>
    <table data-type="client-order-items">
      <tbody>
        <tr><th>品番</th><th>品名</th><th>単価</th><th>数量</th><th>状態</th></tr>
        <tr><td>SHAFT-01</td><td>シャフトA</td><td>8000</td><td>10</td><td>未着手</td></tr>
      </tbody>
    </table>
  </section>
</section>
```

- **ヘッダの `<dl>` に `data-type` は付けない**。`section` が形式を決め、`dt` の表示文字が鍵を運ぶ。
- **明細表は最初の `tr` が見出し行**（列の鍵と型を運ぶ＝文書自身がスキーマを携帯する）。
- ファイル名リンクは**可視の中身**で、正本に保存される（閲覧ゼロJSでもPDFを開ける）。
  PDFプレビュー・ドロップゾーン・AI解析ボタンはエンハンサ（`enhanceFileSections`）が `File` 宣言の
  形式ブロック自身（互換で `file` 容器にも）へ挿す `.vocab-chrome` で、**保存されない**。

**機能見出し形**（D-2・2026-08-31 以降の挿入形。エディタの `buildVocabSkeleton` がこの形で挿す。
⚠ 例の `client-order` は 2026-09-18 にレジストリから消えたので、いまこの見出しは**ただの節**になる）。
`data-type` を書かず、`section` の最初の見出しの言葉（レジストリの表示名）が形式を宣言する。
PDFは容器で包まず、可視のファイル名リンクを `section` の先頭に置く（href は `/<ページID>/<生成ID>.pdf`）。

```html
<section>
  <h2>顧客の発注書</h2>
  <p data-id="m2k3x1">📎 <a href="/000012/m2k3x1.pdf" download="顧客発注書.pdf">顧客発注書.pdf</a></p>
  <dl>
    <dt>発注書番号</dt><dd>PO-A100</dd>
    <dt>発注元</dt><dd>南北</dd>
    <dt>発注日</dt><dd>2026-06-18</dd>
  </dl>
  <table>
    <tbody>
      <tr><th>品番</th><th>品名</th><th>単価</th><th>数量</th><th>状態</th></tr>
      <tr><td>SHAFT-01</td><td>シャフトA</td><td>8000</td><td>10</td><td>未着手</td></tr>
    </tbody>
  </table>
</section>
```

- 素の `table` は明細（`Items` 宣言の形式 `client-order-items` の列）として索引に載る（`syncVocabSection`）。
  ⚠ 素の `dl` は**載らない**（2026-09-18）。`inspection-record` のような単独の表も
  `<section><h2>検査記録</h2><table>…` で書ける。
- 見出し語がレジストリに無ければ静かにただのセクション（告知なし）。`data-type` があればそちらが勝つ。

**名前:値**（`dl`）と**繰り返し明細**（`table`）の単独形式。

```html
<dl data-type="tags"><dt>発注元</dt><dd>株式会社南北</dd><dt>部品番号</dt><dd>SHAFT-01</dd></dl>

<table data-type="inspection-record">
  <tbody>
    <tr><th>品番</th><th>判定</th><th>検査写真</th><th>検査日</th></tr>
    <tr><td>SHAFT-01</td><td>合格</td><td></td><td>2026-08-17</td></tr>
  </tbody>
</table>
```

**計算ビュー**（表示専用）。本文に保存されるのは空のマーカーだけで、中身は
`view_render.go` がページを返すたびに `<div class="vocab-chrome">` として埋める
（仕組みは [アーキテクチャとDBスキーマ.md](アーキテクチャとDBスキーマ.md) 4.4）。

```html
<section data-type="child-list"></section>
<section data-type="required-materials"></section>
```

**ファイル表示**（鏡型。保存されるのは**マーカー1つだけ**で、枠はサーバーが**その下へ足す**
——計算ビューと違って中身を消さない）。**配線は属性 `data-ref`**（2026-09-15。それまでは
中に参照タグを1つ書かせ「最初の `dl` の最初の参照」を読んでいた）。

```html
<section data-type="file-view" data-ref="010272-c3p7"></section>
```

- **中に参照タグを書いても開かない**（`TestFileViewIgnoresInnerTags`）。旧い形のままの
  本文は、`data-ref` へ写すまで「参照がありません」と出る。
- 属性へ移した理由は3つ（[file_view.go](../internal/cms/file_view.go) 冒頭が正本）:
  ①解析が書く本文では、図面ブロックの `受信元`（＝届いた記録）と表示先の指定が
  **同じ値で2行**並んでいた ②タグの名前に意味が無く（`受信元` でも `あ` でも動く）
  読む人が配線を見分けられない ③「最初の1つ」なので2つ書くと2つ目が黙って無視される。
- 属性は**`section` に限って**サニタイザが通す（`structuralElements`。URLではないので
  `safeEmbedURL` は通さない——URLを組むのはサーバー側で、書く人が宛先を選べない性質は
  値の検査ではなくそこで守られる。[セキュリティ設計.md](セキュリティ設計.md) §4）。
- **列は持たない**（中に書くものが無いため）。スラッシュメニューから挿さるのは
  見出し形の `<section><h2>ファイル表示</h2></section>` だけで、参照は編集モードの
  **配線の札**（`button.fv-wire`）→ プロパティ欄 `#w-fv-popover` で入れる
  （[エディタ仕様.md](エディタ仕様.md) §7.5）。
  札が出るのは `data-type="file-view"` を持つブロックだけなので、**スラッシュメニューも
  属性形で挿します**（`usesHeadingForm` の例外。`tags`・`file` と同じ扱いで、理由も同じ
  ——**配線を属性に持つ形式は見出し形にしない**）。

---

## 3. 形式 → テーブルの早見

**テーブルは1つだけです**。D-1（硬いドメイン表の全廃・2026-08-31）で、形式ごとの
専用テーブル8つと所有プラグイン4本を削除しました。

**索引は2つの表です**（2026-09-13 ユーザー決定。それまでは `vocab_index` 1本でした）。

| テーブル | 由来する形式 | 担当 |
|---|---|---|
| **`page_tags`** | **可変タグだけ**（`dl[data-type="tags"]`）。列は `page_id / seq / name / value / norm_value` の5つで、**主鍵は `(page_id, seq)`**（`seq` は**ページ通し**の番号）。**`norm_value` だけ宣言型を持ちません**——型を値に持たせ、`number` のタグが辞書順（`"8000" < "900"`）に並ぶのを防ぐため（2026-09-15。束ねる形は `tagNormBind` が1か所で決める） | [vocab_index.go](../internal/cms/vocab_index.go) |
| `vocab_index` | **登録された語彙の表**——`table[data-type]`・`<caption>` で名乗った `table` に加え、形式を持つ `section`（`data-type` または機能見出し）の**素の `<table>`**（`data-type` を持たない。`section` の側から拾う）。⚠ **素の `<dl>` は 2026-09-18 から載らない**（ヘッダはタグへ）。**登録されていない形式は載らない**（2026-09-20——それまでは空の形式名で入っていた） | 〃 |

**振り分けは書き手1か所**（`insertVocabEntry`）なので、読む側は表を選ぶだけです——
分けた理由は「読む側で `data_type='tags'` を足す」規律が守られなかったことで、正本は
[【考察】タグ表と業務ブロック表の分離.md](【考察】タグ表と業務ブロック表の分離.md) §8。

`vocab_index` での区別は `data_type` 列です——明細は表自身の `data-type`（または `<caption>` の
表示名から解決した形式名）、素の表なら包む形式の `Items`（どちらも `client-order-items`）で載ります。
読む側の入口は [vocab_query.go](../internal/cms/vocab_query.go) の4つ——`VocabTableRowsOf`・
`VocabBlocksOf`（どちらも `vocab_index`）と `PagesByTag`・`PagesByTagLoose`（どちらも
`page_tags`。生の完全一致か、畳んだ一致かを**呼ぶ側が選ぶ**）。③計算（部材手配計算）は
この索引だけを読みます（[【ガイド】プラグイン開発.md](【ガイド】プラグイン開発.md) §8）。

`file`・`file-view` と **View** の9形式（`child-list`・`required-materials`・`order-backlog`・
`unordered-items`・`material-search`・`unlinked-customers`・`unhandled-intake`・`unknown-contacts`）は
索引にも載りません（配線だけの容器・表示専用）。⚠ **移行中のページ**（`移行中` タグ・§4）は
索引には入りますが、**表引き（③計算）からは外れます**（在ることで止める・2026-09-21）。

**コアテーブルは `pages` と `page_perms` だけ**で、業務データは上の2つの索引に載ります
（[internal/database/sqlite.go](../internal/database/sqlite.go)）。

受発注のPDFパスは形式自身ではなく**祖先のファイル容器** `section[data-type="file"]` の
`data-src` にあります。**索引はしません**（配線＝属性であって表示される値ではなく、
読む者もいなかったため。D-1・2026-08-31）——本文には残るので情報は失われません。

## 4. 可変タグ（`dl[data-type="tags"]`）の既知の名前

`tags` の鍵（`dt` の表示文字）は自由語で、すべて **`page_tags`** に `(name, value, norm_value)`
として入る。⚠ **この表は 2026-08-30（D-1）にいったん `vocab_index` へ吸収して廃止し、
2026-09-13 に復活させたものです**——アドレス帳でタグが索引の84%を占め、「読む側で
`data_type='tags'` を足す」規律が守られなかったため（正本は
[【考察】タグ表と業務ブロック表の分離.md](【考察】タグ表と業務ブロック表の分離.md) §8）。
**同じ名前を同一ページに複数置いてよい**（担当者が2人、関連部品番号が複数といった多値属性。
**対の繰り返し**で表す——1つの `dt` に複数 `dd` は形式外だが、来れば鍵の繰り返しとして寛容に読む）。
単一値として読む用途は `cms.TagValue` が先頭を採る。
特別扱いされる名前のみ以下：

> **⚠ 機械が書く／引く名前は、2026-09-16 から生の文字列ではなく定数です。** 拡張どうしを
> 生の日本語で結んでいたところを定数へ通しました——タグ名を変えたときに、書き手と読み手が
> 黙ってすれ違わないようにするためです（2026-09-13 に `差出人アドレス` を廃止したとき、
> 送信の控えが古い名前で書き続けて宛先が空になった事故が実際にありました）。
> **下の表の「定数」列は、その名前を所有するパッケージを表します**——`comm` は
> `w-cms/ext/comm`、`contacts` は `w-cms/ext/comm/contacts`、`subcon` は `w-cms/ext/subcon`。
> 人が本文に書く名前は従来どおり自由語で、定数は**コードが名指しするとき**のものです。
>
> | 定数 | 名前 | 所有 |
> |---|---|---|
> | `comm.FromTag` / `ToTag` / `CcTag` / `ReplyToTag` | `差出人` / `宛先` / `CC` / `返信先` | 通信 |
> | `comm.ReceivedAtTag` / `SentAtTag` / `SentOutAtTag` | `受信日時` / `送信日時` / **`発信日時`** | 通信 |
> | `comm.ChannelTag`（値 `ChannelMail`・`ChannelFax`・`ChannelPhone`・`ChannelMemo`） | `チャネル`（`メール`・`FAX`・`電話`・`メモ`） | 通信 |
> | `comm.DirectionTag`（値 `DirectionIn`・`DirectionOut`） | `向き`（`受信`・`送信`） | 通信 |
> | `comm.HandledTag` | `対応` | 通信 |
> | `comm.ReplySourceTag` | `返信元` | 通信 |
> | `comm.MessageIDTag` / `InReplyToTag` | `メッセージID` / `返信元メッセージID` | 通信 |
> | `contacts.EmailTag` / `DomainTag` | `メールアドレス` / `ドメイン` | アドレス帳（⚠ `取引` は 2026-09-18 に全廃） |
> | `contacts.ContactsRefTag` | `連絡帳`（取引先ページ → 連絡帳の組織への参照。2026-09-17 に `相手` から改名） | アドレス帳 |
> | `subcon.SourceRefTag` | `受信元` | 下請け業務 |
> | `subcon.OrderNoTag` / `OrderClientTag` / `OrderedAtTag` / `DueDateTag` | `発注書番号` / `発注元` / `発注日` / `納期` | 下請け業務（受注ページのヘッダ・2026-09-18 にタグへ） |
> | `subcon.SubtotalTag` / `TaxTag` / `TotalTag` | `小計` / `消費税` / `合計金額` | 下請け業務（検算の材料・2026-09-20） |
> | `subcon.DrawingNoTag` / `DrawingNameTag` / `MachineNameTag` / `ClientNameTag` | `図面番号` / `図面名称` / `装置名称` / `客先` | 下請け業務（加工製品ページ・図面番号と図面名称は 2026-09-18 にタグへ） |
> | `subcon.ItemNoTag` / `OurItemNoTag` / `ItemNameTag` | `品番` / `弊社品番` / `品名` | 下請け業務（`品番` は品物の同一性で **H1 の下**に置く。`ProductCodeTags()` が逆引きに使う名前の組） |
> | `subcon.SupplierTag` / `MigratingTag` | `仕入先` / `移行中` | 下請け業務（`移行中` は在るだけで表引きを止める） |
>
> ⚠ **`送信日時` と `発信日時` は別の欄です**——`SentAtTag`（送信日時）は **w-cms が投函した
> メールの控え**が書き（`ext/comm/mail/reply.go`）、`SentOutAtTag`（発信日時）は**人が発信した
> 記録**（`＋ 記録する`。電話をかけた・FAXを流した）が書きます。**読む側は困りません**——
> `/api/thread` は `受信日時`・`送信日時`・`発信日時` の3つとも見て先に現れた1つを採ります。
> 取り違えが効くのは**書く側**で、消えるのは「人が発信した」と「機械が投函した」の区別です
> （型はどちらも `datetime` なので、いまは静かに等価になります）。
> ⚠ **`受信元` は `subcon` の持ち物で、`comm` にはありません**——「どの通信記録から生まれたか」は
> 下請け業務の解析が書く語だからです。通信の側から名指ししないこと。

| 名前 | 用途 |
|---|---|
| `部品番号` | 部材手配計算の鍵。名前は `part-materials` の `RequiresTag` 宣言が持ち、受注明細の品番から **`PagesByTag` で部材定義ページを逆引き**する（[ext/subcon/materials.go](../ext/subcon/materials.go)。D-1 以前は `part_id` として全部材行へ注入していた）。部材表があるのにこの鍵が引けない保存は `unresolved_fields` で告知 |
| `メッセージID` | `.eml` 取り込みの**重複検知の鍵**（`comm.MessageIDTag`・[ext/comm/intake_eml.go](../ext/comm/intake_eml.go)。2026-09-15 に `internal/cms` から移設）。同じ Message-ID を持つ通信記録ページが索引にあれば再ドロップは弾かれる（`ExistingIntakePage`） |
| `差出人`, `宛先`, `CC`, `返信先` | `.eml` 取り込みが書く**メールの相手**（`writeAddressTags`）。値は `名前 <アドレス>`（メールヘッダそのまま）で、**畳んだ値（`norm_value`）がアドレスだけ**——引くのはそちら（`PagesByTagLoose`）。列型は `email` で、宣言は推論辞書にあります（型は名前で決まる）。相手が複数ならタグを繰り返します。**表示のときはアドレスから連絡先ページへのリンク**になります（登録済みなら・本文は書き換えない）。⚠ もとは `差出人` と `差出人アドレス` の**2つのタグ**でしたが、1人を2行で表すためタグの27%が重複し、`CCアドレス` の持ち主を**1つ手前の `CC`** で当てる必要がありました（CCが3人いると名前がずれる不具合を 2026-09-13 に実データで踏んだ）。同日に1人1タグへ改めています |
| `返信元メッセージID` | `In-Reply-To` ヘッダ＝**スレッドの親**の Message-ID。`PagesByTag(メッセージID, この値)` で親の記録ページが引ける（専用の仕組みは無い）。紛らわしいが `Reply-To`（返信の宛先）とは別のヘッダで、そちらは `返信先` に入る |
| `受信元` ほか参照タグ | 値が `ページID-ブロックID`（ゼロ埋め6桁-英数字）なら**参照**として描画時にリンク化され、指す先のページが無ければ薄赤（`ref-missing`）。名前は固定しない（D-8）。`受信元` は 🤖 解析が生成する受注ページに `元ページID-添付ID` の形で入る（[ext/subcon/analyze_pdf.go](../ext/subcon/analyze_pdf.go)） |
| `必須項目`, `状態` | 製品マスタページの**設計方針**（[アーキテクチャとDBスキーマ.md](アーキテクチャとDBスキーマ.md) 7章。例: `状態=廃版` で新規発注の選択肢から除外しつつ過去参照は保持）。**コード側の特別扱いは未実装**で、いまは索引に入るだけ |
| `発注書番号`, `発注元`, `発注日`, `納期`, `小計`, `消費税`, `合計金額` | **受注ページのヘッダ**（2026-09-18 に業務文書ブロックの素の `dl` からここへ。1文書＝1ページ）。`発注書番号` は `code`、`発注日`・`納期` は `date`（⚠ `最短納期` のように畳めない値は `norm_value` が NULL——並べると先頭・絞ると消える）。`発注元` は `contacts.OrgNameForPage` を通した社名（法人格を落とす）。`小計`〜`合計金額` は検算の材料 |
| `図面番号`, `図面名称`, `装置名称`, `客先` | **加工製品ページのヘッダ**（図面番号・図面名称は 2026-09-18 にタグへ。`図面番号` は `code`——`PagesByTagLoose` で畳んだ一致が引ける） |
| `品番`, `弊社品番`, `品名` | `品番` は**品物の同一性**で図面の属性ではないので **H1 の下**（`移行中` と同じ `dl`）。版の印（`_rev0`）も落とさない。受注行 → 加工製品ページの逆引きは `弊社品番`（ページ番号）が先で、無ければ `品番` の畳んだ一致（`productOfOrderRow`） |
| `連絡帳` | 取引先ページから連絡帳の組織への**参照**（2つの木を結ぶ。完全一致で結ぶので社名は `OrgNameForPage` で揃える） |
| `移行中` | ワンノートから移植したページの印（値は `確認待ち` 等）。**在るだけで表引きを止める** |
| `仕入先` | 発注書ページ（`発注／年／月`）の相手。1枚＝1社 |
| `希望納期`, `支払条件`, `自社担当` など | 自由メタ（記述例。すべて索引に入るだけ） |
| `親ページID` | **旧形式の遺物**。ページ属性はサイドカー `<id>.meta.json` が正本（[エディタ仕様.md](エディタ仕様.md) 9章）で、この語を親として解釈するコードは無い——書けば**普通のタグとして索引に載るだけ**（不活性。旧 `page_tags` の取り込みガードは吸収時に撤去） |

---

## 5. 語彙の増やし方

**既定の増やし方は「レジストリへ宣言を1件足す」**。カスタム要素は作らない（§6）。

1. [internal/cms/vocab.go](../internal/cms/vocab.go) の `vocabRegistry` へ `VocabDef` を1件足す
   （`Type` は kebab-case・一意、`Element` は `table` / `dl` / `section` のいずれか）。
2. 再起動すると `GET /api/tag-schema` の `vocab` に現れ、**スラッシュメニューに自動で並ぶ**
   （`Hidden: true` なら並ばない）。挿入骨格・列型の検証・②汎用索引への同期もそのまま通る。
3. 本ファイル §2 の表を更新する——**手で写さず生成する**（2026-09-22 の手順: リポジトリ直下に
   使い捨ての `_vocabgen/main.go` を置き、`_ "w-cms/ext/comm"`・`ext/comm/contacts`・`ext/comm/mail`・
   `ext/subcon` を blank import して `cms.LoadSettings()` → `cms.VocabDefs()` を §2.1／§2.2 の行の形で
   印字し、貼り替えてからディレクトリを消す。設定の `settings.json` から足した形式も同じ口に出る）。
4. **Goのコードは1行も書かない**。集計API・型付きテーブル・取り込み変換のいずれかが要るときだけ
   ③計算プラグインを追加する（[【ガイド】プラグイン開発.md](【ガイド】プラグイン開発.md) 冒頭の振り分け）。

> **サニタイザは変更不要**。`data-type` は許可済みの属性名で、**値は検査されない**ので、
> 新しい形式名を足しても許可リストに触る必要はない（`table` / `dl` / `section` / `th` を担い手にする限り）。
> 新しい**要素**や**属性**が要る場合だけ `structuralElements` を直す
> （[本文サニタイズ設計.md](本文サニタイズ設計.md) §5・§7）。

> **手書きの写経は件数が増えれば破綻する**。正本がレジストリという1テーブルへ集約されたので当面は
> 本ファイルが鏡でよいが、`vocab` からの自動生成は引き続き課題
> （[要件定義書.md](要件定義書.md)（移動先の案内）「語彙一覧の生成化」。経緯は [変更履歴.md](変更履歴.md)）。

---

## 6. 本文はすべて標準HTML

**本文に独自の要素は1つも無い**（到達は 2026-08-20）。語彙は**標準HTMLの担い手**
（`table` / `dl` / `section` / `th`）に `data-type` と**見出しの表示文字**で意味を載せる形だけです。
これは `vocabulary_test.go` の `TestNoCustomElementVocabulary` が固定していて、
`AllowedVocabulary()` にハイフンを含む要素名が現れたら落ちます。

許可リストに無い要素は**アンラップされます**（要素は捨てられ、中身のテキストは残る）。
つまり、どこかから独自要素の混じった本文が入ってきても、文字は失われません。

表示されない配線だった参照（`受信元`・`前版`）も、**見える名前：値の参照タグ**
（`<dt>受信元</dt><dd>000002-12</dd>`。値は `ページID-ブロックID`・D-4）で決着しました——
見える形のまま保存し、描画時にリンクへ合成します（§4・
[アーキテクチャとDBスキーマ.md](アーキテクチャとDBスキーマ.md) §9.1〜9.3）。
集計を参照で辿る「参照追従JOIN」（[【考察】通信記録処理.md](【考察】通信記録処理.md) §2.5）は未着手。

> **なぜこの形にしたか**——決定と理由の正本は [【考察】語彙モデル.md](【考察】語彙モデル.md) §9 の決定ログ、
> 移行の経緯（何がどう置き換わったか）は [変更履歴アーカイブ.md](変更履歴アーカイブ.md) 2026-08-17〜2026-08-20。
