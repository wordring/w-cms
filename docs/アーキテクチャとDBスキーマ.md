# アーキテクチャとDBスキーマ仕様

w-cms は、フロントエンドのエディタが生成するHTMLドキュメント（**マーカー付き標準HTML** ＝ `<table|dl|section data-type>`。項目の鍵は見出しの表示文字が運ぶ）と、バックエンドのGoが管理するSQLiteデータベースを連携させることで動作します。このドキュメントでは、そのバックエンド側の詳細な構造とデータの持ち方について解説します。

> **カスタム要素（`<m-*>`）は全廃**（2026-08-20 に移行完了）。本文で扱える語彙は
> [cms/htmldoc/sanitize.go](../internal/cms/htmldoc/sanitize.go) の `structuralElements`＋
> `data-*` マーカー**だけ**で、形式（`data-type` の値）の宣言は①語彙レジストリ
> [cms/vocab.go](../internal/cms/vocab.go)（現在12形式）が持ちます。移行の経緯は
> [【考察】語彙モデル.md](【考察】語彙モデル.md) §8、記録は [変更履歴.md](変更履歴.md)
> 2026-08-19〜08-20 の各節。本書に残る `<m-*>` の表記は、後継形式を示すための
> **歴史的な言及**です。

## 1. 全体アーキテクチャ

システムの中核は、HTMLという「非構造化（あるいは半構造化）データ」を、データベース上の「構造化データ（インデックス）」に同期させる処理にあります。

### Goバックエンドの構成（`internal/`）
*   **`database/sqlite.go`**: 物理フォルダの確保と、Pure Go実装のSQLiteの初期化を行います。接続はDSNの `_pragma` で開き、全接続に `busy_timeout`・`foreign_keys` を、DBに `journal_mode=WAL` を適用します（同時書き込みのロック衝突を緩和）。**コアテーブル（`pages` / `page_perms`）のみ**を作成します（`CreateCoreTables`）。ユースケース固有のテーブルは各プラグインが定義します。
*   **`cms/editlock/lock.go`**: 同時編集の**悲観ロック**（ページ単位・競合トリガー方式）。プロセス内 mutex 付き map で保持する揮発的なランタイム状態で、**presence は SSE 接続で判定**し状態変化を push する（`StartLockReaper` の1秒ティッカーが猶予満了を評価）。HTTP/SSE エンドポイントは `cms/editlock/handler.go`（4.3 参照）。
*   **`cms/plugin.go`**: **プラグイン機構**の中核。`Plugin` インターフェース（`Name` / `Schema` / `Tables` / `Sync` の4メソッド）、レジストリ（`Register` / `Plugins`）、スキーマ一括適用（`ApplySchema`）と**既存DBとのずれ検出**（`DriftedSchemaTables`。8.3）、ルート集約（`PluginRoutes`）、およびDOM操作ヘルパー（`Attr` / `WalkElements` / `TagValue` など）を提供します。**語彙集約（必須メソッド `Tags` と `PluginTags`）は 2026-08-20 に撤去**——全プラグインが nil を返す状態になったため機構ごと外しました（下の「語彙の正本」参照）。
*   **`cms/plugin_*.go`**: 1ファイル＝1**計算**ユースケース。各プラグインが自分のテーブル定義（`Schema`）・所有テーブル（`Tables`）・同期処理（`Sync`）を持ち、`init()` で自己登録します。**本文の語彙は持ちません**——仕事は「マーカー付き標準HTMLを読んで自分のテーブルへ同期する」ことに絞られています（[【ガイド】プラグイン開発.md](【ガイド】プラグイン開発.md) 参照）。
*   **`cms/htmldoc/sanitize.go`**: 本文サニタイザ（純粋なHTML部品。import は `x/net/html`＋標準のみ）で、**本文で扱えるHTMLの語彙の唯一の正本**（`structuralElements`＋`data-*` マーカー）。`cms/sanitize.go` は薄いラッパで、`htmldoc.New()` の結果をパッケージ変数として持ちます。依存は `cms → htmldoc` の一方向（**逆向きに import しないこと**）。
*   **`cms/vocab.go` / `cms/vocab_index.go`**: 語彙モデル3層の①と②。`vocab.go` が形式定義の宣言テーブル（`vocabRegistry`。編集支援・型推論・正規化の語彙であって**安全性の門ではない**）、`vocab_index.go` が全 `table[data-type]` / `dl[data-type]` を縦持ちで索引する汎用同期（プラグイン機構へ相乗り）。
*   **`cms/view_render.go`**: 計算ビュー（表示専用）の**サーバー事前描画**。本文中の空マーカーへ中身を埋めます（4.4 参照）。
*   **`cms/parser.go`**: `x/net/html` を用いて、HTML本文（＝ページの内容）から**タイトルのみ**を抽出します（`ParseCore`）。親ページID・作成/更新情報などの**属性はHTMLではなくサイドカーが正本**のため、ここでは扱いません。ユースケース固有の抽出は各プラグインが担当します。
*   **`cms/sync.go`**: `SyncIndex` がコア（pages / page_perms）を同期した後、登録済みの全プラグインの `Sync` を1トランザクション内で呼び出します。各プラグインは「当該ページ分を `DELETE` → `INSERT`」で洗い替えします。`RebuildDatabase` は全テーブルをDROPしてからコア＋全プラグインのスキーマを作り直し、`data/master` 配下の全HTMLを再同期します（詳細は「8. データの正本性と全再構築」を参照）。

> **削除は `data/trash` への移動**（物理削除ではない。取り消せることが要件——[【考察】通信記録処理.md](【考察】通信記録処理.md) §2.7④）。`RebuildDatabase` が走査するのは `data/master` だけなので、フォルダを移すだけで索引からも消えます。逆に、ゴミ箱から `data/master` へ戻して再構築すれば復元できます（復元UIは未実装）。
*   **`cms/handler_*.go`**: コアのHTTPハンドラ。関心ごとに `handler_save.go`（保存）・`handler_view.go`（画面とページ本文API）・`handler_tree.go`（木構造：新規作成・子一覧・親の付け替え）・`handler_meta.go`（属性・語彙・DB再構築）・`handler_delete.go`（ゴミ箱への削除）・`handler_template.go`（テンプレート一覧）へ分けています。集計API（例: `/api/required-materials`）は各プラグインが `RouteProvider` として提供し、`main.go` が `cms.PluginRoutes()` 経由で登録します。
*   **`cms/page/`**: ページの実体を扱うパッケージ。保存ディレクトリの決定（`GetPageDir`）、属性サイドカーの読み書き、Unix風の認可（`RequirePageWrite` 等）、`/data/` 配下の添付配信ゲート（`DataFileHandler`）。**ハンドラ層（`cms`）と編集ロック（`cms/editlock`）の両方がここに依存し、逆向きの依存はありません。**

---

## 2. データベーススキーマ設計

ドキュメント本文はファイルシステムに保存されますが、検索や集計に必要なメタデータはすべてSQLiteにインデックス化されます。

> [!NOTE]
> **コアテーブルは `pages` / `page_perms` の2つだけ**（`database/sqlite.go` が作成）です。
> ユースケース固有のテーブルは、`page_tags` を含めてすべて対応する**プラグイン**
> （`cms/plugin_*.go`）が `Schema()` で定義し、起動時に `cms.ApplySchema()` で作成されます。
>
> 汎用索引 `vocab_index` も例外ではなく、テーブル作成は
> [cms/vocab_index.go](../internal/cms/vocab_index.go) の `Schema()` が担います
> （`CreateCoreTables` は従来どおり `pages` / `page_perms` のみ）。
>
> **「コアはドメイン語彙を知らない」原則はそのまま**——コアにとって `data-type` の値は
> 不透明な文字列で、未知の値もそのまま索引に載り、特定の形式の意味を知るのは計算プラグイン
> だけです。語彙の3層モデル（①レジストリ／②汎用索引／③計算プラグイン）は
> [【考察】語彙モデル.md](【考察】語彙モデル.md) §4、どの形式がどのプラグイン・テーブルへ
> 流れるかは [【一覧】語彙.md](【一覧】語彙.md) §3 を参照してください。

### 語彙の正本（プラグインは語彙を持たない）
本文で扱えるHTMLの語彙——許可要素と **`data-*` マーカーの許可範囲**——は
[cms/htmldoc/sanitize.go](../internal/cms/htmldoc/sanitize.go) の1箇所が正本で、文書側の正本は
[本文サニタイズ設計.md](本文サニタイズ設計.md) §5.2・§5.4 です（同じ表が `GET /api/tag-schema` の
`elements` としてエディタへも配られます）。

**プラグインは語彙を宣言しません**——カスタム要素を作らない理由は
[【考察】語彙モデル.md](【考察】語彙モデル.md) §9 の決定ログ、`Tags()` / `PluginTags()` による
注入機構を撤去した経緯は [変更履歴.md](変更履歴.md) 2026-08-20 の節にあります。**新しい形式は
`cms/vocab.go` の `vocabRegistry` へ宣言を1件足す**だけで済みます（サニタイザ側は要素・属性が
既に許可済みなので変更不要。未知の `data-type` も保存は通り `unknown_types` として告知される）。

### ドキュメント管理テーブル
*   **`pages`**: すべてのドキュメントの基本情報。
    *   `id` (INTEGER, PK): ページID。6桁のゼロ埋め文字列として扱うためのベースとなる数値。
    *   `title` (TEXT): HTMLから抽出したタイトル。
    *   `parent_id` (INTEGER): 親ページのID。
    *   `file_path` (TEXT): 物理ファイルの保存先パス。
    *   `created_at` (DATETIME): 作成日時。サイドカーから同期される。
    *   `created_by` (TEXT): 作成者。サイドカーから同期される。
    *   `updated_at` (DATETIME): 更新日時。サイドカーの値を採用し、無ければ同期時刻（Go 側で RFC3339 UTC・`T` 区切り。2026-08-30 に SQLite の `CURRENT_TIMESTAMP`＝空白区切りから揃えた）にフォールバックする。
    *   `parent_id` を含むこれらページ属性は **サイドカー `<id>.meta.json` が正本**で、`pages` はそこから再生成される派生インデックス（後述 4.1・[エディタ仕様.md](エディタ仕様.md) 9章）。UNIX流に「内容＝HTML / 属性＝サイドカー」を分離するため、DB再構築（8章）でも親ページ・作成日時・作成者・真の更新日時が失われない（`title` のみHTML本文由来）。
    *   `created_at` / `created_by` 列は `CREATE TABLE` に加え、既存DB向けに冪等な `ALTER TABLE ADD COLUMN` マイグレーション（`database/sqlite.go` の `coreMigrations`）でも追加される。
*   **`page_tags` は廃止**（2026-08-30・D-1 の第一歩）——可変タグの行き先は `vocab_index`
    **だけ**になった。専用テーブルは中身が索引と完全に重複し、**読むコードが1行も無かった**
    （単一値の読み口 `cms.TagValue` は昔からHTML木を直接走査する）。同名タグの多値・
    「親ページID」旧ガードの経緯は [変更履歴.md](変更履歴.md) を参照。既存DBに残る
    `page_tags` テーブルは次の全再構築（§8.2 の全テーブルDROP）で消える。
*   **`vocab_index`**: 語彙モデルの**汎用索引**（3層モデルの②。可変タグを含む全マーカーの行き先）。
    全 `<table data-type>` / `<dl data-type>` の値を**縦持ち**で同期する
    （[cms/vocab_index.go](../internal/cms/vocab_index.go)。2026-08-17・縦切り第1段）。
    *   `page_id` (FK), `data_type`（形式）, `block_no`（同形式ブロックの文書順連番）,
        `block_id`（ブロックの `data-id`・無ければ空）, `row_no`（データ行の連番）,
        `field`（鍵＝見出し／`dt` の表示文字）, `value`（生テキスト＝正本）,
        `norm_value`（正規化値。解釈できた値だけ併記・NULL可）,
        `norm_num`（number 型の数値としての正規化値・REAL・NULL可。TEXT の文字列比較
        `"8000" < "900"` を避けるための数値格納クラス。2026-08-30）
    *   主キーは持たない（1セル＝1行の完全正規化。形式の定義変更がDBスキーマ変更を
        要求しない）。検索用に `(page_id)`・`(data_type, field)`・`(field, value)`・
        `(field, norm_value)` の非一意インデックス（後2者は値の逆引きと日付範囲用。2026-08-30）。
    *   **未知の `data-type` もそのまま載る**（オプトインの規約は `data-type` の有無だけ。
        レジストリは安全性の門ではない——[【考察】語彙モデル.md](【考察】語彙モデル.md) §4・§9）。

### 業務データはすべて汎用索引へ（硬いドメイン表は全廃・2026-08-31）

かつてはここに **受注・受注明細・発注・発注明細・販売見積・購入見積・部材・タグの
8テーブル**が並んでいました。D-1（§9）で**すべて削除**し、行き先を `vocab_index`
1本に寄せています。**コアのスキーマにドメイン語彙を持つテーブルは1つも残っていません**
（`pages`・`page_perms`・`vocab_index` の3つだけ）。

| 本文の形式 | 索引での姿 |
|---|---|
| `<section data-type="client-order">` のヘッダ `<dl>` | `data_type='client-order'`・`field` は `発注書番号`・`発注元`・`発注日` |
| `<table data-type="client-order-items">` | `data_type='client-order-items'`・1データ行が1 `row_no` |
| `<section data-type="our-order">` ／ `<table data-type="our-order-items">` | 同上（`発注先` ほか） |
| `<dl data-type="our-estimate">` ／ `<dl data-type="supplier-estimate">` | `data_type` はその値・1ブロック1件 |
| `<table data-type="part-materials">` | `data_type='part-materials'`。**部品番号は行に無く**、同じページの可変タグ `部品番号` が鍵（`pagesByTag` で逆引き） |
| `<dl data-type="tags">` | `data_type='tags'`・`field` は自由語 |

**ヘッダの `<dl>` は `data-type` を持ちません**（役割は包む `section` が宣言し、鍵は `dt` の
表示文字）。配送係は引き金のある要素しか配らないため、素の `dl` は誰にも届きません——
`vocabIndexPlugin` が **`section` の側から拾います**（`syncVocabSectionHeader`）。
硬い表があったころは各プラグインが `section` を受け取って自分で子の `dl` を読んでいたので
この穴は見えていませんでした。拾わないと**発注元・発注日・発注先がどこにも残りません**
（明細表だけが載ってヘッダが黙って消える）。`TestSectionHeaderIsIndexed` が固定しています。

**索引しなくなったもの**: 容器 `section[data-type="file"]` の `data-src`（旧 `pdf_path`）。
配線＝属性であって表示される値ではなく、読む者も1人もいませんでした。本文には残るので
情報そのものは失われていません。

**`order_no` の UNIQUE 制約も無くなりました**。ページ内で同じ番号のブロックが2つあると
1行にマージされていましたが、いまは2ブロックとして索引されます。テンプレートの再採番
（`FreshenTemplateBody`）は引き続き必要で、**DBはもう止めてくれません**。

---

## 3. 部材手配計算APIの仕様

動的に必要な部材数を計算するロジックは `RequiredMaterials(user, pageID)`（`plugin_materials.go`）に
あり、**2つの出口**から使われます（部品単位ではなく、対象ページ単位で計算します）。

1.  **`GET /api/required-materials?page_id={page_id}`**（プラグイン提供API）。
2.  **計算ビューのサーバー事前描画**——本文の `<section data-type="required-materials">` へ
    集計表を埋める（4.4）。**現在のフロントはこちらだけを使い、APIは呼んでいません**
    （かつては `<m-required-materials page-id="...">` がAPIを叩いて描画していた）。

集計結果は map の走査順に依らないよう **部材名の昇順**に整列してから返します。

### 計算ロジック概要

読む先は **`vocab_index` だけ**です。鍵の変換（索引の `field` に入っている見出しの表示文字 →
③計算が使う機械キー）は `vocab_query.go` が引き受け、①語彙レジストリの `Label` ↔ `Field` を
唯一の正本として引き当てます。集計コードへ日本語の魔法文字列を直書きすると、見出しを
改名したときに告知する側と読む側がずれます（設計総点検⑤）。

1.  **対象ページの受注明細を取得**: `data_type='client-order-items'` の行を `page_id` で引き、
    品番（`item-id`）と数量を読みます。ページで絞るので、同じ発注書番号を別ページで
    使っても混ざりません。
2.  **必要部材の特定**: 品番をキーに**可変タグ `部品番号` の逆引き**（`pagesByTag`）で
    部材定義ページを見つけ、そのページの `data_type='part-materials'` を読みます。
    「受注数 × 1個あたり必要数」を部材名ごとに積算し、総必要数（`TotalRequired`）とします。
    鍵の名前はレジストリ宣言（`part-materials` の `RequiresTag`）が持ちます。
    **定義元ページを読めない相手には、その定義を混ぜません**（`page.CanView`）。
3.  **手配済数の算出**: 同じページの `data_type='our-order-items'` から発注済数量（`Ordered`）を
    積算します。仕入先はヘッダ側にあるので、**`block_no`（形式ごとの文書順連番）で対**に
    します——1つの発注書 `section` がヘッダ1つと明細表1つを持つ限り同じ番号どうしが対に
    なり、番号が重複しても取り違えません。
4.  **不足分の計算**: `Remaining = TotalRequired - Ordered`（負数の場合は `0`）を部材名ごとに
    算出し、JSON配列で返却します。

数量の**空セルは 1** として読みます（旧 `<m-material>` の既定を引き継いだ値。硬い表の
ころは索引を書く側が当てていた既定を、いまは読む側 `vocabQuantity` が当てます）。
品番が空の受注明細行は突き合わせ対象から外します——硬い表では `part_id=''` の行
（部品番号タグを持たないページの部材）と一致し、無関係な部材が集計に混ざりえました。

このように、HTMLに書かれた非構造なデータが、バックエンドで**1本の汎用索引**へ写され、
そこから Go のコードが BOM 計算と進捗管理を組み立てています。**形式が増えても
DBスキーマは変わりません**。

---
## 4. 自動保存とデータロードAPI仕様

> **全エンドポイントの一覧は [【一覧】API.md](【一覧】API.md)** を参照（メソッド・認可・ロック要否の早見表）。
> 本章はそのうち保存・読込まわりの**動作の詳細**を扱う。

エディタ（フロントエンド）とファイルシステム・DBを連携させるため、以下の2つのAPIを提供します。

### 4.1. Save API (`POST /api/save`)
フロントエンドからのオートセーブを受け付け、物理ファイルの上書き保存とDB同期を行います。

*   **リクエスト形式**: JSON（本文サイズは**8MiB上限**。超過は413——JSONを受けるAPI共通）
    ```json
    {
      "page_id": "000001",  // 必須。空文字は 400（新規作成は /api/new-page）。ゼロ詰め6桁へ正規化される
      "html": "<h1>...</h1>...", // シリアライズされた最新の本文（bodyの中身に相当する断片）
      "token": "..."        // 編集ロックのトークン（保持者の検証に使う。4.3 参照）
    }
    ```
*   **バックエンド処理フロー**:
    1.  `page_id` が空の場合は **400** を返します。かつてはここで親なし（トップレベル）のページを作っていましたが、「親なしにできるのはトップページ `000000` のみ」というポリシー（`NewPageAPIHandler` が400で拒否し `validateParentChange` も admin に許さない）を迂回する穴だったため塞ぎました。新規作成は必ず親を指定する `/api/new-page` を通します。
    2.  **編集ロックのトークン検証**: 既存ページの保存では、`token` が現在のロック保持者のものかを検証します。他者が保持中／トークン失効なら **409 Conflict** を返します（明け渡し後の古いクライアントが新しい保持者の編集を上書きしないため）。ロックが存在しない場合は許可します（無競合。4.3 参照）。
    3.  HTMLは「内容」なのでそのまま `[page_id].html` に書き込みます（属性の注入・上書きは行いません）。
    4.  **更新日時の前進**: `BumpUpdatedAt()` がサイドカーの `updated_at` を「今」に進めます（所有権・親・作成情報は保持。本文保存で権限を書き換えないため自己認可を防止）。
    5.  `SyncIndex(page_id, html)` を呼び出し、タイトル・タグや受発注データをDBに再同期します（親・作成情報・更新日時はサイドカーから読まれます）。
    *   親ページIDの変更は保存では扱わず、専用API（4.1.2）が担います。
    *   **本文は書き込み前にサニタイズします**（許可リスト方式。
        [本文サニタイズ設計.md](本文サニタイズ設計.md)）。正本ファイルにも `SyncIndex` にも
        サニタイズ後の本文を渡すため、正本は常に清書された状態に保たれます。
*   **レスポンス形式**: JSON
    ```json
    {
      "success": true,
      "page_id": "00002",          // フロントエンドに確定したIDを返す
      "updated_at": "2026-06-20T19:03:00Z", // サーバーが刻んだ更新日時（サイドパネル表示の更新に使う）
      "html": "<h1>...</h1>",      // サニタイズ後の本文
      "sanitized": false,           // 除去が起きたか
      "unknown_types": []           // レジストリ未定義の data-type の告知（拒否ではない。語彙モデル §9）
    }
    ```
*   **エコーバック**: `sanitized` が真のとき、エディタは差分のあったトップレベルのブロックだけを
    `html` の内容へ差し替え、「安全でない記述を除去しました」と通知します。編集者は画面上の変化で
    何が落ちたかを把握できます（フォーカス中のブロックはキャレット保持のため据え置き）。
    保存はブロックの編集完了時（`focusout`）にも走ります。

### 4.1.0. ブロック単位保存 API (`POST /api/save-block`)

本文のうち**1ブロックだけ**を差し替えます。認可・ロック検証・サニタイズ・更新日時の前進は
全文保存（4.1）と同じ経路を通ります。

*   **リクエスト**: `{"page_id", "block_id", "html"（当該ブロック1つ分）, "token"}`
*   **ブロックの識別**: `data-id` 属性（4桁の base36。採番時に文書内の重複を検査して振り直す）。
    `id` ではなく `data-*` なのは、本文がシェルと同じDOMへ合成されるため——`id` を許すと
    本文からシェル側の要素（`html-preview` 等）を乗っ取れてしまう
    （[本文サニタイズ設計.md](本文サニタイズ設計.md) §5.4）。**任意の属性**で、無ければこのAPIは使いません
    （手で書いたHTMLもそのまま扱えるようにするため）。付与はエディタが編集モード移行時に行い、
    その回の全文保存で正本へ永続化されます。
*   **差し替え**: `ReplaceBlock`（[block.go](../internal/cms/block.go)）がトップレベルのブロックを
    探して置き換えます。差し込む断片の前後の空白は落とします（残すと保存のたびに空行が増えるため）。
*   **409 を返す条件**: 編集権の喪失、対象ブロックが**見つからない**、同じIDが**複数ある**
    （ページ間のコピーで重複しうる）。いずれもクライアントは全文保存へフォールバックします。
*   **レスポンス**: `{"success", "page_id", "block_id", "updated_at", "html"（サニタイズ後の当該ブロック）, "sanitized", "unknown_types"（当該ブロック分のみ）}`。
    エコーバックもブロック単位になります。

> **効果の範囲**: 正本はファイル単位で書き直し、`SyncIndex` もページ単位の洗い替えのままです。
> つまりこれで減るのは**送信量とエコーバックの粒度**であって、ディスク書き込みやDB同期の量では
> ありません。同時編集の競合はページ単位の悲観ロック（4.3）が既に防いでいます。

### 4.1.1. 親ページ検証 API (`GET /api/validate-parent?id={編集中ページ}&parent={新しい親}`)
サイドパネルで親ページIDを入力した際の**即時フィードバック用**エンドポイント。付け替えAPI（4.1.2）・保存と同じ検証ロジック（`validateParentChange`）を共有します。対象ページの write 権限を前提とし、`{"ok": true}` または `{"ok": false, "error": "..."}` を返します。

### 4.1.2. 親ページ付け替え API (`POST /api/set-parent?id={ページ}&parent={新しい親}`)
親ページIDは**サイドカーが正本**のため、変更はこの専用APIで行います。対象ページの write 権限を要求し、親が実際に変わるときだけ `validateParentChange`（実在・自己参照/循環の防止・新しい親への write 権限、**親なし（トップレベル）化はトップページ（ID 0／`000000`）自身のみ可能。admin でも他ページを親なしにはできない**）で検証します。妥当ならサイドカーの `parent_id` と `updated_at` を更新し、`pages` インデックスにも反映します。`{"ok": true, "parent_id": "...", "updated_at": "..."}` を返します。

### 4.1.3. ページ属性取得 API (`GET /api/page-meta?id={page_id}`)
サイドパネル表示用に、ページの属性（`parent_id` / **`parent_title`** / `created_at` / `created_by` / `updated_at`）を返します。`parent_title` は親ページの見出し（`pages.title`）で、左レールの「↑ 親ページへ」リンクに表示します（IDだけより辿りやすいため）。対象ページの read 権限を要求します。

### 4.2. Load API (`GET /api/load?id={page_id}`)
ページ本文（生HTML）を返します。**初期表示には使いません**（4.2.1 のサーバー合成が担当）。
用途は編集ロック起点の載せ替え2経路のみです。

*   **使われる場面**:
    1.  編集権を失ったときの再読込（`reloadContent()`）。編集モードへ入るときは
        ロック取得後に `GET /api/load` で読み直した最新HTMLを使います（4.3）。
*   **リクエストパラメータ**: `id` (例: `00001`)
*   **バックエンド処理フロー**:
    1.  `pages` テーブルから対象IDの `file_path` を取得します。
    2.  物理ストレージ上の該当HTMLファイルを読み込みます。
*   **レスポンス**: HTML文字列を **`text/plain; charset=utf-8` ＋ `X-Content-Type-Options: nosniff`** で返却します（エディタは `fetch().text()` で受けて自前で `DOMParser` にかけるため text/html である必要がなく、この URL を直接ブラウザで開いてもHTMLとして実行されない＝多層防御）。**サニタイズはしません**
    （編集は生HTMLから始める必要があるため。ただし保存時サニタイズにより正本自体が清書済み）。
    ただし**計算ビューのマーカーには中身を埋めて返します**（`RenderComputedViews`。4.4）——
    編集モードの載せ替えでもビューが表示されるようにするため。埋めた `.vocab-chrome` は
    シリアライザが保存時に落とすので、正本には混ざりません。
*   **認可**: `RequirePageReadOrPublic`。匿名でも実効公開なら200、非公開は401。

### 4.2.1. ページ本文のサーバー合成 (`GET /{page_id}`)

ページのURLへアクセスすると、`RootHandler` が **本文を埋め込んだ完成HTMLを返します**
（従来は `assets/index.html` をそのまま返し、フロントが `/api/load` で本文を流し込んでいました）。
これにより初期表示のチラつきが無くなり、JSを実行しないクローラ等でも本文が読めます。

*   **合成**: `RenderPageShell`（[shell.go](../internal/cms/shell.go)）が `assets/index.html` の
    プレースホルダ `<!--WCMS_CONTENT-->` へ本文を、既定の `<title>` へ `pages.title` を埋め込みます。
    殻は mtime キャッシュで読むため、開発中の編集は再起動なしで反映されます。
    テンプレートエンジンは使いません——差し込む本文は**サニタイズ済みHTML**であり、
    `html/template` では `template.HTML` へのキャストが要って contextual escaping の
    恩恵が無いため（当初の理由だった殻の巨大なインラインJSは 2026-08-06 に外部化済み）。
*   **サニタイズ**: 埋め込む本文は必ず `Sanitize` を通します（保存経路を通っていない
    既存データ・バックアップ復元・手動配置への防壁。[本文サニタイズ設計.md](本文サニタイズ設計.md)）。
*   **計算ビュー**: サニタイズの**後**に `RenderComputedViews` が子ページ一覧・手配集計の
    中身を埋めます（4.4）。順序が逆だと、埋めた `class` 付きのクロームがサニタイザに落とされます。
*   **認可（画面の作法。APIの401とは分ける）**: 認証済みで read 権限が無ければ **403**、
    匿名で実効公開でなければ **`/login` へ302**、存在しないIDは **404**。
*   **キャッシュ**: 認可結果に依存する内容のため `Cache-Control: no-store`。

### 4.3. 編集ロック API（同時編集の競合対策）

同一ページの同時編集による lost update を防ぐため、**ページ単位の悲観ロック**を提供します（競合トリガー方式・**SSEプッシュ**）。設計の全体像と決定の経緯は [【考察】同時編集の競合対策.md](【考察】同時編集の競合対策.md) を参照。ロックは **プロセス内 mutex 付き map** に保持する揮発的なランタイム状態で、サイドカーにもDBにも永続化しません（単一プロセス前提。再起動で全ロックが消えるのは許容＝ロックは元々揮発的）。**presence（死活）は SSE 接続の有無で判定**します（ポーリングしない）。実装は `internal/cms/editlock/lock.go`（コア）と `editlock/handler.go`（HTTP/SSE）。

*   **`POST /api/lock?id=&token=`**（要 write 権限）: 編集モード移行時の**ロック取得**（および待機者が空き＝`available` を受けたときの自動取得）。保持者は**ユーザーではなくエディタ個体（トークン）**で識別します（同じユーザーが別タブ/別ウィンドウで開いた場合も競合として検知）。
    *   空き、または現在の保持トークンを `token` で提示した同一エディタの再取得なら取得し、`{"ok": true, "token": "..."}` を返す。**本文は同梱しない**——フロントが続けて `GET /api/load` を読み、古い版で上書きしないよう載せ替える（そちらは計算ビューのサーバー事前描画を通る。2026-08-20 変更）。
    *   別のエディタが保持中なら **423 Locked** ＋ `{"holder", "same_user", "grace_remaining_sec"}` を返す。
*   **`GET /api/lock-events?id=&role=holder|waiter&token=`**（要 write 権限・**SSE** `text/event-stream`）: ロック状態の変化をサーバーから push する。**この接続の生存＝presence**。
    *   保持者（role=holder）へ: `waiter`（待機者あり＋残り秒）／`holding`（待機者なし）／`lost`（明け渡し済み）。
    *   待機者（role=waiter）へ: `busy`（まだ埋まっている＋残り秒）／`available`（空いた＝取得を試みてよい）。**待機者の接続で2分の猶予が起動**する。
*   **`POST /api/unlock?id=&token=`**: 保持者本人による明示解放（閲覧モード復帰・タブ離脱の `navigator.sendBeacon` から呼ぶ）。
*   **`POST /api/lock/force?id=`**（admin）: 保持者が落ちてスタックした場合の強制解除（`auth.Audit` に記録）。

**エディタ内の変更操作の直列化（`RequireEditLock`）**: 本文保存だけでなく、**権限変更 `POST /api/page-perms`・所有者変更 `POST /api/page-chown`・親付け替え `POST /api/set-parent`** も、本文編集と同じ編集ロックで直列化します。共通ゲート `editlock.RequireEditLock(w, r, id)`（`editlock/handler.go`）が `X-Lock-Token` ヘッダ（無ければ `token` クエリ）のトークンを `editlock.Locks.Validate` で検証し、**他者保持中／トークン失効なら 409**、ロックが無ければ許可（保存と同一規約）。フロントは共通ラッパ `lockedFetch()` がトークンを自動同梱し、409 を `handleLockLost` に集約します。将来の画像/PDF等のリソース操作も、サーバーは `RequireEditLock`、フロントは `lockedFetch` を通すことで同じロックを共有できます。admin がスタックしたページを直すときは `/api/lock/force` で解除してから操作します。

**有効期間（競合トリガー方式）**: 無競合なら無期限保持。待機者（別エディタ）の SSE 接続から **2分**の猶予で強制明け渡し（満了後は早い者勝ち）。**保持者の SSE 切断**で待機者がいれば即解放、**待機者の SSE 切断**で猶予キャンセル（取得直後〜SSE接続までは `holderConnectGrace`＝約10秒の猶予で present とみなす）。猶予満了の評価は `StartLockReaper` の1秒ティッカーが行い、状態変化は全購読者へ broadcast されます。

### 4.4. 計算ビューのサーバー事前描画（`RenderComputedViews`）

子ページ一覧・部材手配集計のような**表示専用のビュー**は、本文に結果を持たず、
本文を返すたびにサーバーが中身を埋めます（実装は [cms/view_render.go](../internal/cms/view_render.go)。
2026-08-19・移行第4段。旧 `<m-child-list>` / `<m-required-materials>` の後継）。

*   **保存形式は空のマーカーだけ**: `<section data-type="child-list">`・
    `<section data-type="required-materials">`。正本HTMLにはこれ以上入りません。
*   **埋める入口は2つ**: 画面の `RootHandler`（`GET /{id}`）と `LoadAPIHandler`（`GET /api/load`）。
    どちらも `RenderComputedViews(r, pageID, body)` を通します。画面側は**サニタイズの後**に
    呼びます（埋める中身は `class` を持つため、先に埋めるとサニタイザに落とされる）。
    マーカーを含まない本文はパースせず素通しするので、通常ページに追加コストは掛かりません。
*   **埋める中身**: `<div class="vocab-chrome" contenteditable="false">…</div>`。
    マーカーの中に書かれていた内容は捨てて置き換えます（ビューの中身はサーバーが所有する）。
    エディタのシリアライザは `.vocab-chrome` を保存しないため（エンハンサのクロームと同じ規則。
    [エディタ仕様.md](エディタ仕様.md) §6.1.1）、**保存往復してもマーカーだけが残ります**。
*   **中身は閲覧者で変わる**: 子ページ一覧は `/api/children` と同じ絞り込み
    （`visibleChildren`。認証済みは read 権限・匿名は実効公開）。手配集計は
    `/api/required-materials` と同じ集計（`RequiredMaterials`）。**APIと画面で同じ関数**を
    使うので結果が食い違いません。タイトル等の利用者入力は `html.EscapeString` を通します。
*   **更新は再読み込み**: 編集中に子ページを作る・発注を変えても表示は自動更新されません
    （描画ロジックを Go と JS の2箇所に持たないための割り切り）。挿入直後の空マーカーには
    CSS `::before` で案内を出します。
*   **効果**: 閲覧はゼロJSで完結し、JSを実行しないクローラにも中身が読めます
    （公開ページのSEO要件——[要件定義書.md](要件定義書.md) §4.4——と整合。
    ユーザー決定 2026-08-19・[【考察】語彙モデル.md](【考察】語彙モデル.md) §9）。

---

## 5. PDF連携API仕様

発注書のPDFファイルを管理・自動解析するためのAPIです。

### 5.1. Upload PDF API (`POST /api/upload-pdf`)
エディタ上にドラッグ＆ドロップされたPDFを受け取り、ページのディレクトリに保存します。

*   **リクエスト形式**: `multipart/form-data`
    *   `page_id`: 保存先のページID (例: `00002`)。※フロントエンドは必ず一度オートセーブを行ってIDを確定させてから呼び出します。
    *   `pdf_file`: アップロードされるバイナリファイル
*   **受け入れ条件（サーバー側で強制。2026-08-05）**: ページのディレクトリには本文 `<id>.html` と
    属性サイドカー `<id>.meta.json` が同居しているため、**置ける名前と種類を絞ります**。
    *   リクエスト全体を **32MiB** に制限（`http.MaxBytesReader`）。
    *   ファイル名はパス要素（`/` `\`）を落とし、空・`.`・`..`・ドット始まり・制御文字入りを拒否。
    *   拡張子は **`.pdf` のみ**（`allowedAttachmentExts`）。加えて本文・サイドカーと同名は明示的に拒否。
    *   中身の先頭が **`%PDF-`** であることを確認（拡張子は名乗りにすぎないため）。
    *   ※ この制限が無いと、write 権限だけの利用者が **サイドカーを上書きして所有者・権限・
        公開フラグを書き換え**られ、また **`.html` を置いて `/data/` から同一オリジンのHTMLとして
        配信**させられました（[変更履歴.md](変更履歴.md) 2026-08-05 の節）。
*   **バックエンド処理フロー**:
    1.  指定された `page_id` のディレクトリ (`data/master/{prefix}/{id}/`、`prefix`＝ID先頭2桁。`GetPageDir` 参照。例: `data/master/00/000002/`) を作成・確認します。
    2.  上記の受け入れ条件を満たしたPDFファイルをディレクトリ内に保存します。
*   **レスポンス**:
    ```json
    {
      "success": true,
      "file_name": "example.pdf",
      "src": "example.pdf"
    }
    ```

### 5.2. PDF解析API (`POST /api/parse-pdf`)

保存済みのPDFをGoogleのGemini API（`gemini-3.5-flash` モデル）に直接送信し、画像や複雑なレイアウトからでも完璧に明細を抽出して返すエンドポイントです。

**前提条件**:
*   サーバーの環境変数 `GEMINI_API_KEY` にGoogle AI StudioのAPIキーが設定されている必要があります。設定されていない場合はエラーになります。

**リクエスト**:
*   **Method**: `POST`
*   **Content-Type**: `application/json`
*   **Body**:
    ```json
    {
      "page_id": "00A1B",
      "file_name": "sample.pdf"
    }
    ```

**内部処理フロー**:
1.  リクエストの `page_id` と `file_name` から、サーバー上のPDFファイルのパスを特定し、バイナリとして読み込みます。
2.  `github.com/google/generative-ai-go/genai` を使用してGeminiクライアントを初期化します。
3.  PDFバイナリを `application/pdf` のBlobとしてモデルに渡し、以下のプロンプト（指示）とともに送信します。
    *   *「このPDFは発注書または見積書です。記載されているすべての部品明細（品名、単価、数量）を抽出し、以下の形式のJSON配列のみを出力してください...」*
4.  Geminiから返却された文字列から、マークダウン装飾（` ```json `）等を取り除き、純粋なJSONテキストを抽出します。
5.  JSONテキストを `ParsedItem` 構造体の配列にパースします。

**レスポンス例 (成功時)**:
```json
{
  "success": true,
  "items": [
    {
      "item_name": "高耐久ギア",
      "price": "15000",
      "quantity": "2"
    }
  ],
  "raw": "[Geminiからの生レスポンステキスト]"
}
```

**レスポンス例 (エラー時)**:
```json
{
  "success": false,
  "message": "Gemini APIの呼び出しに失敗しました: [エラー詳細]"
}
```
※旧バージョン（正規表現ベース）のテキスト抽出機能は廃止されました。

---

## 6. ページID発番アルゴリズム

フロントエンドで新規ページを作成する際、グローバルで一意の連番IDを安全に生成するための仕組みです。

### 6.1. バックエンドのID採番ロジック (`reserveNewPageID`)
バックエンド（`internal/cms/handler_tree.go`）では、`pages` テーブルへ最小限の行を **`INSERT` する自動採番**でIDを原子的に確定します（`id` は `INTEGER PRIMARY KEY`）。
1. `INSERT INTO pages (title, parent_id, file_path) VALUES ('新しいページ', ?, '')` を実行し、SQLite が採番した `LastInsertId()` を取得します。
2. その数値を桁数（`IDLength = 6桁`）でゼロ埋め（`fmt.Sprintf("%0*d", IDLength, id)`）して返し、`GetPageDir` で先頭2文字をプレフィックスにした保存先パス（例: `data/master/00/000001`）を決定します。

`INSERT` 自動採番のため、**同時に複数のリクエストが新規採番しても一意なIDが得られます**（DBが採番を直列化）。

> [!NOTE]
> 以前は `SELECT MAX(id)+1`（`GenerateNextID`）で採番していましたが、同時実行でID衝突が起こりうる非原子的な方式でした（[【考察】同時編集の競合対策.md](【考察】同時編集の競合対策.md) シナリオE）。`reserveNewPageID` による `INSERT` 自動採番への統一でこれを解消し、`GenerateNextID` は廃止しました。

> [!NOTE]
> `reserveNewPageID` は専用のAPIエンドポイントを持たない内部関数で、`NewPageAPIHandler` から呼び出されます（`SaveAPIHandler` の空 `page_id` 経路と `UploadHandler` は廃止済み）。

### 6.2. 子ページ作成 (`GET /api/new-page?parent={親ページID}`)
フロントエンド（`assets/app.js` の `createSubpage`）で「＋ 子ページを作成」ボタンが押された際のフローは、`NewPageAPIHandler` がサーバー側で完結させます（DOM構造・イベント面の詳細は [エディタ仕様.md](エディタ仕様.md) 6.2参照）。
1. フロントエンドは `/api/new-page?parent={現在のページID}` を呼び出します。`parent` は必須です（**親なし＝トップレベルのページが許されるのはトップページ（`000000`）のみ**で、それは初回起動時に自動生成済みのため、新規作成では `parent` を省略できません。admin でも例外はありません）。
2. バックエンドは `reserveNewPageID()`（6.1）で `pages` への `INSERT` 自動採番により新しいページIDを原子的に確定します。
3. 初期HTMLは「内容」のみ（`<h1>新しいページ</h1>` と `<p>`。子ページ一覧は左サイドパネルが担うため本文には埋め込まない——必要ならスラッシュメニューから `<section data-type="child-list">` を後から挿せる）を生成して物理ファイルへ書き込みます。親ページID・作成者・作成日時などの属性は `EnsureSidecar()` でサイドカー `<id>.meta.json` に記録し（作成者＝所有者、作成日時・更新日時はサイドカーが刻む）、続けて `SyncIndex` でDBへ同期します（親・作成情報はサイドカーから読まれます）。
4. レスポンスとして `/{新しいID}?edit=true` へ302リダイレクトします。ID発番・ファイル生成・DB同期・リダイレクトが単一のリクエストでアトミックに完結するため、フロントエンドが後から個別に `saveToServer()` を呼ぶ必要はありません。

### 6.3. 過去に発生したトラブルと、現在の設計に至った経緯（履歴）

以前は「フロントエンドが `GET /api/new-id` でIDだけを取得し、`window.open()` で新規タブを開いてからフロントエンド側で `saveToServer()` を呼ぶ」という、ID発番とページ実体の確定が2段階に分かれた設計を採用していました。この設計は以下のトラブルを引き起こしたため、現在は6.2の `/api/new-page` によるサーバー主導のアトミックな単一リクエストに置き換えられています。

#### トラブル1: IDが更新されず、ページが上書きされ続ける
**症状**: 子ページを1つ作成した後、2つ目を作ろうとしても同じID（例: `000001`）が返ってきてしまい、別のページが作れない。

**原因**: ブラウザ（特にWindows環境のChrome/Edge）が、GETリクエストであるID発番APIの結果をキャッシュしていました。そのため、2回目以降のボタンクリック時にサーバーへ問い合わせが行かず、記憶していた同じIDが使い回されていました。

#### トラブル2: 3つ目以降の子ページが作れない（UNIQUE制約違反）
**症状**: 1つ目、2つ目のページは作れるように見えたが、3つ目以降が作れない。またはデータベース再構築後にファイルがあるのにDBに登録されない。

**原因**: 新規ページ作成時、当初はテスト用のハードコードされたダミーHTML（`PO-2026-001` という架空の発注書番号が含まれる `<m-file>`）がそのまま使われていました。当時のデータベースは `client_orders.order_no` に **UNIQUE 制約**を持っていたため（このテーブルは 2026-08-31 の D-1 で廃止済み）、2つ目のページで同じ `PO-2026-001` を保存しようとするとSQLiteがエラーを吐き、**トランザクションがロールバック**されていました。DBにページが保存されないため、最大IDが進まず、一生同じIDを再発行し続ける「上書きループ」に陥っていました。

**解決策（現在の設計に反映済み）**:
1. **設計の見直し**: ID発番とページ作成を分離する2段階フローを廃止し、6.2の `/api/new-page` によるサーバー主導の単一リクエストに統合しました。
2. **新規ページのクリーンな初期化**: ダミーデータを含まないクリーンな初期HTML（`<h1>新しいページ</h1>...`）で生成するようにしました。
3. **UPSERTの導入**: 万が一、ユーザーが手動で発注書番号をコピペして重複させた場合でもシステムがクラッシュしないよう、`INSERT INTO ... ON CONFLICT(order_no) DO UPDATE SET ...` を用いて、エラーを無視して最新のページに紐付けを移動させる柔軟な仕様に変更しました。
4. **エラーの可視化**: `SyncIndex` のエラーを握り潰さず、エラー時は HTTP 500 を返し、フロントエンドのステータスに赤字で「保存失敗」と表示されるようにしました。

---

## 7. 製品マスタページの設計方針

2026-06-20の議論を経て確定した、製品マスタ（例: 鋼材の種類など）をページとして表現する際の設計方針。

### 背景
従来のRDBMS的な固定スキーマではなく、本プロジェクトのコンセプト（[コンセプト.md](コンセプト.md) 3章）に沿って「製品マスタ（例: 鋼材の種類、発注時に必要な項目）」もページとして自由に記述・編集できるようにしたい。検討の過程で、当初は「マスタが書き換わると過去の発注済み明細の参照内容まで事後的に変化してしまう」という懸念からバージョン管理（版管理）の導入を検討したが、以下の2点により**不要**と判断した。

1.  **削除されない・状態フラグで表現する**: 鋼材の種類のような製品マスタは、現実の運用として「種類が減る」ことはなく、廃版になった場合も行を削除せず `status`（`有効` / `廃版`）のような状態フラグでマークするだけで十分。これにより、過去の明細からの参照が消失する問題はそもそも発生しない。
2.  **価格・コストはマスタに置かない**: 単価・原価は常に変動するため製品マスタの属性ではなく、`client_order_items.price` や `our_order_items.cost` のように**取引明細テーブル側に都度記録**する（既存スキーマで既にこの方針が採用されている）。マスタの内容が変動するのは価格のような時間変化する値ではなく、種類名・規格・発注時の必須項目ルールなど低頻度にしか変わらない情報のみであるため、過去の明細内容が事後的に変化する問題も発生しない。

### 採用する設計
> **記法の追随（2026-08-20）**: 本節は 2026-06-20 の決定をそのまま残しています。可変タグの記法は
> 語彙モデルへの移行で `<m-tag name="X" value="Y">` から
> **`<dl data-type="tags"><dt>X</dt><dd>Y</dd></dl>`** へ変わりました（決定の内容は不変）。
> 以下の `<m-tag …>` は新記法に読み替えてください。

1.  **マスタページ自体に編集可能なルールを埋め込む**: 例えば鋼材ページに `<m-tag name="必須項目" value="径,材質,長さ">` のようなタグを記述し、これを「発注時に必要な項目」のルール定義として扱う。固定DBスキーマではなく、ページ内容がそのまま型定義になる。
2.  **マスタの保存は通常のUPSERTでよい**: マスタページが保存される際、既存の `SyncIndex` の同期対象にマスタテーブルを追加し、`page_id` をキーとした通常のUPSERT（バージョニングなし）で同期する。
3.  **廃版の表現**: マスタページに `<m-tag name="状態" value="廃版">` のようなタグを記述することで、新規発注時の選択肢からは除外しつつ、過去の発注明細からの参照（`item_id`/`item_name` 等のキー）は引き続き有効なまま残す。

---

## 8. データの正本性と全再構築

### 8.1. 正本はファイル、DBは派生インデックス
w-cms では **`data/master/` 配下のファイル群が唯一の正データ**であり、**`data/cms.db`（SQLite）は検索・集計のための派生インデックス**にすぎません。各ページのファイルは次の2種類で、UNIX流に「内容」と「属性」を分けます。

*   **HTML本体（`<id>.html`）＝ページの内容**: タイトル・本文・マーカー付き標準HTML（可変タグ `<dl data-type="tags">`・受発注の業務文書ブロックなど）。計算ビューは**空のマーカー**だけが保存され、中身は毎回サーバーが描きます（4.4）。
*   **サイドカー（`<id>.meta.json`）＝ページの属性**: 所有権・権限（owner/group/mode）に加え、親ページID・作成日時・作成者・更新日時。

DBの全カラムは `SyncIndex` によって **HTML本体とサイドカーから** 再生成できます（アップロードPDF等の添付も `data/master` 配下に置かれます）。

> [!IMPORTANT]
> **不変条件**: データベースには「ファイルから再生成できないデータ」を一切持たせないこと。新しいプラグインを追加する際も、そのテーブルの全データが `data/master` のファイル（HTMLのマーカー付き標準HTML、またはサイドカー）から `Sync()` で再生成可能であることを保証してください。汎用索引 `vocab_index` にも同じ不変条件が適用されます（DELETE→INSERT の洗い替えのみ）。この不変条件により、`cms.db` はいつでも破棄して作り直せる「使い捨て可能なキャッシュ」として扱え、バックアップはファイルのみで完結します（運用手順は [【ガイド】デプロイ・運用.md](【ガイド】デプロイ・運用.md) の「4. バックアップと復元」参照）。

### 8.2. 全再構築の方式（全テーブルDROP）
`RebuildDatabase`（`POST /api/rebuild-db`）は次の手順でDBを完全に作り直します。

1.  `sqlite_master` から現在の全テーブルを列挙し、すべて `DROP TABLE` する。これにより、廃止されたプラグインの残存テーブルなどスキーマのdriftも含めて完全に初期化される。
2.  コアテーブル（`CreateCoreTables`）と全プラグインのテーブル（`ApplySchema`）を作り直す。
3.  `data/master` 配下の全 `.html` を走査し `SyncIndex` で再同期する。

DBファイル自体は削除せず、接続を開いたまま全テーブルをDROP・再作成します。これは、Windowsでのファイルロック（オープン中のファイルは削除不可）やリビルド中の他リクエストとの競合を避けるためです。処理は冪等であり、途中失敗時は再実行で回復できます（正データはファイル側にあるため失われません）。

### 8.3. 起動時の自動リビルド
起動時に `pages` テーブルが空（DBが新規・復元直後など）で、かつ `data/master` にHTMLファイルが存在する場合、w-cms は自動的に `RebuildDatabase` を実行します。これにより、バックアップからの復元は「`data/master` を戻してアプリを起動するだけ」で完了します。

### 8.4. 決定ログ（正本性まわり・2026-08-21）

| 判断 | 決定 | 理由 |
|---|---|---|
| **派生（DB）から正本（ファイル）へ書き戻さない**（**実装済み** 2026-08-22） | サイドカー `<id>.meta.json` が読めないとき、`page_perms`・`pages` に残っている値から**サイドカーを組み立て直して書き戻すことをやめる**。読めない事実をそのまま残し、**運用者が手作業で直す**。DBから正本への書き込みは、集計など**必要な場合に限る** | ユーザーの言葉:「**これは設計を変えます。サイドカーが読めない場合、運用者が手作業で修正します。自動で治すことはしません。データベースから正本への書き込みは集計など必要な場合に限られます。**」——§8.1 の不変条件（DBはいつでも捨てて作り直せる派生）の**裏返しの規律**にあたる。派生から正本を書くと、正本の欠損が派生の内容で上書きされ「**見た目は健全なのに中身が変わっている**」状態を作る。壊れたときに壊れたままにしておくほうが、気づける。**実装**: `readMetaOrPerms`＋`fillFromIndex` を廃し、`requireSidecar` が読めなければエラーを返す（[page/perms.go](../internal/cms/page/perms.go)）。`BumpUpdatedAt`／`SetSidecarParent` は書かずに止まり、**本文保存・親の付け替えが失敗**する。権限変更（chmod/chgrp/公開切替）と所有者変更も同様に 409 で止め、修復対象のファイルパスを添えて知らせる。**内容（`<id>.html`）は無事**なので、失われるのは属性だけ。なお `SyncIndex` が「サイドカーが読めないときは索引側の `parent_id` を保つ」のは**DB内で完結する保護**であり正本への書き戻しではないので、そのまま維持する。期待は `sidecar_loss_test.go` が固定 |
| **スキーマは速さでなく柔軟性のために設計する**（方針・**未着手**） | DBスキーマは**大幅な設計変更を前提**とし、検索の速さより**定義変更への強さ**を優先する | ユーザーの言葉:「**データベースについては、大幅な設計変更が必要です。速度のためではなく柔軟性のためのスキーマ設計をします。**」——§2 の受発注・見積もり・マスタの各テーブルは、業務ごとに列を決め打ちした横持ちで、形式が増減するたびにスキーマ変更を要求する。[【考察】語彙モデル.md](【考察】語彙モデル.md) §9 が②汎用索引について既に決めている「**完全な正規化（縦持ち）**——小規模事業者用でデータ総数は少なく性能は問題にならない。縦持ちなら形式の定義変更が DBスキーマ変更を一切要求しない」の姿勢を、**コア側のテーブルにも広げる**という指示。**具体案は未着手**。設定の置き場が無い問題（テンプレート置き場の名前を設定で変えたい——[【考察】ページテンプレート.md](【考察】ページテンプレート.md) §9 の残る宿題）も、この設計変更と一緒に考える |

## 9. スキーマ設計し直しの決定ログ（2026-08-27〜30）

「DBスキーマの見直しを行います」（2026-08-27）を受けた設計判断です（論点の原文は
[判断待ち.md](判断待ち.md) の D-1〜D-6）。到達点は **コア2テーブル（`pages`・`page_perms`）
＋汎用索引1本（`vocab_index`）＋設定ファイル1本（`data/settings.json`）**——
**ドメイン語彙を持つテーブルは1つも残りません**。§8.4 の「スキーマは速さでなく柔軟性のために
設計する」を具体化したものです。**D-1・D-3・D-5 は実装済み**（D-1 は 2026-08-30〜31 の2段階で完了）。D-2・D-4 は設計の確定のみ。

| 判断 | 決定 | 理由 |
|---|---|---|
| **硬いドメイン表8つを落とし、汎用索引へ一本化する**（D-1・**実装済み** 2026-08-30〜31） | 受注・受注明細・発注・発注明細・販売見積・購入見積・部材・タグの8テーブルを廃し、`vocab_index` 1本へ寄せる（`page_tags` も中身が重複しているので吸収）。集計は索引から Go で束ねる。**実装は2段階**——2026-08-30 に `page_tags`、翌31日に残る7つ（受注・受注明細・発注・発注明細・販売見積・購入見積・部材）と所有プラグイン3本を削除。同時に、それまで誰も索引していなかった**業務文書ブロックのヘッダ**（`data-type` を持たない素の `dl`）を `section` の側から拾うようにした（拾わないと発注元・発注日が消える。→ 2章） | ユーザーの判断:「**する**」。週2枚の規模では縦持ちの弱点（性能）が発動しない。`単価（ロット20）倉橋` のような**可変列名は横持ちでは受からない**、が実データの結論（[【考察】ワンノート移行.md](【考察】ワンノート移行.md) §3-5）。板金部の語彙がコアのスキーマから消え、「語彙とプラグインは運用者のもの」（[要件定義書.md](要件定義書.md) §1.1・§4.5）と一致する |
| **機能見出しを採用する**（D-2・**読み側は実装済み** 2026-08-31） | セクションの最初の見出し（直接の子の h1〜h6）がレジストリの**表示名**と一致すれば、`data-type` を書かなくてもそのセクションは形式を持ち、**素の dl / table がその形式で索引される**。解決は `vocabTypeOf`（[walk.go](../internal/cms/walk.go)）の1関数で、索引・鏡・種まきが共有。**`data-type` は明示の正として引き続き勝つ**（既存データの意味は見出し次第で変わらない）。索引の「形式」列には**解決後のレジストリの形式名**（`inspection-record`）が入る——見出しの言葉は宣言、列の値は正規形（生の言葉は本文が持ち、③の読み手が安定する）。未登録の見出し語は静かにただのセクション（告知なし・エージェント判断は語彙モデル §11.5-6）。エディタの挿入切り替えは未着手 | ユーザーの判断:「**する**」。提案の原文:「**表を含むセクションの最初の見出しが機能を表し、日付や企業名等は名前：値のタグで表してはどうでしょう**」——「見える文字がデータの手掛かり」（[コンセプト.md](コンセプト.md) §2）の一貫した適用。ワンノートの「■材料 等の見出しがそのまま宣言になる」も効く。設計の本文は [【考察】語彙モデル.md](【考察】語彙モデル.md) §11 |
| **索引は1テーブル縦持ちのまま**（D-3・エージェント判断） | 行/フィールドの2テーブル分割（語彙モデル §10 未決）は今回も見送る | 分ける利益が具体化していない。**業務判断が要らない内部の話**なのでエージェント判断とした（正規化そのものの方針は [【一覧】日付形式と数詞.md](【一覧】日付形式と数詞.md) と §9.2） |
| **参照は本文のタグで表し、`page_refs` は作らない**（D-4・**未実装**） | ページ間の参照線（由来・前版）を**DBのテーブルではなく本文の「名前：値」タグ**で表す。`page_refs` は正本の座から降ろす（→ §9.1） | ユーザーの言葉:「**page_refs は正本の座から降ろしてください。この方式にする理由は、AIが柔軟な操作をしてくれると信じているからです。必ずその時代が来るはずです。**」 |
| **設定の正本はファイル `data/settings.json`**（D-5・**実装済み** 2026-08-30 → §9.4） | 設定はファイルに置き、DBには置かない | ユーザーの判断:「**推奨通りで**」。`cms.db` は全再構築で全テーブルが消える（§8.2）ため、**設定をDBにだけ置くと設定も消える**。「文書（ファイル）が主」の原則（§8.1）どおり。最初の住人は「テンプレート置き場の名前」（[【考察】ページテンプレート.md](【考察】ページテンプレート.md) §9 の残る宿題・要否そのものは未決） |
| **日時は ISO 8601 が全域の正** | 日付は `YYYY-MM-DD`（ゼロ詰め）、時刻付きは `T` 区切り | 正本は [要件定義書.md](要件定義書.md) §3（2026-08-30 決定）。SQLite が日本の業務文書の日付表記をほぼ1つも受け付けない実測が根拠（[【一覧】日付形式と数詞.md](【一覧】日付形式と数詞.md) §1） |

### 9.1. 参照を「名前：値」のタグで表す（`page_refs` の廃止）

**何を決めたか**: 「この発注書は、どのメールで届いたか」「この図面 Rev.C の1つ前は Rev.B」と
いったページ間の参照線を、**専用テーブル `page_refs` ではなく本文中のタグ**で表します。

```
原発注書 : 000002-12
```

これは `<dl data-type="tags">` の1対（`<dt>原発注書</dt><dd>000002-12</dd>`）で、
**既存の仕組みのまま** `vocab_index` に `field='原発注書', value='000002-12'` の1行として載ります。
参照専用のテーブルも、参照専用のプラグインも要りません。

ユーザーの言葉（2026-08-30）:

> 由来への参照は必須ですが、DBに書き込むというより、文書に書き込みます。なんなら、
> **機械的に「名前：値」のタグを入れたって良い**のです。たとえば、「原発注書:000002-12」
> というような形式です。そうすると、DBにも「名前:値」のレコードが入りますから、
> あとから検索できるはずです。

> **page_refs は正本の座から降ろしてください。この方式にする理由は、AIが柔軟な操作をして
> くれると信じているからです。必ずその時代が来るはずです。**

**なぜこの形が良いか**:

1. **正本の置き場が正しくなる**。`page_refs` に参照線を持たせると、それは**本文から再生成
   できない唯一のデータ**になり、§8.1 の不変条件（「DBはファイルから再生成できるものしか
   持たない」）を破ります。本文にタグとして書けば、正本はサイドカー＋本文のまま、索引は
   洗い替えで復元できます。**新しいテーブルが1つも要らなくなる**——D-1 で8テーブルを畳んだ
   流れの、そのまま延長線です。
2. **「見える文字がデータの手掛かり」と一致する**（[コンセプト.md](コンセプト.md) §2）。
   参照が人に読めます。しかも**機械が入れてよい**——正確さは自動挿入で担保しつつ、
   見た目は人間向けのまま保てます。
3. **検索は今日の仕組みでできる**。追加コードなしで引けます。

**値は「ページID-ブロックID」**（2026-08-30 に確定）。`000002-12` の `000002` がページ番号、
`12` がページ内のブロックIDです。ユーザーの言葉:「**URLのリンクみたいな感じです**」——
つまり値は業務番号ではなく**所在地**で、`/000002#12` に相当します。

この形にしたことで**参照は押せば飛べます**。しかも実装は既にあります——`RenderAnchors`
（[anchor.go](../internal/cms/anchor.go)）がページ描画時に `data-id` を `id` へ合成するので、
`#<ブロックID>` は**今日のページでもう解決できます**（書き手が何も書いていない既存ページも遡って
アンカーを持つ）。ユーザーの言葉:「**値はページとIDを指すので、クリックすれば移動すら
できるのでは？**」——できます。

**受け入れた代償**: これは参照整合性のある関係ではなく、**ラベル一致**です。DBが保証しないので
**宙ぶらりん**（指す先が無い）と**多重ヒット**が起きえます。ただしこれは新しい種類のリスクでは
ありません——「項目の鍵は見出しの表示文字」で既に同じ賭けをしているので、設計思想としては
一貫しています。

**この方式が要求する前提条件**:

| 前提 | 状態 |
|---|---|
| **値の索引** | **実装済み**（2026-08-30）——`(field, value)` と `(field, norm_value)` の2本（[vocab_index.go](../internal/cms/vocab_index.go)）。逆引き（「発注書番号が X のページ」「000002-12 を参照しているページ」）は**生テキスト（value）に対して**引く——生が正本のため。`norm_value` 側は date の範囲検索（納期 BETWEEN）用 |
| **参照値の解釈** | 値は `<6桁のページID>-<ブロックID>` という**文法を持つ**ので、一般のテキスト正規化ではなく**構文解析**で扱う（D-7 決定）。ハイフンの種類の揺れは解析側で畳む。**未実装**（リンク描画・宙ぶらりん判定と一緒に作る） |

**値が文法を持つので、正規化より構文解析が向きます**。`<6桁のページID>-<ブロックID>` と
決まっているため、生の文字列一致ではなく**分解して引く**ほうが確実です。ハイフンの種類の揺れ
（`-` / `ｰ` / `−` / `－`）は**畳んでよい**——参照値としてはどれも同じ意味しか持ちません。
ただし**先頭のゼロは絶対に畳めません**（`000002` はページIDのゼロ埋め6桁）。

> **一般のタグ値の揺れは、別問題として残っています。** 参照タグは文法を持つので上の扱いで
> 済みますが、**図面番号のような普通のタグ値**（w-cms を作る動機そのもの——「ワンノートが
> ハイフンで区切られた図面名称をうまく検索できない」）は文法を持たないので、
> ハイフン類・全角半角・英字の大小をどこまで畳むかの判断が要ります。**サイト内検索に着手する
> ときの宿題**（[要件定義書.md](要件定義書.md) §4.4）。

**ブロックIDの現状と、埋めるべき穴**（実測）:

| 事実 | 参照の宛先として見たときの意味 |
|---|---|
| `data-id` は**4桁の base36 ランダム**（`a7k2` のような形。文書順の連番ではない） | 連番にすると途中挿入で全部ずれるので、ランダムが正しい。ただし `000002-12` のような**短い数字を思い描いていると見た目が違う** |
| 採番されるのは**トップレベルのブロックだけ**（`ensureBlockIds` / `topLevelBlocks`） | **`<section>` の中の明細表には付きません**。「PDFや表を貼り付けるとIDが付く必要がある」（ユーザー）は、**入れ子の要素で満たされていない**——`vocab_index.block_id` も入れ子では空 |
| 採番は**エディタ（JS）でしか起きない** | 手で書いたHTMLにはIDが無い。既存ページは初回編集時に採番され、その回の全文保存で永続化される |
| ページ間でコピーすると重複しうるので、`ensureBlockIds` が**振り直す** | **コピーされた側のIDは変わります**。参照先が単独で存在する限り安定ですが、**恒久的な識別子ではありません** |
| `RenderAnchors` は `id` を合成するが、**ページ描画時だけ**（`/api/load` は通さない） | 合成した id をエディタへ入れると本文として保存されてしまうため。参照のリンク化も**描画時に行う**のが筋 |

**`page_refs` が戻るとしたら**: タグ値を解決した**派生のキャッシュ**としてのみ。洗い替えで
再生成できるので不変条件は壊れません。ただし現在の規模（週2枚）では自己JOINで足りるため、
**いまは作りません**。

### 9.2. 参照タグの運用（D-8〜D-10・2026-08-30 決定）

| 判断 | 決定 | 理由 |
|---|---|---|
| **参照タグの名前は固定しない**（D-8） | `原発注書`・`受信元`・`前版` … 運用者が好きに付ける。コアは名前の一覧を持たない | ユーザーの言葉:「**参照タグの名前は、検索時に威力を発揮します。参照タグは既にDBに入ることになっているので、どのような名前でも検索したら良いだけです**」——「語彙とプラグインは運用者のもの」（[要件定義書.md](要件定義書.md) §4.5）と一致する。名前を固定すると、コアが板金部の語彙を知ってしまう |
| **業務番号を持たない相手も同じ形で参照する**（D-9） | 通信記録（メール・FAX・電話）も `ページID-ブロックID`。**特別扱いを作らない** | ユーザーの言葉:「**ページ番号＋ブロックのIDで良いのでは？**」——値が所在地であって業務番号ではないので、そもそも「業務番号を持たない相手」という問題が消えた。D-9 は問い自体が解消した形 |
| **宙ぶらりんの参照は背景色で知らせる**（D-10） | 指す先が見つからない参照タグへ背景色を付ける。**判定はページを読むたびに行う**（保存時ではない） | ユーザーの言葉:「**背景色で警告を出しても良いのでは？この場合、先に参照を書いて、あとから参照元を作る場合もあるので、ページを読むたびに背景の更新が必要かもしれません**」 |

**D-10 の「読むたびに」は、実装上とても効きます。**「先に参照を書いて、あとからページを作る」
——このとき保存時に判定していたら、後からページを作っても**赤いまま残ります**（気づける方法が
無い）。したがってこれは**鏡型の計算**（見るたびに最新・保存しない。[【考察】語彙モデル.md](【考察】語彙モデル.md) §11）です。
D-3 の「認識できた形式は背景色で示す」と同じ描画時の合成に相乗りします。

> **CSP strict なのでインラインの `style=` は書けません。** 背景色はサニタイズ**後**の
> 描画時合成（`RenderComputedViews` と同じ層）でクラスを足し、色は `/assets/` の CSS で当てます。
> サニタイズ後にHTMLを足す関数はエスケープ責任を自前で負う、という規律に従うこと（§8 の注意）。

### 9.3. 参照タグを入れる前に埋める穴

§9.1 の表で挙げた実測から、実装前に片付けるものが2つあります。**どちらも決定というより作業**です。

1. **入れ子の要素にもブロックIDを振る**——いま採番されるのはトップレベルだけなので、
   `<section>` の中の明細表を指せません。ユーザーの「PDFや表を文書に貼り付けると、IDが付く
   必要があるのかもしれません」を満たすには、少なくとも**マーカー付きの `table` / `dl` /
   `section`**（＝②汎用索引が拾う要素）へは入れ子でも振る必要があります。
   `vocab_index.block_id` が入れ子で空なのも同じ原因で、**参照の解決に使う列がそこ**です。
2. **参照タグをリンクとして描画する**——`RenderAnchors` が既に `#<ブロックID>` を解決できるので、
   参照タグの `<dd>` を `<a href="/000002#12">` にすれば飛べます。**描画時だけ**行うこと
   （`/api/load` に混ぜるとリンクが本文として保存される——`RenderAnchors` と同じ罠）。

### 9.4. 設定ファイル `data/settings.json`（D-5・実装済み）

**正本はファイル。DBには置きません。** `cms.db` は全再構築で全テーブルが消える派生索引
（§8.2）なので、設定をDBにだけ置くと設定も消えます。実装は
[cms/settings.go](../internal/cms/settings.go)。

**最初の住人は語→型の推論辞書**です。ユーザーの決定（2026-08-30）:
「**運用中に増やしてDB再構築します**」——Goコード内の map のままだと語を1つ足すたびに
**再ビルドが要る**ため、ファイルへ出しました。Goコード内の並び（`defaultTypeInference`）は
**既定値**として残ります——板金部の語彙は**同梱の既定セットであって、コアの語彙ではない**
（「語彙とプラグインは運用者のもの」——[要件定義書.md](要件定義書.md) §4.5）。

```json
{
  "type_inference": {
    "加工日": "date",
    "員数": "number"
  }
}
```

| 場面 | ふるまい | なぜ |
|---|---|---|
| ファイルが無い | **既定値で作る**（起動時） | 失うものが無く、運用者が中身を見て編集できるようになる |
| 有るのに読めない・値が不正 | **起動を止める**（`log.Fatalf`） | 既定値で黙って上書きすると運用者が足した語が消え、**集計の内容が黙って変わる**。§8.4「派生から正本へ書き戻さない」と同じ流儀 |
| 語を足した | **DB再構築で効く**（`POST /api/rebuild-db`） | `RebuildDatabase` が**先頭で読み直す**。再起動は要らない。この1行が無いと「足したのに効かない」が無症状で起きるので、`TestRebuildReloadsSettings` が固定 |
| キーの打ち間違い | **止める**（`DisallowUnknownFields`） | `type_inferrence` を黙って無視すると、書いたつもりの辞書が丸ごと効かない |

**ファイルの中身は既定値を置き換えます**（足すのではありません）。既定値を書き出した状態から
始まるので、**要らない語は消せます**。

書き込みは**一時ファイル＋rename**（`page.WriteFileAtomic`）です。同日にこのヘルパを
**正本を書く全経路**（サイドカー・本文HTML・添付・版・設定）へ広げたので、どの正本も
書き込み中にプロセスが落ちて「半分だけ書けた切り詰めファイル」になることはありません。

> **バックアップの対象です。** `data/master` と `auth.db` と同じく、**ファイルから再生成
> できません**（[【ガイド】デプロイ・運用.md](【ガイド】デプロイ・運用.md) §4.1）。
