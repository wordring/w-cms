# 作業引き継ぎ

> 別の環境（自宅・職場など）で作業を再開するためのスナップショットです。
> **現況と次の一手だけ**を書き、経緯は [変更履歴.md](変更履歴.md) に置きます
> （古くなったら更新し、大きな区切りではこのファイルごと差し替えてください）。
> 最終更新: 2026-08-21（自宅セッション。**設計総点検の補完調査を完走し、11件を修正**。
> その前は 2026-08-20 の語彙モデル移行完了・ページテンプレート・設計総点検）

## いまここ: 設計総点検の対処を進めている（11件済み・②で中断）

2026-08-20 の [【考察】設計総点検.md](【考察】設計総点検.md) は「31件中5件しか反証検証
できていない」「補完調査を走らせたが結果は未回収」で終わっていました。**2026-08-21 に
それを完走**しています——APIエラーで落ちていた**3領域（データモデル・安全性・エディタ）の
地図を取り直し**、未検証項目を**16件の予定に対し全23件**検証し、**11件を修正**しました。

**結果は同文書の §5（判定と訂正）と §6（対処済み）**にまとめてあります。読むときの注意:

- **§1・§2 の本文は 2026-08-20 当時のまま**（記録として残した）。**必ず §5 の訂正と
  併せて読むこと**——原文のまま通った指摘は**1件もありません**。
- 判定が**覆ったもの**が6件あります（フェイルオープンは到達不能で不成立、往復契約テストの
  結論は誤り、`robots.txt` の因果は誤り、など）。
- **新規に8件**見つかりました（§5.3）。うち最も重いのは**保存型CSRF**——サニタイザは相対URLの
  `<img src>` を通すので、本文へ `<img src="/api/logout">` を保存するだけで、そのページを
  開いた全員を無音でログアウトさせられました（対処済み）。

### 直近のコミット（`6b00696` の続き・11本）

`af5c032` 手配集計の認可越境／`15495dc` section の未知型を告知／`1324807` 添付ロック／
`beb98a1` 保存型CSRF／`3068d11` 部品番号の改名告知／`f6ca63c` parse-pdf の名前検証／
`bd7ee98` parent_id 喪失／`7312850` E2E 取り込み／`ad7c7b3` バイナリ除去・list_models 移設／
`2580632` ルート保護の固定テスト／`f2458a9` order_no のページ内スコープ

**すべて「修正前に落ちるテスト」を先に書いてから直しています**（`upload_test.go` の流儀）。
`go test ./...` は全パス、作業ツリーはクリーン。**プッシュはまだです**。

## 次にやること: ②テンプレート判定（着手直前で中断）

**ユーザー決定済み**（2026-08-21）: 判定を h1 の文字から**サイドカー属性 `template_root` へ
昇格**し、**右レールに「テンプレート置き場」トグルを追加**する（「インターネット公開」
トグルの隣。トップ直下のページのみ・owner/admin のみ）。

**後方互換の方針**: 既存の「テンプレート」ページはタイトル判定で従来どおり動き、
トグルを押せば固定される。`template_root` が真なら h1 を改名しても印は残る。

触る場所（調査済み）:
- `page.PageMeta` へ `TemplateRoot bool`（`template_root,omitempty`）
- `isTemplateRoot`（[template.go](../internal/cms/template.go):101-114）——サイドカーの印を優先、
  無ければ従来どおり `pageTitleFromDisk(id) == TemplateRootTitle`
- `PagePermsHandler`（[perms_admin.go](../internal/cms/perms_admin.go):41-135）——GET 応答へ
  `template_root` と `can_set_template_root` を足し、POST は `Public` と同じ形で受ける
  （**owner or admin ＋ 編集ロック**は既存の関門がそのまま効く）
- `assets/index.html:219-224` の `pp-public-row` と同じ作りでトグルを1つ。`assets/app.js` の
  `loadPerms`（:399-421 付近）で状態を反映

## 覚えておくこと（今回わかった実装の勘所）

- **認可の読み口はサイドカー（正本）ではなく `cms.db`（派生）**。`GetPerms` も
  `EffectivePublic` の親チェーンも DB を読む。「正本と索引がズレたら**認可は索引に従う**」ので、
  `parent_id` の喪失はそのまま公開範囲の問題になる。
- **サニタイズ後にHTMLを足す関数が3つある**（`RenderComputedViews`・`RenderAnchors`・
  `RenderPageShell`）。それぞれ自前でエスケープ責任を負っており、規律が破れると保存型XSS。
- **本文DOMを走査する箇所は29ある**のに `.vocab-chrome` の明示除外は4つだけ。残りは
  「chromeが入れ子」「chromeが `data-type` を持たない」という**現時点の偶然**に依存している。
  レジストリへ `View: true` を1件足すと5箇所が一斉に誤爆する。
- **サニタイザは相対URLの `<img src>` を通す**。`/api/` への GET が状態を変えると保存型CSRFに
  なるので、**状態を変える経路は必ずメソッドを絞る**（`route_guard_test.go` が固定）。
- **プラグインテーブルにマイグレーションは無い**。`ApplySchema` は `CREATE TABLE IF NOT EXISTS`
  を流すだけなので、**列を足すと既存DBでは保存だけが 500 になる**。`f2458a9` で起動時の
  ずれ検出＋自動再構築（`DriftedSchemaTables`）を入れたので、以後スキーマを変えたら
  **起動時に1度だけ再構築が走る**（ログに出る）。
- **`csp_test.go` はポリシー文字列しか見ていない**（配線は見ない）。`CSPProtect` を外しても
  通る。配線の固定は `cmd/w-cms/route_guard_test.go` の `TestCSPIsWired`。
- **ルート表は `buildHandler()`**（`cmd/w-cms/main.go`）。main から切り出したのはテストのため。
- **E2E は `e2e/` へ移設済み**（OneDrive から）。`cd e2e && npm install` で使える。
  **OneDrive 側のファイルはまだ残してある**ので、こちらが正本と確認できたら消してよい。


## いまここ: 語彙モデルへの移行が完了した

2026-08-16 に決めた「カスタム要素をやめ、マーカー付き標準HTMLへ移る」計画
（[【考察】語彙モデル.md](【考察】語彙モデル.md) §8.4）が、縦切り第1〜4段と最終掃除まで
**すべて完了**しました。到達点:

- **カスタム要素は0種**。本文は構造HTML＋`data-*` マーカーだけで、形式（`data-type`）の
  宣言は①語彙レジストリ [vocab.go](../internal/cms/vocab.go)（12形式）にデータとして載る。
  **新しい語彙はここへ1件足すのが既定**——エディタの挿入メニュー・索引・型検証まで自動で揃う。
  カスタム要素は**二度と作らない**（2026-08-17 決定）。
- **プラグインは計算ユースケースだけ**を持つ（`Name`/`Schema`/`Tables`/`Sync`＋任意の
  `RouteProvider`）。語彙宣言の機構（`Tags()`・`PluginTags()`・注入）は撤去済み。
- **計算ビュー**（子ページ一覧・手配集計）は空のマーカーを保存し、中身は
  [view_render.go](../internal/cms/view_render.go) が**サーバー事前描画**で埋める
  （`.vocab-chrome`＝保存されないクローム）。**閲覧はゼロJS**で完結する。
- **CSP は strict**（`'unsafe-inline'` なし）。インラインの script/style と `on*=` は
  リポジトリ全体でゼロ。後戻りは [csp_test.go](../internal/auth/csp_test.go) が検出。
- 両環境（職場・自宅）の実データは変換済みで、**一括変換ツールは撤去済み**。
  未変換の旧要素が後から出てきた場合はサニタイズでアンラップされる（テキストは残る）。
- **本文のマーカーは `data-type`（型名）と `data-src`（配線）の2つだけ**。同日に
  `data-field`（機械キー）を撤去し、**項目の鍵は見出し（`th`／`dt`）の表示文字ただ1つ**に
  なった。③計算プラグインが読む機械キーはレジストリ宣言（`Label`→`Field`）が解決する。
  改名で解決できなくなった列は保存時に `unresolved_fields` で告知される（拒否はしない）。
  あわせて **「w-cms がその要素を見て何かするなら `data-type` を付ける。何もしないなら
  付けない」** を規約として決定（[【考察】語彙モデル.md](【考察】語彙モデル.md) §9）。

詳細は [変更履歴.md](変更履歴.md) の 2026-08-19〜2026-08-20 の各節。文書もこの到達点へ
全面追随済み（2026-08-20。カスタムタグ一覧は [【一覧】語彙.md](【一覧】語彙.md) へ改名・作り替え）。

**あわせて 2026-08-20 に「ページテンプレート」を設計・実装しました**（設計から実装・E2Eまで同日）。
形式の決まった文書を雛形から起こせます——テンプレートは「テンプレート」フォルダ（トップ直下・
同名のページ）の**葉**に置いた普通のWikiページで、枝は分類。コピーは1ページだけで、
空欄は列型の既定値（日付は今日・`order-no` は再採番）で埋まります。
正本は [【考察】ページテンプレート.md](【考察】ページテンプレート.md)。**運用開始前に、
実データのトップ直下へ「テンプレート」ページを作るところから始めます**（無ければ従来どおり
「空のページ」だけが作られるので、作るまで挙動は変わりません）。

## その前: 設計総点検の結果（2026-08-20）

> **⚠ この節は当時の記録です。2026-08-21 の補完調査で判定が動いた項目があります**
> ——下の5件のうち**4件は対処済み**（②だけ残り）、また最後の「`tags` に1行足す」という
> 対処案は**採れないことが分かりました**（自由語なので全ページで誤検知になる）。
> 現況は最上部の節と [【考察】設計総点検.md](【考察】設計総点検.md) §5・§6 を見てください。

14体の調査エージェントで実装を読み直し、**「黙って壊れる」経路を洗い出しました**。
結果は [【考察】設計総点検.md](【考察】設計総点検.md)。うち5件は**実測で再現を確認済み**です。

**運用開始前に潰しておきたいもの**（詳細と対処案は同文書）:

- テンプレートの判定が**トップ直下ページの h1 の文字**で決まり、改名すると部分木まるごとが
  ②索引・③計算から出入りする（告知なし）。対処はサイドカー属性への昇格。
- `order_no` の UNIQUE が**ページ横断**で、番号が空だと**他ページの受注を奪う**（実測再現）。
- `parse-pdf` が拡張子を検証せず、**本文と権限サイドカーを外部LLMへ送れる**。数行で塞がる。
- サイドカーが読めないと**DBが正本を書き戻し**、`parent_id` が失われてページがツリーから消える。
  修正の第一候補は [sync.go](../internal/cms/sync.go) の `parent_id = excluded.parent_id`（COALESCE 無し）。
- 改名の告知が3類型のうち1つしか拾わない。**`tags` に `{Field:"part-id", Label:"部品番号"}` を
  1行足すのが最も安く鋭い**（`plugin_materials.go:64` の日本語魔法文字列が原因）。

**この総点検は 31 件中 5 件しか反証検証できていません**（残りは根拠つきだが未裏取り）。
検証済み5件はすべて「成立するが範囲の訂正が要る」だったので、
**未検証項目は着手前に必ず自分で確かめてください**。

## 次にやること（優先度順・いずれも着手可能）

1. **画像アップロード**（決定済み・実装のみ）——公開サイトで画像を出す経路が現状ゼロ。
   `allowedAttachmentExts` の拡張＋マジックナンバー検証、`DataFileHandler` の検証済み種別の
   インライン配信、エディタの挿入手段（ドロップ／ファイル選択＋**モバイルはカメラ撮影**）。
   EXIF は一律除去、HEIC は理由つき拒否、SVGは「不活性な画像」として配信三点セットで許可。
   要件は [要件定義書.md](要件定義書.md) §2.6。`upload_test.go` の流儀（修正前に落ちるテスト）に倣う。
   **レジストリの `image` 列の入力補助もこれ待ち**。
2. **匿名応答の404統一**（決定済み・実装のみ）——匿名×非公開の302/401が不存在の404と
   区別でき、連番IDで非公開ページを列挙できてしまう。全匿名経路を404へ（例外はトップ経由の
   `/login` 誘導のみ）。要件は [要件定義書.md](要件定義書.md) §2.1。
3. **公開ウェブサイトとしての体裁（公開専用ビュー）**——クローム無しの体裁・
   `description`/OGP/canonical・`sitemap.xml`/`robots.txt`・キャッシュ可能な応答。
   **キャッシュを入れるなら、非公開ページと認証済み応答を絶対にキャッシュさせない切り分けが前提**
   （いまは一律 `no-store`）。正本は [要件定義書.md](要件定義書.md) §4.4・[認証認可設計.md](認証認可設計.md) §10.5。
4. **通信記録の取り込み**（最大の未着手テーマ・設計から）——正本は
   [【考察】通信記録処理.md](【考察】通信記録処理.md)。§11 の未決を詰めてから着手する。
   特に `page_refs` のスキーマ確定と、**部材手配・利益計算を `page_id` 一致から参照追従JOINへ
   書き換える**こと（§2.5 の本体コスト）。参照マーカー（受信元・前版）の移行もここに含まれる
   ——移行第3段でスコープ外にした唯一の残りが、この参照系。
   **設計が大きく進んだ（2026-08-20）**——[【考察】通信記録処理.md](【考察】通信記録処理.md) §2.7 に
   決定5件（添付の置き場・変更発注書は追記・進捗マークの正本・可逆性の要件・`data-src` の一般化）。
   参照マーカーは `<a data-type="ref-source" href>` として見えるリンクのまま表せる
   （サニタイザの `a` へ `data-type` を許可するのが入口）。**ファイル容器の入れ子は維持**される。
5. **スラッシュメニューの絞り込み・分類**——レジストリ駆動化は完了したが、100種規模での
   到達性（絞り込み入力・分類・頻度順）は未実装。正本は [エディタ仕様.md](エディタ仕様.md) §3.1。

### 小粒な残り

- **フッター帯（`.site-footer`）へのリンク追加**——現状は仮表示「w-cms」のみ（ユーザー予告）。
- **`<aside>` サイドパネルの作り込み**——アイコン・余白・折りたたみ既定状態の磨き込み。
- **CSS変数のさらなる移行**——ウィジェット内部配色は据え置き。
- **`updated_at` の表記ゆれ**——サイドカー由来は RFC3339、フォールバックは SQLite の
  `CURRENT_TIMESTAMP`。表示は `formatDateTime` が吸収するが、保存時の正規化は未検討。
- **画面構成の注釈図**——実スクリーンショットへ吹き出しを重ねたもの（[画面構成と用語.md](画面構成と用語.md) §5）。
- **同時編集の残る未決**——複数待機者の順序（FIFO 等）・任意の安全TTL・ロック多重化時の
  共有ストア化・将来の CRDT 化（[【考察】同時編集の競合対策.md](【考察】同時編集の競合対策.md) §8）。
  現状の単一プロセス・揮発的ロックで運用上の支障は出ていない。
- **保存済み文書の版管理（リビジョン／リバート）**——設計のみ
  （[【考察】アンドゥ・リドゥ.md](【考察】アンドゥ・リドゥ.md) §4・§5）。編集中のアンドゥは実装済み。
  **2026-08-20 に「あると嬉しい」から要件へ格上げ**——変更発注書のブロック追記を取り消す手段が
  ページ削除では代替できないため（[【考察】通信記録処理.md](【考察】通信記録処理.md) §2.7④）。

## 覚えておくこと（実装の勘所）

- **テンプレートは「テンプレート」フォルダの葉**（トップ直下・同名のページがルート）。
  **領域全体（ルート含む）は③計算テーブルへ載らない**——載せると仮の発注書が集計に出る。
  除外は「プラグインを飛ばす」ではなく **`SyncIndex` へ空の本文を渡す**形で、
  洗い替えの `DELETE` は走るので**フォルダへ出し入れしても古い行が残りません**。
  判定の先祖辿りは**サイドカー＋本文HTML**（DBを見るとDB再構築の順序で静かに壊れる）。
  正本は [【考察】ページテンプレート.md](【考察】ページテンプレート.md)。
- **`visibleChildren` を使うテストは `:memory:` DBでは動かない**——行を回しながら
  1件ずつ `page.GetPerms` を引く形で、入れ子のクエリが別接続＝別の空DBを見て
  権限がフェイルクローズし、子が1件も見えなくなる。ファイルDBを使うこと
  （`setupTemplateAPITest` が手本）。本番はファイルなので問題は起きない。
- **殻の `id` は接頭辞 `w-` を独占する**（`w-editor-content` 等）。本文の `id` は自由に書けるが、
  接頭辞つきは剥がされる。殻へ接頭辞なしの id を足すと `shell_id_test.go` が落ちる。
- **ページ内アンカーの `id` は描画時に合成される**（`RenderAnchors`）。`RootHandler` だけで行い
  `/api/load` は通さない——通すとエディタが合成 id を本文として保存してしまう。
- **項目の鍵は見出しの表示文字**（`th`／`dt`）。機械キーを本文へ書く属性は無い。
  **見出しを改名すると③計算に読まれなくなる**が、保存時に告知が出る（黙って壊れない）。
- **新しい語彙はレジストリへ1件**。プラグインを書くのは計算が要るときだけ
  （[【ガイド】プラグイン開発.md](【ガイド】プラグイン開発.md)）。カスタム要素は作らない。
- **インラインの script/style は書けない**（CSP strict）。スクリプト/スタイルは `/assets/` の
  外部ファイルへ、イベントは `addEventListener`、動的スタイルは `element.style.x = y`。
- **DOM生成は `createElement`＋`textContent`**。文字列を組み立てて `innerHTML` へ入れない
  （かつて `escapeAttr` 頼みで保存型XSSが実際に発火した。その機構ごと廃止済み）。
- **エディタのクロームは `.vocab-chrome`**（または本文DOM外）。シリアライザが保存しない。
  **本文DOMを走査する新しいコードは、`.vocab-chrome` を除外したか必ず確かめること。**
  クロームは本文に見えるが本文ではない（サーバーの計算ビューやエンハンサが挿す表示専用の飾り）。
  現在この除外を持つのは**4箇所**——`serializeChildren`（保存・app.js:1460）・`buildToc`（目次・app.js:316）・
  `markPageTitleHeading`（タイトルの印・app.js:302）・`RenderAnchors`（アンカー合成・anchor.go:62）。
  **Goテストが固定しているは `RenderAnchors` だけ**（`bugfix_test.go` の `TestRenderAnchorsSkipsChrome`）。
  保存側は JS なので Go テストが無く、E2E だけが守っている。
  2026-08-20 に見つけたバグ2件のうち1件がこの取りこぼし（クロームの見出しが目次に載り、
  本文の見出しが使いたいアンカー名まで消費していた）。本文の `class` はサニタイザが必ず落とすので、
  `.vocab-chrome` の判定は誤爆しない。
- **本文の取得口は `GET /api/load` の1つ**。計算ビューのSSRを通るのはこの経路だけなので、
  本文を読む新しい経路を足すときは注意（`/api/lock` に生HTMLを同梱していて退行した実例あり）。
  **この経路もサニタイズを通す**（2026-08-20。素通しだと殻の要素を乗っ取られる）。
  一方**アンカー合成は通さない**——合成 id が本文として保存されてしまう。
- **E2E は実バグを何度も見つけている**。`assets/` を変更したら `node --check` と
  verify-stage2/3/4 を回す（正本は `%OneDrive%\tools\wcms-e2e\`。落とし穴は同 README）。
  現在のスクリプトは stage2（48）・stage3（20）・stage4（25）・delete（9）・**template（15）**。
  `node` は PATH に無いことがある（`C:\Program Files\nodejs` を足す）。
- **ページ削除は `data/trash` への移動**（物理削除ではない）。トップページと子持ちページは不可、
  編集ロックが要る。ゴミ箱からの**復元UIは未実装**——戻すには手でフォルダを `data/master` へ
  移してDB再構築する。
- **ローカル検証のログインは `a` / `a`**。CSRF対策で `Origin` ヘッダ必須。

## 現在の状態（環境）

- **`origin/main` は `6b00696` まで。本セッションの11コミットは要プッシュ**
  （2026-08-21 の自宅セッション分。`git push origin main`）。
- **E2E は未実行**。`assets/app.js` を3箇所変えた（`openNewPage` の form POST 化・
  添付アップロードの `lockedFetch` 化・告知文言）ので、**職場で一式を回してください**
  ——`cd e2e && npm install && npx playwright install chromium` のあと
  `node verify-stage2.js` ほか。特に verify-template（15項目）が子ページ作成の
  中心経路（`openNewPage`）を直接踏みます。
- ローカル `data/` はトップ `000000` の1枚だけ（2026-08-20 に掃除済み）。旧要素はゼロ。
  スキーマ変更（`f2458a9`）に伴う再構築は**起動時に自動で1度走り済み**。
- **職場のDBは初回起動時に自動再構築が走ります**（`order_no` のスキーマ変更を検出するため）。
  ログに「テーブル定義の変更を検出しました」と出れば正常。data/master が正本なので
  失われるものはありません。
- **`go test -race` が使える**（gcc 導入済み）。PATH に WinLibs の
  `~/AppData/Local/Microsoft/WinGet/Packages/BrechtSanders.WinLibs.POSIX.UCRT_*/mingw64/bin` を
  足し `CGO_ENABLED=1 go test -race ./...`。通常ビルドは Pure Go を保つため `CGO_ENABLED=0` のまま。

## 開発環境の再現（自宅などで再開する手順）

```bash
# 1. 最新を取得
git pull

# 2. 依存取得
go mod tidy

# 3. 初回のみ管理者を作成して起動（ローカルHTTP検証なので Secure Cookie 無効）
#    ローカル開発の管理者は user=a / password=a に統一する（環境間で揃えるため。2026-07-04 決定）。
WCMS_ADMIN_USER=a WCMS_ADMIN_PASSWORD='a' WCMS_SECURE_COOKIES=0 \
  go run cmd/w-cms/main.go
# 2回目以降は環境変数なしで go run cmd/w-cms/main.go
# → http://localhost:8080 （/login にリダイレクト。a / a でログイン）

# 4. テスト
go test ./...
```

注意:
- **ローカル開発のログイン**: `a` / `a`（**ローカル検証専用**。本番では使わないこと）。
  古い環境には `admin` / `Admin` で作られた `auth.db` が残っている場合がある（2026-07-04 に `a`/`a` へ変更）。
  入れないときは下の再設定手順で揃える。
- `data/`（`cms.db` と `master/`、`auth.db`）は `.gitignore` 対象でコミットされない。**環境ごとに独立**するため、
  別環境（自宅など）では初回起動時に上記の環境変数で管理者が新規作成される。`auth.db` は同期されないので、
  「家でログインできない」は data/ が無い＝初回の管理者未作成か、別の認証情報で作成済みのいずれか。
- **既存 `data/auth.db` の認証情報を忘れた／揃えたいとき**は、サーバーを止めてから以下で揃えられる:
  ```bash
  # 一時パッケージで internal/auth を呼ぶ（ルートに別の package main があるためサブdirで実行）
  # 既存ユーザーなら SetPassword、未作成なら CreateUser を使う。
  mkdir -p _resetpw && cat > _resetpw/main.go <<'GO'
  package main
  import ("fmt"; "w-cms/internal/auth"; "w-cms/internal/database")
  func main(){ database.InitAuthDB(); defer database.AuthDB.Close()
    if err := auth.SetPassword("a", "a"); err != nil {
      if err2 := auth.CreateUser("a", "a", true, ""); err2 != nil { fmt.Println(err, err2); return }
    }
    fmt.Println("ローカル管理者 a / a を用意しました") }
  GO
  go run ./_resetpw && rm -rf _resetpw
  ```
- ローカル実行・プレビュー用に `.claude/launch.json`（`go run` 起動設定）を用意済み。
  プレビュー（http://localhost）でCookieが付くよう **`env: { "WCMS_SECURE_COOKIES": "0" }`** を設定してある。

## ユーザーへ再質問する項目（未回答）

> 過去のセッションで質問したが回答が得られていないもの。**関連する話題になったとき、
> または適切なタイミングで再質問すること**（ユーザー指示）。回答が得られたら本節から消し、
> 決定ログ（[【考察】語彙モデル.md](【考察】語彙モデル.md) §9 等）へ移す。
> 回答済みの項目（文書コミット・第1段の着工・論点A・計算ビューのSSR）は削除済み。

**関連作業に入るときに聞くもの:**

1. **挿入経路の優先度**（汎用表エディタの拡張時）——貼り付け変換（Excel/TSV→表）を
   初期範囲に含めるか／バーコード・QRスキャン→品番入力は実務で欲しいか
2. **公開サイトの表現力3案の選択**（サイトインスタンスの設計時）——表示専用タグ／
   `class` 許可リスト／テーマCSS（要件定義書 §4.1 注記）
3. **キャッシュの許容遅延**（公開専用ビューの設計時）——公開/非公開切り替えの反映は
   「即座」が要件だが、ETag/CDN と両立させる許容値（秒か分か）が未定義
4. **`data-type` 改名の扱い**（実運用スキーマ確定時）——別名リストを持つか改名禁止か。
   **2026-08-21 に前提が1つ改善**: `section` の未知 `data-type` が告知されるようになった
   （`15495dc`）ので、改名しても「全ページが無言で③計算から消える」ことはなくなり、
   保存のたびに告知が出る。とはいえ**告知が出るだけで壊れることに変わりはない**ので、
   別名リストの要否は依然として未決。

**要否の判断待ち（2026-08-16 の問題点洗い出しより）:**

5. セッション絶対8時間と長時間編集が衝突したときの挙動（保存401・ロック喪失）を要件化するか
6. 公開サイトの運用要件（アクセスログ・可用性目標・バックアップの RPO/RTO）を定めるか
7. アクセシビリティ・サイト内検索を公開サイト要件に含めるか
8. 要件定義書に要件ID・受け入れ基準を導入するか（§5 が伸びるほどトレーサビリティが効く）

