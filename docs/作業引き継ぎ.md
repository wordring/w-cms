# 作業引き継ぎ

> 別の環境（自宅・職場など）で作業を再開するためのスナップショットです。
> 内容が古くなったら更新し、新しいタスクに入ったらこのファイルを差し替えてください。
> 最終更新: 2026-08-19（自宅セッション。**コードリファレンス追随・汎用表エディタ拡張・
> 監査未達3件の解消**を順に実施。その前は 2026-08-17 の職場③＝縦切り第1段）

## 直近のセッションでやったこと（2026-08-19・自宅）

職場③の pull 後、残っていた3件を順に実施した（ユーザー指示「順番に」）。

1. **コードリファレンスを縦切り第1段＋本日分へ追随**（`/code-reference`）。
   `vocab.go`／`vocab_index.go`・依存図・テストの地図（16ファイル・97テスト）・
   シナリオ 13a〜13c（行・列操作／列設定・型検証・enum 補助／dl 項目操作）。
2. **汎用表エディタの拡張**（[変更履歴.md](変更履歴.md) 2026-08-19 の節。仕様の正本は
   [エディタ仕様.md](エディタ仕様.md) §3.2）——縦切り第1段のスコープ外3項目:
   - 列操作（追加・削除・左右移動。最後の1列は骨格保護）＋**列設定ポップオーバ**
     （属性側の編集口。列型の明示 `th data-type`。`data-field` の手編集口は設けない）
   - **dl の項目操作**（`#dl-toolbar`。項目＝dt＋後続dd・多値・最後の1項目は保護）
   - **型不一致の赤印**（拒否しない）＋ **enum の選択肢メニュー**。推論辞書は手書きせず
     `/api/tag-schema` の `type_inference` で配布（`TypeInferenceDict` 新設）
   - 検証は一時ハーネス（`/assets/` 認証不要の流儀）で**37項目全通過**・ハーネス削除済み。
     → その後 **Playwright 環境を自宅にも構築し、E2E（verify-stage2.js・41項目）でも全通過**
     （下記4）
   - 未実装のまま: date/image の入力補助（image は画像アップロード待ち）
3. **監査未達3件（旧1.6）を解消**——JSONボディ上限（cms=8MiB/413・管理API=1MiB）・
   `page.NormalizeID`（ゼロ詰め6桁。パスに id を使う8入口へ適用）・`/assets/` へ
   `Cache-Control: no-cache`。新テスト4本（[audit_fixes_test.go](../internal/cms/audit_fixes_test.go)）。
4. **Playwright 環境を自宅にも構築し、スクリプトは OneDrive 経由で共有**。
   - 環境は職場と同じ（`winget install OpenJS.NodeJS.LTS`＋`~\tools\wcms-e2e\` に
     playwright＋Chromium）。職場の verify-stage1.js はこちらに無いため、
     **第1段の回帰＋第2段までを検証する `verify-stage2.js`（41項目）を新規作成**し全通過。
   - **スクリプトの正本は `%OneDrive%\tools\wcms-e2e\`**（README あり。両マシンへ自動同期）。
     `node_modules` は同期せず各マシンの `~\tools\wcms-e2e\` に置く。スクリプトは
     playwright を**カレントディレクトリから**解決する（`createRequire`）ので、
     実行はサーバー起動後:
     `cd ~\tools\wcms-e2e && node "$env:OneDrive\tools\wcms-e2e\verify-stage2.js"`
     （`--headed` で表示。場所を間違えると手順つきのエラーが出る）
   - **職場での作業**: 同じ Microsoft アカウントの OneDrive にサインインしていれば
     スクリプトは自動で届く。→ 職場で確認済み・**職場の verify-stage1.js は廃棄**
     （2026-08-19。stage2 が第1段の回帰を含む41項目のため。正本は OneDrive のみ）。
   - **E2E が実バグを1件検出→修正済み**——空の dt/dd への入力が隣の項目へ混入する
     （`<br>` の足場で解決。変更履歴 2026-08-19 の節）。
   - 実行のたびにテストページが増える点は stage1 と同じ（「DB再構築」で整理可）。
5. **文書の総点検（doc-audit・全文書）**——未検証リストの全消化＋最近の変更への追随漏れを
   一掃した（約20件。詳細は [変更履歴.md](変更履歴.md) 2026-08-19「文書の総点検」の節）。
   主な修正: プラグイン開発ガイドの「レジストリ未実装」→実装済み、要件定義書の
   汎用表エディタ／スラッシュメニューの達成状態、アーキテクチャの `part_id` 注入元
   （parser.go→plugin_materials.go）・保存応答の `unknown_types`・新規ページ初期HTML、
   原価と利益の「新カスタム要素を追加する想定」→計算ビューへ（廃止決定との矛盾）、
   デプロイ運用の「フェーズ1」表記、殻の行数誤記（3015→215／195→72）を本修正、
   画面構成と用語へ新クローム4種、用語集へ語彙モデルの中核語5件。

## その前（2026-08-17・職場③）

**語彙モデルの縦切り第1段（[【考察】語彙モデル.md](【考察】語彙モデル.md) §8.4-1）を実装した。**
①コード内レジストリ＋②汎用索引＋汎用表エディタv1 がサンプル語彙「検査記録」で
end-to-end 動作。**既存のカスタム要素には一切触れていない**。詳細は
[変更履歴.md](変更履歴.md) 2026-08-17「語彙モデルの縦切り第1段を実装する」の節。

- **新設**: [vocab.go](../internal/cms/vocab.go)（①レジストリ＝Goコード内宣言テーブル・
  語→型推論辞書・正規化）、[vocab_index.go](../internal/cms/vocab_index.go)
  （②汎用索引＝縦持ち `vocab_index` テーブル。プラグイン機構に相乗り）
- **サニタイザ**: `data-type`→`table`・`dl`・`th`／`data-field`→`th`・`dd` の
  属性名許可（要素限定・値は不活性）
- **API**: `/api/tag-schema` に `vocab`、保存応答に `unknown_types`（未知種別の告知）
- **エディタ**: スラッシュメニューのレジストリ駆動項目＋骨格生成（`createElement`＋
  `textContent` のみ・innerHTML不使用）、行操作ツールバー（`#table-toolbar`・
  本文DOM外のクローム）、機械属性ハイライト、本文の表の基本スタイル
- **検証**: `go test ./...` 全パス（新テスト2本）。実サーバーでも保存→索引→告知を確認。
  **エディタのブラウザ実機確認も完了**——Node.js＋Playwright を導入し
  （`winget install OpenJS.NodeJS.LTS`＋`~\tools\wcms-e2e\`。リポジトリ外の開発ツール）、
  自動E2E（verify-stage1.js（廃棄済み・stage2 に包含）・チェック31項目）が
  全通過: メニュー項目・骨格挿入・セル編集・行操作・保存往復無変化・data-id 採番・
  未知種別トースト・アンドゥ/リドゥ・閲覧モード・既存機能の退行なし・JSエラーなし。
  実行方法: サーバー起動後 `cd ~\tools\wcms-e2e && node verify-stage1.js`（`--headed` で表示）。
  E2E中に見つけた実バグ1件も修正済み——行移動（insertBefore）でキャレットが失われ
  ツールバーが消える → 移動後に元のセルへキャレットを戻す（`placeCaretAtEnd`）
- **スコープ外として残したもの**: 列型の入力補助（enum等）・列設定UI・列操作・
  dl の項目操作UI・型不一致の通知（変更履歴の「やらなかったこと」参照）
- 文書更新: 語彙モデル（状態・§6・§8.4・§10）・エディタ仕様（§3.1 一部実装済み化・
  §3.2 新設）・本文サニタイズ設計（§5.2・§5.4・§7）・【一覧】API・
  アーキテクチャとDBスキーマ（`vocab_index`）・変更履歴。
  **コードリファレンスは未追随**（測り直しは /code-reference で別途）

## その前（2026-08-17・職場②）

**文書のみ。コードは無変更。**

- **Web Components（カスタム要素 `<m-*>`）の全廃を決定**（ユーザー決定。正本は
  [【考察】語彙モデル.md](【考察】語彙モデル.md) §9、経緯は [変更履歴.md](変更履歴.md)
  2026-08-17 の節）。要点: 採用3本柱はバニラJS＋素のDOMの性質でカスタム要素固有ではない／
  動的3系統（容器・参照・計算ビュー）もマーカー付き標準HTMLへ／移行第4段で
  `web-components.js`・`templates/` を撤去し **CSP strict 化も同時に完了**／
  **新規カスタム要素は作らない（即時発効）**／`lit` 再評価は打ち切り／追加ライブラリ不要
  （エンハンサ＝バニラJS、DOM生成＝殻の `<template>`＋`cloneNode`＋`textContent`）。
  反映先: 語彙モデル・コンセプト §3・エディタ仕様 §0・要件定義書・CSP強化 §4・
  プラグインガイド・サニタイズ設計 §5.3。
- **コードリファレンスを拡充**（`/code-reference` スキルの手順で実測）: シナリオ7本追加
  （匿名閲覧・ロック解放と離脱・親付け替え・アンドゥ・スラッシュ挿入・インライン装飾・
  手配集計・管理コンソール）、関数リファレンスに htmldoc/database/auth管理系/
  web-components/admin.js、全体地図に「テストの地図」（13ファイル・77テスト）と
  テンプレート内訳。**発見2件**: `/api/validate-parent` はフロント未使用／
  `shell_test.go` に404のテストが無い（404統一の実装時に追加すべき）。

## その前（2026-08-17・自宅）

**サニタイザを `internal/cms/htmldoc` へ切り出し、循環参照を解消した**（コード変更はこれのみ。
詳細は [変更履歴.md](変更履歴.md) 2026-08-17 の節）。2026-08-06 に見送った2案のうち
**(b) 依存を反転させる**を実施。振る舞いは不変で、`go build`/`go vet`/`go test` 全パス。

- `htmldoc` は純粋なHTML部品（import は `x/net/html`＋標準のみ）。カスタム要素の語彙は
  `htmldoc.New([]TagSpec)` へ**注入**され、`PluginTags()` を呼ぶのは cms 側の合成ラッパ
  （[sanitize.go](../internal/cms/sanitize.go)・60行）だけ。依存は `cms → htmldoc` の一方向。
- 未決だった2点の解: `TagSpec` は htmldoc に置き cms 側はエイリアス（**全プラグイン無変更**）／
  初期化順序は従来どおり `sync.Once`（「暗黙の初期化順序」のねじれは残存・全体地図参照）。

**同セッションの前半（マージ前）でやったこと**:

- **[docs/コードリファレンス/](コードリファレンス/README.md) を新設**（3本＋README。
  2026-08-06・その3の作業をこのセッションでコミット）。実装の現状の写像で、
  対象コミットは htmldoc 切り出し（`4eb3514`）まで反映済み。
- **文書整備のスキル2本とエージェントを追加**:
  [/code-reference](../.claude/skills/code-reference/SKILL.md)（書く側・既存を拡張）、
  [/doc-audit](../.claude/skills/doc-audit/SKILL.md)（検証・修正側・新設。主張を8つの型に
  割って検証し、コード側の誤りは直さず報告する）、
  [doc-keeper](../.claude/agents/doc-keeper.md)（両スキルを使い分ける文書整備エージェント）。
- **doc-audit で文書14件の誤りを修正**（詳細は当該コミット `efed265`）。中心は
  【一覧】API.md の認可の誤り——`/api/lock/force`・`/api/page-chown` は **admin のみ**、
  `/api/lock-events` は write 要、ロック直列化の入口は2種類（保存2本=`Locks.Validate`＋
  JSONボディ／残り3本=`RequireEditLock`＋`X-Lock-Token`）。ほかにカスタム要素は10種、
  `/api/load` は text/plain 化済み、など。**コード内の古いコメント5件も修正**（`3387e39`）。
- **会社の文書セッションを pull しマージ**。衝突2件（コンセプト.md・本ファイル）は
  **会社版を採用**。リンク切れ1件（4分割前の `handler.go` 参照）だけ修正した。

**注意（会社版採用の副作用）**: 本ファイル「2026-08-06・その1」の殻の行数に誤記が復活していた
（2768→198／196→76。実測は 3015→215／195→72）。→ **2026-08-19 の文書総点検で修正済み**。

**doc-audit の未検証文書**: ~~アーキテクチャとDBスキーマ・エディタ仕様・変更履歴・
【考察】各種・【ガイド】2本・【ユースケース】2本・用語集・画面構成と用語~~
→ **2026-08-19 の文書総点検で全文書を検証済み**（下の直近セッション 5）。

## その前（2026-08-16・会社・文書のみ）

コードは触っていない。**[要件定義書.md](要件定義書.md) を新設**し、**2つの前提変更**を
各文書へ反映したうえで、**要件の問題点を洗い出して5つの決定**を得た。セッション後半で
**語彙モデル（3層）を検討して根本判断を決定**し、**`タグ設計.md` を
[【考察】語彙モデル.md](【考察】語彙モデル.md) へ統合・削除**、全文書を上書き注記なしの
現在形へ刷新した（詳細は [変更履歴.md](変更履歴.md) 2026-08-16 の各節）。

| 変更前の前提 | 変更後 |
|---|---|
| カスタム要素6〜9種程度 | **最終的に100種規模を予定** |
| 社内CMS（イントラ想定） | **社内業務システム＋一般公開のウェブサイト** |

**洗い出しからの決定5件**（いずれも文書へ反映済み・実装は未着手）:

1. **画像アップロードを付ける**（png/jpeg/webp/gif・マジックナンバー検証・SVG拒否・
   検証済み種別のみインライン配信。要件定義書 §2.6）
2. **業務とサイトは別サーバー・別ドメインの2インスタンス**で運用（データ・認証は完全独立。
   誤公開は構成で防ぐ。§1.1・§4.3）
3. **匿名への応答は404で統一**（読めない＝存在しない。連番IDによる存在列挙を塞ぐ。
   例外はトップ経由の `/login` 誘導のみ。§2.1、認証認可設計 §10.5）
4. カスタム要素のサーバー側展開（公開ページのSEO）は**検討項目**
5. 公開サイトの表現力（表示専用タグ／`class` 許可リスト／テーマCSS）は**検討項目**

**同日の追加決定**:

6. **カメラ挿入**: モバイルではカメラ撮影から直接画像を挿入できること（OS標準UI。
   EXIF のサーバー側一律除去・HEIC の理由つき拒否も要件化。§2.6）
7. **SVGは「不活性な画像」として許可**（当初の拒否から変更。`<img>` 限定＋配信ヘッダ
   三点セットで無害化）。**将来のSVG編集アプリ構想**を §5 に記録——編集対象になったら
   「編集器の語彙への許可リストサニタイズ」へ拡張（本文HTMLと同じ流儀）
8. **設定は環境変数のみで確定**（設定ファイル・フラグは持たない）。§4.3 の一覧表が正本。
   `WCMS_ADDR`・`WCMS_DATA_DIR` と起動時の設定ログ（秘密は有無のみ）を新設予定（未実装）。
   運用の一覧性は systemd `EnvironmentFile=`（デプロイガイド §2 に追記）
9. **タグ設計を「標準HTML＋`data-*` マーカー」路線へ**（旧タグ設計.md は
   [【考察】語彙モデル.md](【考察】語彙モデル.md) へ統合・廃止済み）。
   明細＝生 `<table data-type>`（`<m-table>` 案を差し替え）、
   名前:値＝`<dl data-type="tags">`（`<m-tag>` の後継候補）。決め手は公開ページの
   **閲覧ゼロJS**（SEO検討項目が表・リスト形式では消える）。マーカーは `class` でなく
   `data-*`（サニタイザの `class` 拒否に例外を作らない）。配線タグ（`受信元` 等）は
   dl へ移せないため参照要素へ分離、`Tags()` の語彙宣言に標準要素＋`data-*` マーカーの
   拡張が要る——実装は従来どおり §2.5（参照追従）と一括
10. **語彙と拡張の3層モデル**——①語彙レジストリ（宣言データ）②汎用索引（コアに1実装・
   `page_tags` の一般化）③計算プラグイン（少数のGoコード）。**100種の大半は①のデータ1件**で
   増え、プラグインは「1計算ユースケース＝1プラグイン」へ。挿入経路の列挙と不変条件
   「どの経路でも出口は同じ」、エディタの形式知識3原則、汎用表エディタ（列型
   `text`/`number`/`date`/`enum`/`image`・型数に不変）を要件化（要件定義書 §2.2・§2.5）。
   **設計の正本は新設の [【考察】語彙モデル.md](【考察】語彙モデル.md)**。
11. **語彙モデルの根本判断5件を決定**（正本は同書 §9 決定ログ）: レジストリ v1 ＝
   **Goコード内の宣言テーブル**／未知 `data-type` ＝ **通す＋保存時通知**／移行＝
   **まず標準HTMLへの移行の検討から**（同書 §8 に移行マップ初版・論点A＝業務文書ブロックの
   外形が最大の未決）／定義スキーマ＝**サンプルで実装先行**／識別子＝**kebab-case**。
   **次の実装候補**: 縦切り第1段（コード内レジストリ＋汎用索引＋汎用表エディタv1 を
   新語彙「検査記録」等で end-to-end。既存9要素に触れない）。

**前提変更2点で、次の作業候補の優先度も変わった**（下の「残課題」にも反映済み）。

- **スラッシュメニューが最初に壊れる**。現状は `assets/index.html` に `.slash-menu-item` を
  **10項目手書き**し、`showSlashMenu` がそのDOMを読むだけ。100種では
  ①絞り込み・分類による到達性 ②`/api/tag-schema` からの生成 の2点が要る
  （[エディタ仕様.md](エディタ仕様.md) §3 に要件を書いた）。
- **公開専用ビューが「任意」から「必須」へ**。ウェブサイト用途では、クローム無しの体裁・
  SEO/OGP・`sitemap.xml`/`robots.txt`・キャッシュ可能な応答が要る。
  いま画面応答は一律 `Cache-Control: no-store` なので、キャッシュ導入時は
  **非公開ページと認証済み応答を絶対にキャッシュさせない切り分けが前提**
  （[要件定義書.md](要件定義書.md) §4.4、[認証認可設計.md](認証認可設計.md) §10.5）。
- **`lit` の再検討の契機に到達した**（未評価）。ただし React/Vue が選択肢外である理由
  （保存形式＝DOM・ビルド工程ゼロ）は語彙数に依らないので変わらない。
- **【一覧】カスタムタグ.md の手書き維持が破綻する**ため、`Tags()` からの生成へ移す方針を注記。
- ついでに [【ガイド】デプロイ・運用.md](【ガイド】デプロイ・運用.md) §5.1 の食い違いを修正
  （コアテーブルを `pages`/`page_tags` と書いていたが、正しくは `pages`/`page_perms`）。

## その前（2026-08-06・その2）

**サーバー側の構造を分けた**（[変更履歴.md](変更履歴.md) 2026-08-06 の該当節が詳細）。振る舞いは変えていない。

- **`handler.go`（779行）を4分割**: `handler_save.go`／`handler_view.go`／`handler_tree.go`／
  `handler_meta.go`。分割前後で**トップレベル宣言19個の集合が完全一致**することを確認済み。
- **`internal/cms/page`** を新設（保存パス・サイドカー・認可・`/data/` 配信ゲート）。
- **`internal/cms/editlock`** を新設（ロック状態＋HTTP/SSE＋`RequireEditLock`）。
  保存APIから使うマネージャは `editlock.Locks`。
- 依存は **`cms` → `editlock` → `page`** の一方向。

> **サニタイザ（`sanitize.go`）の切り出しは見送り**。`sanitize.go` が `PluginTags()` を呼び、
> ハンドラが `Sanitize` を呼ぶため、そのまま別パッケージにすると **`cms → htmldoc → cms` の
> 循環参照**になる。解くには「レジストリごと `plugin` パッケージへ出す（全プラグインで約50箇所の
> 修飾変更）」か「語彙を注入して依存を反転させる」かの**設計判断**が要る。変更履歴の該当節に
> 選択肢と代償を整理してあるので、次に着手するときはそこから読むこと。
>
> → **2026-08-17 に解消済み**。「語彙を注入して依存を反転させる」(b) を実施し、
> `internal/cms/htmldoc` へ切り出した（[変更履歴.md](変更履歴.md) 2026-08-17 の節）。

## その前（2026-08-06・その1）

**殻（`index.html`・`admin.html`）からインラインを外へ出した**（CSP strict 化の前提リファクタ。
[変更履歴.md](変更履歴.md) 2026-08-06 の節が詳細）。振る舞いは変えず、置き場所と結び方だけを変えた。

- `index.html` は **3015行 → 215行**、`admin.html` は **195行 → 72行**（どちらも markup のみ）。
  新設は `app.css`・`app.js`・`admin.css`・`admin.js`。
- **`on*=` はリポジトリ全体でゼロ**。`admin.html` のユーザー表は文字列でJSを組み立てていた
  （`onclick="resetPw('${esc(u.username)}')"`、`esc` は `'` を落とさない）ため、
  イベント委譲＋`data-*` への置換はバグ修正でもある。
- 表示/非表示は `.is-hidden` ＋ `setHidden()` に統一。**markup の `style="display:none"` をCSSへ
  移したら、JS の `el.style.display=''` も同時に潰す**必要がある（規則が残ると二度と表示できない）。
- 残るCSPの障害は `templates/*.html` の `style=`（75個）と `web-components.js` の生成 `style=`（8行）。

## その前（2026-08-05・その2）

**添付アップロードの脆弱性を修正**した（[変更履歴.md](変更履歴.md) 2026-08-05 の節が詳細）。
`POST /api/upload-pdf` が任意の名前・種類を受け付けており、保存先にはページの本文と
**権限の正本であるサイドカーが同居している**ため、write 権限だけの利用者が

1. `<id>.meta.json` を上書きして**所有者の奪取・匿名公開・mode の書き換え**（権限昇格）
2. `evil.html` を置いて `/data/master/...` から**同一オリジンのHTMLとして配信**（保存型XSS。
   本文の二層サニタイズを完全に迂回し、中間版CSPの `'unsafe-inline'` では防げない）

を実行できた。入口（`.pdf` のみ・先頭 `%PDF-` 必須・32MiB上限・ファイル名の正規化）と
出口（`nosniff`／PDF以外はダウンロード扱い／本文とサイドカーは配信しない）の両方を塞ぎ、
[upload_test.go](../internal/cms/upload_test.go) で固定した（**修正前のコードでは落ちる**ことを確認済み）。

**この監査で見た範囲**: 認証（セッション・Cookie・ロックアウト）、CSRF（Origin検証）、CSP、
本文サニタイザ、認可（`RequirePageWrite`／`RequirePageReadOrPublic`／admin API）、パス組み立て
（`GetPageDir`・`DataFileHandler`）、SQLの組み立て（すべてプレースホルダ）。**上記以外に
成立する脆弱性は見つからなかった**。残る既知の弱点は「未着手」の節を参照。

## その前（2026-08-05・その1）

**カスタム要素の Shadow DOM 方針を統一**した（[コンセプト.md](コンセプト.md)「既知の課題」の最優先項目）。
着手して分かったのは、これが見た目の不統一ではなく**編集機能が壊れていた**話だったこと。

- `<m-child-list>` だけが Shadow DOM で描画しており、その残骸 `getRootNode().host` が Light DOM の
  テンプレートに残っていた。**Light DOM では `.host` が `undefined`** なので、`<m-tag>` の値編集・
  `<m-item>` の項目編集と削除ボタンは `TypeError` で**動いていなかった**（ハーネスで再現確認）。
- **基底クラス `MElement` を導入**して全要素が継承。描画先を Light DOM に統一し、ホストへの
  書き戻しは **`data-attr`／`data-remove` に一本化**（インライン `on*=` を除去）。
- **自己更新では再描画しない**ようにした（`updateAttr`）。従来は1文字打つごとに入力欄が作り直され、
  フォーカスが飛んで実質1文字しか入力できなかった。
- `applyMode` の再描画対象が固定の要素名リストで、業務要素（`m-client-order` 等）が漏れていたのも修正。
- 確定仕様は [エディタ仕様.md](エディタ仕様.md) §0.1、経緯は [変更履歴.md](変更履歴.md)（2026-08-05）。
- **検証**: `/assets/` が認証不要なことを利用し、一時ハーネスHTMLを置いて実ブラウザで
  修正前後を確認（修正前＝3種の `TypeError`、修正後＝値の書き戻し・打鍵中のフォーカス保持・
  親子で属性が混ざらないこと・削除・閲覧モードの描画・CSS移設をすべて確認）。ハーネスは削除済み。
  **Goのテストは全パス（フロントのみの変更で影響なし）。**

---

## その前のセッション（`origin/main` 反映済み・3コミット）

| コミット | 内容 |
|---|---|
| `f221f5b` **fix** | テンプレート変数展開の保存型XSSを塞ぐ |
| `b6a735c` **feat** | 許可タグ・属性を拡張し、エディタの語彙をサーバーへ一元化（`execCommand` 廃止を含む） |
| `686a6c3` **docs** | 実装への追随とドキュメント整理（24本→22本） |

### 後半：XSS修正とドキュメント整理

- **テンプレート展開の保存型XSSを修正**。`コンセプト.md` に「未修正」と書かれていた課題が
  実際に発火する状態だった（`escapeText`/`escapeAttr` は存在したが `<m-file>` にしか
  適用されていなかった）。**サニタイザでもCSPでも止まらない**経路——`value` はURL属性でないので
  中身を検査せず、`'unsafe-inline'` はインラインイベントハンドラを許す——なので、
  **描画側のエスケープは独立して必要**。全コンポーネントへ `escapeAttr` を適用済み。
- **ドキュメントを 24本 → 22本 に整理**。削除2本（`要件定義書.md`・`【考察】【ユースケース】受注処理.md`）、
  統合2本（`【考察】拡張性とプラグイン化.md`→変更履歴、`【考察】エディタの実装方針.md`→エディタ仕様§0＋タグ設計§6.1）、
  新設2本（`【一覧】API.md`・`画面構成と用語.md`）。
- **`認証認可設計.md` を現行化**（「未実装」のままだった）、**`README.md` を刷新**（文書一覧が
  24本中12本しか無かった）。詳細は [変更履歴.md](変更履歴.md) の該当節。

### 前半：許可タグ・属性の拡張

本文で扱えるHTMLの語彙を広げ、**エディタとサニタイザを1つの宣言から導く**構造にした。
経緯は [【考察】許可タグと属性.md](【考察】許可タグと属性.md)、決定の正本は
[本文サニタイズ設計.md](本文サニタイズ設計.md) §5、まとめは
[変更履歴.md](変更履歴.md)（2026-08-04 の節）。

- **語彙の一元化**: `GET /api/tag-schema` が構造HTMLとカスタム要素の**両方**を返すようにし
  （`elements` / `void` / `block_id`）、エディタは手書きの `TEXT_BLOCK_TAGS` を捨てて
  DOMを辿る汎用シリアライザ（`serializeBlock` / `serializeNode`）に置き換えた。
  以後は `structuralElements` を直すだけでエディタが追随する。
- **許可リストの拡張**: `h1`〜`h6`・`dl`/`dt`/`dd`・ルビ・`section` 等・`details`/`summary`・
  `ins`/`del`・表の一族・`video`/`audio` などを追加（方針「タグは寛容・属性は厳格」）。
- **URL規則を2種類に分離**: リンク（`href`・`cite`）は外部可、埋め込み（`src`・`srcset`・`poster`）は
  **相対のみ**。あわせて**プロトコル相対URL `//host/…` が素通りしていた穴を塞いだ**。
- **`document.execCommand` を廃止**: Range 操作で常に `<strong>`/`<em>`/`<u>` を出す
  `toggleInlineFormat` に置き換え、`tidyInline` で空要素・入れ子・隣接・入れ子順序を正規化。

**着手前に判明していた要点**: サニタイザが許可していても、エディタのシリアライザが知らない
要素は**保存時に黙って捨てられる**（`h4`・`ul`・`table` が「画面に出るのに消える」状態だった）。
許可リストを広げる前に語彙の一元化が必要、という順序がこのセッションの肝。

**検証方法のメモ**: エディタ側のロジックは、`assets/index.html` から関数を名前で抜き出して
ハーネスHTMLを生成し、実ブラウザで動かして確認した（本体をコピペしないので実装と乖離しない）。
サーバー側はGoのテストで、**エディタの出力が無変更でサニタイザを通ること**まで含めて固定した。

（このセッションでは `docs/` 配下の通信記録処理の設計検討には触れていない。その内容は
[【考察】通信記録処理.md](【考察】通信記録処理.md) が正本のまま、下記「残課題」に引き続き残っている。）

## 現在の状態

- **ローカル main は 2026-08-19 の自宅セッションぶん（コードリファレンス追随・
  汎用表エディタ拡張・監査未達3件の解消）まで反映済み。未コミットの変更なし・
  push はユーザー判断待ち**。
- **縦切り第1段は実装・実機確認済み**（職場③）、**§5.2 の段階的追加も実施済み**
  （2026-08-19・上記）。**残る決定済みの実装候補**:
  ①縦切り第2段（`<m-tag>`→`<dl>`・`<m-material>`→表。語彙モデル §8.4-2）
  ②画像アップロード（要件定義書 §2.6・決定済み。**image 列の入力補助もこれ待ち**）。
- E2E検証で作ったテストページ（000001〜など数枚）がローカル `data/` に残っている
  （git管理外・影響なし。「DB再構築」または放置で可）。
- **通信記録の取り込みは未実装（設計のみ）**。実装に入る前に、[【考察】通信記録処理.md](【考察】通信記録処理.md) §11 の未決（特に下記）を詰める必要がある。
- 編集ロックは、本文保存に加え権限変更・所有者変更・親付け替え・公開切替まで `RequireEditLock`/`lockedFetch` で直列化済み。
- 親なし（トップレベル）にできるのはトップページ（`000000`）のみ。**匿名公開**は `public` フラグ＋パスゲートで運用中（owner/admin のみ・既定非公開）。アクセスは正規URL `/000000` に統一（`/` はリダイレクト）。
- 匿名ユーザーは実効公開ページで、本文の読み取り専用表示＋子ページ一覧＋親ページへのリンク＋目次まで見られる（右レールの属性・権限編集系は引き続き非表示）。
- ローカル検証ページが `data/` に残っている場合あり（git管理外・影響なし。「DB再構築」で整理可）。
- **編集中のアンドゥ・リドゥは実装済み**（Ctrl+Z / Ctrl+Y。本文HTMLのスナップショット方式、
  上限100、キャレットは `data-id` ＋ 文字オフセットで復元）。粒度は保存が走る単位。
  **保存済み文書の版管理（リビジョン／リバート）は未実装**で、設計のみ
  [【考察】アンドゥ・リドゥ.md](【考察】アンドゥ・リドゥ.md) §4・§5 に残っている。
- **保存はブロック単位**（変わったブロックだけ `POST /api/save-block` へ送る）。識別子
  `data-id` は**任意**で、無い本文は従来どおり全文保存にフォールバックする。
  変更が無ければ何も送らない。追加・削除・並べ替えは全文保存。正本の書き込みと `SyncIndex` は
  ページ単位のままなので、減るのは送信量とエコーバックの粒度だけ
  （[アーキテクチャとDBスキーマ.md](アーキテクチャとDBスキーマ.md) 4.1.0）。
- **カスタムタグはすべてプラグインが所有する**。`Plugin` の必須メソッド `Tags()` が語彙の唯一の
  正本で、サニタイズ許可リストと `/api/tag-schema`（エディタのシリアライザ）はそこから導かれる。
  コア（`htmldoc`・`parser.go`）は `<m-*>` を一切知らず、コアテーブルは `pages`・`page_perms`
  だけ（`page_tags` も `plugin_page_tags.go` へ移設済み）。正本は
  [【一覧】カスタムタグ.md](【一覧】カスタムタグ.md) §1 と [【ガイド】プラグイン開発.md](【ガイド】プラグイン開発.md) §2。
- **`<m-file>` は純粋なファイル容器**（`src`/`name`/`ext`）。業務上の意味は中身の要素
  （`m-client-order`/`m-supplier-order`/`m-our-estimate`/`m-supplier-estimate`）が持ち、
  `tag` 文字列による分岐は全廃した。PDFのパスは `ClosestAttr` で容器から拾う。
  値を属性から中身へ移す転換（2026-08-16 に標準HTML化へ更新: 明細＝生 `<table data-type>`・
  名前:値＝`<dl data-type>`。[【考察】語彙モデル.md](【考察】語彙モデル.md) §8）は
  §2.5 と合流させる予定で**未着手**。
- **`go test -race` が使える**（gcc 導入済み）。PATH に WinLibs の
  `~/AppData/Local/Microsoft/WinGet/Packages/BrechtSanders.WinLibs.POSIX.UCRT_*/mingw64/bin` を
  足し `CGO_ENABLED=1 go test -race ./...`。通常ビルドは Pure Go を保つため `CGO_ENABLED=0` のまま。
- **ページはサーバー合成方式**。`RootHandler` が `assets/index.html` の
  `<!--WCMS_CONTENT-->` へ本文を、`<title>` へページ名を埋め込んだ完成HTMLを返す
  （[shell.go](../internal/cms/shell.go) `RenderPageShell`）。初期表示で `/api/load` は叩かない
  （同APIは編集ロック起点の載せ替え専用として現存）。認可・404も画面側の作法でサーバーが返す
  （権限無し=403、匿名×非公開=`/login` へ302、不存在=404）。
- **本文で扱えるHTMLの語彙は「サーバーの1箇所」が正本**。`internal/cms/htmldoc/sanitize.go` の
  `structuralElements`（構造HTML）と各プラグインの `Tags()`（カスタム要素）を合成した結果が
  `GET /api/tag-schema`（`elements` / `void` / `block_id` / `vocab` / `type_inference`）として返り、
  **エディタのシリアライザはそれに従う**。エディタ側に手書きの要素リストは無い。方針は「**タグは寛容・属性は厳格**」で、
  `h1`〜`h6`・`dl`/`dt`/`dd`・ルビ・`section` 等・`details`・表の一族・`video`/`audio` まで許可。
  URLは**リンク（`href`・`cite`）が外部可／埋め込み（`src` 等）は相対のみ**。正本は
  [本文サニタイズ設計.md](本文サニタイズ設計.md) §5、経緯は [【考察】許可タグと属性.md](【考察】許可タグと属性.md)。
- **インライン装飾（太字・斜体・下線）は `document.execCommand` を使わない**。Range を直接
  操作して常に `<strong>`/`<em>`/`<u>` を出し、`tidyInline` が空要素・入れ子・隣接・入れ子順序を
  正規化する（[エディタ仕様.md](エディタ仕様.md) 4.1）。`Ctrl+B`/`I`/`U` も横取りする。
- **本文サニタイズは保存時・描画時の二層で実施済み**（許可リスト方式。実装は
  [internal/cms/htmldoc/sanitize.go](../internal/cms/htmldoc/sanitize.go)、正本は
  [本文サニタイズ設計.md](本文サニタイズ設計.md)）。保存時はサニタイズ結果をエディタへ返し、
  除去が起きたブロックだけ差し替えて通知する（エコーバック方式）。
  無検査で正本へ書き込めた未使用の `/upload` は削除済み。
- **CSP（Content-Security-Policy）は中間版を全レスポンスへ付与済み**（`auth.CSPProtect`。
  `default-src 'self'; object-src 'self'; base-uri 'self'; frame-ancestors 'self'` は目標strictと同値、
  `script-src`/`style-src` のみ暫定で `'unsafe-inline'`）。strict 版（`'unsafe-inline'` なし）への
  格上げは前提リファクタ待ち。正本は [【考察】CSP強化.md](【考察】CSP強化.md)。
- **⚠ フロントでテンプレートへ値を差すときは必ず `escapeAttr` を通すこと**
  （[web-components.js](../assets/web-components.js) 冒頭）。テンプレートは文字列置換＋`innerHTML`
  なので、素通しすると値がHTMLとして解釈される。**サニタイザでもCSPでも止まらない**——
  `value` 等はURL属性でないので中身を検査されず、`'unsafe-inline'` はインラインイベントハンドラを
  許すため。2026-08-04 に実際に発火する保存型XSSとして修正済み。`escapeAttr` は
  二重引用符の属性値と本文テキストの両方で安全なので、迷ったらこちらを使う。

## 開発環境の再現（自宅などで再開する手順）

```bash
# 1. 最新を取得
git pull

# 2. 依存取得
go mod tidy

# 3. 初回のみ管理者を作成して起動（ローカルHTTP検証なので Secure Cookie 無効）
#    ローカル開発の管理者は user=a / password=a に統一する（環境間で揃えるため。2026-07-04 決定）。
WCMS_ADMIN_USER=a WCMS_ADMIN_PASSWORD='a' WCMS_SECURE_COOKIES=0 \
  go run cmd/w-cms/main.go
# 2回目以降は環境変数なしで go run cmd/w-cms/main.go
# → http://localhost:8080 （/login にリダイレクト。a / a でログイン）

# 4. テスト
go test ./...
```

注意:
- **ローカル開発のログイン**: `a` / `a`（**ローカル検証専用**。本番では使わないこと）。
  古い環境には `admin` / `Admin` で作られた `auth.db` が残っている場合がある（2026-07-04 に `a`/`a` へ変更）。
  入れないときは下の再設定手順で揃える。
- `data/`（`cms.db` と `master/`、`auth.db`）は `.gitignore` 対象でコミットされない。**環境ごとに独立**するため、
  別環境（自宅など）では初回起動時に上記の環境変数で管理者が新規作成される。`auth.db` は同期されないので、
  「家でログインできない」は data/ が無い＝初回の管理者未作成か、別の認証情報で作成済みのいずれか。
- **既存 `data/auth.db` の認証情報を忘れた／揃えたいとき**は、サーバーを止めてから以下で揃えられる:
  ```bash
  # 一時パッケージで internal/auth を呼ぶ（ルートに別の package main があるためサブdirで実行）
  # 既存ユーザーなら SetPassword、未作成なら CreateUser を使う。
  mkdir -p _resetpw && cat > _resetpw/main.go <<'GO'
  package main
  import ("fmt"; "w-cms/internal/auth"; "w-cms/internal/database")
  func main(){ database.InitAuthDB(); defer database.AuthDB.Close()
    if err := auth.SetPassword("a", "a"); err != nil {
      if err2 := auth.CreateUser("a", "a", true, ""); err2 != nil { fmt.Println(err, err2); return }
    }
    fmt.Println("ローカル管理者 a / a を用意しました") }
  GO
  go run ./_resetpw && rm -rf _resetpw
  ```
- ローカル実行・プレビュー用に `.claude/launch.json`（`go run` 起動設定）を用意済み。
  プレビュー（http://localhost）でCookieが付くよう **`env: { "WCMS_SECURE_COOKIES": "0" }`** を設定してある。

## ユーザーへ再質問する項目（未回答・2026-08-16時点）

> 2026-08-16 のセッションで質問したが回答が得られていないもの。**関連する話題になったとき、
> または適切なタイミングで再質問すること**（ユーザー指示）。回答が得られたら本節から消し、
> 決定ログ（[【考察】語彙モデル.md](【考察】語彙モデル.md) §9 等）へ移す。

**すぐ聞いてよいもの:**

1. ~~本日の文書変更をコミットするか~~ → **回答済み・コミット済み**（2026-08-16 に4コミット＋
   正本反映の1コミット）
2. ~~次の作業は実装（縦切り第1段）か~~ → **回答済み・実装済み**（2026-08-17 職場③。
   「縦切り第1段から順番に」の指示により着工・完了。次は第2段へ順番に進む想定）
3. **監査未達3件に着手してよいか**（JSONボディ上限・`page_id` 正規化・`/assets/` キャッシュ
   制御。2026-08-05 監査で判明済み・設計判断不要の小粒な修正）

**関連作業に入るときに聞くもの:**

4. **挿入経路の優先度**（汎用表エディタの設計時）——貼り付け変換（Excel/TSV→表）を初期範囲に
   含めるか／バーコード・QRスキャン→品番入力は実務で欲しいか
5. **論点A: 業務文書ブロックの外形**（移行第3段＝受発注の移行までに）——
   `<section data-type>` で dl＋table を包む案1への傾きを提示済み
   （[【考察】語彙モデル.md](【考察】語彙モデル.md) §8.2）
6. **公開サイトの表現力3案の選択**（サイトインスタンスの設計時）——表示専用タグ／
   `class` 許可リスト／テーマCSS（要件定義書 §4.1 注記）
7. **計算ビューのサーバー事前描画をやるか**（公開専用ビューの設計時）——Web Components
   廃止決定により「マーカー容器へGoが中身を埋める」自然な形になった（旧: カスタム要素のSSR）
8. **キャッシュの許容遅延**（公開専用ビューの設計時）——公開/非公開切り替えの反映は
   「即座」が要件だが、ETag/CDN と両立させる許容値（秒か分か）が未定義
9. **`data-type` 改名の扱い**（実運用スキーマ確定時）——別名リストを持つか改名禁止か

**要否の判断待ち（洗い出しの軽微項目・2026-08-16 の問題点洗い出しより）:**

10. セッション絶対8時間と長時間編集が衝突したときの挙動（保存401・ロック喪失）を要件化するか
11. 公開サイトの運用要件（アクセスログ・可用性目標・バックアップの RPO/RTO）を定めるか
12. アクセシビリティ・サイト内検索を公開サイト要件に含めるか
13. 要件定義書に要件ID・受け入れ基準を導入するか（§5 が伸びるほどトレーサビリティが効く）

## 残課題・次の作業候補（任意）

★ **ブロック挿入UI（スラッシュメニュー）の再設計** — 2026-08-16 の前提変更（カスタムタグ100種）で
   浮上した課題。**語彙が増えると最初に壊れるのがここ**。
   ②「`GET /api/tag-schema` から生成し、殻の markup を手で足さないこと」は
   **レジストリの語彙について実装済み**（2026-08-17 職場③。項目・骨格ともレジストリから
   動的生成。カスタム要素10種の手書き項目は移行＝廃止で消える）。
   残るのは ①絞り込み入力・分類・頻度順で到達できること。
   正本は [エディタ仕様.md](エディタ仕様.md) §3.1。

★ **公開ウェブサイトとしての体裁（公開専用ビュー）** — 同じく前提変更で「任意の磨き込み」から
   **必須要件**へ格上げされた（下の 7 も参照）。クローム無しの体裁・`description`/OGP/canonical・
   `sitemap.xml`/`robots.txt`・キャッシュ可能な応答。**キャッシュを入れるなら、非公開ページと
   認証済み応答を絶対にキャッシュさせない切り分けが前提**（いまは一律 `Cache-Control: no-store`）。
   正本は [要件定義書.md](要件定義書.md) §4.4 と [認証認可設計.md](認証認可設計.md) §10.5。

★ **画像アップロード（決定済み・実装のみ）** — 2026-08-16 決定。公開サイトで画像を表示する
   経路が現状ゼロ（添付は `.pdf` のみ・埋め込みURLは相対のみ）。実装は
   [pdf_handler.go](../internal/cms/pdf_handler.go) の `allowedAttachmentExts` 拡張＋
   マジックナンバー検証、[perms.go](../internal/cms/page/perms.go) `DataFileHandler` の
   検証済み画像種別のインライン配信（`image/png` 等を明示・`nosniff` 維持）、
   エディタへの挿入手段（ドロップ／ファイル選択に加え、**モバイルではカメラ撮影から直接挿入**。
   `<input type="file" accept="image/*" capture>` のOS標準UIでよい）。
   HEIC は許可リスト外として理由つきで拒否。**EXIF（GPS位置情報等）はサーバーで一律除去**
   （orientation を捨てるなら画素の向きを補正してから保存）。サニタイザは変更不要
   （`img`＋相対URL許可済み）。要件は [要件定義書.md](要件定義書.md) §2.6。
   `upload_test.go` の流儀（修正前に落ちるテスト）に倣うこと。
   **SVGは「不活性な画像」として許可**（当初のSVG拒否から同日変更。将来SVG編集アプリを
   作る構想があるため）: `<img>` 経由のみ（画像コンテキストはスクリプト不実行）＋配信は
   `image/svg+xml`・`Content-Disposition: attachment`・レスポンス限定 CSP
   `sandbox; default-src 'none'` の三点セット、入口で `<script>`/`<foreignObject>`/`on*=`/
   `javascript:` を含むものを拒否（安全性の本体は配信側）。編集アプリ化の際は
   「編集器の語彙への許可リストサニタイズ」へ拡張する（要件定義書 §2.6 の将来項）。

★ **匿名応答の404統一（決定済み・実装のみ）** — 2026-08-16 決定。匿名×非公開の
   302（画面）/401（API）が不存在の404と区別でき、連番IDで非公開ページの存在・総数が
   列挙できてしまう。全匿名経路（`requirePageViewable`・`RequirePageReadOrPublic`・
   `/api/page-meta`・`/api/children`）を404へ。例外はトップ経由（`/`→`/000000`）の
   `/login` 誘導のみ。要件は [要件定義書.md](要件定義書.md) §2.1。

★ **通信記録の取り込み（設計を詰める／実装着手）** — 最大の未着手テーマ。正本は
   [【考察】通信記録処理.md](【考察】通信記録処理.md)。実装前に詰める §11 の主な未決：
   - `<m-quote>` の参照テーブル `page_refs` のスキーマ確定と、**部材手配計算・利益計算を `page_id` 一致から
     参照追従JOINへ書き換え**（§2.5 の決定の本体コスト。受注と進捗・原価と利益の両ユースケース文書も改訂）。
   - 取り込みエンドポイント（`.eml` ドロップ→MIME解析、`golang.org/x/text` で和文文字コード対応）と
     `<m-message>` 要素・`plugin_message.go`（`received_messages`）。
   - 分納（納品レコード＋充足の集計）・進捗 `status` の数量ベース化、添付分類（図面/ZIP/Excel）の確定。
   - 「未確認／下書き」状態の持たせ方、通信記録→文書ページの fan-out UI。
   - ※ まだ「煮詰め中」。実装着手は設計確定後でよい。

0. **画面構成の用語**: [画面構成と用語.md](画面構成と用語.md) を新設済み（呼び名と範囲の表＋ASCIIレイアウト図）。
   **残るのは実スクリーンショットへの注釈図**（吹き出し・枠を重ねたもの）。追加する場合は
   `docs/images/` を作ってそこへ置く。同文書 §5 参照。
0.5 **Web Components に残るプロトタイプ的な粗さ**（[コンセプト.md](コンセプト.md) 「既知の課題」）:
   XSS（2026-08-04）と **Shadow DOM方針の不統一（2026-08-05）** は解消済み。残るのは
   グローバル結合と外部属性変更での全体再描画。
   → **2026-08-17 に方針確定**: 個別に直さず、**Web Components の全廃**（語彙モデル §8.4 の
   移行第4段で `web-components.js`・`templates/` ごと撤去）により機構ごと解消する。
   `lit` の再評価は打ち切り。移行完了までに現行要素へ手を入れる場合は
   [エディタ仕様.md](エディタ仕様.md) §0.1 の作法に従う（新規カスタム要素は作らない）。
1. **`<aside>` サイドパネルの本レイアウト作り込み**: 現状はカードを縦に並べた素直な構成。表示の磨き込み
   （アイコン・余白・折りたたみ既定状態など）は後追いで可能。
1.5 **CSP を strict へ格上げ（前提リファクタ・第1段は完了）**: 中間版CSPは付与済み。
   **2026-08-06 に殻の外部化を完了**した——`index.html`（3015行→215行）と `admin.html`（195行→72行）は
   markup だけになり、`app.css`/`app.js`/`admin.css`/`admin.js` を新設。**`on*=` はリポジトリ全体でゼロ**。
   **残りは `templates/*.html` 10ファイルの `style=`（75個）と `web-components.js` が文字列で組み立てる
   `style=`（8行）**だけで、そこを片付ければ `'unsafe-inline'` を外せる（FOUC head スクリプトは
   per-request nonce で残す方針）。詳細チェックリストは正本 [【考察】CSP強化.md](【考察】CSP強化.md) §4。
   > **2026-08-17 再計画**: Web Components の全廃決定により、`templates/` と
   > `web-components.js` は移行第4段で**ファイルごと撤去**される。`style=` の個別除去は
   > **一切行わず**、strict 格上げの完了はその撤去に相乗りさせる（CSP強化 §4(4)(5) が正本）。
   > 移行時の注意: markup の `style="display:none"` をCSSへ移すなら、JS の `el.style.display=''` も
   > 同時に潰すこと（CSSに規則が残ると `''` では二度と表示できない）。殻では `.is-hidden` ＋
   > `setHidden()` に統一済みなので、同じ手を使えばよい。
1.6 ~~**監査で見つけた「脆弱性ではないが直す価値のある」点**~~ **3点とも解消済み（2026-08-19）**:
   JSONボディ上限（cms=8MiB/413・管理API=1MiB）・`page.NormalizeID` によるゼロ詰め6桁正規化
   （保存2本・添付2本・set-parent・page-perms・page-chown・/api/lock で適用）・
   `/assets/` への `Cache-Control: no-cache`（Last-Modified の再検証で304）。
   詳細は [変更履歴.md](変更履歴.md) 2026-08-19 の節、テストは
   [audit_fixes_test.go](../internal/cms/audit_fixes_test.go)。
2. **既存データの移行**: 旧 `<m-page-info>` 方式で作られたHTML（属性がHTML内にある）が実環境に存在する場合、
   サイドカーへ移す一度きりの移行が要る。本リポジトリの `data/` はローカル限定のため通常は不要。
   ローカル検証で作った旧テストページ（`000001` 等）が気になる場合は管理コンソールの「DB再構築」で整理可能。
3. **CSS変数のさらなる移行**: トップレベルとサイドパネルはトークン化済み。スラッシュメニュー・ブロック操作・
   スイッチ等のウィジェット内部配色は据え置き。段階的に移行可能。
4. **`updated_at` の表記ゆれ**: サイドカー由来は RFC3339、フォールバックは SQLite の `CURRENT_TIMESTAMP`。
   表示は `formatDateTime` が吸収するが、必要なら保存時に正規化を検討。
5. **同時編集の競合対策（Phase 1+2＋エディタ内操作の直列化まで実装完了・運用中。一部の設計判断のみ未決）**:
   旧来の last-write-wins による lost update は悲観ロック＋SSEで解消済み。さらに権限変更・所有者変更・
   親付け替えも本文保存と同じロックで直列化済み（共通ゲート `RequireEditLock`／フロント `lockedFetch`）。
   経緯メモは [【考察】同時編集の競合対策.md](【考察】同時編集の競合対策.md) §9 決定ログに集約。
   **残る未決**（§8「今後の再検討候補」参照）: 複数待機者の順序（FIFO 等）、任意の安全TTL、ロック多重化時の
   共有ストア化、将来の CRDT 化。いずれも現状の単一プロセス・揮発的ロックで運用上の支障は出ていない。
   将来エディタ内に画像/PDF等のリソース操作が増える際は、UIに `.edit-only`、送信に `lockedFetch`、
   サーバーに `RequireEditLock` を通すだけで同じロックに乗せられる（[アーキテクチャとDBスキーマ.md](アーキテクチャとDBスキーマ.md) 4.3参照）。
6. **フッター帯（`.site-footer`）へのリンク追加**: 現状は仮表示「w-cms」のみ。よくあるウェブサイトのように
   利用規約・お問い合わせ等のリンクを並べる想定（ユーザー予告・未着手）。
7. **匿名公開の磨き込み（~~任意~~ → 2026-08-16 に必須化。上の★へ移動）**: 一般公開のウェブサイトとしても
   使う前提になったため、以下は「任意の磨き込み」ではなく要件
   （[要件定義書.md](要件定義書.md) §4.4）。現状の匿名ビューは本文の読み取り専用表示＋子ナビ＋
   親ページへのリンク＋目次まで実装済み。
   - **子ナビ・パンくず（親への遡り）は実装済み**: `/api/children`・`/api/page-meta` を `OptionalAuth` 化し、
     匿名には実効公開（`EffectivePublic`）の子・親情報だけを絞って返す
     （[handler_tree.go](../internal/cms/handler_tree.go) の `ChildPagesAPIHandler`・
     [handler_meta.go](../internal/cms/handler_meta.go) の `PageMetaAPIHandler`）。フロントは左レールの子ページ一覧・親ページへのリンクを
     匿名にも表示（右レール・子ページ作成ボタン等の書き込み系は引き続き非表示。`body.anonymous` のCSS、
     [index.html](../assets/index.html)）。
   - **公開ページ専用レンダリング（必須）**: いまは編集用テンプレートをCSSでクローム非表示にしている。
     クローム無しの体裁・SEO/OGP/canonical・`sitemap.xml`/`robots.txt`・キャッシュ可能な応答が要る。
     **キャッシュ導入時は非公開ページと認証済み応答を絶対にキャッシュさせないこと**
     （現状は一律 `Cache-Control: no-store`）。
   - 既定トップページ本文に残る「右上のスイッチで編集モード…」は匿名には不適。公開運用時は本文を整える。
   - **エラー画面**: 404・403 が一般の訪問者にも意味の分かる体裁であること。
