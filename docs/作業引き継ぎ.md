# 作業引き継ぎ

> 別の環境（自宅・職場など）で作業を再開するためのスナップショットです。
> 内容が古くなったら更新し、新しいタスクに入ったらこのファイルを差し替えてください。
> 最終更新: 2026-06-28（設計検討セッション継続。通信記録の取り込みに加え、**文書命名規約（【考察】/【ガイド】/【ユースケース】/【一覧】）の確立**と、アンドゥ・リドゥ／エディタ実装方針／**タグ設計**の考察を追加。コードは無変更・ドキュメントのみ）

## 直近のセッションでやったこと（設計のみ・実装なし・`origin/main` 反映済み）

**「通信記録の取り込み（メール・FAX・電話 → ページ化）」を設計検討**し、設計メモと関連ドキュメントを整備した。
**コードは一切変更していない**（`go test` は前回から不変で全パスのまま）。正本は
[【考察】通信記録処理.md](【考察】通信記録処理.md)（中身は多チャネルに拡張済み・状態は「検討中／煮詰め中」。2026-07-02 に `【考察】通信記録の取り込み.md` から改称）。

このセッションで作った／直したドキュメント：

- **新規** [【考察】通信記録処理.md](【考察】通信記録処理.md): 通信記録取り込みの設計メモ（中身は多チャネルに拡張・状態は「検討中／煮詰め中」）。主な節：
  - §2.5 **1文書＝1ページ＋参照ハブ＋参照追従集計**（最重要決定）、§2.6 **文書ページの独立性と「格納/集計/由来」3軸**
    （受注は通信記録と独立・変更/取消可、物理保存はフラット `data/master/{prefix}/{id}/`・論理階層は `parent_id` の木、
    集計は「キュレート参照ハブ」と「計算ビュー（受注残・納期ビュー）」の2モード）。
  - §3 人間ゲート型取り込みEP（`.eml` ドロップ）、§4 `<m-message channel>` 通信記録ブロック、§4.1 添付分類、
    §4.2 図面↔部品の多対多、§4.3 **分納（受注数は納品で不変・残は計算／受注変更では改訂）・納期は部品単位・グルーピング**、
    §4.4 引用要素 `<m-quote>`／`page_refs`、§4.5 **`<m-file>` 責務分離（ファイル容器＋中身の `<m-order>` 等）**、
    §4.6 **変更の2パターン（その場編集＝受注／版管理＝仕様・図面）**、§4.7 **版管理の仕組み（1版=1ページ・追記型・後ろ向きリンク・ピン）**。
  - §9.1 **原本性と自己完結性の分離**＋「正本性/受信原本/引用の原本1つ」3層の区別＋由来参照（受信元）。
  - **本セッション最後にメモ全体をリファクタ**し、古い「1メール=1ページ／通信記録に業務明細／既存プラグイン無変更」前提を
    §2.5 の確定方向へ整合（§3.2・§4・§6・§10、コミット `f0357c5`）。
- **新規** [【一覧】カスタムタグ.md](【一覧】カスタムタグ.md): 現行**実装済み**カスタムタグのリファレンス
  （m-tag/m-file/m-item/m-material/m-child-list/m-required-materials、m-file の tag サブタイプ、タグ→テーブル早見）。
  `<m-file>` には再設計予定（§4.5）の注記あり。
- **新規** [【一覧】用語集.md](【一覧】用語集.md): 業務ドメインの中核語彙の正本。**受け取る側/出す側＝`受注`/`発注`、
  見積2種＝`販売見積`/`購入見積`** に確定（予定→確定・売上側/原価側の2×2）。ページID、部品/図面/部材、
  納品/分納、受注残・納期（部品単位）・納期ビュー、版管理（リビジョン）、受信元（由来参照）、
  正本性/受信原本/自己完結性等も整理。既存コードの呼称は実装時にこの用語へ揃える。
- **修正** [開発方針.md](開発方針.md) §1 の承認済み外部依存リストを go.mod の実態に同期（x/net・generative-ai-go・
  google.golang.org/api を追記、通信記録で使う x/text も承認）。[アーキテクチャとDBスキーマ.md](アーキテクチャとDBスキーマ.md)・
  [エディタ仕様.md](エディタ仕様.md) の PDF 保存パス表記をプレフィックス付き（`data/master/{prefix}/{id}/`）に補正。
- **命名規約の確立**: 文書を種別接頭辞で分類（`【考察】`検討メモ／`【ガイド】`手順／`【ユースケース】`業務／`【一覧】`参照。`【考察】` は状態マーカーとして重ねられる）。[開発方針.md](開発方針.md) §2 に規約化し、既存docを一括リネーム（プラグイン開発ガイド→`【ガイド】プラグイン開発`、ユースケース_*→`【ユースケース】*`、用語集→`【一覧】用語集`、カスタムタグ一覧→`【一覧】カスタムタグ`、同時編集の競合対策→`【考察】同時編集の競合対策`、拡張性とプラグイン化の考察→`【考察】拡張性とプラグイン化`、メール取り込み→`【考察】通信記録の取り込み`）。参照（docs・README・コードコメント）とH1も全更新（旧名残存なし・`go build` 通過）。
- **新規** [【考察】アンドゥ・リドゥ.md](【考察】アンドゥ・リドゥ.md): 編集中＝独自スナップショットundo（ネイティブの不整合を回避）、保存済み＝**本文HTMLのみ**の線形バージョンログ＋リバート（サイドカー/付け替え/添付は対象外）、各版 gzip。
- **新規** [【考察】エディタの実装方針.md](【考察】エディタの実装方針.md): バニラ（Web Components）維持／React等は不向き、`lit` は粗さ一掃の候補、ProseMirror等は堅牢undo・共同編集が要件化したら。§6 **編集の2面（値=インライン／属性=インスペクタ）**。
- **新規** [【考察】タグ設計.md](【考察】タグ設計.md): **役割=属性／値=中身**（表の列=役割・セル=値、機械可読性は構造で担保）。**`<m-field>`=親ブロックの単一項目／表=繰り返し明細／`<m-tag>`=ページメタ**の使い分け。**`<m-table>`=汎用編集テーブル**（自己記述ヘッダ＋contenteditable＋行操作、列の役割/型はインスペクタで編集）。
- **新規（ユーザー加筆中）** [【考察】【ユースケース】受注処理.md](【考察】【ユースケース】受注処理.md): 新モデルでの受注（capture＝受け取り）ユースケースのスカフォールド。進捗管理（運用・可視化）は別文書に分ける方針。

**この設計セッションの最重要決定（§2.5）**: ページの単位を「案件／メール」ではなく **「1文書＝1ページ」** とし、
案件は文書ページを `<m-quote>` で束ねる**参照ハブ**として表す。集計は **`page_id` 一致ではなく参照を辿る方式**に
作り替える。これに伴い [【ユースケース】受注と進捗管理.md](【ユースケース】受注と進捗管理.md)・[【ユースケース】原価と利益.md](【ユースケース】原価と利益.md)
の集計前提（現行は `page_id` 単位）は**実装着手時に改訂が必要**。

**用語の確定**: 業務語を `受注`／`発注`／`販売見積`／`購入見積` に統一（[【一覧】用語集.md](【一覧】用語集.md)）。
既存コード・ドキュメントの呼称（`client_orders`／`顧客の発注書` 等）は §2.5・§4.5 の実装時にこの用語へ揃える。

（その前のセッションでは「匿名公開＝ログイン不要の閲覧」を実装・プッシュ済み。仕様は [認証認可設計.md](認証認可設計.md) §10。
コミット `9ffc035`/`9b10823`/`4c49176`。）

## 現在の状態

- ブランチ `main`。直近の設計ドキュメントは `origin/main` 反映済み。**アプリのコードは無変更**で `go test ./...` 全パス（前回から不変）。
- **通信記録の取り込みは未実装（設計のみ）**。実装に入る前に、メモ §11 の未決（特に下記）を詰める必要がある。
- 編集ロックは、本文保存に加え権限変更・所有者変更・親付け替え・公開切替まで `RequireEditLock`/`lockedFetch` で直列化済み。
- 親なし（トップレベル）にできるのはトップページ（`000000`）のみ。**匿名公開**は `public` フラグ＋パスゲートで運用中（owner/admin のみ・既定非公開）。
- ローカル検証ページが `data/` に残っている場合あり（git管理外・影響なし。「DB再構築」で整理可）。

## 開発環境の再現（自宅などで再開する手順）

```bash
# 1. 最新を取得
git pull

# 2. 依存取得
go mod tidy

# 3. 初回のみ管理者を作成して起動（ローカルHTTP検証なので Secure Cookie 無効）
#    ローカル開発の管理者は user=admin / password=Admin に統一する（環境間で揃える）。
WCMS_ADMIN_USER=admin WCMS_ADMIN_PASSWORD='Admin' WCMS_SECURE_COOKIES=0 \
  go run cmd/w-cms/main.go
# 2回目以降は環境変数なしで go run cmd/w-cms/main.go
# → http://localhost:8080 （/login にリダイレクト。admin / Admin でログイン）

# 4. テスト
go test ./...
```

注意:
- **ローカル開発のログイン**: `admin` / `Admin`（このパスワードは**ローカル検証専用**。本番では使わないこと）。
- `data/`（`cms.db` と `master/`、`auth.db`）は `.gitignore` 対象でコミットされない。**環境ごとに独立**するため、
  別環境（自宅など）では初回起動時に上記の環境変数で管理者が新規作成される。`auth.db` は同期されないので、
  「家でログインできない」は data/ が無い＝初回の管理者未作成か、別パスワードで作成済みのいずれか。
- **既存 `data/auth.db` のパスワードを忘れた／揃えたいとき**は、サーバーを止めてから以下で再設定できる（admin を `Admin` に）:
  ```bash
  # 一時パッケージで internal/auth.SetPassword を呼ぶ（ルートに別の package main があるためサブdirで実行）
  mkdir -p _resetpw && cat > _resetpw/main.go <<'GO'
  package main
  import ("fmt"; "w-cms/internal/auth"; "w-cms/internal/database")
  func main(){ database.InitAuthDB(); defer database.AuthDB.Close()
    if err:=auth.SetPassword("admin","Admin"); err!=nil { fmt.Println(err); return }
    fmt.Println("admin のパスワードを Admin に再設定しました") }
  GO
  go run ./_resetpw && rm -rf _resetpw
  ```
- ローカル実行・プレビュー用に `.claude/launch.json`（`go run` 起動設定）を用意済み。
  プレビュー（http://localhost）でCookieが付くよう **`env: { "WCMS_SECURE_COOKIES": "0" }`** を設定してある。

## 残課題・次の作業候補（任意）

★ **通信記録の取り込み（設計を詰める／実装着手）** — このセッションの主題。正本は
   [【考察】通信記録処理.md](【考察】通信記録処理.md)。実装前に詰める §11 の主な未決：
   - `<m-quote>` の参照テーブル `page_refs` のスキーマ確定と、**部材手配計算・利益計算を `page_id` 一致から
     参照追従JOINへ書き換え**（§2.5 の決定の本体コスト。受注と進捗・原価と利益の両ユースケース文書も改訂）。
   - 取り込みエンドポイント（`.eml` ドロップ→MIME解析、`golang.org/x/text` で和文文字コード対応）と
     `<m-message>` 要素・`plugin_message.go`（`received_messages`）。
   - 分納（納品レコード＋充足の集計）・進捗 `status` の数量ベース化、添付分類（図面/ZIP/Excel）の確定。
   - 「未確認／下書き」状態の持たせ方、通信記録→文書ページの fan-out UI。
   - ※ まだ「煮詰め中」。実装着手は設計確定後でよい。

0. **画面構成の用語を画面画像つきで可視化**（ユーザー要望・予定）: ヘッダー／レール（左右の
   サイドパネル）／ドロワー／文書（doc-column）／カード等の**呼び名と範囲**を、実際のスクリーンショットに
   注釈（吹き出し・枠）を重ねた図で示すドキュメントを用意する。置き場所は `docs/`（例: `画面構成と用語.md`）。
   既存の用語定義は [エディタ仕様.md](エディタ仕様.md) 9.1（3ペイン表）にあるので、それと突き合わせて図示する。
1. **`<aside>` サイドパネルの本レイアウト作り込み**: 現状はカードを縦に並べた素直な構成。表示の磨き込み
   （アイコン・余白・折りたたみ既定状態など）は後追いで可能。
2. **既存データの移行**: 旧 `<m-page-info>` 方式で作られたHTML（属性がHTML内にある）が実環境に存在する場合、
   サイドカーへ移す一度きりの移行が要る。本リポジトリの `data/` はローカル限定のため通常は不要。
   ローカル検証で作った旧テストページ（`000001` 等）が気になる場合は管理コンソールの「DB再構築」で整理可能。
3. **CSS変数のさらなる移行**: トップレベルとサイドパネルはトークン化済み。スラッシュメニュー・ブロック操作・
   スイッチ等のウィジェット内部配色は据え置き。段階的に移行可能。
4. **`updated_at` の表記ゆれ**: サイドカー由来は RFC3339、フォールバックは SQLite の `CURRENT_TIMESTAMP`。
   表示は `formatDateTime` が吸収するが、必要なら保存時に正規化を検討。
5. **同時編集の競合対策（Phase 1+2＋エディタ内操作の直列化まで実装完了・運用中。一部の設計判断のみ未決）**:
   旧来の last-write-wins による lost update は悲観ロック＋SSEで解消済み。さらに権限変更・所有者変更・
   親付け替えも本文保存と同じロックで直列化済み（共通ゲート `RequireEditLock`／フロント `lockedFetch`）。
   経緯メモは [【考察】同時編集の競合対策.md](【考察】同時編集の競合対策.md) §9 決定ログに集約。
   **残る未決**（§8「今後の再検討候補」参照）: 複数待機者の順序（FIFO 等）、任意の安全TTL、ロック多重化時の
   共有ストア化、将来の CRDT 化。いずれも現状の単一プロセス・揮発的ロックで運用上の支障は出ていない。
   将来エディタ内に画像/PDF等のリソース操作が増える際は、UIに `.edit-only`、送信に `lockedFetch`、
   サーバーに `RequireEditLock` を通すだけで同じロックに乗せられる（[アーキテクチャとDBスキーマ.md](アーキテクチャとDBスキーマ.md) 4.3参照）。
6. **フッター帯（`.site-footer`）へのリンク追加**: 現状は仮表示「w-cms」のみ。よくあるウェブサイトのように
   利用規約・お問い合わせ等のリンクを並べる想定（ユーザー予告・未着手）。
7. **匿名公開の磨き込み（任意）**: 現状の匿名ビューは本文の読み取り専用表示まで。次の改善余地がある。
   - **公開ページの子ナビ・パンくず**: `/api/children`・`/api/page-meta` は要認証のままで、匿名ビューでは
     辿れない（公開された子だけを匿名に見せるなら、実効公開を条件にこれらを `OptionalAuth` 化する設計判断が要る）。
   - **公開ページ専用レンダリング**: いまは編集用テンプレートをCSSでクローム非表示にしている。SEO・OGP・
     キャッシュ等を本格対応するなら、軽量な公開専用ビューを別に用意する案もある。
   - 既定トップページ本文に残る「右上のスイッチで編集モード…」は匿名には不適。公開運用時は本文を整える。
