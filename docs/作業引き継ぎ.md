# 作業引き継ぎ

> 別の環境（自宅・職場など）で作業を再開するためのスナップショットです。
> **現況と次の一手だけ**を書きます——経緯は [変更履歴.md](変更履歴.md)、決定の理由は
> 各設計書の決定ログが正本です。**ここに残すのは、知らずに書くと壊れるものだけ。**
>
> 最終更新: 2026-09-21（自宅・夜）。**社名の法人格は、連絡帳に居なくても落ちます**
> ——ユーザー:「発注元タグに株式会社が入るのが気になります」。⚠ 09-20 の口は
> **候補が見つからないと読んだ名前のまま**だったので、**新しい客先の1通目では一度も
> 揃いませんでした**（そして完全一致で結ぶ2つの木が黙って結ばれない）。
> いまは `contacts.OrgNameForPage` の1つの口——候補1つならその実物の題、
> 2つ以上なら読んだまま（⚠ 第三の名前を作らない）、0なら法人格を落とす。
> 同日、**表の折り返しは見出しで決めるようになりました**
> ——`備考` だけ折り返し、ほかは1行。⚠ **表は全幅で自分の中を横スクロール**します
> （それまで広い画面では**ページごと横へ伸びて**いました）。⚠ **元から在った不具合を
> 1つ直しました**——セルの印は**編集モードに入るときだけ**付いていて、閲覧で開き直すと
> 消えていました（「一度編集へ入ると直る」という気づきにくい形）。
> **検算の合格が薄い緑＋`✓`** になりました（⚠ 赤と緑は赤緑色覚で近いので、
> 色だけに頼らず印の形で見分けます）。番人は `e2e/verify-table-wrap.js`。
> 同日、**受注ページに原本のPDFが載りました**
> ——「読んだまま」の写しの上に、**通常は畳んだ状態**で。並びは
> **原本（PDF）→ 読んだまま → 弊社の明細**＝確かさの順です。あわせて
> **弊社の明細の品名・品番・単位・納期を正規形で書き起こす**ようにし
> （⚠ 実データの品名が**半角カナ**でした——`ブラケット` と `ﾌﾞﾗｹｯﾄ` は画面では
> 似て見えるのに検索では別物）、**品番に何を入れるかを Gemini に選ばせ、
> 選んだ列を本文に1行残す**ようにしました（⚠ **0c の積み残し**——それまで
> 元の見出しはどこにも残らなかった）。記録は [変更履歴.md](変更履歴.md) 2026-09-21 夜。
> ⚠ **「最短納期」は、並べると先頭・絞ると消えます**（実測。下の「罠」）。
> 同日の午前は **Go の全ファイルを順にリファクタリングしました**
> ——振る舞いは不変・全試験緑・`staticcheck` 緑。写されていた入口・走査・SQL を
> コアの口（`GateJSONPost`・`TagRowsNamed`・`ChildPages`・`openUpload` 等）へ寄せました。
> ⚠ **元から在った不具合を1件直しました**（連絡帳の登録で担当者を入れたとき、
> アドレスを足せなかったエラーが黙って落ちていた）。記録は [変更履歴.md](変更履歴.md) 2026-09-21。
> ⚠ **`assets/app.js` は触っていません**（番人が E2E だけなので別の回に）。
> 前日 09-20 は**発注書の検算が入りました**
> ——`数量 × 単価 = 金額`／`Σ金額 = 小計`／`小計 + 消費税 = 合計`。
> ⚠ **`Σ金額 = 小計` だけが「行の落丁」を捕まえます**（残った行は互いに辻褄が
> 合ったままなので、行の検算では気づけません）。⚠ **検算を Gemini にやらせません**
> ——自分の読みを自分で検算させると数字を合うように書き直し、**誤りが消えるのでは
> なく見えなくなります**。同日、**「解析に失敗しました」の原因が判明**
> （`source_table` を構造に直接当てていたので、Gemini が行を object で返すと
> **応答全体が落ちていた**）。受注明細に**行の納期・出荷済み・キャプション**も。
> ⚠ **`config/settings.json` に3語足したので DB再構築が要ります**（下の「環境」）。
> ⚠ **既にある受注ページには小計のタグがありません**——もう一度 🤖 解析が要ります。
>
> 同日（深夜）、⚠ **公開リポジトリの履歴を書き換えました**
> ——実在の顧客名・人名・アドレス・図番を、**698コミットすべてと全コミットメッセージ**
> から落として強制プッシュしました。**他の環境は再クローンが要ります**（下の「環境」）。
> 同日、**図面の重複判定を機械にさせるのをやめました**
> ——部品図と溶接図、1つのPDFに複数の図面、同じ図番で別図面……**手掛かりが1つも
> 残らない**ことが4段階で分かり、「機械は疑わしいと言うまで、決めるのは人」に落ち着き
> ました。⚠ その過程で**打ち消し合っていた2つの誤り**（本文の切り出し）が出ています。
> 同日、社名の表記ゆれ 0b が3箇所とも完了、**登録された語彙だけDBに入る**、
> **表は `<caption>` で名乗れる**、加工製品テンプレートを作成。
> 前日 09-18 は**WebDAV で CAD を編集する輪が、実物で閉じました**
> ——RootPro CAD の `.rpcd` を開き、線を引き、保存し、w-cms に届いて版が残り、
> 引いた線が往復しました。⚠ **ただし Ctrl+S では届きません**（アプリを閉じるまで
> Windows が溜める）。同日、**開いただけで添付が0バイトになる穴**を塞ぎました。
> 前日 09-17 は**図面から作るページの肩書が「加工製品」になりました**
> （お客様から見れば装置の部品、当社から見れば作って売るもの）。取引先から連絡帳への
> 参照タグも `相手` → **`連絡帳`** に。同日、**メール添付の ZIP は取り込みで展開する**ようになり——中の
> ファイルが1つずつ添付になり、加工製品ページのファイル表示が PDF を開きます。あわせて
> メールの添付が人が落とす口と同じ検査を通るようになり、和文の添付名の化けも直りました。
> **FAX＋APサーバーの機械を購入**（HP のワークステーション・Windows 11。モデムと
> ディスプレイは未購入）。前日 09-16 は**アドレス帳の作り直しが5段とも完了**
> ——`ドメイン` が組織の連絡先タグになり、木が `連絡帳`／`取引先` に分かれ、
> 2つの木は参照で結ばれ、「メールは来ているのに結びついていない相手」が光ります。
> 同日、**拡張の組み替えも3段目まで完了**（3d-3・3d-4）で**コアのコードから業務語がゼロ**
> （`/api/parse-pdf` を下請けへ寄せた）、**管理画面に「置き場」と「データの初期化」**が入りました。
> **次は「次にやること」の1番以降**（0番のアドレス帳が片付いたため）。
> 前日（09-15）に**アドレス帳を `ext/comm/contacts` へ出し**
> （拡張どうしの import が解禁・素の w-cms から業務語が消えた）、**タグの畳んだ値の宣言型をやめ**
> （型は値が持つ）、アドレス帳の**判断待ちがゼロ**になりました。同日、**ファイル表示の配線を属性
> `data-ref` へ出しました**（中の参照タグは廃止・PDF以外も開く）。
> **次の一手は「次にやること」0b2**（発注書から受注明細へ）。⚠ **着手の前に、発注書
> `250715-304　発注書.pdf` を人が開くこと**——品番が7行すべて空で、顧客の表の見出しが
> 何なのかデータからは分かりません。**0c の答えもそこにあります**。
> ⚠ **`InsertRevisionRow` の文字列検索**（罠の節）を、`caption` 化の前に直すこと。
> 組み替えの続き（4段目の FAX）は、FAXサーバーを用意してからです。
>
> 文書の構成: **いまここ**（この1週間）→ **次にやること** → **決定はしたが着手は先** →
> **現状維持と決まったもの**（蒸し返さない）→ **未決** → **罠** → **環境** → **再現手順**。
> 2026-09-15 に組み直しました（912行→この形。中身は移しただけで、消していません）。

## いまここ（この1週間で変わったこと）

**2026-09-20（夜）——発注書の検算**

- **数どうしの辻褄で OCR を確かめます**（ユーザー:「顧客の発注書に数量と単価が
  あって、金額や総額もあるなら、検算が出来ると思います」）。**外の正解なしに
  読み取りを確かめられる、たぶん唯一の手段**です——他の検査はすべて「人が紙と
  見比べる」に帰着します。正本は [変更履歴.md](変更履歴.md) 2026-09-20 夜。
  - ⚠ **`Σ金額 = 小計` だけが「行の落丁」を捕まえます。** 1行まるごと落ちても
    **残った行は互いに辻褄が合ったまま**なので、行の検算は全部通ります。
  - ⚠ **`小計 + 消費税 = 合計` に税率を使いません。** `小計 × 税率` と書くと
    10%／8%（軽減）を当てにいくことになります。
  - ⚠ **検算を Gemini にやらせないこと。** 自分の読みを自分で検算させると
    **数字を合うように書き直し**、誤りが消えるのではなく**見えなくなります**。
    プロンプトに「足し算・掛け算をして求めないでください」と明示済み。
  - ⚠ **辻褄が合うことは「正しい」を意味しません**（揃えて読み違えることはある）。
    **合わないときに疑える、それだけの道具です。**
- **小計・消費税・合計金額はページのタグ**（ユーザー:「変形した表になっていて、
  **表の右下の方に、小計、消費税、合計金額が飛び出しています**」）。書面のヘッダに
  ある値なので納期と同じ扱い。⚠ **飛び出しているので Gemini は明細の行としても
  返してきます**——3つの数が揃う行だけ足して二重計上を避け、外した行数は知らせます。
- **⚠ は鏡型**で弊社の受注明細の足元（`tfoot`）に出ます。本文には書きません
  ——焼き込むと人が数字を直しても ⚠ が残ります。⚠ **原本側に出さないのは
  `<details>` で畳まれていて「開くまで読まれない警告」になるため**。
  ⚠ **合っているときも黙りません**（黙ると「検算して合った」と「そもそも検算して
  いない」が見分けられない）。
- **「解析に失敗しました」の原因が判明**（ユーザー報告）。`source_table` を**構造に
  直接当てていた**ので、⚠ **Gemini が行を object（見出しが鍵）で返すと応答全体が
  読めなくなり**、発注書番号も明細も丸ごと失われていました。いまは
  `json.RawMessage` で受けて寛容に読み、**読めなければ原本の枠を出さないだけ**。
  **おまけのために本題を落とさない。** エラー文に**返答の頭300文字**も添えます。
- **受注明細に行の納期・出荷済み・キャプション**。行の納期は受注時にページのタグと
  同じ値を**配ります**（「その後顧客の依頼や弊社の事情で個別に納期が変わる」）。
  出荷済みは空で始めます（⚠ 0 を埋めると人が入れた 0 と見分けが付かない）。

**2026-09-20——図面の重複判定を機械にさせるのをやめた**

- ⚠ **公開リポジトリから実在の顧客情報を落としました**（ユーザー決定:「公開リポジトリの
  顧客名は消してください」「履歴は丸ごと消して大丈夫です」「自社のアドレスも消して
  よいです。ジャンクメールが来るとめんどくさいので」）。2026-09-07 から「未回答の質問」に
  残っていた件で、**外へ向ける話が出たことで判断が出ました**。
  - 落としたもの: 社名・人名・アドレス（自社を含む）・装置名称・図面名称・図番・発注書番号。
    **作業ツリー 84ファイル/534箇所＋履歴 698コミット＋全コミットメッセージ**。
  - ⚠ **`--replace-text` はコミットメッセージを書き換えません**（`--replace-message` が
    別に要ります）。一度それで落としかけました。
  - ⚠ **置換漏れが5回、試験に出ました**——部分形（`P103-227` だけ残る）・大小混在・
    アンダースコア形・全角ハイフン／長音、そして**置換先に長音を入れて落ちた**件
    （`フレーム` は `code` の畳み込みで `フレ-ム` になる）。**長いものから当てること**、
    **置換先に長音を入れないこと**。
  - **控え**: `デスクトップ\w-cms-履歴の控え-20260920.bundle`（14.5MB・検証済み）。
  - ⚠ **「完全に消えた」とは言えません**——GitHub の内部にしばらく残り（古いハッシュを
    直接指せば見えることがある。消したいならサポートへ GC を依頼）、誰かが既に
    clone/fork していればその写しにも残ります。
- **受注明細に列が3つ増えました**（`bdf48d1`）:
  `弊社品番(ref) / 品番(code) / 品名 / 単価 / 数量 / 納期(date) / 備考 / 状態`。
  ⚠ **`弊社品番` は加工製品ページのページ番号**（ユーザー:「加工中に番号を使うので」）。
  `品番` は**顧客の言葉**で別の列です。⚠ **結ぶのは人**なので解析は空で出します。
  ⚠ **見出しは宣言から組みます**——手書きに戻すと**足した列がどこからも読めません**。
- **0c が実データで解けました**（`23a8ec4`）。ユーザー:「この客先は品番を送らないので、
  **図番を品番として入れてきた**」。⚠ なので **`品番` は `code` 型**にしました
  （`図面番号` と揃えないと、**同じ番号が置き場所によって別の鍵になります**）。
  ⚠ **設定を変えたので DB再構築が要ります**（`POST /api/rebuild-db`）。

- ⚠ **手掛かりが4段階で消えました**（この順番に意味があります）。「加工製品ページには
  複数の図面が入る」→「部品図と溶接図で**品物としては一つ**」→「**一つのPDFに複数の
  図面**が入っている」→「**同じ図面番号で別図面**が入っているものがあった」。
  最後には機械が使える手掛かりが**1つも残りません**。正本は
  [変更履歴.md](変更履歴.md) 2026-09-20。
- **整理に「二つ目の図面として追加」**（`mergeAsDrawing`）。行き先に同じ題のページが
  あるとき、**改定か・同じ品物の別図面か**を人が選びます。⚠ **既定は未選択**で、
  選ばない行は動きません（`needs_choice`）。
  ⚠ **それまでは黙って改定**——部品図と溶接図も「図面番号が違う」ので、**片方が旧版
  として子ページへ押し込まれて**いました。
- **打ち替えのたびに行き先を聞きます**（`GET /api/filing-target`）。
  ⚠ **調べるだけで1枚も作りません**（`ensureChildPage` を使うと問い合わせのたびに
  空のページが増え、**増えたページが次に「在る」と答えます**）。
- **1つのPDFに複数の図面 → 1枚につき1ページ**。⚠ それまでプロンプトが単数で、
  **3枚入っていても1枚ぶんしか記録されません**でした。**まとめるのは人**——
  「一つのPDFに複数の製造製品が入っている場合があるから」（最初から1ページにすると
  別物だったとき切り離せない）。
- **社名の表記ゆれ 0b が3箇所とも完了**（連絡帳の登録・受注の整理）。⚠ 連絡帳が
  **発生源**でした。受注は `発注元` を直せる欄を足し、本文へ書き戻します。
- **登録された語彙だけDBに入る**（ユーザー決定）。⚠ それまで型が解決しない表も
  **形式名が空のまま索引に入って**いました。
- **表は `<caption>` で形式を名乗れる**。⚠ `data-type` へ寄ったのは **09-18 に節を
  やめた副作用**で、意図した判断ではありませんでした。①属性 ②節の機能見出し
  ③表の caption の順で解決します（①が優先なので既存の本文は壊れません）。
- **加工製品のテンプレート**（`テンプレート／加工製品`）。図面・改訂履歴・材料・
  外注加工・購入部品・支給部品。⚠ **`data-id` は書きません**——複製で振り直されない
  ので、改定の合流で衝突します。
  ⚠ **解析をテンプレートで埋める形にする**のはユーザー合意済み・**未着手**
  （テンプレートが無いときのフォールバックと、題でテンプレートを引く口が要る）。

**2026-09-18（夜）——WebDAV で CAD を編集する輪が閉じた**

- **実物の RootPro CAD で往復しました**（`.rpcd` ＝ **OLE複合ドキュメント**）。
  開く → 線を引く → Ctrl+S → **閉じる** → w-cms に届き、前の中身が版に残り、
  ⚠ **引いた線が本当に残っていました**（ユーザー確認）。`.rpcd` はこれまで
  許可リストに1行あるだけで**実物では一度も確かめていませんでした**。
- ⚠ **Ctrl+S では届きません。アプリを閉じて初めて届きます**（実測で75秒後）。
  Windows の `WebClient` が手元に溜めて、最後のハンドルが閉じたときに書き戻すため。
  **サーバー側では直せません。** 下の「罠」に詳しく。
- **穴を1つ塞ぎました**——`x/net/webdav` は **PROPPATCH でも `O_RDWR` で開く**ので、
  「書きの気配があれば一時ファイルを作り、Close で無条件に被せる」形が
  **1バイトも書かないまま添付を0バイトにしていました**。いまは一時ファイルが
  空なら差し替えません。⚠ **`PROPPATCH` を通す判断と一組**で、片方だけでは壊れます。
  正本は [変更履歴.md](変更履歴.md) 2026-09-18。
- **加工製品の展開ファイル**（ユーザー:「これをw-cmsでもできるようにしたい。しかも自動で」）。
  レーザー加工用の展開を RootPro CAD で描き、`.rpcd` を加工製品ページに貼り、
  **ファイル名を手で図面名称に打ち替えて**いる——その手作業を無くす話。**設計は合意済み・実装は未着手**:
  - **引き金は人が押す**（「要らない製品もあります」）。全ページに自動で置かない。
  - **名前は焼いてよい。** ⚠ **整理の時点で3つ揃う**——書ける場所へ移り
    （`webdav_readonly` は通信箱）、人が図面名称を確定し、ページの題がそれになる。
  - **1ページに1つ**（当面）。**雛形は空のファイル**なのでコピーの機構は小さい。
  - 汎用の形: テンプレート置き場に雛形ページを1枚（添付1つ＋`ファイル名` タグに
    `展開　{図面名称}`）。**Goのコードを増やさずに2枚目を足せる**。
  - ⚠ **先に1行**: 描けない形式のリンクに `download` 属性が無く、画面には目録の
    表示名が出るのに**押すと保存名で落ちます**（[file_view.go](../internal/cms/file_view.go) の `kindOther`）。
    足せば `.rpcd`・DXF・Excel・ZIP が**一斉に**正しい名前で落ちます。
- **配布の検討**（ユーザー:「Microsoftstoreでヘルパーを配布するのはどうですか？」）。
  **2025-09-10 から個人開発者の登録は無料**。w-cms は **MIT で公開**されているので、
  Nextcloud 等と同じ型（公開サーバー＋公式クライアント）に当てはまります。
  ⚠ **Store for Business は2023年に廃止**＝出せば世界中から見えます。
  **決めるのはヘルパーができてから**。ただし作るときは**最初から Store に出せる形**
  （サーバーは設定で指す・社名を焼き込まない）で書くこと。

**2026-09-17——メール添付の ZIP を取り込みで展開する**

- **ZIP の中のファイルが1つずつ添付になります**（ユーザー:「ZIP本体がEMLの中に存在する
  のであれば、ZIPを展開しても良いと思います」）。きっかけは「メール添付のZIPから作った
  加工製品ページはPDFが表示されません」——加工製品ページの配線が ZIP を指すしかなく（中の
  ファイルにはブロックが無い）、ファイル表示が開けませんでした。**展開してよい根拠は
  受信原本（.eml）が ZIP を持っていること**なので、⚠ **人が手で落とした ZIP は展開しません**。
  ZIP 本体も残り、目録（▶ 中身）はそのまま。リンク文字は**フォルダを含む中のパス**
  （フォルダ名が装置名称を含む実データのため）、保存名はファイル名だけ。
  正本は [変更履歴.md](変更履歴.md) 2026-09-17（セキュリティの検討8点の表もそこ）。
- **解析の ZIP の枝は撤去**（`entry` 引数・`元ファイル`・`対応DXFファイル`・`matchDXFInZip`）。
  ⚠ 同日の朝の「`元ファイル` をファイル名だけにする」（`578f83f`）はタグごと消えました。
- ⚠ **既存の穴を2つ塞ぎました**——①メール取り込みは添付を**検査せずに**保存していた
  （拡張子の許可リストも `GuardUploadContent` も通らず）。いまは通し、通らないものは保存せず
  名前と理由を本文に残す。②**和文の添付名が化けていた**（`XkxMISEbKEIyMDI0MDkx?= 3.zip`）。
  Go 標準の `Part.FileName()` が復号前に `filepath.Base` を掛け、base64 の `/` で切れる。
  `partFileName` で復号してから区切りを落とす（下の「罠」）。
- **FAX＋APサーバーの機械を買いました**（ユーザー・09-17）。HP のワークステーション
  （Windows 11・pasel.co.jp の中古）。これに FAX モデムを繋ぎ、w-cms も同居させて運用する。
  **モデムとディスプレイはまだ**。組み替え4段目（`ext/comm/fax`）はこの機械が整ってから。
- **DBに入るのは「タグと表」だけになりました**（2026-09-18 ユーザー決定）。素の定義リストは
  どちらの表にも入りません。⚠ **素の「表」は入ります**——`■材料` のような見出しの下に表が
  並ぶ形は**ワンノート移行の受け皿**なので残しました（一度消して気づき、戻した）。
  拡張の形式は**14個→9個**（ヘッダだけの5つが消え、行が並ぶ表7つとビューの器2つが残る）。
  ⚠ **1文書＝1ページ**が規則です（ヘッダがページのタグになるため）。
- **図面番号・図面名称はタグになりました**（2026-09-18 ユーザー決定）。それまで
  `<section><h2>図面</h2>` の**素の `dl`**（業務ブロックのヘッダ）に置いていて、
  `vocab_index` には入っていましたが**横断検索の口（`PagesByTag`）が読む表ではありません**
  でした。いまは `page_tags` に入り、`code` 型の畳んだ一致で引けます（大小・全角・
  前後の空白の揺れを越える。⚠ **区切りの有無は畳まない**）。
  ⚠ **説明が1文になりました——「タグと表だけがDBに入る」。** 素の `dl` とタグの `dl` は
  画面で区別が付かないのに振る舞いが違い、そこが混乱の元でした。
  ⚠ **次は受注ページのヘッダも同じ形へ**（合意済み・**1文書1ページ**を規則に）。
  そこまで済めばコアのヘッダ拾い（`syncVocabSection` の素の中身）を削除できます。
  正本は [変更履歴.md](変更履歴.md) 2026-09-18。
- **図面から作るページの肩書は「加工製品」**（ユーザー決定）。お客様から見れば装置の部品、
  当社から見れば作って売るもの。⚠ **`部品` は捨てていません**——お客様の装置の中での
  位置づけ（「顧客 ＞ 完成品機種 ＞ 部品」）と、`構成部品`・`購入部品`・`部品番号`（タグ名）は
  そのままです。置き換えたのは**肩書を指す言い回しだけ**（`部品ページ`・`部品階層`）。
  正本は [【一覧】用語集.md](【一覧】用語集.md) の `加工製品`。
- **未登録の連絡先は「組織」「担当者」の2欄**（ユーザー設計）。候補は同じドメインの組織（上限20）
  と「個人」。候補に無い社名を打てば新規、**同じ題が既にあれば寄る**。「個人」は取引が人に付き
  ドメインは付かない。口は `POST /api/contacts/register` 1本のまま、3つの道を1本に畳んだ。
  ⚠ E2E `verify-contacts.js` は**既にある組織が入った行しか押しません**——連絡帳が空の日は
  構造の確認だけで終わります（正常）。
  ⚠ **行にあるのは「組織・担当者・登録」だけです**（同日）。**ドメインのチェックと取引の選択は
  外しました**——どちらも**組織の性質**なので、必要になったとき**組織のページでタグを1行**書きます
  （口 `domains`・`relation` は残っているが、送る画面は無い）。
  - `ドメイン` を付けないと、そのドメインの**新しい人**からの初メールは組織に結びつきません
    （整理の `連絡帳` の参照と顧客名の推奨値がそのタグを見るため）。
  - ⚠ **`取引`（顧客・仕入先・自社）は 2026-09-18 に全廃しました**（ユーザー決定）。
    読んでいたのは `自社` だけで、他の2つは誰も読んでいませんでした。代償は承知のうえで、
    **同僚が「未登録の連絡先」に並び、自社が顧客名の推奨値に出ます**。戻すなら、タグでは
    なく**設定に「自社のドメイン」を置く**のが筋です（`contacts.go` の冒頭に経緯）。
- **添付の目録 `files/meta.json`**（ユーザー提案）。届いたときの名前・保存日時・利用者・大きさ・
  SHA-256・由来を、保存の作法1か所（`cms.SaveAttachmentFrom`）が書き留めます。**WebDAV の一覧が
  届いたときの名前で出る**ようになりました（⚠ 割り当て済みのパスは変わるが、保存名でも引ける）。
  本文のリンク文字は表示名、目録は届いた事実——正本は2つにならない。正本は
  [attachment_meta.go](../internal/cms/attachment_meta.go) の冒頭。⚠ **既存の添付には目録が無い**
  （保存名で振る舞う）。ZIP の中身の由来は `zip:<ZIPの保存名>/<中のパス>`。
- **管理画面に「開発メニュー」**（ユーザー要望）。**データの初期化**（メンテナンスから移動）と
  **メールを50通取り込む**（`POST /api/mail/import` を押すだけ・結果は内訳まで出す）。
  入れ直しは「初期化 → 置き場 → 取り込み」の3押し。⚠ **実運用に入る前にこの節ごと外す**。
- **職場のデータを入れ直しました**（初期化→置き場→メール50通、その後ボタンで50通追加。下の「環境」）。

**2026-09-16（夜）——アドレス帳の作り直し（5段とも完了）**

- **`ドメイン` は「組織の連絡先」**（ユーザー決定。**09-12 の「独立させない」を覆した**）。
  `メールアドレス` が**その人**を、`ドメイン` が**その組織**を言い当てます。
  ⚠ **暗黙の切り出しをやめたのが本体**——登録済みアドレスから機械がドメインを
  切り出していたので、`@yahoo.co.jp` の個人客を1人登録すると**そのドメインの全員が
  その人に化けて**いました。いまはタグが無ければドメインでは引きません。
  **同じドメインを2社が持つと1つに決めず、候補を出します**（⚠ 件数制限つき——
  「ヤフーのようなドメインでは候補が1万件ということもあり得ます」）。
- **木を2つに分けました**（ユーザー決定「両方を別の木にする」）。
  **`連絡帳／組織／人`**（アドレス帳・通信の持ち物）と
  **`取引先／社名／段／装置名称／図面名称`**（加工製品の階層・下請けの持ち物）。
  ⚠ **`担当者` の箱はやめました**——挟んだ理由（社名の子に装置が並ぶ）は
  **同じ 2026-09-05 に「段」が入った時点で消えていました**。
  個人のお客様も `連絡帳／個人／山田太郎` と組織の段を置きます。
  2つの木は **`連絡帳`（ref）の参照タグ**で結びます（題だけだと改名で切れる。
  ⚠ 2026-09-17 に `相手` から改名——取引先のページ自身が相手なので「自分は自分です」
  になっていた。`相手` は通信記録の「誰と話したか」で引き続き使う）。
- ⚠ **§5.3 の警告は、設計文書の条件では一度も発火しませんでした**。
  「アドレスから題に解決できるのに連絡先が無い」＋「解決は `PartnerTitleForAddress` と
  同じ規則で」は**自分を打ち消します**（解決に連絡先タグが要るため）。
  代わりに**整理が辿る鎖**（`受信元` → 通信記録 → `差出人`）を使いました——
  **FAXだけの客先は静かなまま**（FAXの記録に `差出人` は無い）。作業面は `取引先` ページ。
- **自社を登録しました**（§5.4 済）。`取引：自社` ＋ ドメインつきで登録すると、
  **社内のアドレスが一覧からまとめて消えます**（同僚は取引の相手ではない）。
  実データで 12件 → 6件。
- ⚠ **変異試験が、試験そのものの誤りを暴きました**。番人が効かないので調べたら、
  **明示IDで作った記録ページが自動採番の加工製品ページを上書き**していました。
  **試験でページIDを明示しないこと**（衝突して静かに上書きします）。
- ⚠ **既にある箱には、新しい作業面が入りません**。箱の本文は作られた時点のもので、
  あとからビューを足しても現れません（実データで踏み、手で貼りました）。
  **新しいビューを足したら、既にある箱の本文にも貼ること。**

**2026-09-16（昼）——管理画面の2つのボタン**

- **拡張が要る置き場**（ユーザー提案）。管理画面（⚠ **URLは `/assets/admin.html`**）の
  「置き場」に**通信箱・連絡帳・取引先・受注・テンプレート**が並び、**足りないものだけ**を
  1押しで作ります。**起動時には作りません**——人が押したことがそのページを置く意図です。
  **同じ題が2枚あると ⚠ で知らせます**（使われるのはいちばん古い1枚だけで、
  残りは誰からも見えないまま残るため）。
- **データの初期化**（ユーザー要望）。合言葉 `初期化` を打って押すとページを全部
  片付けます。⚠ **消す前に `data/_reset-<日時>/` へ丸ごと移すので取り消せます**
  （`<控え>/master` を `data/master` へ戻すだけ）。**残すのは3つ**——`data/mail`
  （トークン）・`auth.db`（利用者）・`data/tls`（証明書）。⚠ **実際に1度助かりました**
  （下の「罠」の最後）。

**2026-09-16（朝）**

- **拡張どうしを結んでいた生の文字列を定数へ通しました**（3d-3 済）。**振る舞いは1つも
  変えていません**——同じ文字を定数経由で書くだけ。`ext/comm/contacts` と `ext/subcon` が
  `ext/comm` を import し、`差出人`・`宛先`・`CC`・`返信先` と読み手の SQL 5か所が
  `comm.FromTag` 等を通ります（SQL は `name = ?` の束縛引数へ）。
  **新しい定数3組**——`comm.ChannelMail`／`ChannelFax`／`ChannelPhone`／`ChannelMemo`
  （**FAX・CTI を足すときはここを増やす**）、`comm.SentOutAtTag`（`発信日時`。
  `送信日時`＝`SentAtTag` とは**別の欄**——人が発信した／機械が投函した）、
  `contacts.ContactsViewType`。`受信元` は**下請けの語なので `comm` へ出さず**、
  `SourceRefTag` で下請けの中を通しました。
  ⚠ **定数へ通す作業は、取り違えてもコンパイルが通ります**。わざと壊して確かめたところ
  **2か所は試験が気づきませんでした**——未処理一覧の SQL は `?` が6つあり、`受信日時` と
  `差出人` を入れ替えても全試験が緑（欄の中身が入れ替わるだけで SQL は通る）。
  `senderAddressOf`（顧客名の推奨の鎖）は試験そのものが無し。**番人を2本足しました**
  （`TestUnhandledColumnsMatchTheirTags`・`TestSenderAddressOfFollowsSourceRef`）。
- **3d-4 の画面も済み**。ただし**予定していた4つのうち、出し分けが要ったのは2つだけ**でした
  ——`refreshMailChrome`（箱ごと。中の2つが `/api/thread`・`/api/replies` を叩くので、
  通信が無いと**通信記録を開くたびに404が2本**飛んでいた）と、電話の「記録を作る」
  （`/api/intake/memo`。既定オンなので掛けるたびに通知が出ていた）。
  ⚠ **未処理一覧と「＋ 記録する」には要りません**——押す相手はサーバーが描いたもので、
  通信が無ければ `unhandled-intake` が語彙レジストリに載らず**鏡の引き金が立たない**ので、
  配線は空振りします。**一般則: 出し分けが要るのは JS が自分で作るクロームだけ**
  （§4.4 に表で残しました）。⚠ `tel:` のリンクは通信が無くても出します——**掛けることは
  通信拡張の持ち物ではない**。
  番人は `e2e/probe-extension-gating.js` で、**3つのビルドすべてで通しました**。
- **文書も済み**（プラグイン開発ガイド・【一覧】API・【一覧】語彙・通信記録処理・
  コードリファレンス4本）。直しながら検証したところ、**組み替え以前から間違っていた記述**が
  いくつも出ました——ガイドの登録表が `cms.RegisterIntake`／`cms.RegisterMailer`
  （**どちらもコアに存在しない**。正しくは `comm.*`）、【一覧】API が `/api/intake/memo` を
  admin 限定の節に置いていた（実際は通信箱への write）、語彙の内訳「コア7」は**実際は5**
  （合計21が合っていたので隠れていた）、「すべてのリクエストは CSRFProtect を通る」
  （**`/dav/` は外側**）。**実体を失ったコメントも10件**潰しました。
- **`/api/parse-pdf` を `ext/subcon` へ寄せました**（ユーザー判断:「PDF解析は業務に密着
  せざるを得ないので、ext/subcon ではないでしょうか？さらに立ち入った解析をするように
  なると、弊社独自の拡張になるやもしれません」）。プロンプトが「発注書または見積書」と
  業務を語る口が、コアに残っていた最後の1本でした。**これでコアのコード（コメント以外）
  から業務語がゼロ**です。
  ⚠ **`/api/analyze-attachment` とは別の仕事なので統合していません**——あちらは受注ページを
  1枚作り、こちらは人が開いているブロックへ明細を差し込むだけ。
  ⚠ **名前の関門はコアの `cms.SafeAttachmentName` を通すこと**（`ext/subcon/parse_pdf.go`）。
  移設先で自前の検査を書くと、かつての非対称——**置く側は守られ、送る側だけ素通しで
  本文とサイドカーを Gemini へ送れた**——が復活します。試験も一緒に移しました。
- **次は4段目の `ext/comm/fax`**——FAXサーバーと一緒に。
  正本は [【考察】通信拡張と下請け拡張への組み替え.md](【考察】通信拡張と下請け拡張への組み替え.md) §2。

**2026-09-15**

- **拡張を「通信」と「下請け業務」に組み替え中**（ユーザー:「メール拡張は通信拡張に変更して、
  FAXサーバーとの通信やCTIとの通信も行ってはどうでしょう？アドレス帳もその範疇に」
  「板金拡張としているものは、下請け業務拡張として一般化できませんか？」）。
  **2段目まで済み**——`ext/sheetmetal` → **`ext/subcon`**（`06706ed`）、`ext/mail`・`ext/contacts` →
  **`ext/comm/mail`・`ext/comm/contacts`**（`0edb633`）。どちらも移すだけで振る舞いは変えていない。
  **3段目も移設まで済み**（09-15 夜）——通信箱・取り込み・未処理一覧・スレッド・記録する・返信・
  メールの口が **`ext/comm`**（`338d13d`）。コアからの名指しはフックに裏返し済み（アップロードの受け口
  `cms.RegisterUploadInterceptor`）。**3d-3 は 09-16 に済み**（生の文字列を定数へ）、
  残るは **3d-4**（画面の通信のボタンの出し分け・文書）。手順と**接点の地図**は
  [【考察】通信拡張と下請け拡張への組み替え.md](【考察】通信拡張と下請け拡張への組み替え.md)。
  ⚠ **3段目（コアの通信箱を移す）の前に決めることが2つ**——拡張の画面がコアの `app.js` に
  約1,240行あり**ビルドタグで外しても画面は残る**件（§4.1）と、拡張の設定の置き場（§4.2）。
  ⚠ **FAXサーバーは w-cms と同じ機械に同居**（**09-17 に購入済み**——HP のワークステーション・
  Windows 11。モデム・ディスプレイは未購入）——機械の口の
  認証が「同じ機械の中からしか届かない口」で足りるようになる（§1.3）。
- **送信の控えが、09-13 に廃止した相手のタグ名で書き続けていました**（`cd3af94`）。
  `差出人アドレス` 等で書くので、返信の一覧の宛先・未登録の連絡先が黙って空になる。
  実データに送信記録が0件で誰も踏んでいなかった。書き手2つを定数（`cms.FromTag` 等）に通した。

- **ファイル表示の配線は属性1つ**（`a1cb6e4`）。
  `<section data-type="file-view" data-ref="010272-c3p7"></section>`——中に参照タグを
  書く形はやめました（`firstRefInTags` は削除）。やめた理由3つ: ①解析が書く本文では
  図面ブロックの `受信元`（届いた記録）と表示先の指定が**同じ値で2行**並んでいた
  ②タグの名前に意味が無く（`受信元` でも `あ` でも動く）配線を見分けられない
  ③「最初の1つ」なので2つ書くと2つ目が黙って無視される。
  **URL（`data-src="/010272/c3p7.pdf"`）は採りません**——URLは派生で、配信のアドレスは
  一度変わっている（`/data/` → きれいなURL）。**本文は正本**なので参照IDを置く。
  **PDF以外も開きます**（画像・動画・音声）。表に無い拡張子は**開く口**を出す。
  ⚠ **表示の表は配信側（`setAttachmentHeaders`）の帰結**——配信が inline で返さない形式を
  「描ける」と宣言しても描けない（09-15 に `.txt` 等でやって、コードレビューで発覚）。
  ⚠ **配線が見えなくなった代償に、編集モードの札**（`button.fv-wire`）で参照を入れます
  ——空の `section` はクリックもキャレット移動もできないため。詳細は
  「現状維持と決まったもの」の『ファイルは「ファイル表示」のマーカーで開く』。
- **`verify-drawing-size.js` が生き返りました**（`4d332a2`）。落ちていた理由は2つで、
  引き継ぎに書いてあったのは片方だけでした（もう一方は**消えた class `.drawing-inline`**）。
- **上の4コミットを8観点でレビューし、穴を塞ぎました**（記録は
  [【考察】ファイル表示のコードレビュー.md](【考察】ファイル表示のコードレビュー.md)・
  修正は `eda14f2`）。**塞ぎながらもう1つ、元から在った穴が出ました**（`0c9045b`・下の「罠」）。
  残りの行き先は同文書 §3——`file-edit` を作る前に、「配線を属性に持つ形式」を
  `VocabDef` の1つの宣言にする再設計（#10〜#13）を判断すること。
- **アドレス帳は `ext/comm/contacts`**（案B・移設済み）。コアとの境目は任意のフック3つ
  （`RegisterVocab`・`RegisterView`・**`RegisterContactResolver`**）。詳細は
  「現状維持と決まったもの」の同名項目。
- **`page_tags.norm_value` は宣言型を持たない**（型は値が持つ。`"8000" < "900"` の
  辞書順事故が消えた）。束ねる形は `tagNormBind` 1か所。同じく「現状維持」に。
- **「識別子」は「連絡先」に改めました**（ユーザー決定）。**連絡先＝相手を言い当てる手掛かり**
  （`メールアドレス` タグ）、**相手／取引先ページ＝相手そのもの**。
  ⚠ 実体のほうを「連絡先」と呼ばない（「連絡先の無い連絡先」という書けない文になる）。
  正本は [【考察】アドレス帳の作り直し.md](【考察】アドレス帳の作り直し.md) §0。
- **同じ問いを1つの口へ**——題の引き方は `PageTitleByID`、IDの整形は `page.FormatID`
  （`%06d` 直書き4＋`%0*d` 15 の19箇所を通した）。公開ヘルパは
  [page_lookup.go](../internal/cms/page_lookup.go) に集めました。
  `NormalizeForLookup` は撤去（宣言型を外した日に罠へ変わったため——「罠」に理由）。

**2026-09-14**

- **やりとりの前後へ移れます**。通信記録ページに `🧵 やりとりの前後`（`GET /api/thread`・
  [handler_thread.go](../ext/comm/handler_thread.go)）。鎖は `In-Reply-To`——
  `メッセージID` と `返信元メッセージID` の**逆引き2回**だけ。
  ⚠ **`/api/replies` とは別の鎖**——あちらは w-cms が送った返信だけ（`返信元` タグ）、
  こちらはヘッダの鎖なので**受信どうしの返り**も繋がる。**混ぜて書くと誤解を生みます**。
- **解析・取り込みのあと、生まれた子ページがその場で出ます**。古くなるのは2か所
  ——左レールの子ページ一覧（`loadChildNav()`）と本文の「子ページ一覧の鏡」（`reloadContent()`）。
  ⚠ **取り込み側は `loadChildNav()` だけ**——落とすのは編集モードなので、`reloadContent()` を
  呼ぶと書きかけが消える。
- **返信の宛先は `返信先` → `差出人` の順に、素のアドレスだけ**（`replyTo`・`bareAddress`）。
  ⚠ 09-13 に `差出人アドレス` タグを廃止したとき、ここが追随せず `宛先` が空のまま出ていた
  （空欄は「まだ書いていない」に見えるので誰も気づかない）。素にするのは SMTP の `RCPT TO:` の都合。
- **語彙の設定は `vocabulary` の1本**（ユーザー:「語彙の設定に名前と型をセットで登録しては？」）。
  `type_inference` と `tag_enums` を畳み、1語につき `{"type": …, "values": [...]}`。
  `/api/tag-schema` のキーも `vocabulary`、Go は `VocabularyDict()`、エディタは `vocabWords`。
  **`enum` は `text` と同じに畳む**（直後に**DB再構築**が要る）。
- コードのリファクタリング（逐語コピーの併合・失敗を 200 で返していた13箇所・関門の統一）。
  記録は [変更履歴.md](変更履歴.md) の同日。

**2026-09-13**

- **索引は2つの表**（ユーザー決定）。可変タグは **`page_tags`**、業務ブロックは `vocab_index`。
  09-06 の「分けない」の**覆し**——根拠2つが崩れた（①タグが業務データの本体になった・索引の84%
  ②「読む側で `data_type='tags'` を足す」規律が守られず1週間で13箇所）。
  正本は [【考察】タグ表と業務ブロック表の分離.md](【考察】タグ表と業務ブロック表の分離.md) §8。
  **振り分けは書き手1か所**（`insertVocabEntry`）。`page_tags` の列は
  `page_id / seq / name / value / norm_value`、**主鍵 `(page_id, seq)`**、`seq` は**ページ通し**。
- **メールの相手は1人1タグ**（「メールアドレスを正規のコンポーネントとして扱えば」）。
  値は `名前 <アドレス>`、畳んだ値がアドレスだけ。列型 **`email`**（`差出人`・`宛先`・`CC`・
  `返信先`）。`〜アドレス` のタグは廃止——タグの27%が消え、位置で対応づける仕掛けも消えた。
  **表示のときアドレスから連絡先ページへリンク**（本文は書き換えない）。
- **職場のデータを入れ直しました**（「試行データが雑然としてきたので、一度全部消して、
  メールを数点取り込んでやり直しましょう」）。**6ページ＋実メール5通**。
  控えは `data/_wipe-backup-20260913/`（303MB・要らなければ消してよい）。

## 次にやること（優先度順）

各項目は「**何を・なぜ・正本はどこか**」だけです。着手時は正本を読み直すこと。

0. ~~**アドレス帳の作り直し**~~ **→ 2026-09-16 に5段とも完了**（`ドメイン` タグ・
   木の分割・登録画面・参照での接続・警告）。正本は
   [【考察】アドレス帳の作り直し.md](【考察】アドレス帳の作り直し.md)、この日の決定と
   実装は [変更履歴.md](変更履歴.md) 2026-09-16。**残っている小さな宿題**:
   `連絡帳` に `個人` の箱を作る操作は未実装（人が手で作れば動く）／
   `取引先` に「どの会社にどれだけ部品があるか」を見るビューは未実装。

0b. ~~**社名の表記ゆれを入口で揃える**~~ **→ 2026-09-20 に3箇所とも完了**（2026-09-20 ユーザー決定:
   「見たままと裏の動作が一致してほしいので、できればページを作る時に表記ゆれを
   無くしたい。株式会社や有限会社、（株）などを無くした社名が連絡帳にあれば、それを
   提案するような形で、編集者の承認を得てはどうでしょう？ちょうどメールの整理のように」
   「受注、加工製品、連絡帳を**同じマナーで**提案の欄を作ってはどうでしょう？」）。

   **土台は済み**（`5d3cdbb`・`75fbe84`）——コアの `cms.FoldCompanyName`（法人格を落とす・
   表は設定 `company_forms`）と、連絡帳の `contacts.SuggestOrg` / `SuggestOrgTitle`
   （3箇所が共有する1つの口）。**繋いだのは加工製品の整理だけ**（`c8f329c`）。

   **残り2つも同日に済みました**:
   - **連絡帳への登録**（`5262a4c`）——**発生源**でした。同じドメインの組織が1つも
     無いとき、欄の初期値が**メールの表示名そのまま**（`株式会社南北…`）になり、
     押した瞬間に会社ページが2枚できます。⚠ **候補に足すだけで初期値も直ります**
     （`contactRowInitials` は「『個人』以外がちょうど1つならそれ」を採るため）。
   - **受注の整理**（`d03e065`）——欄を1つ増やし、本文へ書き戻すようにしました
     （`syncOrderClient`）。⚠ **効き目は加工製品と違います**——`発注元` を引く読み手が
     まだ居ないので、いま直しても何も起きません。**効くのは横断検索が入ったとき**で、
     そのとき揃っていないデータは遡って直せない、というのが入口で揃える理由です。

   ⚠ **画面はもともとコンボボックス**（顧客名の `datalist`・2026-09-11）。ユーザー:
   「コンボボックスで両方入れては？初期表示は、候補を表示しておけば良い」——
   **変えるのは初期値だけ**で、人は一覧から読んだ名前へ戻せる形にすること。

   ⚠ **候補が2つ以上なら黙ります**（`SuggestOrgTitle` は読んだ名前を返す）。
   `株式会社あさひ` と `有限会社あさひ` は法人格を落とすと同じ鍵ですが**実在しうる
   別会社**——「名寄せを機械にやらせない。候補を出すところまでが機械の仕事」。

   ⚠ **2026-09-21 に穴が1つ残っていたのが分かりました**（ユーザー:「発注元タグに
   株式会社が入るのが気になります」）。口は**候補が見つからないと読んだ名前のまま**
   返していたので、**連絡帳に居ないうち＝新しい客先の1通目は一度も揃いません**でした。
   いまは `contacts.OrgNameForPage`——候補1つならその実物の題、**2つ以上なら読んだまま**
   （⚠ 畳んだ `あさひ` は第三の名前）、**0なら法人格を落とす**。通した先は4つで、
   **解析が書く `発注元`・`客先` も含みます**。⚠ **4つとも揃えないと意味がありません**。

   ⚠ **これは体裁ではなく穴埋めです**。整理は `PartnerByTitle`（**完全一致**）で
   加工製品の木と連絡帳を結びます（`linkPartner`）。人が `株式会社南北…` と打つと
   連絡帳の `南北…` に当たらず、**2つの木が黙って結ばれません**——`linkPartner` は
   「結べないことは異常ではありません」と黙って戻るので、誰も気づけません。

0a. **製造という軸——検討中・実装は未着手**（2026-09-21）。ユーザー:「材料の発注は
   終わったか？」「納品書は出したか？」などの記録事項は受注とは別か、という問い。
   **正本は [【考察】製造という軸.md](【考察】製造という軸.md)**（決定6件・未決5件）。
   ⚠ **器を作るかはまだ決めていません**——筆者の当初の論拠（多対多）は、単位が
   「品番ごと」と分かって弱まりました。いま器を推す理由は**分納・加工時間・並べる軸**の3つで、
   ⚠ **3つ目はビューで足りる可能性があります**。
   - ⚠ **「材料発注が終わった」は記録しない**——`手配状況リスト`の**残要手配数 0** が答え。
     印と事実を二重に持つと必ずずれます（解析済みの印を保存しないのと同じ判断）。
   - ⚠ **製造順は本文に書かない**（納期と段取りで動く。並べ替えは見る側の仕事）。
   - **機械の空き状況は持たない**（ユーザー決定・数人の零細が対象）。
   - **次の一手は §7**——⚠ **ワンノートから数ページを w-cms の流儀へ移し**、実物を見ながら
     **納品書・材料発注書・加工発注書・塗装発注書**を設計する。**器の判断はそのあと**。

0b2. **発注書から受注明細へ——大半は実装済み**（2026-09-20）。
   各社ばらばらの発注書を弊社の様式へ落とし込む話。**正本は
   [【考察】発注書から受注明細へ.md](【考察】発注書から受注明細へ.md)**（決定12件・罠5件）。

   **実装済み**: 弊社品番（ページID）の列・原本の写し
   （`<details>` で畳む）・caption での形式宣言・登録済み語彙だけDBに入る・
   `settings.json` から表の形式を足せる・単位・納期（ページのタグ＋行へ配る）・
   出荷済み・備考・**発注書の検算**（以上 09-20）。**09-21 夜に3つ足しました**——
   **原本のPDF**（写しの上・通常は畳む）・**品名/品番/単位/納期の正規化**
   （⚠ 実データの品名が半角カナでした）・**品番に何を入れるかを Gemini に選ばせ、
   選んだ列を本文に1行残す**（⚠ これで 0c の積み残しが片付きました）。

   **残っているのは3つ**:
   - **様式ページ**（`取引先／社名／発注書の様式`）と**整理のフォーム**。
     ⚠ ただし**いまは無くても動いています**（Gemini が毎回翻訳する）。
     **何通か流してぶれるかを見てから**、が提案済みの進め方です。
     検算の見出し探し（`数量`・`単価`・`金額`）も、入ればそちらが正本になります。
   - **解析をテンプレートで埋める形にする**（合意済み・未着手）。
     テンプレートが無いときのフォールバックと、題でテンプレートを引く口が要ります。
   - **納期・受注残を見るビュー**（下の「次にやること」4番と同じもの）。

   当初の要点（判断の根拠なので残します）:
   - **加工製品番号＝ページ番号**を明細に持つ（同一性の根拠。ユーザー:「文脈から担当者が
     糸をくみ取るので自動化は無理」）。⚠ `品番`（顧客の言葉）とは**別の列**。
   - 受注ページは**2つの表**（顧客の原本の写し＋弊社の明細）。原本は整理後も残し、
     `<details>` で畳む（⚠ **素のHTMLだけで畳めるので CSP strict でも動く**）。
   - ⚠ **形式の宣言は `<caption>` へ**（`data-type` は既存本文の互換）。
     「見た儘が全て」の原則どおり。⚠ **`data-type` へ寄ったのは 09-18 に節をやめた副作用**で、
     意図した判断ではなかった。`VocabDefByHeading` を流用するので十数行。
   - ⚠ **登録済みの語彙だけDBに入る**（本日決定）。いまは型が決まらない表も
     **空の形式名で索引に入っている**（`vocab_index.go` の「未定義でもゼロ値の def で続行」）。
   - **表の形式も `settings.json` から足せるようにする**——⚠ いまは Go にしか無く、
     「運用者が語彙を足せる」要件（2026-08-26）を落としている。上と対の決定。
   - 対応表は `取引先／社名／発注書の様式` **ページ**（版が残る・機械の書き込み先が分かれる）。
     1社に複数様式あり、⚠ **どれを使うかは見出しの組み合わせで選ぶ**（caption は人のため）。
   - ⚠ **Gemini はページに書かない**。返りはフォームの初期値で、書くのは人が整理を実行したとき。
   - `備考` を足す（⚠ 既存4表と同じく `区分` の手前）。⚠ **顧客の備考と弊社の備考は別物**。

   ⚠ **着手の前に発注書 `250715-304　発注書.pdf` を人が開くこと**（通信記録「発注依頼」の添付。
   ⚠ **IDで探さないこと**——初期化のたびに振り直されます）。実データは**品番が7行すべて空**で、
   顧客の表の見出しが実際に何なのかデータからは分かりません。**0c の答えもたぶんそこにあります**。

0c. ~~**⚠ 受注明細の見出しが、発注書に書いてある言葉と違う**~~ **→ 2026-09-20 に決着**。
   ユーザーが実データで答えを出しました:「この客先は品番を送らないので、**図番を品番と
   して入れてきた**」。採ったのは**案B＋原本**——`品番` のまま**型を `code` に直し**
   （`図面番号` と揃えないと、同じ番号が置き場所によって別の鍵になる）、あわせて
   **顧客の発注書の写し（原本）をページに残す**ようにしました。⚠ **言い換えたことが
   見えるようになったのが本体**です。⚠ **残っていた積み残しは 09-21 夜に片付きました**
   ——それまで**元の見出しは返り値のどこにも残らず**、原本と見比べない限り気づけません
   でした。いまは Gemini が `item_no_source`（どの列から採ったか）も返し、言い換えた
   ときだけ本文に1行残ります。⚠ **品番の列そのものを機械に選ばせるようにもしました**
   ——品番を持たず図番を送ってくる客先と、図番とは別に品番を持つ客先の**両方がある**
   ので、表を見ないと決まりません。以下は当時の記録:

   （2026-09-20 ユーザーの問い:「受注ページで発注書の『図面番号』が『品番』に変わって
   いるのは、Geminiが変えているのですか？」）**Gemini ではありません**——こちらが2箇所で決めています:
   プロンプトが `"item_no": "品番"` と指示し（`analyze_pdf.go` の `judgeOrderPDFWithGemini`）、
   表を組むときも `<th>品番</th>` と直書き（同 `buildOrderPageHTML`）。

   ⚠ **根幹と噛み合っていません**——「項目の鍵は見出しの表示文字」「見える文字がすべて」
   なのに、受注明細だけは文書に何と書いてあっても `品番` に置き換わります。**同じファイルの
   中で方針が割れてもいます**——図面の枝は「表題欄に書かれている文字列を**そのまま**」と
   指示していて、そちらは根幹に忠実。
   ⚠ **元の見出しは返り値のどこにも残らない**ので、人が原本と見比べない限り気づけません。

   型の食い違いも**推測ですが**あります（⚠ 実データ未確認）: `図面番号` は設定で `code`
   （空白・ハイフン・長音・大小を畳む）、`品番` は `text`。**同じ番号が置き場所によって
   別の鍵になる**はず。

   選択肢は3つ: **A** 元の見出しも返させて表の見出しにする（根幹どおり。⚠ ③計算が
   `item-id` へ解決できなくなるので Label との対応づけを別に考える要あり）／
   **B** `品番` のままで型を `code` に直す（小さい。揺れだけ解ける）／**C** そのまま。
   **まず実データで1枚通して、いまの発注書に何と書いてあるか見るのが先**。

1. **WebDAV「クリックで開く」**——輪（クリック→開く→Ctrl+S→保存）は実データで閉じています
   （09-07）。ブラウザからはローカルアプリを開けない（実測。`file://` は3通りとも
   `Not allowed to load local resource`）ので、**プロトコルハンドラ**が要ります。
   WebDAV があるので**ヘルパーは「開くだけ」の数十行**。当面は「パスをコピー」ボタンでも回せます。
   ⚠ **Windows は拒否されても「保存できた」と返します**（実測）。守りたい範囲は
   `webdav_readonly` より **`webdav_hidden` で隠すほうが確実**——見えていると、保存できたと
   信じたまま作業を失う。正本は
   [【考察】添付をローカルアプリで編集する.md](【考察】添付をローカルアプリで編集する.md) §6e。

2. **社外公開の構築**（09-07 に構成決定・**VPS リバースプロキシ**）。主にスマホなので
   **自己署名は捨て、公的な証明書へ**（iOSは端末ごとの信頼が重い）。アプリ側の備えは実装済み
   （`/dav/` は転送されてきた要求を断る・`X-Forwarded-For` は扱える）。残るはVPSの構築で、
   nginx に要る3行は正本に。**先に決まっていること**: ①**WebDAV は公開しない**
   ②**split-horizon DNS は必須**（社内でもホスト名で繋ぐ）。
   正本は [【考察】社外公開とHTTPSの構成.md](【考察】社外公開とHTTPSの構成.md)、
   外に出す前の確認は同 §5。

3. **電話チャネル**——ファイルが無いので取り込み係の外。手入力フォームで通信箱へ記録を作り、
   `向き` は人が選ぶ（かけた電話は `送信`）。正本は
   [【考察】通信記録処理.md](【考察】通信記録処理.md) §3.2b。

4. **整理の残り**（整理そのものは 09-10 に実データで通り、穴3つのうち2つは 09-11 に片付いた
   ——装置名称の候補が出る／直した値が図面ブロックと索引にも入る）。残るのは:
   **未処理一覧は整理では減らない**（仕様どおり・「済」は人の印。手順は `解析 → 整理 → 済`）と、
   **納期・受注残を見るビューが無い**こと（整理すると責任が通信箱の外へ出るのに、その先を
   見る画面がない）。

### 決定はしたが、着手はもっと先

- **メール周りの残り**（`ext/comm/mail/`）:
    - **`SMTP.Send` はアプリ登録に無くてよい**（09-03 確認）。Entra の「所属する組織で使用している
      API」に Office 365 Exchange Online が出てこない（App ID `00000002-0000-0ff1-ce00-000000000000`
      でも出ない）。**実害は無い**——動的同意で足りる。利用者は初回サインインで同意画面を通る。
    - **自動取り込み**——いまは人が押したときだけ。実測1.2通/日なので当面これで足りる。
    - **CTI連動**（形は用意済み）——着信で記録が立ち、電話番号から `PagesByTag` の逆引きで相手を
      名指しできる。要るのは**機械の口の認証**（FAXサーバーと共通）と**無人生成の歯止め**
      （「着信を知らせて、記録するかは人が押す」を推奨）。
    - **トークンのファイル権限**——0600 は **Windows では効かない**（実測 666）。デプロイ手順に書くこと。
- **表のセルの参照がリンクにならない**——参照リンクの描画はタグの `dl` だけ（`ref_render.go`）。
- **最新単価と価格推移の提示**——ユーザー:「この情報はDBに入っているべきです。そして必要な時に
  提示されるべきです」。データは見積もりと発注書にあり、**出す側（計算ビュー）が未実装**。
- **改定図面の扱い**——移した先に同名ページが在ったとき、新しい図面を先頭へ差し込む
  （`cms.InsertAfterH1` が既にある）。正本は「現状維持」の『同じ部品のページが既にあれば』。
- **1通のメールの中でPDFとDXFを突き合わせる**——「PDFにプラスしてDXFがある場合が大部分」。
  手掛かりの優先順位は「現状維持」の『DXF解析の目的』。DXF側は `ext/subcon/dxf.go` で
  既に取れる（Gemini不要）。**同じ部品なら1つの加工製品ページへ**。
- **DXFから形状を取る（要否はユーザー判断・未確認）**——CAM向けDXFが必ず持つ形状から
  外形寸法・切断長・穴数が取れる。外部依存ゼロで計算できる。提案しただけ。
- **サイト内検索**——「発注書番号からページを検索できる必要があります」「図面番号、図面名称など
  様々なタグ…これらを検索できるようにしたい」。**w-cms を作る動機の1つ**（ワンノートが
  ハイフン区切りの図面名称を検索できない）。未実装・設計から（[要件定義書.md](要件定義書.md) §4.4）。
  ⚠ 引くときは `PagesByTagLoose` を通すこと（「罠」の `NormalizeForLookup`）。
- **見積もりの横断比較**——「見比べることはよくあります」。読み出す機能が無い。設計から。
- **語彙の入力支援**——「ボキャブラリに似たものとして入力支援があっても良いかもしれません」
  （08-31）。見出し語の薄青ハイライトと対になる「書く前に教える」側。設計から。
- **添付ファイルの表示と操作の残り**——ファイルへのコンテキストメニュー（OCR・編集）と
  ZIP内ファイルの個別取り出し（要望が出たら）。正本は
  [【考察】添付ファイルの表示と操作.md](【考察】添付ファイルの表示と操作.md)。
- **保存した画像の編集**（08-26）——回転・切り抜き・注記は Canvas API で実装できる見込み。
  **添付には版が無い**ので、編集結果は**別ファイル保存（原本残置）が本命**。着手時に確認
  （[要件定義書.md](要件定義書.md) §5）。
- **ワンノートの読めない2形式は後回し**——**■見積もり の下の表**（第1列が鍵・497件）と
  **■購入品 の下の表**（見出し行が無い2列）。「後回しに出来るなら後回しにします」（08-30）。
  移行の直前までに入れれば足りる（[【考察】ワンノート移行.md](【考察】ワンノート移行.md)）。
  ⚠「工程表」「部材表」は調査時の仮名で**実データにその言葉は無い**——**■工程 は写真主体の
  作業手順で、表ではない**。
- **ワンノートからの移行**——「板金部」ノート（766ページ）は調査済み。受け皿6条件のうち
  画像・可変列名・添付15拡張子・機能見出し形が実装済み、残るは OCRテキストの置き場と上の2形式。
  実施は**運用開始後**。
- **セッション失効の数分前に告知する**（[認証認可設計.md](認証認可設計.md) §2.2a・§9.3）。
- **要件番号と受け入れ基準の採番**——導入は決定済み、方式から（[要件定義書.md](要件定義書.md) 冒頭）。

### 現状維持と決まったもの（作業なし・蒸し返さないこと）

**土台**

- **スキーマ再設計 D-1〜D-10 は全部実装済み**（09-04）。業務データの行き先は索引だけ
  （ドメイン表ゼロ）、本文は機能見出し形、参照は「名前：値」タグ（`ページID-ブロックID`・
  押せば飛ぶ・宙ぶらりんは薄赤）、設定は `config/settings.json`。決定ログは
  [アーキテクチャとDBスキーマ.md](アーキテクチャとDBスキーマ.md) §9、索引を読む入口は
  [vocab_query.go](../internal/cms/vocab_query.go)（`VocabTableRowsOf`・`VocabBlocksOf`・`PagesByTag`）。
  **実データで通した**——実メール → 🤖解析 → 受注ページ → 通信記録ページの上で明細が見える。
- **設定は `config/settings.json` 1本・Git管理・必須**（09-07 ユーザー:「コードに埋め込まれる
  規定を無くし、Settings.jsonを必須にし、Githubに入れてはどうでしょう？」）。
  **Goコードに既定値は無い**——無ければ起動を止める（例外は添付上限の32MiBだけ。安全の柵）。
  `data/settings.json` は廃止。**語を足せば `git pull` で全環境へ届く**——これが移した最大の
  理由（09-06 に `ref`・`datetime` が既存環境へ届かず参照リンクが消えかけた）。**秘密は入れない**。
  ⚠ DB再構築のときにファイルが無くても止めない（読み込み済みの設定のまま続ける）。
- **タグの型は名前で決まる**（09-06 ユーザー:「名前が『受信元』であれば値は『参照』」）。
  `data-type` で明示する案は採らない（コピペで型が落ち、100ページに同じ属性を書くことになる）。
  型は9つ: text / code / number / date / datetime / enum / image / ref / email（`validColumnTypes`）。
  参照の宣言も辞書（`vocabulary`）にあるので**運用者が自分の参照タグを足せる**。
- ~~**タグと業務ブロックは同じ `vocab_index`・分けない**（09-06）~~ **→ 09-13 に覆った**
  （「いまここ」）。当時の残件 C-1 は表が分かれたことで問題ごと消えた。正本は
  [【考察】タグ表と業務ブロック表の分離.md](【考察】タグ表と業務ブロック表の分離.md)
  （§6 が当時の結論、§8 が覆した記録）。
- **`page_tags.norm_value` は宣言型を持たない——型は値が持つ**（09-15。ユーザー:「norm_value は
  文字列、数値、時刻のような種類が考えられます」）。SQLite は宣言の無い列で値ごとの格納クラスを
  保つ。`TEXT` だと数まで文字列に化けて **`"8000" < "900"`**（実測: 宣言 TEXT は
  `12.5, 8000.0, 900.0, abc`／宣言なしは `12.5, 900, 8000, abc`。索引はそのまま効く）。
  混ざっても平気なのは①並びが格納クラスのグループ順で範囲が重ならない②問いは必ず `name` で
  絞り**名前が型を決める**ため。⚠ **代償は「型を取り違えると、エラーにならず0件」**——束ねる形は
  **`tagNormBind` 1か所**に閉じ、書き手（`insertVocabEntry`）と読み手（`PagesByTagLoose`）が
  同じ関数を通る。**範囲で引く口を足すときも同じ関数を通すこと**。整数は整数の格納クラスへ
  （REAL だと `3` が `3.0` になる）。番人は
  [page_tags_norm_type_test.go](../internal/cms/page_tags_norm_type_test.go) の4件。
  **`vocab_index.norm_num` は残したまま**（統合は別途・あちらは読み手が実在する）。
- **直径記号の揺れは設定表で畳む**（`char_folding`・既定は `Φ ϕ ⌀ Ø ø Ф ф` → `φ`。09-06
  「こういった文字は、設定できた方が良いかも」）。**NFKC では畳めない**（別系統の文字）。
  掛かる場所は `NormalizeText` 1か所。**足したらDB再構築**（`POST /api/rebuild-db`・再起動は不要）。
- **機械が書き起こす「名前」は書く時点で NFKC**（09-06「表記の揺れは恐ろしく多く、コピペに
  よって増殖する」）。対象は顧客名・装置名称・図面名称・図面番号・発注書番号
  （`NormalizeNameForIngest`）。**長音には触らない**——`NormalizeCode` を使うと `レーザー` が
  `レ-ザ-` になる。**メールの件名には掛けない**（届いたままが正本）。
  正本は [【考察】テキストの正規化.md](【考察】テキストの正規化.md) §3a。
- **テンプレート置き場・通信箱は h1（ページ名）で決まる。これは仕様**——「機能が名前で
  変わるのは目論見通りです。表示されている言葉が機能を表します」。受け入れたリスクの正本は
  [【考察】ページテンプレート.md](【考察】ページテンプレート.md) §9。
- **集計の見せ分けは残し、欠けたことは知らせない**（C案）——読めないページ由来の行は黙って落ちる。
- **パスワード変更で既存セッションは切らない**（「書きかけが保存できないほうが困ります」）。
  締め出しは「利用停止」が既に効く。
- **添付ファイルの配信にキャッシュ抑止は足さない**——共用PCも個人IDでログインする前提。
- **PDF解析は自動では走らない**（「自動ではなくボタンのclickなどで解析が始まると良い」09-01）。
  Gemini の呼び出しは常に人の操作の直後だけ（人間ゲート型）。

**拡張の切り方**

- **アドレス帳は `ext/comm/contacts`**（09-15 移設・案B）。**拡張どうしの import が解禁**——
  `ext/subcon` → `ext/comm/contacts` が最初の1本。
  ⚠ **`ext/comm/contacts` にビルドタグは付けない**。付けると「板金は入れたがアドレス帳は外した」
  という組み合わせが作れてしまい、整理が取引先ページを作れずに黙って壊れる。**依存は import で
  表す**。拡張は `init()` で**自分を名簿に載せる**（`cms.RegisterExtension`・09-15）——名簿は起動ログと
  `/api/tag-schema` の `extensions` に出て、**画面はこれで拡張のボタンを出し分ける**。載せ忘れると
  ボタンが消える。`cmd/w-cms/ext_contacts.go` は既定の組を明示するための1枚——**消しても機能は消えない**。
  **コアとの境目はフック3つ**——`RegisterVocab`・`RegisterView`・**`RegisterContactResolver`**
  （アドレス→連絡先ページ。`ref_render.go` が `ContactPageForAddress` を名指ししていたのを
  裏返した）。**どれも任意**で、積まなければ `email` 型のタグは素のテキストで出る——
  `-tags minimal` の実行ファイルに `取引先`・`未登録の連絡先`・`仕入先`・`api/contacts` は
  **1つも入っていない**（実測）。
  ⚠ **`ext/comm/contacts` の試験はファイルDB**（`setup_test.go`）——`visibleChildren`・`page.CanView`
  を通るので `:memory:` では静かに空を返す。`ext/subcon` がインメモリなのは、あちらが
  その経路を通らないから。
- **メール送受信は IMAP／SMTP**（どちらも OAuth2 の XOAUTH2・**外部依存ゼロ**・IMAP は必要な
  5コマンドだけ自前）。**Microsoft Graph は 09-05 に完全撤去**——メールサーバーをレンタルへ移す
  ことが決まり、Graph のままだと受信を丸ごと書き直すことになるため。**パスワードは w-cms を
  通らない**（デバイスコードフロー・預かるのはリフレッシュトークンだけ・`data/mail/<利用者>.json`）。
  正本は `ext/comm/mail/`、口はコアの `internal/cms/mail.go`（Mailer）。`-tags nomail` で外せる。
  **SMTP の同意は南さん個人ぶん**。他の利用者は初回サインインで同意画面を通る。
  ⚠ **移設の継ぎ目**——接続先は環境変数（`WCMS_MAIL_IMAP_HOST` 既定 `outlook.office365.com:993`・
  `WCMS_MAIL_SMTP_HOST` 既定 `smtp.office365.com:587`）。**書き換わるのは接続先と認証方式だけ**
  ——パスワード認証（`AUTHENTICATE PLAIN`）は**移設先が決まってから**足す。`ext/comm/mail/auth.go` は
  M365 のあいだだけの持ち物。

**通信箱**

- **受信も送信もトップ直下の「通信箱」1つ**（09-05 統合）。年／月フォルダに時系列。
  **向きは `向き：受信/送信` のタグ**で、`チャネル`（メール／FAX／電話）とは**直交する別の軸**
  （「送信 × FAX」が要る）。記録は動かさない——作業は一覧で行い、完了は `対応：不要` のタグ。
- **未処理の作業面は通信箱のページに載る**——1行1件・矢印＋チャネルの記号（`→☎` `←✉` `📠` `📝`）・
  📎の数・「不要」を1クリック・チェックして**まとめて片付け**（`/api/intake/handled`）。
- **通信以外の案件も通信箱に入る**（「自分が使う作業台の製造などは、どことも通信しません」）。
  タグを1つも書かずに通信箱の下へページを作れば作業待ちに並ぶ。
- **手で記録を作れる**——「＋ 記録する」でチャネルと向きを選び、できたページへファイルを落とす。
- **⚠ PDF・DXF の自動取り込みは 09-05 に取り止め**（「通信箱のPDF、DXF取り込みはやめましょう。
  メモに添付するようにしましょう」）——落ちてきた PDF を機械が `チャネル：FAX` と決めつけていた。
  **取り込み係が居るのは `.eml` だけ**。**FAXサーバーを繋ぐときは機械専用の口を作ること**。
- **電話をかけられる**——`電話番号` タグのあるページに `☎ …へかける`＋「記録を作る」チェック。
  `tel:` なのでソフトフォンがあれば動く。
- **⚠ 未処理は「人の印」だけで決まる**（09-05 に2度改めた）。向きでも子ページの有無でも決めない
  ——「かけた電話で受注することもある」「一つのメールに複数の案件が含まれることもある」。
- **取り込みで生まれたページには家が要る**——未処理一覧は通信箱の子孫を全部拾う
  （`isDescendantOf` は深さ無制限）ので、受注ページに置き場が無かったころはメール1通が
  2行にも3行にもなった。新しく「解析が作るページ」を足すときは**同時に行き先を決めること**。

**部品・取引先の木**

- **加工製品ページの階層は「顧客名／装置名称／図面名称」**（ワンノートの形）。**顧客名ページは
  「取引先」の下**（09-05「トップページの直接の子が個人名や社名はちょっと具合が悪い」）。
- **相手のページは1枚・箱も1枚**。アドレス帳が作る相手ページと加工製品の階層の顧客名ページは
  **同じページ**。箱を `顧客` `仕入先` で割らないのは「顧客であり、仕入れ先である場合も
  あります」——役割は `取引` タグ。**`取引先` ページは無ければ自動で作られる**
  （[ext/comm/contacts/contacts.go](../ext/comm/contacts/contacts.go) の `EnsurePartnerBox`）。
- **装置名称の上に「段」**（`取引先／社名／段／装置名称／図面名称`。09-05「旧型、現行、試作などが
  あったほうが探しやすい」）。既定は現行・旧型・試作（`extensions.subcon.machine_stages`・**先頭が整理の初期値**）。
  **段は人が毎回選ぶ**。`【試作】装置名称` という題の付け方は不要になった。
- **窓口の人は `取引先／社名／担当者／名前`**（`ContactPersonBoxTitle`）——社名ページの子には
  装置名称が並ぶので、人を直接ぶら下げると埋もれる。**人のページを作る操作は未実装**
  （その人に添付や記録を紐づけたくなったときに作る）。
- **顧客名の推奨値は「既にある取引先ページの題」**（09-06「社名の揺れは、エイリアスの表かAIで
  なくせませんか？」）。鎖は `加工製品ページ → 受信元 → 通信記録 → 差出人アドレス → 取引先ページ →
  その題` で**全部が完全一致**——エイリアスの表もAIも要らない。完全一致がドメインより先
  （自社の工場長だけ別プロバイダ、が実データにある）。`取引：自社` は推奨値にしない。
- **2つ目のドメインは既存の相手へ足せる**（アドレス帳の「既にある相手へ足す…」）。
- **受注ページは `受注／年／月`**（09-06）。顧客の下へは置かない——**受注は複数の装置に
  またがる**。年月は**発注日**。移すのは 📁 整理の操作。正本は
  [filing_order.go](../ext/subcon/filing_order.go)。
- **置き場所は解析では決めない**（「人間が見てもなにを言っているのか判断に困る場合も結構多い
  です。**なぜなら顧客は適当だからです**」09-03）。解析は加工製品ページを通信記録の子として作り、
  読めたものをタグに入れるところまで。**通信箱がそのまま「まだ分からないものの置き場」**。
  整理は分かったときに別操作で。
- **整理で直した値は図面ブロックと索引にも入る**（09-11「検索するときに困るからです」）。
- **見積だけ・試作は装置名称から新しく作ることがある**（段の「試作」に入れる。「メールの内容から
  判断するしかありません」——機械には決められない）。
- **同じ部品のページが既にあれば、その図面は改定図面**（09-03）。**旧版の図面は最新版の
  子ページ**（09-06「旧版を最も新しい版の子にしてはどうでしょう」）——加工製品ページに載るのは
  最新の図面1つだけで、古い図面の赤枠は要らなくなった。正本は `mergeAsRevision`
  （[filing.go](../ext/subcon/filing.go)）。
- **解析済みの印は保存していない**——解析が書いた `受信元` の逆引き（`/api/analyzed`）。
  間違った解析をゴミ箱へ入れれば印も消え、押し直せる。
- **ファイルは「ファイル表示」のマーカーで開く**（09-14 に作り直し・**09-15 に配線を属性へ**）。
  `<section data-type="file-view" data-ref="010272-c3p7"></section>` と書くと、**サーバーが**
  その添付を開く枠を足す（`internal/cms/file_view.go`・鏡型）。
  ⚠ **中に参照タグを書いても開かない**（`firstRefInTags` は削除）。旧い形の本文は
  `data-ref` へ写すまで「参照がありません」と出る。
  **貼り方は編集モードの札**——各ブロックの先頭に `button.fv-wire` が出て、押すと
  プロパティ欄（`#w-fv-popover`）が開く。手順は **添付で「🔗 参照」→ 行き先で
  📄 ファイル表示 → 札を押して欄に貼る**。
  ⚠ **配線を属性に持つ形式は `usesHeadingForm` の例外に入れること**（`tags`・`file` と同じ）。
  見出し形で挿すと `data-type` が付かず、札の絞り込みから外れる——09-15 に
  file-view で入れ忘れ、**スラッシュメニューから挿すと配線できない**状態になった。
  鏡は機能見出しでも立つので「参照がありません」とだけ出て、**その欄を開く手段が
  画面に無い**という気づきにくい壊れ方をする。
  **PDF以外も開く**（画像・動画・音声）。表に無い拡張子（DXF・Excel・ZIP・テキスト）は
  **開く口（リンク）を出す**——「見つかりません」は嘘だった。
  ⚠ クライアントで組むのはやめた——`RenderReferenceLinks` は `/api/load` を通らず、編集モードを
  出入りすると図面が黙って消えていた。鏡は両方の経路で走り、**公開ページ（匿名・ゼロJS）でも出る**。
  ⚠ 旧実装は `<section><h2>図面</h2>` という**板金の語彙でコアが分岐**していた（消えた）。
  `<embed>` を本文に書かせない理由は [セキュリティ設計.md](セキュリティ設計.md) §4。
- **DXFは2種類あり、取り込み口を通るのは片方だけ**（「弊社は加工業者ですからCADからCAMへ
  移すためだけにDXFを作っています」「メールに添付されてやってくるDXFは殆ど〔お客様の図面〕」）。
  実データ911件:

  | | 同名PDFあり | 同名PDFなし | 正体 |
  |---|---|---|---|
  | **表題欄あり** | 4 | 7 | **お客様の図面**（取り込み口を通る） |
  | 表題欄なし | 4 | 896 | 自社のCAM用（**取り込み口を通らない**） |

  **「911件中11件しか取れない」と読んではいけない**——分母に自社のCAM用を入れた誤り。取り込み口
  から見える分母は11件で**10件が取れている**。
- **DXF解析の目的は「身元の特定」ではなく「PDFとの突き合わせ」**（「PDFにプラスしてDXFがある
  場合が大部分です」）。手掛かりは強い順に ①DXFの表題欄の図面番号（`dxf.go`・Gemini不要）
  ②**ファイル名の完全一致**（同居12組のうち8組は同名）③図面名称（重複しうる）。**未実装**。
- **図面番号でページを自動的に束ねない**（「別の製品の図面番号が一致してしまう場合もあり…
  社内で識別番号を割り当てるしかありません」）。同一性は常にページID、図面番号は探すためのタグ。
  w-cms は6桁を必ず振るので「社内で識別番号」は構造として既に満たされている。

**HTTPS と WebDAV**（09-07 に一式実装・両環境とも導入済み）

- **HTTPS**: `go run ./cmd/w-cms -gencert` で自己署名を `data/tls/` に作り、`WCMS_TLS_CERT` と
  `WCMS_TLS_KEY` を置くと **:8443**（両方そろわないと起動を止める——片方だけは書きかけ）。
  各PCで1度だけ `Import-Certificate`（管理者PowerShell・**1行で貼る**）。
  ⚠ `-gencert` を走らせるたびに別の証明書ができる——**信頼させたものとサーバーが出している
  ものの指紋が一致しているか**を確かめること（1度食い違わせた。照合は `Get-PfxCertificate` の
  Thumbprint と、サーバーの証明書の SHA256 を突き合わせる）。
  ⚠ TLS の環境変数を置くと **:8443 だけ**になる——古い E2E は :8080 を見る（「環境」）。
- **WebDAV**（`WCMS_DAV=1`・**既定は閉じる**・`x/net/webdav` は既存依存）: エクスプローラで `Z:` に
  割り当て、**実物のCADで DXF を開けた**（SHA256 一致・シーク読み可）。**HTTPS が前提**——
  `BasicAuthLevel=1` が平文の Basic を送らせない（エラー67・認証失敗として報告されない）。
  **ページの木をそのまま見せる**（`Z:\取引先\社名\現行\装置\部品\図面.dxf`）。見せないものは
  設定 `webdav_hidden`（コアが `通信箱` を名前で特別扱いしない）。題はフォルダ名へ付け替える
  （`:`→`：`・重複はページIDを添える）——⚠ **規則を変えると割り当て済みのパスが全部変わる**
  （`safeFolderName`）。
  **上書きできる**——前の中身が版になる（`files/.versions/<名前>/<時刻>.<拡張子>`・一覧には
  出ない）。ページの更新時刻も進み、監査に `dav.write`。**新しいファイルは作れない**。
  消す・作る・動かす（`DELETE`・`MKCOL`・`MOVE`・`COPY`・`PROPPATCH`）は通さない。`LOCK` は通す。
  書ける範囲は設定 `webdav_readonly`（祖先まで効く）——**編集の対象は自社のファイルだけ**、
  通信箱の下の添付は「届いた事実の証拠」。
  `.rpcd` は **RootPro CAD** の形式（Radan は誤記）。試験は31バイトの偽物なので**実物での確認は未了**。
  正本は [【考察】添付をローカルアプリで編集する.md](【考察】添付をローカルアプリで編集する.md)。

### 小粒な残り

- **⚠ トップ直下にE2Eの残骸が29枚**（職場・09-10 時点・`010273` 以降）。`新しいページ`・`案件ホスト`・
  `部品X1`・`受注A`・`会社案内` などの定型名。**本物のテンプレート木は `010115`**——そこだけ
  避ければ安全に消せる（子持ちは子から）。
- **見た目の磨き込み**——フッター帯（`.site-footer`）へのリンク追加（仮表示「w-cms」のみ・
  ユーザー予告）／`<aside>` のアイコン・余白・折りたたみ既定状態／CSS変数の移行。
- **スマホ幅**（09-07 に Playwright で実測済み）: 浮くパネルは `placeFloating` で当時の5か所すべてを
  画面内へ寄せた（**09-15 にファイル表示の欄が加わって6か所**——新しい浮くパネルは
  この関数を通すこと）。表は `th/td` に幅を与えて横スクロール（表そのものに min-width を置くと**枠ごと
  広がってページが横スクロール**——1度間違えた。効くのは `@media (max-width: 760px)` の中だけ）。
  ⚠ **浮くパネル・横並びのクロームは、スマホ幅で必ず確かめること**（「＋ 記録する」の行に
  `flex-wrap` が無くページ全体を横スクロールさせていた）。測り方は
  [e2e/probe-phone.js](../e2e/probe-phone.js) と [e2e/probe-table.js](../e2e/probe-table.js)。
  ⚠ **2色の見分けにコントラスト比を使わないこと**——同じ明るさの青と赤は 1.00:1 になる。
  見るのは ΔE（薄青と薄赤は 18.3。赤緑色覚でも 17〜21）。
  ⚠ **測るときは `e2e/probe-phone.js` の計算を使うこと**（2026-09-21）。同じ ΔE でも
  **色覚シミュレーションの行列で数字が変わります**——自前で書いたものと repo の道具で
  赤×緑が 3.3／4.4 と 11.1／9.5 に割れました。**次に測る人が走らせるのは repo の道具**
  なので、そちらを正にします。
  ⚠ **薄赤と薄緑は、色だけでは足りません**（2026-09-21・検算の合格）——
  正常視 24.4 に対し 2型 11.1・1型 9.5 で、「10以上で別の色」の**ちょうど境目**。
  だから行頭に `⚠`／`✓` を付けてあります。**印の形は色覚にも白黒印刷にも影響されない。**
- **画面構成の注釈図**（[【一覧】用語集.md](【一覧】用語集.md) §2.4）。
- **同時編集の残る未決**——複数待機者の順序・任意の安全TTL・共有ストア化・将来の CRDT 化
  （[【考察】同時編集の競合対策.md](【考察】同時編集の競合対策.md) §8）。現状で支障なし。

### 未決（次にその辺りを触るときに確かめる）

- **テンプレート置き場・通信箱の名前とタグ名を、設定で変えられるようにするか**——判定を h1 のままに
  する決定は出たが、「テンプレートという名前は設定でかえられる方が良いかもしれません」とも。
  「かもしれません」なので要否は未決（`config/settings.json` へ2つまとめて、が自然）。

### 未回答の質問（次に機会があれば伺う）

- **公開リポジトリに業務情報が入っている件**（09-07 に伺ったが未回答）。`github.com/wordring/w-cms`
  は未認証で読める。顧客名（南北スポーツ機械等）・メールドメイン・実在の図面番号が、
  設計判断の根拠として docs とテストに入っている。**問題ないというご判断ならそのままで構わない**
  ——実データを根拠に残す価値は大きいので、確認だけ取りたい件。
  ⚠ **2026-09-18 に再度お伺いしたが、やはり未回答**。この日「w-cmsは無料公開されて
  いますので、もしかすると世界中で使われるなんてことがあるかも？」という話が出たので、
  **外へ向けるなら先に片付ける件**に格上げした。数えたところ
  **実在の顧客名を含む公開ファイルは21件**（`ext/subcon` 7・`ext/comm/contacts` 5・
  `internal/cms` 3・`docs` 3・`ext/comm` 1・`e2e` 1・`assets` 1）で、
  ⚠ **文書だけでなくコードと試験に入っている**。

## 覚えておくこと（知らないと必ず踏む罠）

設計書に書いてあることはリンク先が正本。ここに残すのは**知らずに書くと壊れるもの**だけ。

> **コードの現状を知りたいときは `go doc` から**（各パッケージの冒頭コメントに正本性・3層モデル・
> 回覧機構・最小権限の要点）。横断的な話——依存の向き・呼び出しの連鎖——は
> [コードリファレンス/](コードリファレンス/README.md) の3本。**古くなったら直さず、
> スキル /code-reference で作り直すこと**。

**道具**

- **⚠ 表のセルの見た目は、しきい値ではなく見出しで決めること**（2026-09-21）。
  2026-09-01〜09-21 は「20字までなら折り返さない」でした。**しきい値は「その値が
  自由文かどうか」を当てにいく代理**でしかなく、実データで品名が20字を超えて
  **同じ列の中で折れる行と折れない行が混ざりました**。いまは `WRAP_COLUMNS`
  （`備考` だけ）を**見出しの表示文字**で引きます——列を足してもずれません。
  ⚠ **折り返す列には幅の下限が要ります**——ほかが折り返さないので、表はその列だけを
  細らせて収めようとします（実測 69px・高さ417pxの短冊）。
- **⚠ パッケージ変数の初期化は `init()` より先に走る**（2026-09-21 に踏んだ）。
  語彙の宣言から列の見出しを引く口を `var x = labelOf(...)` で持ったら、
  **`cms.RegisterVocab` が走る前に引いて常に空**になりました。空はたいてい
  「無かった」と同じ扱いをされるので、**比較が素通りして条件が逆になります**
  （実際、言い換えていないときも注意書きが出る形で現れました）。
  **レジストリから引くものは関数にすること。**

- **⚠ 表の中へ `<p>`・`<div>` を足さないこと**（2026-09-20）。**HTMLパーサが表の外
  （手前）へ追い出します**——明細の上に文字が飛び出す、気づきにくい壊れ方です。
  表の足元に何か出すなら **`<tfoot>` の `<tr>`**（検算の行がそうしています）。
  コアの `appendChrome` は `<div>` を足すので、**表には使えません**（非公開なので
  拡張からは呼べませんが、真似して書くと踏みます）。
- **⚠ 機械の読みを、同じ機械に検算させないこと**（2026-09-20）。LLM は自分の出力を
  検算させると**数字を合うように書き直します**——誤りが消えるのではなく**見えなく
  なります**。掛け算・足し算はこちら（Go）でやり、Gemini には「書かれているまま」
  だけを頼むこと。プロンプトにも「求めないでください」と書いておくこと。
- **⚠ 応答の一部が読めないだけで、全体を捨てないこと**（2026-09-20）。`source_table` を
  構造に直接当てていたので、Gemini が行を object で返すと `json.Unmarshal` が落ち、
  **発注書番号も明細も丸ごと失われて**いました（画面には「解析に失敗しました」だけ）。
  ⚠ **Go は型が合わなくても他のフィールドは埋めます**——それでも `err != nil` で
  捨てていました。**おまけの構造は `json.RawMessage` で受けて別に解くこと。**
  ⚠ **エラー文には返ってきたものの頭を添えること**（直すのはたいていプロンプトなので、
  何が返ったかが唯一の手掛かり）。⚠ **文字の途中で切らないこと**——和文は1文字3バイト
  なので `head[:300]` は化けます（rune で数える）。
- **⚠ 本文のブロックを切り出すときは `cms.SectionBlockAt` を通すこと**（2026-09-20）。
  図面ブロックは**中にファイル表示の節を含みます**（`<section data-type="file-view">`）。
  「最初に出会う `</section>` まで」で切ると**閉じ足りないHTML**になり、本文へ入れると
  サニタイズが後ろの節を**その中へ飲み込みます**——図面ブロックは人が消せるので、
  **消した瞬間に改訂履歴も材料も一緒に消えます**。
  - ⚠ **2026-09-20 まで、誤りが2つあって打ち消し合っていました**——運び出す側
    （`FirstBlockHTML`）が1つ閉じ足りず、取り除く側（`extractDrawingSections`）が
    1つ余らせる。**結果は正しく見えます**。片方だけ直すと崩れます。
  - **番人は「結果」ではなく「釣り合い」を見ること**。打ち消し合っている限り、
    結果だけの試験は通ります。
- **⚠ 図面の重複は機械には判定できません**（2026-09-20）。同じ添付でも、同じ図面番号
  でも、別の図面がありえます（「本来は同じ図番で別の図面はあってはならないのですが、
  どういうわけか指摘しても理解できない人も居るようです」）。**止めずに人へ確認します**。
  ⚠ **二重整理を機械が止める最後の砦は無くなりました**——守りは「人が確認の文を読む」
  ことだけです。通したことは監査 `file-drawing.same-number-accepted` に残ります。
- **⚠ 「調べるだけ」の口で `ensureChildPage` を使わないこと**（2026-09-20）。
  無ければ作るので、**問い合わせのたびに空の顧客ページ・段ページ・装置ページが
  増えます**。しかも増えたページは次の問い合わせで「在る」と答えるので、
  **誤りが自分を正当化します**。辿るだけなら `findChildByTitle`。
- **⚠ `InsertRevisionRow` は `<table data-type="drawing-revision-items">` を文字列で
  探しています**（2026-09-20 に気づいた・未対応）。**テンプレートや本文を `caption` へ
  移した日に、改定が黙って積まれなくなります**。caption 化の前にここを直すこと。
- **⚠ 変異試験は「何を測っているか」まで確かめること**（2026-09-20 に6回踏んだ）。
  番人が空振りする形が、少なくとも6通りありました: 配送係が届けない要素を測っていた／
  対象が索引の対象外だった／入れ子でも他の条件が全部通る／下ごしらえが無くて早期戻り
  していた／`"" == ""` が偶然一致した／余った閉じタグをパーサが黙って捨てた。
  **守りを外しても通る試験は、守りではなく別の何かを測っています。**
  ⚠ **7通り目（2026-09-20 夜）——変異そのものが当たっていない。** `sed 's|…||'` の
  区切りが Go の `||` と衝突してエラーになり、**ファイルが変わらないまま「試験は通った」**
  ＝「番人が空振り」と読めてしまいました。**変異を当てたら、当たったことを
  `grep` で確かめてから試験を走らせること**（コンパイルエラーになる変異も同じ——
  「落ちた」のが番人なのかビルドなのかを見分けること）。
  ⚠ **鏡型はとくに空振りしやすい**——引き金が立たなければ、番人は「合っている」では
  なく**ただの静けさ**を見ます。**「⚠ が出ないこと」だけを見る試験を書かないこと**。
  合っているときに**合っていると言わせて**、静けさと成功を区別します。
- **⚠ WebDAV の Ctrl+S は w-cms に届きません。アプリを閉じるまで**（2026-09-18・実測）。
  Windows の `WebClient` がファイルを手元に写して作業させ、**最後のハンドルが閉じたときに
  まとめて書き戻します**（実測で Ctrl+S の75秒後）。**サーバー側では直せません。**
  - **「エラーが出なかった＝保存された」は成り立ちません。** この日1度騙されました。
    確かめるのは**サーバー側**——WebDAV ログの `PUT`・`files/.versions/` に版が1つ・
    監査 `dav.write`・ハッシュの変化。**CADの言葉は見ない。**
  - **Ctrl+S を5回押しても版は1つ**（閉じたときの1回ぶん）。
  - ⚠ **これを画面に出す手はあります**——`LOCK`／`UNLOCK` は届いているので、
    「いま開かれています」と言えます（未実装・`webdav.NewMemLS()` を薄く包む）。
- **⚠ `PROPPATCH` を塞ぐと Ctrl+S が通りません**（2026-09-18）。Windows は保存の途中で
  必ず送り、403 を返すと **`PUT` まで進みません**（RootPro CAD は
  「DocFileが壊れています（0x80030109）」と言って止まりました。ファイルは無傷）。
  ⚠ **通すなら `davWriteFile` の「空なら差し替えない」が要ります**——`x/net/webdav` は
  PROPPATCH でも `O_RDWR` で開くので、無いと**添付が0バイトになります**。**一組です。**
- **⚠ Go の埋め込みで `Write` を差し替えても素通りします**（2026-09-18）。
  `davWriteFile` は `*os.File` を埋め込むので **`ReadFrom` が昇格し、`io.Copy` は
  そちらを直に呼びます**。「書いたか」を `Write` で数える形にして**上書きが全部死にました**。
  見るのは**一時ファイルの大きさ**——どの経路で書かれても効きます。
- **⚠ 本文の無い `PROPPATCH` は 400 で弾かれます**（2026-09-18）。試験でそれを投げると
  危ない関数まで届かず、**守りを外しても通る番人**ができます（変異試験で発覚）。
  Windows が実際に送る XML を本文に入れ、**207 まで確かめること**。
- **⚠ Windows の curl は和文のファイル名を壊します**（2026-09-18）。`-F file=@...` が
  ローカルコードページに落とすので、`展開　○○.rpcd` が「拡張子が許可されていません」で
  弾かれます（**w-cms は正常**）。APIを外から試すときは、multipart の本文を手で組んで
  UTF-8 のまま送ること。
- **⚠ メールの添付名に Go 標準の `Part.FileName()` を使わないこと**（2026-09-17）。
  RFC 2047 の復号**前**に `filepath.Base` を掛けるので、符号化文字列の中の `/`（base64 の
  1文字）で名前が切れます——和文の名前は必ず符号化されるので、何割かが確実に化ける
  （実データで `P220-DC装置D…20240913.zip` が `XkxMISEbKEIyMDI0MDkx?= 3.zip` に）。
  使うのは `partFileName`（復号してから区切りを落とす）。
- **⚠ ZIP の展開はメール取り込みだけ**（2026-09-17）。展開してよい根拠は「原本の .eml が
  ZIP を持つ」ことで、人が落とした ZIP にはそれが無い。展開の口を別の経路へ足すときは、
  同じ根拠が立つかを先に確かめること。上限は3つ（1件・件数・合計）で `cms.ExpandZip` の中。
- **⚠ データの初期化は「掴まれていて」断られることがある**（2026-09-17）。Windows では、
  書いたばかりのファイルをウイルス対策・検索の索引・エディタの見張りが数百ミリ秒掴み、
  **そのあいだ `data/master` の改名が `Access is denied.` になります**。実データで2度続けて
  断られ、40秒後に手で試すと通りました（そのとき E2E がページを作っていた）。
  いまは**5回・200ミリ秒おきにやり直します**。それでも駄目なら、エディタで `data/master` の
  中のファイル（`<ID>.meta.json` など）を開いていないか見ること。
- **⚠ データの初期化は「押した覚えがなくても起きる」**（2026-09-16）。この日、
  **21:23 に実データ67ページが初期化されました**——監査記録には利用者 `a` の
  `data.reset` が残っていますが、**何がそれを押したのかは特定できていません**
  （試験は一時ディレクトリへ移るので無実。E2Eにも初期化を押すものはありません）。
  **控えから1手で戻せたので実害はゼロ**でした（`<控え>/master` を `data/master` へ
  動かし、`cms.db` を消して起動）。**初期化を控えつきにしておいたことが効きました**——
  「取り消せる形にする」は、押し間違いだけでなく**原因不明のときにも効きます**。
  ⚠ 次に触るときは、**初期化のボタンを押す自動操作を書かないこと**（書くなら実データの
  在る環境では流さない）。
- **⚠ 試験でページIDを明示しないこと**（2026-09-16）。`cms.CreateChildPage` の自動採番と
  衝突し、**明示IDのページが自動採番のページを静かに上書き**します——`受信元` の鎖が
  切れて、**番人が一度も効かない**状態になっていました（変異試験で発覚）。
- **⚠ パスを一括で置き換えるとき、変更履歴と決定ログは除くこと**（09-15 に2度やった）。
  `ext/sheetmetal` → `ext/subcon` を文書全体へ流したら、「09-03 に `ext/subcon` へ出た」「09-05 に
  `ext/mailgraph` → `ext/comm/mail` へ改名した」のような**起きていない過去**が書かれた。規則は
  **説明文は今の場所／変更履歴・決定ログの本文は当時の名前（リンク先だけ今の場所）**。
  対応表は [【考察】通信拡張と下請け拡張への組み替え.md](【考察】通信拡張と下請け拡張への組み替え.md) の冒頭。
- **サーバーは `taskkill //IM main.exe` では止まらない**（09-07 に半時間溶かした）。実行ファイルは
  **`w-cms.exe`**。止め損なうと古いプロセスがポートを掴んだままで、**直したはずのコードが
  効かない**ように見える。PIDで止める:
  ```powershell
  Get-NetTCPConnection -LocalPort 8443 -State Listen | ForEach-Object {
    Stop-Process -Id $_.OwningProcess -Force }
  ```
- **`staticcheck ./...` が使えます**（2026-09-21 に最新版を `go install` した・`~/go/bin`）。
  ⚠ 古い版は「module requires at least go1.25.8」で止まります。この日それで
  **`err` の `:=` 取り違え（SA4006）を1件**見つけました——同じ形は `go vet` では出ません。
  意図して残している未使用（`deleteToken`）は出ますが、消さないこと。
- **⚠ 「先に読み切ってから絞る」は、もう写さないこと**（2026-09-21）。`cms.TagRowsNamed`・
  `cms.ChildPages` が読み切って返します。行を読みながら `page.CanView` を呼ぶ形を
  新しく書くと、`:memory:` の試験で静かに全部落ちます（罠の説明はその2つの関数の冒頭）。
- **⚠ `python3` はこの機械では動きません**（2026-09-20 に踏んだ）。Windows のスタブが
  `Python` とだけ出して**何もせず終わります**——しかも**終了コードは 0** なので、
  ヒアドキュメントで書いた編集が**空振りしたのに成功したと見えます**。私は2回それで
  「足しました」と報告し、ビルドエラーで初めて気づきました。
  **ファイルの編集に python を使わないこと**（Edit ツールか `sed`）。JSON を読むだけでも
  `grep`／`sed` で足ります。
- **`gofmt -w` を glob で走らせると無関係なファイルまで書き換わる**（全角の桁幅で位置揃えが
  直し直される）。名指しすること。`gofmt -l` は CRLF のため全ファイルが挙がるので判断材料にしない。
- **`sed -i` は CRLF を LF に変える**（09-15）。git の `autocrlf` が正規化するので内容の差分は
  ゼロになり実害は無いが、`git status` に触っていないファイルが並ぶ。**本当の差分は
  `git diff --numstat` で見る**（行末だけの変更は行数が出ない）。
- **deadcode の結果を鵜呑みにしない**（09-07・09-15 に2度）。試験だけが使う公開API・設計上の対で
  残している口・意図して残した土台（`deleteToken`）が全部「未到達」に出る。**1件ずつ呼び手と
  コメントを読んでから消す**。
- **Goの到達率（カバレッジ）はパッケージごと**（09-14 に誤読）。`go test ./internal/cms/page -cover`
  の 13.9% はそのパッケージ自身の試験ぶんだけ。`-coverpkg=./...` で測ると `CanView` 等は 100%。
  **低い数字を見て「試験が無い」と読まないこと。**
- **API を外から叩くときの作法**（09-11 に順に踏んだ）: ログインは**フォーム形式**
  （`--data-urlencode 'username=a'`・JSONだと `error=1` で302）／`/api/load?id=` は**生のHTML**／
  **`/api/lock/force` はロックが無いと 204（本文なし）**——JSONとして読むと落ちる／
  **別の口から握ったロックは同じ利用者でも `/api/lock` で取り直せず、`/api/unlock` でも外れない**
  （`Locks.Release` は保持者名とトークンの両方一致が要る。`ok:false, same_user:true` が返る。
  admin の `/api/lock/force` を通す）／
  Python の `subprocess` から curl を呼ぶと `/tmp` が MSYS のパス変換を通らない（`$TEMP` の実パス）。
  ⚠ **同じ理由で、bash のヒアドキュメントで `/tmp` に書いたファイルを python から
  開けません**（2026-09-20 に踏んだ）——bash から見える `/tmp/x` と python から見える
  `/tmp/x` は**別の場所**です。bash の中だけで完結する `cp`・`sed` は通ります。
- **`:memory:` DBは接続ごとに別のDB**。行を読みながら中で別のクエリを投げると**空の別DBに当たり、
  絞り込みが静かに全部落ちる**（09-03 に本番で踏んだ。`view_unhandled.go` は先に読み切ってから絞る）。
  `visibleChildren`・`page.CanView` を通る試験は**ファイルDB**——コアは `newTestFileDB`
  （`testsetup_test.go`）、拡張は自前の `setup_test.go`。
- **E2E は実バグを何度も見つけている**。`assets/` を変更したら `node --check` と `e2e/` を回す
  （毎回回してよい・ユーザー承認済み）。`node` が PATH に無いことがある（`C:\Program Files\nodejs`）。
- **ローカル検証のログインは `a` / `a`**。CSRF対策で `Origin` ヘッダ必須。
- **QRの検証ツールは開発機に pip で入れている**（`opencv-python-headless`・`qrcode`）。製品の依存では
  ない。`tools/qr_verify.py` を走らせるときだけ。

**索引・スキーマ**

- **⚠ 畳めなかったタグは、並べると先頭・絞ると消えます**（2026-09-21・SQLite で実測）。
  ユーザーの問い:「納期に最短納期と入っている場合、検索で一番納期が短いものとして
  出てくるでしょうか？」。`最短納期` は `date` として畳めないので `norm_value` は
  **NULL** です（生の値は正本として残る）。

  | 書き方 | `最短納期` の行 |
  |---|---|
  | `ORDER BY norm_value` | **先頭に来る**（SQLite は NULL を最小として並べる） |
  | `WHERE norm_value <= ?` | **黙って消える**（NULL の比較は偽） |

  **正反対の振る舞いが同じ列から出ます。** 並べて先頭に来るのは意味としては合って
  いますが、`都度`・`未定` も同じ場所に来るので**たまたま**で、根拠にはできません。
  ⚠ **納期・受注残のビューを作るときは** `norm_value IS NOT NULL` を明示し、
  **畳めなかった納期は別枠で必ず見せること**——黙って落とすと「納期が書いてあるのに
  一覧に出ない」になります。いまは並べる口も範囲で引く口も無いので、まだ踏めません。

- **⚠ 新しいテーブルを足しても自動再構築は走らない**（09-13）。`ApplySchema` は空の表を作るだけ
  ——`page_tags` を足した日にタグ88行が古い表に取り残された（画面は無言で空）。いまは
  `DriftedSchemaTables` が「索引に中身があるのに表が無い」もずれとして扱う。
  ⚠ `CREATE INDEX` を表と取り違えて起動のたびに再構築が走る二次災害も出した（`createdTableName`）。
- **⚠ スキーマを変えたら、作り直しが `ApplySchema` より先**。逆だと列を変えた表へ新しい索引を
  張ろうとして起動が落ちる（`no such column: name`）。
- **⚠ 主鍵を付けると隠れていた重複が出る**（試験側で2件——同じページ番号へ洗わず書き足し／
  タグの番号を `dl` ごとに 0 へ戻していた）。
- **⚠ 一括置換で SQL を直すときは UNION を跨がない**——`page_tags` を含む文字列をまとめて置換したら
  `vocab_index` 側の列名まで書き換わった。表ごとに列名が違うので、混ざる場所は手で直す。
- **プラグインテーブルにマイグレーションは無い**。スキーマを変えたら起動時に1度だけ自動再構築。
- **業務ブロックの行き先は `vocab_index`、タグは `page_tags`**（09-13 分離）。③計算は
  [vocab_query.go](../internal/cms/vocab_query.go) を通して読む——**索引の `field` は見出しの
  表示文字**（`品番`）で、機械キー（`item-id`）ではない。
- **業務文書ブロックのヘッダ（素の `dl`）は `section` の側から拾っている**（`syncVocabSection`・
  `eachPlainVocabChild`）。外すと発注元・発注日が索引から黙って消える（`TestSectionHeaderIsIndexed`）。
  ヘッダと明細の対応づけは `block_no`。
- **`text` と `code` は畳む強さだけが違う**。`code`（図面番号・発注書番号）は空白・ハイフン類・
  長音・大小まで畳み、`text` は**長音に触らない**。引くのは `PagesByTag`（生）と
  `PagesByTagLoose`（畳んだ一致）で、**2段にせず呼ぶ側が選ぶ**。
  ⚠ **`NormalizeForLookup`（畳んだ文字列だけ返す口）は 09-15 に撤去した**——`norm_value` に
  宣言型が無くなった日から、文字列で引くと number のタグが**エラーにならず0件**になる。
  09-14 に「設計判断を1日で捨てない」と残したが、捨てたのは判断ではなく古くなった実装。
- **語→型の推論辞書はコードではない**。正本は `config/settings.json` の `vocabulary`。
  **壊れていると起動が止まる**（黙って既定値で上書きすると運用者の語が消える——仕様）。
  語を足したら**DB再構築で効く**。
- **観察係の `descend` は OR で畳まれる**。AND にすると明細が索引から消える
  （`TestObserversSeeNestedItemsTable`）。
- **本文の走査はコアの配送係だけ**（`walk.go`）。`WalkElements` を自分で回す新しいコードは
  たいてい設計を間違えている。
- **テンプレート領域（ルート含む）は③計算に載らない**。判定の先祖辿りは**サイドカー＋本文HTML**
  ——DBを見ると静かに壊れる。
- **③計算の集計スコープは `cms.RelatedPages`**（自ページ＋参照で直接つながるページ・深さ1・両向き）。
  `block_no` はページの中の連番なので、**スコープが広がったあとはページごとに閉じて突き合わせる**。

**本文・参照**

- **⚠ 本文の空欄は `<dd><br/></dd>`**（サニタイズが入れる）。`<dd>([^<]*)</dd>` だけで探すと
  **空欄を素通りする**（09-11 に `客先` を3枚取りこぼした。`replaceFirstFieldValue` は `<br>` も空と扱う）。
- **項目の鍵は見出しの表示文字**（`th`／`dt`）。機械キーを本文へ書く属性は無い。
- **参照タグと読むのは可変タグ（`dl[data-type="tags"]`）の中だけ**。素の `dl` まで見ると
  **発注書番号が参照に化ける**（`250401-203` は「6桁-英数字」に当てはまる）。桁を絞るだけでは足りない。
- **参照が飛ぶ先は常に本文のブロック**（`#<data-id>`）。添付そのものを指す経路は無い——だから
  **添付の識別子もブロックIDと同じ4桁**（ファイル名＝URL＝`data-id` の3役は不変。既存の8桁は
  そのまま）。一意にすべき範囲は `files/` の中と本文の `data-id`。
- **社内コードは「ページ番号-ブロックID」**。指し先は**消えないもの**にすること（図面ブロックは
  人が消せるので、改訂履歴の行や構成部品の行を指す）。
- **本文の取得口は `GET /api/load` の1つ**（サニタイズを通す）。**アンカー合成
  （`RenderAnchors`）と参照リンク（`RenderReferenceLinks`）は通さない**——合成HTMLが本文として
  保存されてしまうため、ページ描画だけに掛ける。
- **⚠ 自動保存は1.5秒のデバウンス。退出はそれを流してから**（09-15 に直した）。
  `onModeToggle` が保存を待たずに `releaseLock()` を呼んでいたので、**最後の入力から1.5秒
  以内に閲覧へ戻すと、その分が黙って消えていた**。打つそばから反映するプロパティ欄
  （リンクの href・ファイル表示の参照）ほど踏みやすい。保存の3関数は約束を返すので、
  **「保存してから何かする」経路を足すときは `await saveToServer()` を通すこと**。
- **エディタのクロームは `.vocab-chrome`**（または本文DOM外）。シリアライザが保存しない。
- **見た目の印は本文に保存できない**——`class` はサニタイズで落ちる。宙ぶらりんの参照の薄赤
  （`ref-missing`）のように、**表示時に付ける鏡型**で作る。
- **殻の `id` は接頭辞 `w-` を独占する**。本文の `id` は自由だが、接頭辞つきは剥がされる。
- **子ページの並びは「並び順キー → 題 → ID」**（`sortChildren`）。キーの正本はサイドカーの
  `sort_key`。ドラッグ並べ替えはその親の子を全部振り直す（`ReorderKey`）。
- **版は本文HTMLだけ**。リバートで権限・所有者・親は巻き戻らない——この線引きが安全の要。
- **スラッシュメニューの項目は絞り込みのたびに作り直す**。click / mouseenter は作るたびに配線。

**機械が書くとき**

- **⚠ 社名を書く口は4つあり、揃っていないと意味がない**（2026-09-21）。
  `contacts.OrgNameForPage` を通るのは**解析が書く `発注元`・`客先`／受注の整理／
  加工製品の整理／連絡帳の登録フォームの初期値**の4つ。1つでも生の名前を書くと、
  そこから `株式会社○○` のページが生まれ、**完全一致で結ぶ2つの木が黙って
  結ばれません**（`linkPartner`・`PartnerByTitle`）。新しく社名を書く口を足すときは、
  必ずこの関数を通すこと。
  ⚠ **候補が2つ以上のときは畳みません**——`株式会社あさひ` と `有限会社あさひ` が
  どちらも連絡帳に在るとき、畳んだ `あさひ` は**どちらでもない第三の名前**です。
  ⚠ **残る代償**: 2社が**どちらも連絡帳に居ないうちに**続けて届くと、`あさひ` として
  1つの取引先ページへ入りえます。生の社名は受注ページの「顧客の発注書（読んだまま）」に
  残るので、気づいた人が直せます。
- **⚠ 機械が既存ページの本文を書き換えるときは関門が要る**（09-14）。`RewriteBody` /
  `SetPageH1` は読んで・変えて・書くので、エディタが開いているとオートセーブと黙って上書きし合う。
  `RequireEditLock` は**使えない**（一覧画面のボタンはトークンを持てず必ず409）。使うのは
  `editlock.RefuseWhileEditing`（1ページ）か `editlock.Locks.EditorOpen`（まとめ押し・1件ずつ飛ばす）。
  **自分が開いていても断る**／**保持者が居なくなったロックは無視する**。正本は
  [【考察】同時編集の競合対策.md](【考察】同時編集の競合対策.md) §9。
- **機械が作るページは行き止まりにしない**（09-11）。計算ビューは**本文にブロックを置いて初めて
  描かれる**——`取引先` が見出しだけで、アドレス帳が100通のあいだ誰にも見られていなかった。
- **取り込み係・解析はコアの回覧と共通の芯に乗る**——ページ作成の芯は `CreateChildPage`、
  重複検知の分担は「鍵の取り出しは取り込み係（`SourceRefFinder`）・照合はコア
  （`ExistingIntakePage`）」。専用テーブルは無い。
- **新しい語彙はレジストリへ1件**——コアの器は `internal/cms/vocab.go`、業務の語彙は拡張が
  `RegisterVocab` で持ち込む。**カスタム要素は作らない**
  （[【ガイド】プラグイン開発.md](【ガイド】プラグイン開発.md)）。
- **ページ削除は `data/trash` への移動**。トップと子持ちは不可・編集ロックが要る。
  **復元UIは未実装**——手で `data/master` へ戻してDB再構築。

**安全**

- **インラインの script/style・`on*=` は書けない**（CSP strict・[開発方針.md](開発方針.md) §4）。
  `csp_test.go` はポリシー文字列しか見ていない——配線の固定は `TestCSPIsWired`。
- **DOM生成は `createElement`＋`textContent`**。`innerHTML` へ文字列を入れない
  （`escapeAttr` 頼みの保存型XSSが実際に発火した過去がある）。
- **サニタイズ後にHTMLを足す関数**（`RenderComputedViews`・`RenderAnchors`・`RenderPageShell`・
  `RenderReferenceLinks`）は自前でエスケープ責任を負う。破れると保存型XSS。
- **本文の埋め込み（`<img src>` 等）の宛先は `/data/` 配下ときれいなURLに限る**。リンクは制限して
  いないので、**クロームがリンクから埋め込みを導出するときは形を必ず検査する**
  （`refreshAttachmentPreviews` が手本）。状態を変える経路はメソッドを絞る（`route_guard_test.go`）。
- **認可の読み口はサイドカー（正本）ではなく `cms.db`（派生）**——ズレたら認可は索引に従う。
  **派生から正本へは書き戻さない**（サイドカーが読めなければ止まる）。
- **匿名には「読めない」と「存在しない」を区別させない**（どちらも404。例外はトップだけ）。
  戻り先は `safeNextPath` が同一サイト内の絶対パスだけに絞る。
- **添付の保存先は `files/` サブフォルダ**——名前衝突の上書きは構造的に不可能。名前の検査
  `SafeAttachmentName` は全アップロード口が共有する。**画像は保存前に EXIF を落とす**
  （JPEG は再符号化）。**SVG の安全性は配信側が本体**（`attachment`＋sandbox CSP）。**PDF は
  インライン配信**、ZIPは目録読みだけで展開しない（ZIP爆弾対策）。
- **Gemini は有料枠のキーで運用する**——無料枠では入力データが製品改善に使われる。コードからは
  判定できず**運用側で守るしかない**。キーの値はチャット・ログ・リポジトリに残さない。
- **ルート表は `buildHandler()`**（`cmd/w-cms/main.go`）。拡張の口は各プラグインの `Routes()`。

**メール**

- **IMAP と SMTP は同じリソース**（Exchange Online）なので**トークンは1つ**（Graph 時代の
  `AADSTS70011` の罠は IMAP 化で消えた）。
- **SMTP で投函したメールは Outlook の「送信済みアイテム」に残らない**（仕様）。控えは通信箱。
- **新しい権限は動的同意で足せる**（`AADSTS65001` が出たらサインインし直す）。同意はその利用者ぶんだけ。
- **⚠ `AADSTS65001` は `invalid_grant` の形で返ってくる**。「失効した」とみなしてトークンを捨てると
  **権限を増やした日にサインインが消える**（09-05 に実際に消した）。**保管は自動で捨てないこと**
  ——`ext/comm/mail/store.go` の `deleteToken` は将来のサインアウトの土台で、**自動では呼ばない**。
- **既読にしない。** IMAP は `EXAMINE`＋`BODY.PEEK[]`。**IMAP のリテラル**は行単位で読むと崩れる
  （`fetch` が先にサイズを読む）。

**WebDAV・QR**

- **`x/net/webdav` の `handlePut` は `OpenFile` のエラーを「存在しない」以外すべて 404 に潰す**
  ——書き込みの断りは `DavHandler` 側で返す（`refuseDavPut`）。判定は `OpenFile` にも残す
  （ハンドラ＝体裁の層、FS＝安全の層）。
- **⚠ QRは「他所のライブラリと同じ絵」にならない**（09-10 に半日溶かした）。segno・qrcode・w-cms は
  3つとも違う絵を出し、どれも読める（詰め草とマスク選択の差で、どちらも結果に影響しない）。
  **絵の一致を正しさの尺度にしない**——`python tools/qr_verify.py` が OpenCV で復号して確かめる
  （固定データを `-update-qr` で更新したら**必ず走らせる**）。**QRの壊れ方は静か**——誤り訂正を
  数バイト間違えても絵は出て、読み取り機が黙って諦めるだけ。

## 現在の状態（環境）

- **E2E は28本**（`verify-*.js` 16本＋`probe-*.js` 12本）＋共通ヘルパ `e2e/lib.js`。
  09-15 に `probe-extension-gating`——**期待値を `/api/tag-schema` から導くので、通常・`-tags nomail`・
  `-tags minimal` のどのビルドのサーバーに当てても通る**。3つとも流して初めて確かめたことになる。
  `cd e2e && npm install && npx playwright install chromium`。
  09-14 に足した7本: `probe-fail-message`（失敗の理由が画面に出る）・`probe-upload`
  （添付3経路。⚠ 画像の欄名は `image_file`）・`probe-thread`・`probe-analyze-refresh`
  （Gemini を呼ばずに配線だけ）・`probe-coltypes`・`probe-inline-pdf`・`probe-file-view-elsewhere`。
  ~~**⚠ 見る先が2つに割れている**~~ **→ 2026-09-16 に畳んだ**。古い `verify-*` 11本に
  `ignoreHTTPSErrors` を足したので、**`WCMS_BASE=https://localhost:8443` を渡せば一式が
  1つのサーバーで流せます**（既定はいまも `http://localhost:8080`）。
  **⚠ 当て先を焼き込まないこと**（2026-09-16）。ページIDも添付IDもデータを入れ直すたびに
  変わり、**2回続けて E2E を壊しました**（09-13 と 09-16）。いまは `e2e/lib.js` が
  **走るときに探します**——通信箱は「トップ直下で題が『通信箱』」、通信記録は
  「`チャネル` タグと `.pdf` の添付を持つページ」、返信の鎖は「`メッセージID` と
  `返信元メッセージID` の突き合わせ」。新しいスクリプトもこれを通すこと。
  ⚠ **前提が無いときは「飛ばす」**（失敗にしない）。一掃直後は取引先ページも顧客ページも
  無いので、`probe-extension-gating`・`verify-filing-machines` はその項目を飛ばします
  ——**壊れたのか、まだ登録していないだけなのか**が読む人に分かる形にすること。
  ~~**⚠ `verify-drawing-size.js` はいま落ちる**~~ **→ 09-15 に直した**（`4d332a2`）。
  **落ちていた理由は2つあり、知られていたのは片方だけだった**——①当て先 `010268` が
  09-13 の入れ直しで別のページになっていた ②見る先が `.drawing-inline` で、これは
  09-14 の作り直しで**消えた class**（いまは `.file-view`）。**①だけ直しても通らない**。
  いまは当て先を自分で2枚作って最後に消すので、データを入れ直しても落ちない。
  **⚠ 流すとトップ直下に定型名のページが積もる**——本物のテンプレート木も「テンプレート」なので
  中身と更新時刻で見分ける（一度巻き込んで復元した）。**続けて流すとたまに1本落ちる**
  （前のスクリプトの編集ロック残り。まず再実行）。
- ⚠ **職場で次に触るとき、先にやること**（2026-09-21 夜・自宅から）:
  0. **`git pull`**——自宅から**17コミット**押してあります（09-21 のリファクタリングと、
     同日夜の受注ページまわり5件）。⚠ **職場の写しが 09-20 の履歴書き換えより前なら
     `git pull` は通りません**——下の「履歴を書き換えたので…」のとおり**再クローン**です。
  1. **サーバーを入れ替える。** ⚠ **入れ替えたら、実行ファイルの更新時刻とプロセスの
     起動時刻を必ず突き合わせること**（下のコード片）。09-21 にこれを怠って、
     **「できていない」**とご指摘を受けました——動いていたのは8時間半前のビルドでした。
  2. **ブラウザを Ctrl+F5。** `assets/app.css` と `app.js` が変わっています
     （表の折り返し・横スクロール・検算の緑）。古いままだと**直っていないように見えます**。
  3. **DB再構築**（`POST /api/rebuild-db`・管理画面のメンテナンス）。09-20 に
     `config/settings.json` へ `小計`・`消費税`・`合計金額` を足したので、
     **再構築するまで索引に載りません**。⚠ 検算そのものは本文を読むので再構築なしでも効きます。
  4. **既にある受注ページを 🤖 解析し直す。** ⚠ **09-21 の変更はどれも「解析がページを
     組むとき」に効きます**——原本のPDFの枠・品名/品番/単位/納期の正規化・品番の選び方・
     `発注元` の法人格落とし・小計/消費税/合計のタグ。**既にあるページは何も変わりません。**
     ⚠ そのとき見たいのは「**実物の発注書の見出しで、数量・単価・金額の列が見つかるか**」
     （見つからなければ検算の行に理由が出ます）と、**品番に何が入ったか**
     （言い換えたなら表の手前に「解析は、先方の『◯◯』を弊社の『品番』に入れました」が出ます）。
  5. **連絡帳に取引先を登録する。** ⚠ **登録の前と後で社名の形が変わりえます**——
     連絡帳に居なければ法人格を落とした形、居ればその実物の題を書きます
     （`OrgNameForPage`）。**先に連絡帳へ入れてから解析し直すほうが、題が一度で決まります。**
- ⚠ **職場のサーバーは 2026-09-20 21:23 のビルドのままです**
  （`scratchpad/w-cms-full.exe`・:8443・`WCMS_DAV=1`）——**09-21 の変更は1つも入っていません**。
  自宅は 09-21 10:35 のビルドで動かしていました（`go run ./cmd/w-cms`・同じ環境変数）。
  ⚠ **入れ替えたら、実行ファイルの更新時刻とプロセスの起動時刻を必ず突き合わせること**——同日、それを怠って
  **「試験も変異試験も通っているのに実物では何も変わっていない」**と報告させました。
  ```powershell
  Get-NetTCPConnection -LocalPort 8443 -State Listen | ForEach-Object {
    $p = Get-Process -Id $_.OwningProcess
    [PSCustomObject]@{ Start=$p.StartTime; ExeWritten=(Get-Item $p.Path).LastWriteTime } }
  ```
- ⚠ **履歴を書き換えたので、古い写しからは `git pull` できません**（2026-09-20）。
  **再クローンしてください**:
  ```bash
  mv w-cms w-cms-old          # ⚠ data/ は clone に含まれないので先に退避
  git clone https://github.com/wordring/w-cms.git
  cp -r w-cms-old/data w-cms/data   # 骨組みだけの環境なら要りません
  ```
  ⚠ **`config/settings.json` を手元で直していたら、それも移すこと**（Git管理なので
  普通は要りませんが、試していた語が消えます）。
- **`data/` は環境ごとに独立**（`.gitignore`）。**職場**: 2026-09-17 の夕方に**初期化ボタンで
  入れ直しました**（ユーザー:「残すべきデータはありません。設定のみ残してください」）。
  いま**78ページ**——トップ・テンプレート（`000001`）・通信箱（`000002`・実メール50通・
  ZIP は中身まで展開済み・添付には目録 `files/meta.json` つき）・連絡帳（`000003`）・
  取引先（`000004`）・受注（`000005`）。連絡帳には**東邦金属工業所**と
  **南北スポーツ機械**、取引先には**南北スポーツ機械**（`連絡帳` の参照つき）。
  控えは3つ（`data/_reset-20260917-*`。要らなければ消してよい）。
  サーバーは `scratchpad/w-cms-full.exe`（フルビルド・:8443・`WCMS_DAV=1`）。
  ⚠ **E2Eを一式流すとトップ直下にゴミが積もります**（09-16 は45枚）。初期化ボタンで
  片付けるのがいちばん速い。**開発メニュー**（同じ管理画面）に「メールを50通取り込む」も
  あるので、入れ直しは「初期化 → 置き場 → 取り込み」の3押しで回る（2026-09-17）。
  ⚠ **控えが1つ残っています**（`data/_reset-20260917-125513/`・09-16 の67ページ）。要らなければ消してよい。
  サーバー側には521通見えていて、`POST /api/mail/import` は**1回で最大50通**（件数を指定する
  口はありません）。⚠ **ページIDは総入れ替えになりました**（`010153`→`000001` 等）——
  E2Eの当て先は焼き込みをやめて**走るときに探す**形にしたので、次の入れ直しでは落ちません
  （`e2e/lib.js`）。`data/mail/a.json`（リフレッシュトークン）があるのでサインイン済み。
  ⚠ **一掃で消してはいけないのは3つ**——`data/mail/`（トークン。消すと取り込めない）・
  `data/auth.db`（a/a のログイン）・`data/tls/`（**各PCの信頼ストアに入れた証明書**。
  作り直すと指紋が変わり全PCで入れ直し）。
  ~~⚠ **一掃直後は `取引先` ページが在りません**~~ **→ 2026-09-16 に仕組みで直した**。
  **管理画面（`/assets/admin.html`）の「置き場」で、足りないものをボタンで作れます**
  ——通信箱・取引先・受注・テンプレート置き場の4つ。⚠ **URLは `/assets/admin.html`**
  （`/admin` ではありません）。正本は [required_pages.go](../internal/cms/required_pages.go)。
  **現在のID**: 通信箱 `000001`・受注 `000105`・テンプレート `000106`・取引先 `000107`。
  **自宅**: 骨組み5枚だけ（トップ・受信箱・テンプレート・新しいページ・通信箱）——
  **取引先ページもメールも無い**。メールを試すならそちらでサインインが要る（`/api/mail/signin`）。
- **一掃のやり方**: サーバーを止め、残すページ以外の `data/master/*/*` を消し、`data/trash` を
  空にし、`data/cms.db*` を消してから起動する（ログに「空のDBとHTMLファイルを検出」）。
  **`data/mail/` は消さないこと**（サインインのトークン）。**`data/trash` は普段は消さないこと**
  （削除したページの唯一の写し）。エディタでデータファイルを開いていると `data/master` を
  動かせない（PowerShell の `Move-Item` なら通る）。
- **スキーマ変更を含む版で初めて起動する環境では自動再構築が1度走る**（ログに「テーブル定義の
  変更を検出しました」・無害）。
- **`go test -race` が使える**（gcc 導入済み）。PATH に WinLibs の
  `~/AppData/Local/Microsoft/WinGet/Packages/BrechtSanders.WinLibs.POSIX.UCRT_*/mingw64/bin` を足し
  `CGO_ENABLED=1 go test -race ./...`。通常ビルドは Pure Go（`CGO_ENABLED=0`）。
- **秘密にすべき設定は `~/OneDrive/デスクトップ/w-cms.env`**（リポジトリ外。家と職場で同じ内容を
  使うため OneDrive——**キーがクラウドと全端末に載る**引き換えは承知のうえ）。住人は
  `WCMS_MAIL_CLIENT_ID`・`WCMS_MAIL_TENANT_ID`・`GEMINI_API_KEY`・`WCMS_SECURE_COOKIES`。
  **書くのは人**——値をチャットへ出すと文脈とログに残る（09-05 に実際に残った）。
  読み込みは `set -a; . ~/OneDrive/デスクトップ/w-cms.env; set +a`。**アプリは読まない**
  （env を読む口を足すと本番の設定経路が2本になる）。
- **Gemini の実測は職場マシンで済み**（有料枠）。スマホからの実機確認も済み（LAN内
  `http://<IP>:8080`・`WCMS_SECURE_COOKIES=0` 必須・本番の値では絶対にやらない）。

## 開発環境の再現（自宅などで再開する手順）

```bash
git pull && go mod tidy

# 初回のみ管理者を作成して起動（ローカルHTTP検証なので Secure Cookie 無効）。
# ローカル開発の管理者は user=a / password=a に統一する（環境間で揃えるため。2026-07-04 決定）。
WCMS_ADMIN_USER=a WCMS_ADMIN_PASSWORD='a' WCMS_SECURE_COOKIES=0 go run ./cmd/w-cms
# 2回目以降は環境変数なしで go run ./cmd/w-cms
#
# ⚠ **ファイルを名指し（go run cmd/w-cms/main.go）してはいけません**——cmd/w-cms/ の
#   ext_mail.go・ext_subcon.go・ext_contacts.go がコンパイルされず、**拡張が1つも載りません**
#   （起動ログが「拡張セット: なし（素の w-cms）」になります・2026-09-05 に踏んだ）。
# ⚠ **メールを使うなら WCMS_MAIL_CLIENT_ID と WCMS_MAIL_TENANT_ID も渡すこと**。
#   無いと起動はしますが「メール送受信: 設定待ち」になります。
#
# **秘密にすべき値はデスクトップの w-cms.env に置きます**（リポジトリ外・2026-09-05）。
# 起動はこの形で:
set -a; . ~/OneDrive/デスクトップ/w-cms.env; set +a; go run ./cmd/w-cms
# → http://localhost:8080 （/login にリダイレクト。a / a でログイン）

go test ./...
```

注意:

- **ローカル開発のログイン**: `a` / `a`（**ローカル検証専用**。本番では使わないこと）。
- `data/` はコミットされないため、別環境では初回起動時に上記の環境変数で管理者が作られます。
  **「家でログインできない」は、初回の管理者未作成か別の認証情報**。
- **既存 `data/auth.db` の認証情報を忘れた／揃えたいとき**は、サーバーを止めてから以下で揃う:
  ```bash
  # 一時パッケージで internal/auth を呼ぶ（ルートに別の package main があるためサブdirで実行）。
  mkdir -p _resetpw && cat > _resetpw/main.go <<'GO'
  package main
  import ("fmt"; "w-cms/internal/auth"; "w-cms/internal/database")
  func main(){ database.InitAuthDB(); defer database.AuthDB.Close()
    if err := auth.SetPassword("a", "a"); err != nil {
      if err2 := auth.CreateUser("a", "a", true, ""); err2 != nil { fmt.Println(err, err2); return }
    }
    fmt.Println("ローカル管理者 a / a を用意しました") }
  GO
  go run ./_resetpw && rm -rf _resetpw
  ```
- ローカル実行・プレビュー用に `.claude/launch.json`（`go run` 起動設定）を用意済み
  （プレビューでCookieが付くよう `WCMS_SECURE_COOKIES=0` 設定済み）。
- 取り込み・解析を試すには、トップ直下に「通信箱」ページを作り（名前が機能）、
  `GEMINI_API_KEY` を設定して起動する（無ければ解析ボタンが設定を促すだけで、他は全部動く）。
