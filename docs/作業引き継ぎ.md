# 作業引き継ぎ

> 別の環境（自宅・職場など）で作業を再開するためのスナップショットです。
> **現況と次の一手だけ**を書きます——経緯は [変更履歴.md](変更履歴.md)、決定の理由は
> 各設計書の決定ログが正本です（古くなったら更新し、大きな区切りではこのファイルごと
> 差し替えてください）。
> 最終更新: 2026-08-31（**D-1 完了**——硬いドメイン表を全廃し、業務データの行き先は
> `vocab_index` 1本になりました。コアのスキーマに**ドメイン語彙を持つテーブルは1つも
> ありません**（`pages`・`page_perms`・`vocab_index`）。削除したのは受注・受注明細・発注・
> 発注明細・販売見積・購入見積・部材の7テーブルと所有プラグイン3本。先に集計
> （`RequiredMaterials`）を索引読みへ書き換え、緑を確認してから消しました。
> 索引を読む層 `vocab_query.go` を新設（仕事は**鍵の変換**——見出しの表示文字→機械キー）。
> 途中で穴が1つ見つかり先に塞いでいます——**業務文書ブロックのヘッダが索引に載って
> いなかった**（素の `dl` は引き金が無く配送されない）。放置すると発注元・発注日が
> どこにも残りません。`go test` 全パス・**E2E 9本218項目すべて通過**。
> ⚠ **既存の `cms.db` を持ち越すとヘッダの行だけが入りません**——スキーマが変わって
> いないので自動再構築が発火しません。**一度 `cms.db` を消して起動**してください。
> **次は D-2（機能見出し）＝本丸**。
>
> 前回更新: 2026-08-30 夜（**決定の日であり、実装が始まった日**。前半で D-1〜D-10 が全部決まり
> **[判断待ち.md](判断待ち.md) は空**——決定ログは [アーキテクチャとDBスキーマ.md](アーキテクチャとDBスキーマ.md) §9。
> 後半で実装に着手し、**この日のうちに入ったもの**: `data/settings.json`＋語→型辞書の移設（§9.4）／
> `norm_num` と値の索引2本＋`updated_at` の ISO 化／**`page_tags` テーブルの吸収**（D-1 の第一歩・
> プラグインごと削除）。あわせて大掃除——deadcode 実測で死者11件削除・dt/dd 走査6重複を
> `eachDLPair` へ併合・**正本を書く全経路を原子的書き込みへ**（`page.WriteFileAtomic`・
> 小粒な残り「WriteSidecar が O_TRUNC」も解消）・コードリファレンス測り直し。
> `go test` 全パス・E2E は stage2/3・image・version を実測。**次は D-1 の残り7テーブル**
> ——③計算プラグインが読んでいるので、集計の書き換えと一体で（下の 1 番）。
> ⚠ この版を初めて起動する環境では**自動再構築が1度走る**（norm_num 追加のため・無害）
>
> 前々回更新: 2026-08-26（**実装可能な項目をすべて消化した日**——「決定済みで小さいもの」5件・
> 画像アップロード・公開専用ビュー・版管理・形式ごとの視覚的区別・**回覧化**・スラッシュメニュー。
> 同日の職場での実機確認から追加3件——**ブロックの削除ボタン**・**可変タグのチップ表示**・
> **タグは名前：値の1対の決定**（＋将来要件「保存した画像の編集」）。職場マシンでも
> `go test` と E2E 全本を実測済み。残るのは**設計から始める2件**だけで、どちらも
> ユーザーの決定が要ります（下の 1・2 番）。
> 行き先は [変更履歴.md](変更履歴.md) 2026-08-25〜26、各設計書の決定ログ）

## いまここ: 決まったことを実装していく段


- **D-1 が完了した**（2026-08-31・自宅）——硬いドメイン表は**1つも残っていません**。
  読む側の入口は [vocab_query.go](../internal/cms/vocab_query.go) の3つだけ:
  `vocabTableRowsOf`（表の行）・`vocabBlocksOf`（名前：値・ヘッダ）・`pagesByTag`（逆引き）。
  詳細は [変更履歴.md](変更履歴.md) 2026-08-31、正本は
  [アーキテクチャとDBスキーマ.md](アーキテクチャとDBスキーマ.md) 2章・§9。
  **次は D-2（機能見出し）**——索引の `data_type` に入るものを `data-type` の値から
  見出しの言葉へ移す本丸。差し替え点は `triggerOf`（[walk.go](../internal/cms/walk.go)）1関数に
  隔離済みですが、**索引の中身が総取り替えになる**ので全再構築が要ります。
- **DBスキーマ設計し直しの骨格が決まった**（2026-08-30）——決定は
  [アーキテクチャとDBスキーマ.md](アーキテクチャとDBスキーマ.md) **§9 の決定ログが正本**
  （D-1 汎用索引へ一本化／D-2 機能見出しの採用＝本丸／D-3 索引は1テーブル縦持ちのまま・
  正規化はPDF取り込み時＋認識できた形式は背景色／D-4 **参照は本文の「名前：値」タグで表し
  `page_refs` は作らない**／D-5 設定はファイル正本 `data/settings.json`／日時は ISO 8601）。
  到達点は **コア2つ＋汎用索引1本＋設定ファイル1本**で、ドメイン語彙を持つテーブルは残りません。
  **同日夕に D-3残・D-6〜D-10 も回答をいただき、[判断待ち.md](判断待ち.md) は空になりました**。
  参照の値は業務番号ではなく **`ページID-ブロックID`**（ユーザー:「**URLのリンクみたいな感じです**」）
  で、`RenderAnchors` が既に `#<ブロックID>` を解決できるため**押せば飛べます**。
  ただし**ブロックIDは入れ子の要素に振られていない**ので、`<section>` 内の明細表を指せません
  ——実装前に埋める穴として §9.3 に書いてあります。

- **語彙モデルの移行は完了**（2026-08-20）——カスタム要素は0種、本文は構造HTML＋`data-*`
  マーカー、計算ビューはサーバー事前描画、CSP は strict。経緯は [変更履歴.md](変更履歴.md)
  2026-08-19〜08-20、設計の理由は [【考察】語彙モデル.md](【考察】語彙モデル.md) §9。
  ページテンプレートも同日に実装済み（**運用開始前に、実データのトップ直下へ「テンプレート」
  ページを作るところから始めます**）。
- **設計総点検の対処が一段落**（2026-08-21）——補完調査を完走して11件、続けて編集画面のバグ2件
  （貼り付けで編集不能・「保存済」と出たまま入力が消える）を修正。何を直したかは
  [変更履歴.md](変更履歴.md) 2026-08-21 の各節、判定と残りは
  [【考察】設計総点検.md](【考察】設計総点検.md) §2（対処）・§3（未対処と行き先）。
  （同文書は 2026-08-21 に記録として圧縮済み——個別指摘の原文は git 履歴にしか無く、
  **原文のまま通った指摘は1件もなかった**ことだけ覚えておけばよい）。
- **判断待ちの回答が全部返った**（2026-08-21）——決定は各設計書の決定ログへ、着手待ちの作業は
  下の「次にやること」へ移してあります。
- **「黙って壊れる」3件を片付けた**（2026-08-22）——起動時の索引再構築の中断・
  サイドカーの自動修復・匿名応答の404統一。詳細は [変更履歴.md](変更履歴.md) 2026-08-22。
- **決まっていた実装をすべて片付けた**（2026-08-25〜26）——「決定済みで小さいもの」5件
  （監査記録・自動集計の表・未定義種別の赤い告知・公開ページのちらつき・小さな整理2件）に加え、
  **画像アップロード／公開専用ビュー／版管理（リビジョン・リバート）／形式ごとの視覚的区別／
  パーサとプラグインの回覧化／スラッシュメニューの到達性**。詳細は
  [変更履歴.md](変更履歴.md) 2026-08-25〜26。
- **いま残っているのは「設計から始める2件」だけ**（下の 1・2 番）。どちらも**ユーザーの決定が
  要る論点**を含むので、着手の前に相談してください。実装だけで進められる仕事はありません。

## 次にやること（優先度順）

各項目は「**何を・なぜ・正本はどこか**」だけです。着手時は正本を読み直すこと
（調査済みの実装詳細は意図的に削っています——古い詳細はむしろ有害なので）。

**どちらも設計から。着手前にユーザーと決めることがあります**

1. **データベースのスキーマを設計し直す**——ユーザー:「データベースについては、大幅な設計変更が
    必要です。**速度のためではなく柔軟性のためのスキーマ設計**をします」。**方針は決まりました**
    （[アーキテクチャとDBスキーマ.md](アーキテクチャとDBスキーマ.md) §9 の決定ログが正本）が、
    **実装は全部これから**。着手前に [判断待ち.md](判断待ち.md) の D-7〜D-10・D-3残・D-6 を読むこと。
    実装の作業項目はいまのところこれだけ判明しています:
    - **硬いドメイン表8つを落とし、`vocab_index` へ寄せる**。集計は Go で束ねる。
      **`page_tags` は吸収済み**（2026-08-30・読む者がいない書き込み専用テーブルだった。
      「親ページID」の旧ガードも同時に撤去）。残りは受注・受注明細・発注・発注明細・
      販売見積・購入見積・部材の7つ——こちらは③計算プラグインが読んでいるので、
      **集計クエリの書き換えと一緒に**やる必要がある
    - **コードリファレンスの作り直し**——page_tags 吸収を反映していない。
      スキーマ実装が一段落したら /code-reference で（毎回やらず、まとめて1回）
    - ~~**機能見出しへの切り替え**~~ **完了**（2026-08-31・D-2 読み側＋書き側）——
      `<section><h2>検査記録</h2><table>…` が属性ゼロで索引・鏡・種まき・告知に効き、
      エディタも見出し形で挿す（明細表は Items 宣言で解釈・本文から業務の機械語が消えた）。
      メニューは「骨組みの下書き」になり、汎用の「表」「定義リスト」も追加。
      規則の決着は [【考察】語彙モデル.md](【考察】語彙モデル.md) §11.5 の追記
    - **テキストの正規化**——いま `NormalizeValue` は number と date しか正規化せず text は素通し。
      **参照タグ（D-4）が引けるための前提条件**。畳む範囲は D-7 の回答待ち
    - ~~**数値を数値の格納クラスへ**~~ **実装済み**（2026-08-30）——`norm_num` REAL を追加。
      number 型は数値としても入り、`ORDER BY norm_num` で `"8000" < "900"` の罠を踏まない
    - ~~**値の索引**~~ **実装済み**（2026-08-30）——`(field, value)` と `(field, norm_value)` の2本。
      逆引きは**生テキスト（value）**に対して引く（生が正本）
    - ~~**`data/settings.json`**~~ **実装済み**（2026-08-30・§9.4）。最初の住人は
      **語→型の推論辞書**で、テンプレート置き場の名前は未決のまま（下の未決 1）
    - ~~**`updated_at` フォールバックを `T` 区切り ISO へ**~~ **実装済み**（2026-08-30）——
      サイドカーが無いときも Go 側で RFC3339 UTC を入れる。SQL の `CURRENT_TIMESTAMP` は撤去
    - **PDF取り込み時の正規化と、認識できた形式の背景色**（D-3）
    - **入れ子の要素にもブロックIDを振る**（§9.3）——いま `data-id` はトップレベルにしか
      付かないので、`<section>` 内の明細表を参照で指せない。`vocab_index.block_id` も入れ子で空
    - **参照タグをリンクとして描画する**（§9.3）——`RenderAnchors` が `#<ブロックID>` を既に
      解決できる。**描画時だけ**行うこと（`/api/load` に混ぜるとリンクが本文として保存される）
    - **宙ぶらりんの参照を背景色で知らせる**（D-10・§9.2）——**読むたびに判定する鏡型**。
      保存時に焼くと「あとから参照先を作っても赤いまま」になる
    - ~~**型推論辞書の置き場**~~ **実装済み**（2026-08-30・ユーザー:「辞書もそこへ移してください」）。
      `data/settings.json` の `type_inference` が正本で、**足してDB再構築すれば効きます**
      （再起動は不要）。Goコード内の並びは既定値として残ります
2. **通信記録の取り込み**（最大の未着手テーマ・設計から）——正本は
    [【考察】通信記録処理.md](【考察】通信記録処理.md)（決定5件は §2.7、未決は §11）。本体コストは
    **部材手配・利益計算を `page_id` 一致から参照追従JOINへ書き換える**こと（§2.5）。
    **参照の持ち方は決着済み**——`page_refs` は作らず本文の「名前：値」タグ
    （アーキテクチャとDBスキーマ.md §9.1）。同メモは全編が `page_refs` 前提で書かれているので、
    **冒頭の ⚠ 読み替え表を先に読むこと**。参照マーカー（受信元・前版）の移行もここ
    ——**語彙モデル移行でスコープ外にした唯一の残りがこの参照系**です。
    1番のスキーマ設計と表裏なので、順序も一緒に決める。

### 決定はしたが、着手はもっと先

- **サイト内検索**——ユーザー:「発注書番号からページを検索できる必要があります。これは汎用的な
  タグの名前が『発注書番号』、値がその番号にすればよい」「図面番号、図面名称など様々なタグが
  あります。のちのち、これらを検索できるようにしたいです」。**w-cms を作る動機の1つが「ワンノート
  がハイフンで区切られた図面名称をうまく検索できない」こと**でした。**検索そのものが未実装**なので
  設計から（要件上は努力目標・[要件定義書.md](要件定義書.md) §4.4）。
- **見積もりの横断比較**——ユーザー:「見比べることはよくありますので、いずれその機能も必要に
  なります」。いまは書き出すだけで読み出す機能が無い。**新機能**として設計から。
- **添付ファイルの表示と操作**——種類別表示（画像インライン・PDFサムネイル・バイナリは
  サムネ＋クリックで開くかDL）・ファイルへのコンテキストメニュー（OCR・編集）・
  リンク挿入とプロパティ欄方式の編集（2026-08-31 ユーザー提案3件）。
  正本は [【考察】添付ファイルの表示と操作.md](【考察】添付ファイルの表示と操作.md)
  （順序の見立てと未決の細部も同文書 §5）。
- **保存した画像の編集**——ユーザー:「保存した画像を編集するようなことも、後々可能にしたい」
  （2026-08-26・画像アップロードの実機確認中に）。回転・切り抜き・注記は Canvas API の
  バニラJSで実装できる見込み。**設計の要点は「添付には版が無い」こと**——同名上書きすると
  原本が失われるため、編集結果は**別ファイル保存（原本残置）が本命**。どの操作が要るかは
  着手時にユーザーへ確認（正本は [要件定義書.md](要件定義書.md) §5）。
- **ワンノートの読めない2形式は後回し**——**■見積もり の下の表**（第1列が鍵・497件）と
  **■購入品 の下の表**（見出し行が無い2列）を受ける語彙2形式。ユーザー:「後回しに出来るなら
  後回しにします」（2026-08-30）。**後回しにできます**——索引の書き込み側だけの話で、
  スキーマは D-1 の縦持ちが吸収するため。**ワンノート移行の直前までに入れれば足ります**
  （正本は [【考察】ワンノート移行.md](【考察】ワンノート移行.md)）。
  なお「工程表」「部材表」は調査時の仮の呼び名で**実データにその言葉はありません**——
  **■工程 は写真主体の作業手順で、表ではない**ので混同しないこと。
- **PDF取り込みの拡張はしない**——ユーザー:「発注書PDFから発注書へ拾う項目は、あとで決めます。
  PDF取り込みはまだつくり込みません」。**いまは触らない**のが決定です。
- **ワンノートからの移行**——実物の「板金部」ノート（現役766ページ）を読み取り調査済み
  （2026-08-25）。**w-cms 側に必要な受け皿5条件**（画像・OCRテキスト・縦持ち表の語彙・
  添付15拡張子・可変列名）が判明。実施は**運用開始後**（画像アップロードは 2026-08-26 に実装済み）。
  正本は [【考察】ワンノート移行.md](【考察】ワンノート移行.md)。
- **セッション失効の数分前に告知する**——残り時間をクライアントへ渡す口が要る（正本は [認証認可設計.md](認証認可設計.md) §2.2a・§9.3）。
- **要件番号と受け入れ基準の採番**——導入は決定済み、方式（節番号＋連番か通し番号か）から（[要件定義書.md](要件定義書.md) 冒頭）。

### 現状維持と決まったもの（作業なし・蒸し返さないこと）

- **テンプレート置き場は h1（ページ名）で決まる。これは仕様**——「機能が名前で変わるのは目論見
  通りです。表示されている言葉が機能を表します」。サイドカー属性 `template_root` への昇格と右レール
  のトグルは**着手直前で取り消し**。決定と受け入れたリスク（改名すると配下が②索引・③計算から
  出入りし告知は出ない）の正本は [【考察】ページテンプレート.md](【考察】ページテンプレート.md) §9。
- **集計の見せ分けは残し、欠けたことは知らせない**（C案）——読めないページ由来の行は黙って落ちる。
  「表の数字が人によって違い、しかもそれが分からない」状態を許容した、ということ（全員 admin か
  同一グループなら発動しません）。
- **パスワード変更で既存セッションは切らない**——ユーザー:「書きかけが保存できないほうが困ります」。
  即時に締め出したいときは「利用停止」が既に効きます。
- **添付ファイルの配信にキャッシュ抑止は足さない**——ユーザー:「共用のパソコンも個人のIDで
  ログインして使う前提です。ダウンロードしたファイルは、ほかの人には見えません」。

### 小粒な残り

- **見た目の磨き込み**——フッター帯（`.site-footer`）へのリンク追加（現状は仮表示「w-cms」のみ・
  ユーザー予告）／`<aside>` のアイコン・余白・折りたたみ既定状態／CSS変数の移行（内部配色は据え置き）。
- ~~**`updated_at` の表記ゆれ**~~ **解消**（2026-08-30）——フォールバックも RFC3339 UTC
  （`T` 区切り）になり、サイドカー由来と表記が揃った。既存DBの空白区切りの行は
  次のDB再構築で消える（updated_at はフォールバック由来なら再構築で引き直されるため）。
- ~~**`WriteSidecar` が一時ファイル＋rename でない**~~ **解消**（2026-08-30）——
  `page.WriteFileAtomic` を新設し、**正本を書く全経路**（サイドカー・本文HTML・添付・版・設定）
  を原子的書き込みへ揃えた。本番コードに `os.WriteFile` は残っていない。
- **画面構成の注釈図**——実スクリーンショットへ吹き出しを重ねたもの（[【一覧】用語集.md](【一覧】用語集.md) §2.4）。
- **同時編集の残る未決**——複数待機者の順序・任意の安全TTL・共有ストア化・将来の CRDT 化
  （[【考察】同時編集の競合対策.md](【考察】同時編集の競合対策.md) §8）。単一プロセス・揮発的
  ロックの現状で支障は出ていません。
（OneDrive 側の E2E スクリプトは 2026-08-21 に削除済み。正本は `e2e/`。
`cd e2e && npm install` で使えます）

### 未決（次にその辺りを触るときに確かめる）

1. **テンプレート置き場の名前を、設定で変えられるようにするか**——判定を h1 のままにする決定は出た
   が、同時に「テンプレートという名前は設定でかえられる方が良いかもしれません」とも。**「かも
   しれません」なので要否は未決**（設定の置き場そのものが無いので、1 番と一緒に）。


## 覚えておくこと（知らないと必ず踏む罠）

設計書に書いてあることはリンク先が正本です。ここに残すのは**知らずに書くと壊れるもの**だけ。

> **コードの現状を知りたいときは `go doc` から**（2026-08-27 整備）。各パッケージの冒頭コメントに
> 正本性・3層モデル・回覧機構・最小権限の要点が入っています（`go doc ./internal/cms` ほか）。
> `go doc` では書けない横断的な話——依存の向き・関数と呼び出し元・操作ごとの呼び出しの連鎖——は
> [コードリファレンス/](コードリファレンス/README.md) の3本。**古くなったら直さず、
> スキル `/code-reference` で作り直すこと**（2026-08-21 に一度畳んだ理由がそれです）。

- **語→型の推論辞書はもう Goコードではない**。正本は `data/settings.json` の `type_inference` で、
  Goコード内の `defaultTypeInference` は**ファイルが無いときの既定値**にすぎない。
  **設定ファイルが壊れていると起動が止まる**（既定値で黙って上書きすると運用者が足した語が
  消えるため——これは仕様）。語を足したら**DB再構築で効く**（再起動は不要）。
- **殻の `id` は接頭辞 `w-` を独占する**（`w-editor-content` 等）。本文の `id` は自由に書けるが、
  接頭辞つきは剥がされる。殻へ接頭辞なしの id を足すと `shell_id_test.go` が落ちる。
- **本文の取得口は `GET /api/load` の1つ**。計算ビューのSSRを通るのはこの経路だけなので、
  本文を読む新しい経路を足すときは注意（`/api/lock` に生HTMLを同梱していて退行した実例あり）。
  **この経路もサニタイズを通す**（素通しだと殻の要素を乗っ取られる）。一方**アンカー合成
  （`RenderAnchors`）は通さない**——合成 id が本文として保存されてしまう。
- **エディタのクロームは `.vocab-chrome`**（または本文DOM外）。シリアライザが保存しない。
  **本文DOMを走査する新しいコードは除外したか必ず確かめること**——除外は保存・目次・タイトルの印・
  安全化の比較・アンカー合成の数箇所だけで、**Goテストが固定しているのはアンカー合成だけ**
  （保存側は JS で E2E のみ）。本文の `class` はサニタイザが落とすので、この判定は誤爆しない。
- **認可の読み口はサイドカー（正本）ではなく `cms.db`（派生）**。`GetPerms` も `EffectivePublic` も
  DB を読む——**ズレたら認可は索引に従う**ので、`parent_id` の喪失は公開範囲の問題になる。
- **派生（DB）から正本（サイドカー）へは書き戻さない**。サイドカーが読めなければ
  `requireSidecar` がエラーを返し、**本文保存・親の付け替え・権限変更が止まる**（運用者が
  手作業で直す、というのが決定）。組み立て直して書くと、欠損が派生の内容で埋まって
  「見た目は健全なのに中身が変わっている」状態になる。
- **匿名には「読めない」と「存在しない」を区別させない**（どちらも404）。例外はトップページ
  だけで、そこは `/login` へ誘導する。404画面は `/login?next=...` を持ち、ログイン後に
  元のページへ戻す——戻り先は `safeNextPath` が同一サイト内の絶対パスだけに絞る。
- **サニタイズ後にHTMLを足す関数が3つある**（`RenderComputedViews`・`RenderAnchors`・
  `RenderPageShell`）。それぞれ自前でエスケープ責任を負っており、規律が破れると保存型XSS。
- **DOM生成は `createElement`＋`textContent`**。文字列を組み立てて `innerHTML` へ入れない
  （かつて `escapeAttr` 頼みで保存型XSSが実際に発火し、その機構ごと廃止した）。
- **本文の埋め込み（`<img src>` 等）の宛先は `/data/` 配下に限る**（2026-08-21 決定）。
  かつては相対URLというだけで通していたため、本文へ `<img src="/api/logout">` を1つ置くだけで
  そのページを開いた全員を無音でログアウトさせられた（保存型CSRF）。**状態を変える経路は
  引き続きメソッドを絞ること**（`route_guard_test.go` が固定）——宛先制限は多層防御の1枚目で、
  リンク（`a[href]`）は制限していない。
- **`csp_test.go` はポリシー文字列しか見ていない**（`CSPProtect` を外しても通る）。配線の固定は `cmd/w-cms/route_guard_test.go` の `TestCSPIsWired`。
- **ルート表は `buildHandler()`**（`cmd/w-cms/main.go`）。main から切り出したのはテストのため。
- **プラグインテーブルにマイグレーションは無い**。`ApplySchema` は `CREATE TABLE IF NOT EXISTS`
  を流すだけなので、**列を足すと既存DBでは保存だけが 500 になる**。起動時のずれ検出＋自動再構築
  （`DriftedSchemaTables`）が入ったので、以後スキーマを変えたら**起動時に1度だけ再構築が走る**。
- **`visibleChildren` を使うテストは `:memory:` DBでは動かない**——行を回しながら1件ずつ
  `page.GetPerms` を引く形で、入れ子のクエリが別接続＝別の空DBを見て権限がフェイルクローズし、
  子が1件も見えなくなる。**ファイルDBを使うこと**（`setupTemplateAPITest` が手本）。本番はファイル。
- **テンプレート領域（ルート含む）は③計算テーブルへ載らない**（載せると仮の発注書が集計に出る）。
  除外は「プラグインを飛ばす」ではなく `SyncIndex` へ空の本文を渡す形なので、フォルダへ出し入れ
  しても古い行は残らない。判定の先祖辿りは**サイドカー＋本文HTML**——DBを見ると静かに壊れる。
- **項目の鍵は見出しの表示文字**（`th`／`dt`）。機械キーを本文へ書く属性は無い。改名すると③計算に読まれなくなるが、保存時に告知は出る（黙っては壊れない）。
- **業務データの行き先は `vocab_index` だけ**（2026-08-31・D-1）。ドメインごとの
  テーブルはもう1つも無い。③計算は [vocab_query.go](../internal/cms/vocab_query.go) を通して読む
  ——**索引の `field` は見出しの表示文字**（`品番`）で、機械キー（`item-id`）ではない。
  集計コードへ日本語を直書きすると、改名の告知と読み取りがずれる。
- **業務文書ブロックのヘッダ（素の `dl`）は `section` の側から拾っている**
  （`syncVocabSectionHeader`）。ヘッダの `dl` は `data-type` を持たないので、
  引き金で配る配送係は**届けてくれない**。ここを外すと発注元・発注日が索引から
  黙って消える（`TestSectionHeaderIsIndexed` が固定）。**ヘッダと明細の対応づけは
  `block_no`**（形式ごとの文書順連番）。
- **新しい語彙はレジストリへ1件**（`internal/cms/vocab.go`）。**カスタム要素は作らない**（理由は
  [【考察】語彙モデル.md](【考察】語彙モデル.md) §9）。プラグインは計算が要るときだけ書く
  （[【ガイド】プラグイン開発.md](【ガイド】プラグイン開発.md)）。
- **インラインの script/style・`on*=` は書けない**（CSP strict）。詳細は [開発方針.md](開発方針.md) §4。
- **Gemini（明細AI解析）は有料枠のキーで運用する**——**無料枠では入力データが製品改善に使われる**
  ため、ユーザーの前提「学習に使用されないという条件で使用しています」は**課金が有効なキーである
  ことが条件**。コードは `GEMINI_API_KEY` を渡すだけで枠を判定できず、**運用側で守るしかない
  必須条件**です（[【ガイド】デプロイ・運用.md](【ガイド】デプロイ・運用.md)）。
- **ページ削除は `data/trash` への移動**（物理削除ではない）。トップページと子持ちページは不可で、
  編集ロックが要る。**復元UIは未実装**——戻すには手で `data/master` へ移してDB再構築する。
- **E2E は実バグを何度も見つけている**。`assets/` を変更したら `node --check` と `e2e/` の
  スクリプトを回す（ユーザー承認済み——毎回回してよい）。`node` が PATH に無いことがある
  （`C:\Program Files\nodejs` を足す）。
- **ローカル検証のログインは `a` / `a`**。CSRF対策で `Origin` ヘッダ必須。
- **添付の名前の検査は `safeAttachmentName` の1箇所**（PDF と画像が共有）。アップロードの口を
  増やすときは必ずここを通すこと——通さないと「サイドカーを上書きして権限を書き換える」穴が
  その口だけ復活する。
- **画像は保存前に EXIF を落とす**（`StripImageMetadata`）。JPEG は復号→向き補正→再符号化なので
  **保存される画素は元と厳密には一致しない**。原本をそのまま保ちたい用途（証跡など）が出たら
  設計を見直すこと。
- **SVG の安全性は配信側が本体**（`attachment` ＋ 応答限定の sandbox CSP）。入口の検査は網に
  すぎないので、**配信のヘッダを外すと入口を通り抜けたSVGが同一オリジンで動く**。
- **本文の走査はコアの配送係だけ**（`walk.go`）。プラグインは引き金を宣言して要素を受け取る
  ——`WalkElements` を自分で回す新しいコードを書いたら、たいてい設計を間違えている
  （残る正当な利用者はサニタイザ・アンカー合成・タイトル抽出・保存時の告知だけ）。
- **観察係の `descend` は OR で畳まれる**（全員が降りないと言ったときだけ降りない）。
  AND にすると、受注の観察係が明細表を担当した瞬間に②汎用索引から明細が消える
  ——**動いているのに中身が欠ける**形の退行なので、`TestObserversSeeNestedItemsTable` が固定。
- **版は本文HTMLだけ**（`versions/<版ID>.html.gz` ＋ `.json`）。サイドカーは対象外なので、
  リバートで権限・所有者・親は巻き戻らない。**この線引きが安全の要**（古い版に戻すことが
  権限昇格の抜け道にならない）。**JPEG は保存時に再符号化される**ので、画素は元と厳密には
  一致しない（EXIF を確実に落とし、向きを反映するため）。
- **スラッシュメニューの項目は絞り込みのたびに作り直す**。項目の click / mouseenter は
  作るたびに配線すること——初期化で一度だけ配線すると外れる。

## 現在の状態（環境）

- **E2E は9本すべて通過**（stage2 67／stage3 20／stage4 29／delete 9／template 15／
  editor-loss 11／image 21／public 29／version 17 ＝ 計218項目）。
  `cd e2e && npm install && npx playwright install chromium` で使える。
  **2026-08-26 の実装分すべて（回覧化を含む）を通した版で実測**。`go test ./...` も全パッケージ通過。
  > `verify-template.js` の「発注日が今日で埋まる」は **`toISOString()` を使っていて日本時間の
  > 00:00〜09:00 に落ちる**バグがあり、同日に現地時刻へ直しました（コード側は正しかった）。
  > **一式を続けて流すと、たまに1本だけ落ちます**（stage2・public・delete で各1回観測。
  > いずれも再実行で通る）。原因は前のスクリプトが握ったままの編集ロックで、
  > ロックを要するヘルパーは**409 なら少し待って取り直す**作りにしてあります
  > （`verify-public.js` の `setPublic` が手本）。**同じ形の落ち方をしたら、まず再実行して切り分ける**。
- ローカル `data/` は E2E の生成物が大半で、実データではありません。`.gitignore` 対象で環境ごとに独立。
- **スキーマ変更を含む版で初めて起動する環境では、起動時に自動再構築が1度走る**
  （直近では 2026-08-30 の `norm_num` 追加。それ以前は `f2458a9`）。
  ログに「テーブル定義の変更を検出しました」と出れば正常（`data/master` が正本なので無害）。
- **`go test -race` が使える**（gcc 導入済み）。PATH に WinLibs の
  `~/AppData/Local/Microsoft/WinGet/Packages/BrechtSanders.WinLibs.POSIX.UCRT_*/mingw64/bin` を
  足し `CGO_ENABLED=1 go test -race ./...`。通常ビルドは Pure Go を保つため `CGO_ENABLED=0` のまま。

## 開発環境の再現（自宅などで再開する手順）

```bash
git pull && go mod tidy

# 初回のみ管理者を作成して起動（ローカルHTTP検証なので Secure Cookie 無効）。
# ローカル開発の管理者は user=a / password=a に統一する（環境間で揃えるため。2026-07-04 決定）。
WCMS_ADMIN_USER=a WCMS_ADMIN_PASSWORD='a' WCMS_SECURE_COOKIES=0 go run cmd/w-cms/main.go
# 2回目以降は環境変数なしで go run cmd/w-cms/main.go
# → http://localhost:8080 （/login にリダイレクト。a / a でログイン）

go test ./...
```

注意:

- **ローカル開発のログイン**: `a` / `a`（**ローカル検証専用**。本番では使わないこと）。
  古い環境には `admin` / `Admin` で作られた `auth.db` が残っている場合がある（2026-07-04 に変更）。
- `data/`（`cms.db` と `master/`、`auth.db`）はコミットされないため、別環境では初回起動時に上記の
  環境変数で管理者が作られます。**「家でログインできない」は、初回の管理者未作成か別の認証情報**。
- **既存 `data/auth.db` の認証情報を忘れた／揃えたいとき**は、サーバーを止めてから以下で揃う:
  ```bash
  # 一時パッケージで internal/auth を呼ぶ（ルートに別の package main があるためサブdirで実行）。
  # 既存ユーザーなら SetPassword、未作成なら CreateUser を使う。
  mkdir -p _resetpw && cat > _resetpw/main.go <<'GO'
  package main
  import ("fmt"; "w-cms/internal/auth"; "w-cms/internal/database")
  func main(){ database.InitAuthDB(); defer database.AuthDB.Close()
    if err := auth.SetPassword("a", "a"); err != nil {
      if err2 := auth.CreateUser("a", "a", true, ""); err2 != nil { fmt.Println(err, err2); return }
    }
    fmt.Println("ローカル管理者 a / a を用意しました") }
  GO
  go run ./_resetpw && rm -rf _resetpw
  ```
- ローカル実行・プレビュー用に `.claude/launch.json`（`go run` 起動設定）を用意済み。
  プレビュー（http://localhost）でCookieが付くよう **`env: { "WCMS_SECURE_COOKIES": "0" }`** を設定してある。
