# 判断待ち

> **AIエージェントが挙げた問題点に、あなたがコメントを付けるための文書です。**
>
> **使い方**: 各項目の「**あなたの判断**」欄に書いてください。短くて構いません
> （「A で」「これは要らない」「後回し」「意味が分からない」でも十分です）。
> エージェントは着手前に必ずここを読み、**空欄の項目には手を出しません**。
>
> **片付け方**: 判断が決まった項目はこの文書から**消し**、決定そのものは
> 各設計書の決定ログへ移します。この文書は溜める場所ではなく、**空にしていく待ち行列**です。
>
> 最終更新: 2026-08-30（**D-1・D-2・D-4・D-5 が決定し、この文書から消しました**——行き先は
> [アーキテクチャとDBスキーマ.md](アーキテクチャとDBスキーマ.md) §9。残っているのは D-3 の残件・
> D-6 と、D-4 の決定から新たに出た D-7〜D-10 です）

---

## なぜこの文書があるか

2026-08-20〜21 の[設計総点検](【考察】設計総点検.md)で多数の問題が挙がり、そのうち11件を
エージェントが自動で修正しました。**修正が多すぎて追えない**のと、**一部が設計変更に
踏み込んでいる**ため、判断を書き手（あなた）へ戻すのがこの文書の目的です。

**エージェントが代行してはいけない判断の見分け方**は、実例で示すのが早いです。

> エージェントの推奨: 「`tags` に `{Field:"part-id", Label:"部品番号"}` を宣言すれば改名告知が効く」
> あなたの却下: 「`tags` は自由語。列として宣言すると、部品番号を書かない大多数のページで
> 『無い』と鳴って**全ページ誤検知**になる」

技術的には正しいが、**業務を知らないと出せない判断**でした。以下の項目は、
できるかぎり「〜という実装にしますか」ではなく
「**業務上〜ということはありますか／〜されると困りますか**」の形で聞いています。
技術用語のまま残っている問いがあれば、それは翻訳に失敗しているので指摘してください。

---

## いま判断が要るもの: DBスキーマ設計し直しの残り（2026-08-30 現在）

「DBスキーマの見直しを行います」（2026-08-27）を受けた論点 D-1〜D-6 のうち、
**D-1（汎用索引へ一本化）・D-2（機能見出し）・D-4（参照は本文のタグ）・D-5（設定はファイル正本）は
決定済み**で、[アーキテクチャとDBスキーマ.md](アーキテクチャとDBスキーマ.md) §9 の決定ログへ
移しました。ここに残っているのは**まだ決まっていないもの**だけです。

到達点は **コア2つ（`pages`・`page_perms`）＋汎用索引1本（`vocab_index`）＋
設定ファイル1本（`data/settings.json`）**——ドメイン語彙を持つテーブルは1つも残りません。

### D-3残 日付語の辞書を増やすか

D-3 は「**正規化はPDF取り込み時に行い、表の中で認識できた形式は背景色で示す**」で決定
（→ §9 の決定ログ）。ただし**手書きのページには依然として辞書が効きます**。

いま型が `date` になるのは `納期`・`日付`・`検査日`・`発注日`・`納品日` の**5語だけ**
（`typeInference`）。「加工日」「出荷日」「着手日」などは素の文字列扱いで、
**時期で絞る集計に載りません**。

**あなたの判断**（業務で日付を書く見出しの言葉を、思いつくまま挙げていただければ足ります）:


### D-6 縦持ち表・ヘッダ無し表の受け皿

ワンノートの工程表（第1列が鍵・497件）と購入部品表（ヘッダ無し2列）を受ける語彙2形式。
スキーマはD-1のEAVが吸収するので**索引の書き込み側だけの話**。移行前のどこかで入れれば
足りるので、優先度はお任せします。

**あなたの判断**:
「何を工程表や部品票と認識していますか？」（2026-08-30）

**ご指摘のとおり、呼び名がずれていました。**「部材表」「工程表」は**調査時にエージェントが
付けた仮の呼び名**で、実データにその言葉はありません。実際の見出しはこうです
（`01 資料/事務用` の「コピペ ノート 各製品用」＝手動テンプレートの節構成）:

```
■不具合 ■参考図 ■図面 ■図面番号 ■データ（CADファイル）
■材料  ■外注加工  ■購入品  ■支給品  ■塗装
■見積もり  ■工程  ■完成品
```

仮の呼び名と、実際の場所の対応:

| 仮の呼び名 | 実際はどこの表か | 件数 | いまの w-cms で読めるか |
|---|---|---|---|
| 部材表 | **■材料** の下の表 | 471件 | **読める**（1行目が見出し） |
| 工程表 | **■見積もり** の下の表 | 497件 | **読めない**（鍵が第1列にある） |
| 購入部品表 | **■購入品** の下の表 | — | **読めない**（見出し行が無い2列） |

**「工程表」という呼び名が特に紛らわしい**ので、以後使いません。これは **■見積もり の下の表**で、
第1列に工程名（ブランク・ドリル・ブレーキ・組み立て…）が並ぶために、そう呼んでしまったものです。
**■工程 は別物**——写真主体の作業手順で、表ではありません。

読めない2つの形は具体的にこうです。

**■見積もり の下の表**（第1列が鍵。`工程|工数|備考` 102件、列側にロットを並べる
`工程|ロット6|ロット7|ロット20|備考` などの変種あり）:

```
工程   | 数 | 単位 | 備考          ← 1行目だが、見出しではない
ロット |    | 個   |               ← 鍵は第1列（この行は「ロット」の行）
材料   |    | 円   |
ブランク・ドリル・ブレーキ・組み立て …
```

**■購入品 の下の表**（見出し行そのものが無い2列）:

```
ウェルドナット…M8 | 5個
```

**D-2（機能見出しを採用する）を決めたので、認識は見出しの言葉で行います**——
「■見積もり の下だから見積もりの表」「■購入品 の下だから購入品の表」。形を機械が推測するのでは
なく、**あなたが書いた見出しが形式を宣言する**、という形です。

**残る判断**: この2形式をいつ入れるか（移行前のどこかで足りる、というのが元の見立てです）。
呼び名の訂正が済んだので、改めて優先度をうかがえればと思います。

---

## D-4（参照は本文のタグ）の決定から新たに出た4件（2026-08-30）

参照を「名前：値」のタグで表す決定（→ [アーキテクチャとDBスキーマ.md](アーキテクチャとDBスキーマ.md) §9.1）に
伴って、**業務を知らないと決められない問い**が4つ出ました。いずれも設計を確定する前に要ります。

### D-7 番号の「揺れ」をどこまで同じものとみなすか（最重要）

参照をタグで表すと、**値が一致しないと辿れません**。ところが現状、テキストは正規化されて
いないので、`000002-12` と `000002ｰ12`（半角カタカナの長音記号）は**別物として扱われます**。
これを畳む正規化を入れるのが前提条件です。

**しかし畳みすぎると、区別しなければならない番号が衝突します。** 業務上、次のうち
「**違えば別物**」として扱わなければならないものはありますか。

| 揺れ | 例 | 畳むと |
|---|---|---|
| ハイフンの種類 | `A-1` / `A‐1` / `A−1` / `A－1` / `Aｰ1` | 全部同じ番号になる |
| 全角と半角の英数 | `ABC-123` / `ＡＢＣ－１２３` | 同じになる |
| 英字の大小 | `Rev.C` / `rev.c`、`SUS304` / `sus304` | 同じになる |
| 空白 | `A 123` / `A123` | 同じになる |
| 先頭のゼロ | `000123` / `123` | 同じになる（**ページIDは6桁ゼロ埋めなので要注意**） |

**あなたの判断**（「全部畳んでよい」でも十分です。**区別が要るものだけ**挙げてください）:


### D-8 参照タグの名前を何にするか

「原発注書」で固定しますか。それとも線の種類ごとに名前を変えますか
（例: 由来＝`原発注書`／メール由来＝`受信元`／版＝`前版`）。

**機械が入れる前提なら名前は自然に揃います**が、人が手で書いたときの揺れ
（`原発注書` / `元発注書` / `発注書番号`）をどう扱うかが残ります。

**あなたの判断**:


### D-9 業務番号を持たない相手をどう参照するか

参照の値は**業務番号（発注書番号・図面番号）を使うのが原則**です（ページ名は改名で
黙って切れるため）。ところが**通信記録ページ（メール・FAX・電話）には業務番号がありません**。

- 案A: ページ番号（`000123`）を値にする——確実だが、人が読んで意味が分からない
- 案B: 件名＋受信日時を値にする——読めるが、一意にならないことがある
- 案C: メールには参照タグを付けない（PDFから起こした発注書側にだけ番号を書く）

**あなたの判断**:


### D-10 指す先が無い参照タグを知らせるか

タグの値が、どのページの番号とも一致しないとき（打ち間違い・相手のページがまだ無い・
削除された）、**画面で知らせますか**。

- 案A: 知らせる——未定義の種別を赤く出すのと同じ流儀。**黙って切れるのが一番困る**、が推奨
- 案B: 黙る——書きかけの段階で赤が出るのが鬱陶しい場合

**あなたの判断**:



---

## 移送先の索引（片付けたもの）

**回答をいただいた全項目の行き先**です。決定の本文・理由・あなたの言葉の原文は各行の先にあります。

| 移送先 | 移した決定 |
|---|---|
| [認証認可設計.md](認証認可設計.md) §9.3 | **B-2** 見せ分けは使い欠けは知らせない（C案。A-1 の絞り込みを維持）・**B-6** 匿名へはすべて「ありません」（404）＋ログイン後に元のページへ戻す・**B-7** パスワード変更でセッションは切らない・**B-8** 認証イベント（ログイン成功/失敗・ログアウト）を記録・**B-10** 添付配信に `no-store` は足さない・**C-5** 失効は「最後の操作から8時間」＋事前告知 |
| [要件定義書.md](要件定義書.md) §2.3・§2.4・§3・§4.1・§4.4・§5 | **A-3** 添付アップロードも編集ロックで直列化（§2.4）・**A-4** 状態を変える口は GET を受け付けない（§4.1）・**B-8** 帳票の5年保持は版管理で満たす（§3・§5）・**B-11** 公開ページのちらつきを消す（§4.4）・**B-12** 見積もりの横断比較は「いずれ必要」（§5）・**C-2**〜**C-3**・**C-6**〜**C-8** |
| [【考察】語彙モデル.md](【考察】語彙モデル.md) §9 | **A-5／B-3** `RequiresTag`（形式の外にある鍵）を残す＝A案・**B-13** 発注書番号は汎用タグ（名前＝`発注書番号`）で引く・**C-4** `data-type` の改名を許す（視覚的区別が前提・§10） |
| [エディタ仕様.md](エディタ仕様.md) §0.2・§3.3・§7.3・§10.1 | **A-2** 未定義の種別の告知を見落とせない配色にする（§10.1）・**B-4** 自動集計の表は触れなくする＝a案（§0.2）・**B-5** PDF取り込みの拾う項目は保留・作り込まない（§7.3）・**C-1** 貼り付け変換は明示メニュー方式／バーコード・QRはいずれ必要（§3.3） |
| [アーキテクチャとDBスキーマ.md](アーキテクチャとDBスキーマ.md) §9<br>（2026-08-30） | **D-1** 硬いドメイン表8つを落として汎用索引 `vocab_index` へ一本化する・**D-2** 機能見出しを採用する（本丸）・**D-3** 索引は1テーブル縦持ちのまま（エージェント判断）・**D-4** 参照は本文の「名前：値」タグで表し `page_refs` は作らない（§9.1）・**D-5** 設定の正本はファイル `data/settings.json` |
| [アーキテクチャとDBスキーマ.md](アーキテクチャとDBスキーマ.md) §8.4 | **A-7** 派生（DB）から正本（ファイル）へ書き戻さない＝運用者が手作業で直す（**現在の実装はこの決定に反する・未実装**）・**A-11** スキーマは速さでなく柔軟性のために設計し直す（**未着手**） |
| [【考察】ページテンプレート.md](【考察】ページテンプレート.md) §9 | **B-9** 置き場の判定は h1（ページ名）のまま＝「表示されている言葉が機能を表す」。`template_root` への昇格は取り消し。**残る宿題**: 「テンプレート」という名前を設定で変えられるようにする（設定の置き場が無く、A-11 と一緒に考える） |
| [【ガイド】デプロイ・運用.md](【ガイド】デプロイ・運用.md) | **B-1** 発注書PDFを Gemini へ送ってよい。ただし「学習に使わない」は**有料枠のキーであることが条件**（コードからは判定できないので運用上の必須条件） |
| [【考察】設計総点検.md](【考察】設計総点検.md) §2.2 | **B-14** 起動時の再構築は「ファイルから作り直す」方式のまま（実測 1500ページ=7.0秒／3000ページ=15.8秒、100万ページで約1時間半の楽観値）。**未実装の穴**: 始めた印・途中なら次回やり直し・完了記録 |
| [変更履歴.md](変更履歴.md) 2026-08-21 | 判断の要らなかった実施記録——**A-6**・**A-9**・**A-10**（バグ修正・雑務・テスト追加）、**B'-1**〜**B'-4**（E2Eは毎回回す／コードリファレンスは畳む／`.vscode` は共同管理／調査残骸は削除）、**B''-E-1**〜**E-5**（編集不能・無言の保存失敗の修正ほか） |
