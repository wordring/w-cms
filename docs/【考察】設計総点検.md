# 【考察】設計総点検

> **状態: 調査結果（2026-08-20）＋補完調査と対処（2026-08-21）。**
> 14体の調査エージェントで領域別に実装を読み、横断的な批判を経て、
> 上位5件を**反証的に検証**した結果です。検証済みの5件は**実際に再現を確認**しています。
>
> **2026-08-21 に §4 の限界を解消しました**——APIエラーで落ちていた3領域を再取得し、
> 未検証だった項目を**全23件**反証検証したうえで、**11件を修正**しています。
> 各項目の判定と訂正は §5、対処済みの一覧は §6 を見てください。
> **§1・§2 の本文は当時のまま**（記録として残す）なので、**§5 の訂正と必ず併せて読むこと**。
>
> この文書は**問題の一覧**であって、設計の説明ではありません。設計の正本は各設計書です。

---

## 0. 総評

**骨格は健全で、破れているのは末端です。** 3層の語彙モデル・一方向依存（htmldoc がドメインを知らない）・
サニタイズの二層化・決定ログの文化は、いずれも文書が主張するとおり実装として成立していました。
文書と実装の乖離を探しに行って、**見つかったのは文書の誤りではなく文書が触れていない領域**
（メソッドガード・キャッシュヘッダ・派生索引の認可）ばかりだった、というのが調査の結論です。

一方で、**「黙って壊れる」経路が想定より多い**ことが分かりました。「見える文字がすべて」の代償が
告知機構で十分に補われておらず、いくつかは実測で再現しました。以下はその一覧です。

---

## 1. 検証済み（再現を確認した5件）

### ① 派生索引が正本を書き戻し、親ページIDを失わせる 【高】

`readMetaOrPerms`（[page/perms.go](../internal/cms/page/perms.go):116-124）は**サイドカーが読めないと
DB の `page_perms` へ落ち**、`BumpUpdatedAt`（同:128-136）がその値を `WriteSidecar`（同:75-94）で
**正本へ書き戻します**。`GetPerms` が返すのは owner/group/mode/public だけなので、
`ParentID`・`CreatedAt`・`CreatedBy` は空のまま書かれます。

**実測**: サイドカーを切り詰めた子ページを1回保存すると、owner/group/mode は DB 由来で復活する一方
`ParentID` が空になり、`pages.parent_id` が NULL、**親から見た子ページ一覧が空**になりました。
`created_at` は「今」に打ち直されます。

**訂正（検証者による）**: 親・作成者が失われるのはサイドカーが読めなくなった時点で、書き戻しが
消しているのではありません。書き戻しの罪は**復元できないのに見た目が健全なサイドカーを新造して
欠損を隠蔽すること**です。また親の最後の1コピーを潰しているのは perms.go ではなく
[sync.go](../internal/cms/sync.go):75 の `parent_id = excluded.parent_id`——`created_at`/`created_by` は
COALESCE で守られているのに `parent_id` だけ無条件上書きです。**修正の第一候補はここ**。

補足: `WriteSidecar` は `os.WriteFile`（O_TRUNC）で、リポジトリ全体で一時ファイル＋rename を
使っていません（`os.Rename` は handler_delete.go の ゴミ箱移動のみ）。**この経路自身が前提条件
（切り詰め破損）を作れます**。

### ② 索引に載るかがトップ直下ページの h1 で決まる 【高】

テンプレート判定（[template.go](../internal/cms/template.go)）は「サイドカーの親が `000000`」かつ
「本文の最初の h1 が定数 `"テンプレート"`」の2条件で、これが**部分木まるごとの②索引・③計算への
登録可否**を切り替えます（[sync.go](../internal/cms/sync.go):103-113）。

**実測**: ルートの h1 を「テンプレート」→「テンプレート集」に直しただけで、配下の再同期時に
`client_orders` が 0→1 行、`vocab_index` が 0→5 行。逆向きも再現。告知は一切出ません
（`UnknownVocabTypes`・`UnresolvedVocabFields` はどちらも `data-type` を持つ table/dl しか見ない）。

**訂正**: 反転は即時ではなく各ページの次の保存か DB 再構築で効きます。また完全な無症状ではなく、
`/api/templates` が空になり**テンプレートメニューが消える**という可視の副作用が同時に起きます。

これは自分（2026-08-20 の実装）が作った面なので、対処案は考察文書に既に書いた
**サイドカー属性 `template_root: true` への昇格**が素直です。

### ③ `order_no` の UNIQUE がページ横断で受注を奪う 【高】

`client_orders.order_no` / `our_orders.order_no` は UNIQUE で、Sync が
`ON CONFLICT(order_no) DO UPDATE SET page_id = excluded.page_id` を使うため、
**発注書番号がページを跨ぐグローバル主キー**になっています。しかも `orderNo` は `NullableString` を
通さないので、空値は NULL でなく `''` として衝突します。

**実測**: 発注書番号を空にした2ページを順に同期すると、`RequiredMaterials(51)` が `[]` になり、
`RequiredMaterials(52)` に**ページ51由来の部材**が現れました。例外もログも出ません。

**訂正**: 発火経路は「PDF取り込み・見出し改名」ではなく実質「人が番号欄を空にする／手で重複させる」
の1経路です（PDF取り込みは `order_no` に触れません）。エディタとテンプレートの再採番は
この衝突を避けるための**回避策**であって、根治ではありません。

### ④ `parse-pdf` が本文と権限サイドカーを外部へ送れる 【高】

`ParsePDFHandler`（[pdf_handler.go](../internal/cms/pdf_handler.go):183-184）は
`filepath.Join(pageDir, filepath.Base(req.FileName))` だけで、**拡張子も名前も検証しません**。
読んだバイト列はそのまま Gemini へ送られます。`file_name` に `<id>.html` や `<id>.meta.json` を
指定すれば、本文と権限サイドカーが外部LLMへ渡ります。

**防御の非対称が本質**です——アップロード側（同:65-72）は拡張子許可リスト＋本文名・サイドカー名の
名指し拒否、配信側 `DataFileHandler`（[page/perms.go](../internal/cms/page/perms.go):403-407）も
名指しで404にするのに、**読み出し側だけが素通し**。

**訂正**: 応答にファイルのバイト列は含まれないので「本文の取得口が増えた」わけではなく、
**外部への一方向の egress** です。パストラバーサルも不可（対象は自ページのディレクトリ内）。
修正は pdf_handler.go:184 の直後に名指し検査を1つ置くだけで、対応するテストもありません。

### ⑤ 改名の告知は3類型のうち1つしか拾わない 【高】

「見える文字がすべて」は改名を告知で補う設計ですが、`UnresolvedVocabFields` が捕まえるのは
「どの宣言列にも当たらない見出しがある（stray）」場合だけです。

- **類型①（同一形式の別の宣言済みラベルへ改名）**: `client-order-items` の「品名」→「品番」で
  告知は `nil`、`VocabTableRows` が同じ機械キーへ後勝ちで代入し、**実測で `item-id` が
  "SHAFT-01" から "軸" へ、`item-name` が空**になりました。
- **類型②（Field を持たない形式の改名）**: `tags` の dt「部品番号」→「部品No」で `part_id` が空になり、
  部材手配計算が丸ごと空になるのに**告知はゼロ**。`部品番号` は
  [plugin_materials.go](../internal/cms/plugin_materials.go):64 に直書きされた**日本語の魔法文字列**で、
  レジストリにもエディタ補助にも現れません。

**訂正**: 類型②は実装バグではなく**明文化された設計判断**（vocab.go:430-432＋
vocab_unresolved_test.go が挙動を固定）。したがって問題は「告知が仕様どおり動いていない」ではなく、
**その設計判断の根拠が `plugin_materials.go:64` の1行によって反証されている**ことです。
「Field を持たない列は改名しても壊れない」が `部品番号` だけ成り立ちません。
**修正は `tags` に `{Field:"part-id", Label:"部品番号"}` を宣言するだけ**で足ります。

なお本文の**見出し重複を検出するコードはリポジトリに存在しません**。

---

## 2. 未検証（根拠はあるが裏取りが要る）

着手前に必ず自分で確かめてください。深刻度は報告者の自己申告です。

### 安全性・認可

- **`/api/new-page` にメソッドガードが無く GET でページが作られる**【高】。`CSRFProtect` は
  GET を素通しし（[middleware.go](../internal/auth/middleware.go):127-133）、Cookie は SameSite=Lax なので
  トップレベルGETナビゲーションでは送られる。フロント自身が `window.open` で GET 遷移として
  使っているため、**意図的なGET設計と CSRF 防御モデルが正面衝突**している。
  `/api/logout`・`/api/page-chown` も同様にガード無し。
- **派生索引にページ単位認可が及んでいない**【高】。`RequiredMaterials` は `part_materials` を
  `part_id` だけで引き、**定義元ページの read を見ない**。しかも API は匿名を401で拒否するのに
  SSR の計算ビューは匿名に描画するので、**公開ページに `required-materials` マーカーを1つ置くと
  非公開ページ由来の仕入先・原価が外部に出る**可能性がある。
- **`/api/page-meta` が親のタイトルを親の read を確認せずに返す**【中】。
- **`/api/upload-pdf` と `/api/parse-pdf` だけ編集ロックを通らない**【中】。画像アップロードを
  実装するとこの穴がそのまま広がる。
- **保存2本のロック検証が `u != nil && !Validate(...)` でフェイルオープン**【中】。
  現状 RequireAuth 配下なので実害は無いが、共通ゲート `RequireEditLock` は逆に u==nil で401。
- **ルートの保護レベルを固定するテストが1本も無く、`CSRFProtect` のテストもゼロ**【高】。
  CSP は `csp_test.go` が後戻りを検出するのに、同じ最外周の CSRF とルート表は無防備。
- **認可付き応答のキャッシュ制御が画面だけ**【中】。`/api/load` と `/data/` にヘッダが無く、
  `/data/` は `http.ServeFile` の Last-Modified でヒューリスティックキャッシュの対象になりうる。
  **公開キャッシュを検討する前にここの切り分けが要る**。

### 語彙モデル

- **`View: true` の語彙は「宣言1件」では動かない**【中】。`RenderComputedViews` が
  child-list / required-materials を文字列で決め打ち。3つ目を足すと**無言の空白**になる
  （JS側は `def.view` を汎用に読むので非対称）。
- **`section[data-type]` の未知形式は告知されない**【中】。`UnknownVocabTypes` が table/dl しか
  見ないので、`<section data-type="cliet-order">` のようなタイポが完全に無症状。
  **業務文書ブロックは section が担い手なので、最も壊れやすい形式が告知対象外**。
- **業務ブロックのヘッダ dl が②に載らない**【中】。ヘッダは `data-type` を持たない裸の dl なので、
  発注書番号・発注元・発注日は③にしか存在しない。将来の横断検索で業務文書だけ穴が空く。
- **派生9テーブルのうち本番コードが読むのは5つだけ**【高】。`vocab_index`・`page_tags`・
  `our_estimates`・`supplier_estimates` は**書き込み専用**で、`SELECT` が1つも無い。
  読み手は `RequiredMaterials` ただ1関数。②の設計判断は実地で検証されないまま溜まり続けている。
- **①と③で読み取りロジックが重複**【中】。`plugin_materials` が `VocabTableRows` とほぼ同じ処理を
  自前で持つ。
- **エディタに語彙知識が残っている**【中】。`parsePDFIntoSection` が client-order / our-order と
  機械キーを決め打ちするので、新しい受発注系形式を足しても**AI解析だけ効かない**。

### エディタ・その他

- **`MutationObserver` の `attributeFilter` が旧カスタム要素時代の属性名のまま**【中】。
  `data-type`/`data-src`/`data-id` が無く、属性変更で自動保存が走る経路が事実上死んでいる。
  `applyColType` の明示的な `triggerAutoSave` はその個別回避。
- **`insideCustomElement` の停止条件が接頭辞リネーム前の id (`editor-content`) を参照**【低】。
  いま実害は無いが、殻の DOM を変えた瞬間に誤判定へ変わる。
- **保存時の通知文が実在しない機能を約束している**【低】。「検索の索引にも載ります」
  （app.js:632-634）だが検索機能は無い。
- **公開サイト未実装のコストが HTTP 境界に直接出ている**【高】。匿名読者にもエディタの殻が
  配信され、**隠しているのは JS が付ける `body.anonymous` の CSS だけ**——JS 無効の匿名読者には
  ログアウトボタン・編集スイッチ・「保存されるHTML」が見える。`robots.txt`/`sitemap.xml` は
  `.` を含むパスが404に落ちて到達不可。
- **18MB の Linux バイナリ `w-cms` が git に追跡されている**【中】。`.gitignore` は `*.exe` だけ。
- **ルートに孤立した `list_models.go`（package main）が残っている**【低】。これが原因で
  「サブdirで実行」という回避策が引き継ぎに恒久手順として書かれている。

### 文書・テスト

- **コードリファレンスの行数主張20件中11件が実測とずれ、対象コミットが26コミット古い**【高】。
  直近5ファイル（anchor.go・handler_delete.go・template.go・template_new.go・handler_template.go）が
  全体地図に1件も載っていない。**この文書群は現在まるごと疑わしい**。
- **E2E の正本がリポジトリ外（OneDrive）にあり CI も無い**【高】。最も価値の高い安全網が
  git 管理外でレビュー不能・引き継ぎ不能。
- **往復契約テストに語彙形式が1つも無い**【中】。`TestSanitizeAcceptsEditorOutput` の fixture は
  素のブロック10種だけで `data-type` がゼロ。**エディタが最も多く吐く形式でエコーバックが
  起きても検出されない**。
- **`.claude/worktrees/` に移行前のコードが丸ごと残っている**【中】。`web-components.js` と
  `assets/templates/` の `style=` 属性が**ちょうど75個**——「撤去した」と記録された当のもの。
  追跡下の HEAD では「カスタム要素0種」「インライン style ゼロ」は真だが、
  **作業ディレクトリを grep すると偽になる**。grep に頼る調査（特にAIエージェント）が誤結論を出す。

---

## 3. 着手するなら（優先順の私案）

1. **⑤の `tags` へ `{Field:"part-id", Label:"部品番号"}` を1行**——最も安く、最も鋭い穴が塞がる。
2. **④の `parse-pdf` に名指し検査**——数行＋テスト1本。外部送信という性質上、先に潰す価値がある。
3. **①の `sync.go:75` を `parent_id` も COALESCE に**——正本を失う唯一の経路。
4. **`/api/new-page` の POST 化**——ただし `window.open` を使う UI の作り直しを伴う。
5. **ルート保護レベルの固定テスト**——CSP と同じ流儀で、退行を機械に検出させる。
6. **E2E をリポジトリへ取り込む**——引き継ぎ可能性の問題であり、いま最も安く効く。

**②③は運用開始前に**。テンプレートの h1 依存（②）は属性への昇格で、
`order_no` の全域UNIQUE（③）は `(page_id, order_no)` への変更で解けますが、
後者は③計算の JOIN も併せて直す必要があります。

---

## 4. この調査の限界

- 6領域のうち**3領域（データモデル・安全性・エディタ）はAPIエラーで初回取得に失敗**し、
  横断批判はその3領域のマップ無しで行われました（各エージェントは実コードを直接読んでいるので
  致命的ではありませんが、網羅性は落ちています）。
- **31件中5件しか反証検証していません**。未検証の26件には、検証済み5件で実際に起きたような
  **誇張や因果の取り違えが含まれている可能性が高い**です（検証済み5件はすべて「成立するが
  範囲の訂正が要る」でした）。
- 補完の調査（3領域の再取得＋16件の検証）を走らせましたが、**セッション終了により結果は
  未回収**です。再開時にやり直すか、この文書の未検証項目を個別に確かめてください。

---

## 5. 補完調査の結果（2026-08-21）

§4 に書いた限界——3領域の取得失敗と、26件が未検証のまま——を解消しました。
**3領域（データモデル・安全性・エディタ）の地図を取り直し**、未検証項目を
**16件の予定に対し全23件**、反証側に立って検証しています。

**原文のまま通った指摘は1件もありません。** 検証済み5件がすべて「成立するが範囲の
訂正が要る」だったのと同じ傾向が、残り全部でも再現しました。この文書を読むときは
**§1・§2 の本文を単独で信用せず、必ず下の訂正と併せて読んでください**。

### 5.1 判定が覆ったもの

| 指摘 | 原文 | 検証後 |
|---|---|---|
| 保存2本のロック検証がフェイルオープン【中】 | 要修正 | **不成立**。`u == nil` はハンドラ内の `RequirePageWrite` とルート表の `RequireAuth` で二重に到達不能。予防的価値のみ |
| `.claude/worktrees/` に移行前コード【中】 | grep が誤結論を出す | **リポジトリの性質ではない**。作業ディレクトリ固有の現象で、git 追跡下には無い。自宅環境には存在すらしなかった |
| 往復契約テストに語彙形式が無い【中】 | エコーバックが検出されない | **結論が誤り**。`save_sanitize_test.go` の `TestSaveKeepsNormalContentIntact` が tags / file+data-src / client-order の**3形式にバイト一致の往復契約**を与えている。残るのは12形式中8形式が未カバーという網の目の粗さ |
| `robots.txt` が `.` で404 | ドット禁止が原因 | **因果が誤り**。そもそもハンドラが登録されていない。Go 1.25 の ServeMux は具体パターン優先なので1行足せば届く |
| `/api/page-chown` も GET で危ない | 「同様にガード無し」 | **影響が誤り**。`decodeJSONBody` が空ボディを 400 で弾くため GET では状態が変わらない |
| `/api/load` がキャッシュされうる【中】 | ヒューリスティックキャッシュ対象 | **`/api/load` では起きない**（検証子が無く再利用できない）。実害があるのは **`/data/` だけ**（`Last-Modified` のみ＝経過時間の約10%が鮮度期間） |
| 公開サイト未実装のコスト【高】 | HTTP境界に出ている | **【中】へ**。認可境界は無傷でマークアップ経由の漏洩はゼロ（textarea も権限欄も空）。ただし**重心が違う**——`anonymous` を付けるのが `window.onload` なので、**JS有効の匿名訪問者全員が毎回FOUCを見る**。`RootHandler` は既に `CurrentUser(r)` を持つので `<body class="anonymous">` をサーバーで付ければ両方消える |

### 5.2 成立したが範囲を絞るべきもの

- **派生索引の認可漏れ【高】**——成立。ただし**匿名漏洩は原文より軽い**（SSRの表は6列で `cost` を含まず、匿名は `part-id` を選べない）。**重いのは指摘に無い方**で、write を1つ持つ一般ユーザーが自分のページに任意の品番を1行置けば、読めない部品定義ページの**原価まで API から列挙できた**。【高】は維持、理由を差し替え。
- **`/api/page-meta` の親タイトル漏れ【中】**——成立。**匿名には漏れない**（パスゲートで構造的に不可能）、**1階層のみ**。重いのは、タイトルが本文の最初の `<h1>` そのもの＝read権の無いページの中身の1行である点と、`/api/children` は同じ `pages.title` を1件ずつ `CanRead` で絞っているという内部規約との非対称。**未対処**。
- **PDF系がロックを通らない【中】**——**`upload-pdf` だけ**が問題。`parse-pdf` は永続状態を変えない（結果はDOMに足すだけで保存は `/api/save` がロック検証する）ので**ロック不要が正しい**。
- **`View: true` は宣言1件では動かない【中】→【低〜中】**——実測で再現（3つ目を足すと無言の空白。ビルドもテストも `go vet` も通る）。ただし現在 `View:true` は2件で決め打ち側と完全一致＝**現時点の実害ゼロ**、追加手順は[【ガイド】プラグイン開発.md](【ガイド】プラグイン開発.md)に明記済み。**未対処**——[【ユースケース】原価と利益.md](【ユースケース】原価と利益.md) は分岐追加に触れていないので、次の計算ビューで踏む。
- **派生9テーブルの読み手が5つ【高】→【低〜中】**——事実は正確（読み手ゼロは4テーブル）。ただし**4つともテストは読んでいる**ので「実地で検証されないまま」は言い過ぎ。バグではなく未使用の投資で、`vocab_index` は要件（②で検索・単純集計）の先行実装。**【高】に値するのは最初の読み手を足す瞬間の認可**で、それは「派生索引に認可が及ばない」と同じ根。
- **ヘッダ dl が②に載らない【中】→【低】**——実測で再現。ただし**決定済み・文書済みの帰結**（[【考察】語彙モデル.md](【考察】語彙モデル.md) §361-364 で「付けない」と決着、[【一覧】語彙.md](【一覧】語彙.md) §84-89 が結果を明記）。「黙って壊れる」類ではない。**範囲はヘッダ dl より広く**、②に載らないのは `section[data-type]` 系すべて＝文書としての同一性が丸ごと無い。
- **エディタの語彙決め打ち【中】**——成立。**主犯は `parsePDFIntoSection` ではなく `enhanceFileSections`**（解析ボタンを出す門）。また**範囲は原文より広い**——天井はサーバー側で、`ParsePDFItem` とプロンプトが3キー固定なので**既存形式でも品番と状態は永久に空セル**。**未対処**。
- **①③の読み取り重複【中】→【低〜中】**——`diff` の結果、実質的な差は `make` の容量ヒント 4 vs 8 とコメントだけ＝**逐語コピー**。原因も特定（`VocabTableRows` は第3段で生まれ、materials の移行は第2段。共有ヘルパが後から生まれ materials だけ取り残された）。**未対処**。
- **CSRF/ルート表のテストがゼロ【高】**——成立。`Origin` を設定するテストは全リポジトリで0件だった。ただし認可はハンドラ層に複数の固定テストがあり「全面ノーガード」ではない。**鋭い派生指摘**: `csp_test.go` が固定しているのは**ポリシー文字列だけ**で、`main.go` の配線は見ていない——**`CSPProtect` を外してもテストは通る**。
- **コードリファレンスの誤り【高】→【中】（内訳に【高】2件）**——「まるごと疑わしい」は過剰。**信用できないのは**行数（30件中16件が誤り）・`app.js` を指す `file:line`（7件中6件）・殻の `id` 参照（7件すべてが `w-` 未追随）。**今も正しいのは**依存の向き・ミドルウェア入れ子・ルート表・サニタイズ二層・不変条件8項目・テスト地図19/20。**例外の【高】2件**は、§3・§15c の `/api/lock` 記述（内容が逆。修正済みの退行を「現在の仕様」として説明しており、読んだ者が正しい実装をバグと誤認しうる）と「既知のねじれ」（該当コードは存在せず、片付いた宿題を掲げている）。**未対処**。
- **E2E がリポジトリ外【高】**——成立。**理由の重心が違う**: 「レビュー不能」だけなら【中】止まりで、決定的なのは `serializeChildren` の `.vocab-chrome` 除外に Go テストが無く E2E だけが守っている点。

### 5.3 新規に見つかったもの（当初の31件に無い）

1. **保存型CSRF**——サニタイザは相対URLの `<img src>` を通すので、本文へ `<img src="/api/logout">` を保存するだけで、そのページを開いた全員を無音でログアウトさせられた。同一オリジンなので SameSite も CSP も止めない。**対処済み**。
2. **匿名がページIDを列挙できる**——`/` だけが認可より先に `pages` を引くため、404（不存在）と302（非公開）の差が出る。**未対処**（次の作業候補②「匿名応答の404統一」がこれ）。
3. **監査ログの空白域**——ログイン成功/失敗・ログアウト・`new-page`・`rebuild-db` が一切記録されない。索引を全DROPする `rebuild-db` が無記録。**未対処**。
4. **パスワード変更でセッションが失効しない**——漏洩後にリセットしても盗まれた Cookie が最大8時間有効。**未対処**。
5. **`csp_test.go` が配線を検証していない**——**対処済み**（§6）。
6. **レール開閉ボタンが機能していない**——`getElementById('toggle-' + side)` に対し実体は `w-toggle-left`（app.js:236）。`aria-pressed` がマークアップの `true` に固定で、**開閉状態と支援技術への通知が常に不一致**。`shell_id_test.go` は HTML 側しか見ないので永久に緑。**未対処**。
7. **サニタイズのエコーバックでブロックが編集不能になる**——`applySanitizedHtml` が `outerHTML` 比較するため `.vocab-chrome` を持つブロックは毎回置換され、その後 `applyMode()` を呼ばないので `contenteditable` を失う。**未対処**。
8. **`tagSchema` 取得失敗＋Ctrl+Z で本文が空で上書きされる**——`loadTagSchema` は失敗時に `tagSchema = {}` を代入するが、ガードが `if (!tagSchema)` なので `{}` は素通り。通常の打鍵では `decideSaveStrategy` が `none` を返して保存が飛ばない（画面には「保存済」が出続ける）が、`populateEditor` が `lastSavedBlocks = null` を書くため、**Ctrl+Z を押すと全文保存で `html: ""` が書かれる**。**未対処**。
9. **`insideCustomElement` は常に false を返す**——カスタム要素が0種になったため停止条件も一致条件も成立せず、本来の目的（計算ビューの表で表ツールバーを出さない）が果たされていない。SSRの手配集計は `.vocab-chrome` 内に `<table>` を描くので、行を足しても保存されず黙って消える経路が疑われる。原文の【低】「いま実害は無い」は誤りの可能性が高い。**未対処・実測が要る**。

### 5.4 横断でしか見えない論点（3領域の地図より）

- **認可の読み口はサイドカー（正本）ではなく `cms.db`（派生）**——`GetPerms` も `EffectivePublic` の親チェーンも DB を読む。「正本と索引がズレたら認可は索引に従う」ので、①の `parent_id` 喪失は**そのまま公開範囲の問題**になる。データモデルと安全性の2体が独立に同じ結節点を指した。
- **サニタイズ後にHTMLを足す関数が3つある**（`RenderComputedViews`・`RenderAnchors`・`RenderPageShell`）。それぞれが自前でエスケープ責任を負っており、規律が破れると保存型XSS。
- **`.vocab-chrome` の不変条件は「型」でなく「規律」**——本文DOMを走査する箇所は**29ある**のに明示除外は4つだけ。残りは「chromeが入れ子」「chromeが `data-type` を持たない」という現時点の偶然に依存しており、レジストリへ `View: true` を1件足すと5箇所が一斉に誤爆する。

---

## 6. 対処済み（2026-08-21）

いずれも**修正前に落ちるテスト**を先に書いてから直しています（`upload_test.go` の流儀）。

| コミット | 内容 |
|---|---|
| `af5c032` | 手配集計が定義元ページの read を見ずに越境するのを直す（判定を `page.CanView` へ集約し、`visibleChildren` も寄せた） |
| `15495dc` | `section` の未知 `data-type` を告知対象に加える（＋告知文言。「検索の索引にも載ります」は実在しない機能の約束だった） |
| `1324807` | 添付アップロードを編集ロックで直列化する（`parse-pdf` は対象外が正しい） |
| `beb98a1` | `/api/new-page`・`/api/logout` を POST 限定にする（保存型CSRFの遮断。フロントは form POST ＋ `target=_blank` へ） |
| `3068d11` | 部品番号タグの改名を保存時に告知する（鍵をレジストリ宣言 `RequiresTag` へ移し、魔法文字列を消した） |
| `f6ca63c` | `parse-pdf` が本文・権限サイドカーを外部へ送れるのを塞ぐ |
| `bd7ee98` | サイドカーが読めないときに親ページIDを失うのを直す（2段: 書き戻しの補完と upsert の保護） |
| `7312850` | E2E スクリプトをリポジトリへ取り込む（`e2e/`） |
| `ad7c7b3` | 追跡された18MBバイナリを外し、`list_models.go` を `cmd/list-models/` へ |
| `2580632` | ルートごとの保護レベルと CSRF・CSP の**配線**を固定する（`buildHandler()` を切り出し、変異テストで有効性を確認） |
| `f2458a9` | 発注書番号の UNIQUE をページ内スコープへ（＋起動時のスキーマずれ検出と自動再構築） |

**総点検の私案（§3）で採らなかったもの**: 「`tags` に `{Field:"part-id", Label:"部品番号"}` を1行足す」は**採れません**。`tags` は自由語なので列として宣言すると、担当者しか書いていないページでも「部品番号が無い」と鳴り、**全ページで誤検知**になります。代わりに「部材表があり、タグを実際に使っており、それでも鍵が引けない」ときだけ報告する形にしました。

### 残っている対処（優先度順）

1. **②テンプレート判定の h1 依存**——**着手直前で中断**。ユーザー決定は「右レールにトグルを追加」（サイドカーへ `template_root` を足し、公開トグルの隣に「テンプレート置き場」トグルを出す。トップ直下のページのみ・owner/admin のみ）。既存の「テンプレート」ページはタイトル判定で従来どおり動き、トグルを押せば固定される、という後方互換で設計する。
2. **5.3 の未対処6件**——特に ⑧（Ctrl+Z で本文が空になる）と ⑨（実測が要る）。
3. **コードリファレンスの `/api/lock` 記述と「既知のねじれ」**——誤った記述が実装の差し戻しを誘発しうるので、行数のずれより優先。
4. **`/api/page-meta` の親タイトル**・**エディタの語彙決め打ち**・**①③の読み取り重複**。
