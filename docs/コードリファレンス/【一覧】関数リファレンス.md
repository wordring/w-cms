# 【一覧】関数リファレンス

**対象コミット: `cac2079`（2026-08-27 作成）**

主要な関数と**その呼び出し元**の対応表です。呼び出し元は `grep` の実測で、
テスト（`*_test.go`）とコメント内の言及は除いています。

> **各関数の説明は `go doc` が持ちます。** ここは「誰から呼ばれるか」＝
> 変更したときに何が壊れうるかの地図です。
>
> ```bash
> go doc ./internal/cms SyncIndex      # 1関数の説明
> go doc -all ./internal/cms/page      # そのパッケージの全公開API
> go doc -src ./internal/cms Sanitize  # 実装も見る
> ```

---

## 1. 本文の入口と出口

| 関数 | 定義 | 主な呼び出し元 | 覚えておくこと |
|---|---|---|---|
| `RootHandler` | `handler_view.go` | `cmd/w-cms/main.go`（`/`） | ページ描画の入口。**体裁を相手で分ける**（匿名＝公開専用の殻／認証済み＝編集用の殻） |
| `LoadAPIHandler` | `handler_view.go` | `main.go`（`/api/load`） | **本文の取得口はこの1つだけ**。編集モードの載せ替え専用 |
| `SaveAPIHandler` | `handler_save.go` | `main.go`（`/api/save`） | 全文保存。サニタイズ→ファイル→索引→版 |
| `SaveBlockAPIHandler` | `handler_save.go` | `main.go`（`/api/save-block`） | 1ブロックだけ。対象が無い／重複なら 409 で全文保存へ落とす |
| `Sanitize` | `sanitize.go`（実体は `htmldoc`） | `handler_view.go`・`handler_version.go`・`template.go`・`version.go` | **二層目の防壁**。保存経路を通っていない本文に効く |
| `SanitizeReport` | 〃 | `handler_save.go` | 変化したかも返す（エコーバックの判定） |
| `RenderComputedViews` | `view_render.go` | `handler_view.go` | 計算ビューの中身を埋める（鏡型の段の入口） |
| `RenderAnchors` | `anchor.go` | `handler_view.go` | **描画時だけ**。`/api/load` へ通すと合成 id が保存される |
| `RenderPageShell` | `shell.go` | `handler_view.go` | 編集用の殻。届くのは認証済みだけ |
| `RenderPublicShell` | `shell_public.go` | `handler_view.go` | 公開専用の殻。SEOメタを組み立てる |
| `ReplaceBlock` | `block.go` | `handler_save.go` | `data-id` で本文の1ブロックを差し替え |
| `ParseCore` | `parser.go` | `sync.go`・`template.go` | 本文からタイトルだけを取る |

> ⚠ **サニタイズの後にHTMLを足す関数が4つ**あります（`RenderComputedViews`・
> `RenderAnchors`・`RenderPageShell`・`RenderPublicShell`）。それぞれ自前で
> エスケープ責任を負っており、規律が破れると保存型XSSになります。

---

## 2. 回覧機構（本文の走査）

| 関数 | 定義 | 呼び出し元 | 段 |
|---|---|---|---|
| `walkers.walkObserve` | `walk.go` | `sync.go` | 保存時（観察係） |
| `walkers.walkMirror` | `walk.go` | `view_render.go` | 表示時（鏡型） |
| `walkers.walkSeed` | `walk.go` | `template_new.go` | 新規作成時（種まき） |
| `triggerOf` | `walk.go` | `walk.go` のみ | **将来の差し替え点はここ1つ**（機能見出しが採用されたら変わる） |
| `SyncIndex` | `sync.go` | `handler_save.go`・`handler_tree.go`・`handler_view.go`・`sync.go`・`version.go` | コア同期＋観察係の段 |
| `RebuildDatabase` | `sync.go` | `handler_meta.go`・`cmd/w-cms/main.go` | 全テーブルをDROPして作り直す |
| `RebuildIfNeeded` | `sync.go` | `cmd/w-cms/main.go` | 起動時。中断検出が**空判定より先** |

登録されている担当（実測）:

| 段 | 引き金 | 担当 |
|---|---|---|
| 観察係 | `*`（全マーカー） | `vocabIndexPlugin`（②汎用索引） |
| 観察係 | `tags` | `pageTagsPlugin` |
| 観察係 | `client-order` | `clientOrderPlugin` |
| 観察係 | `our-order` | `ourOrderPlugin` |
| 観察係 | `our-estimate`・`supplier-estimate` | `estimatesPlugin` |
| 観察係 | `part-materials` | `materialsPlugin` |
| 鏡型 | `*` | 計算ビュー（`view_render.go` の `init`。担当かはレジストリの `View: true` で判定） |
| 種まき | `*` | テンプレート新規化（`template_new.go` の `init`。同じくレジストリで判定） |

> **`descend` は OR で畳まれます**（全員が降りないと言ったときだけ降りない）。
> AND にすると、受注の観察係が明細表を担当した瞬間に②汎用索引から明細が消えます。

---

## 3. 認可の関門

**すべてのハンドラはこのどれかを最初に通ります。** 通さないハンドラを足したら設計ミスです。

| 関数 | 定義 | 呼び出し元 | 未認証／権限なしの応答 |
|---|---|---|---|
| `page.RequirePageRead` | `page/perms.go` | `handler_version.go`・`plugin_materials.go`・`template.go` | 401 ／ 403 |
| `page.RequirePageReadOrPublic` | 〃 | `handler_meta.go`・`handler_tree.go`・`handler_view.go`・`page/perms.go` | **匿名は404**（読めないと存在しないを区別させない）／ 認証済みは403 |
| `page.RequirePageWrite` | 〃 | `editlock/handler.go`・`handler_delete.go`・`handler_save.go`・`handler_tree.go`・`handler_version.go`・`pdf_handler.go`・`upload_image.go` | 401 ／ 403 |
| `page.RequireAdmin` | 〃 | `editlock/handler.go`・`handler_meta.go`・`perms_admin.go` | 401 ／ 403 |
| `auth.RequireAdmin` | `auth/admin.go` | `auth/admin_handlers.go` | 同上（**同名だがパッケージが違う**） |
| `editlock.RequireEditLock` | `editlock/handler.go` | `handler_delete.go`・`handler_tree.go`・`handler_version.go`・`pdf_handler.go`・`perms_admin.go`・`upload_image.go` | 他者が保持中なら **409** |
| `page.CanView` | `page/perms.go` | `plugin_materials.go` | 集計の見せ分け（真偽値。HTTPは返さない） |
| `page.EffectivePublic` | 〃 | `handler_sitemap.go`・`handler_view.go`・`page/perms.go`・`perms_admin.go` | 実効公開＝自分と全先祖が public |

ミドルウェア（`cmd/w-cms/main.go` の実測）:

```
CSPProtect( CSRFProtect( root ) )
                          ├─ /assets/     … 認証不要（Cache-Control: no-cache）
                          ├─ /login, /api/login, /api/tag-schema … 認証不要
                          ├─ /sitemap.xml, /robots.txt          … 認証不要
                          ├─ OptionalAuth … /api/load, /data/, /api/me,
                          │                 /api/children, /api/page-meta, /  (RootHandler)
                          └─ RequireAuth(protected) … 上記以外の /api/
```

- `CSRFProtect` は GET/HEAD/OPTIONS を検証しません。**状態を変える経路は
  ハンドラ側でメソッドを絞ること**（`route_guard_test.go` が固定）。
- `RequireAuth` は入口で `Cache-Control: no-store` を一律に付けます
  （API ごとに付けるとどれか1本が漏れるため）。

---

## 4. 属性（サイドカー）と保存先

| 関数 | 定義 | 呼び出し元 | 覚えておくこと |
|---|---|---|---|
| `page.ReadSidecar` | `page/perms.go` | `page/perms.go`・`perms_admin.go`・`sync.go`・`template.go` | 読めなければ `ok=false`。**既定値で補完しない** |
| `page.WriteSidecar` | 〃 | `handler_view.go`（トップページ自動生成）・`page/perms.go`・`perms_admin.go` | **書くのはここだけ**。本文保存APIは権限に触れない |
| `page.GetPerms` | 〃 | `handler_tree.go`・`handler_view.go`・`page/perms.go`・`perms_admin.go` | **読むのは cms.db（派生）** |
| `page.NormalizeID` | `page/storage.go` | 11ファイル（保存・削除・添付・版・ロック・権限ほか） | パスに使う前に必ず通す。揺れた表記で別ディレクトリが生まれる |
| `page.GetPageDir` | 〃 | 多数 | `data/master/<先頭2桁>/<id>/` |
| `page.DataURLFor` | `page/perms.go` | `upload_image.go` | 本文の `<img src>` に入れる絶対パス |
| `safeAttachmentName` | `pdf_handler.go` | `pdf_handler.go`・`upload_image.go` | **名前の守りは1箇所**。口を増やすときは必ず通す |

---

## 5. 語彙と読み取りヘルパ

| 関数 | 定義 | 呼び出し元 |
|---|---|---|
| `VocabDefByType` | `vocab.go` | 5つのプラグイン＋`template_new.go`・`view_render.go`・`vocab_index.go` |
| `VocabDLFields` | `vocab_index.go` | `plugin_client_order.go`・`plugin_estimates.go`・`plugin_our_order.go` |
| `VocabTableRows` | 〃 | `plugin_client_order.go`・`plugin_materials.go`・`plugin_our_order.go` |
| `FirstVocabChild` | 〃 | `plugin_client_order.go`・`plugin_our_order.go`・`template_new.go`・`vocab.go` |
| `ClosestFileSrc` | 〃 | `plugin_client_order.go`・`plugin_estimates.go`・`plugin_our_order.go` |
| `TagValue` | `plugin.go` | `plugin.go`・`sync.go`（`ctx.Tag` の実体）・`vocab.go` |
| `UnknownVocabTypes` / `UnresolvedVocabFields` / `ShellPrefixedIDs` | `vocab.go`・`sanitize.go` | `handler_save.go`（保存応答の3つの告知） |

> **鍵は見出しの表示文字**（`th`／`dt`）ただ1つ。機械キー（`Field`）は本文に現れず、
> レジストリの `Label` を通じて解決されます。見出しを改名すると③計算に読まれなくなり、
> 保存時に `unresolved_fields` で告知されます（拒否はしません）。

---

## 6. 版管理

| 関数 | 定義 | 呼び出し元 |
|---|---|---|
| `RecordVersion` | `version.go` | `handler_save.go`（保存時・コアレッシングあり）・`version.go`（`init` のロック解放フックとリバート） |
| `ListVersions` | 〃 | `handler_version.go`・`version.go` |
| `ReadVersion` | 〃 | `handler_version.go`・`version.go` |
| `RevertToVersion` | 〃 | `handler_version.go` |
| `PruneVersions` | 〃 | `version.go`（版を書いた直後） |

> **対象は本文HTMLだけ**。サイドカーは触らないので、リバートで権限・所有者・親は
> 巻き戻りません——「古い版に戻す」が権限昇格の抜け道にならない、という線引きです。

---

## 7. 画像・添付

| 関数 | 定義 | 呼び出し元 |
|---|---|---|
| `SniffImageKind` | `image.go` | `upload_image.go` |
| `ValidateSVG` | 〃 | `upload_image.go` |
| `StripImageMetadata` | 〃 | `upload_image.go` |
| `page.DataFileHandler` | `page/perms.go` | `cmd/w-cms/main.go`（`/data/`） |

---

## 8. 数え方（再現手順）

```bash
# 呼び出し元（テストとコメントを除く）
grep -rn --include='*.go' -F 'SyncIndex(' internal cmd \
  | grep -v '_test.go' | grep -vE ':[0-9]+:\s*//' | grep -v ':[0-9]*:func '

# 公開APIの一覧
go doc ./internal/cms
```

**偽陽性に注意**（このプロジェクトで実際に踏んだもの）:

- **メソッド名**——`Name` / `Schema` / `Tables` / `Triggers` / `OnElement` は全プラグインが
  実装するので、「最初に宣言したファイルへの依存」に見えます。
- **同名関数**——`RequireAdmin` は `auth` と `page` の両方にあります。`page.` の有無で分けること。
- **同一パッケージ内の呼び出し**——`auth.Audit(` で grep すると `auth` 自身の
  9箇所（ログイン・ログアウト・管理API）が漏れます。
- **コメント内の言及**——件数だけ見て内訳を見ないと外します。
