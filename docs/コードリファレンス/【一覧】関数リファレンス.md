# 【一覧】関数リファレンス

**対象コミット: `f29bc0b`（2026-09-15 測り直し）**

主要な関数と**その呼び出し元**の対応表です。呼び出し元は `grep` の実測で、
テスト（`*_test.go`）・コメント内の言及・宣言行は除いています。**`ext/` も数えます**
（2026-09-03 以降、業務セットと計算プラグインはそこに在ります）。

> **各関数の説明は `go doc` が持ちます。** ここは「誰から呼ばれるか」＝
> 変更したときに何が壊れうるかの地図です。
>
> ```bash
> go doc ./internal/cms SyncIndex      # 1関数の説明
> go doc -all ./internal/cms/page      # そのパッケージの全公開API
> go doc -src ./internal/cms Sanitize  # 実装も見る
> ```

---

## 1. 本文の入口と出口
| 関数 | 定義 | 主な呼び出し元 | 覚えておくこと |
|---|---|---|---|
| `RootHandler` | `handler_view.go` | `cmd/w-cms/main.go`（`/`） | ページ描画の入口。**体裁を相手で分ける**（匿名＝公開専用の殻／認証済み＝編集用の殻）。`/<6桁>/<名前>` は添付の配信へ回す |
| `LoadAPIHandler` | `handler_view.go` | `main.go`（`/api/load`） | **本文の取得口はこの1つだけ**。`RenderComputedViews(Sanitize)` は通すが、アンカー合成と参照リンクは**通さない** |
| `SaveAPIHandler` | `handler_save.go` | `main.go`（`/api/save`） | 全文保存。サニタイズ→ファイル→版→索引 |
| `SaveBlockAPIHandler` | `handler_save.go` | `main.go`（`/api/save-block`） | 1ブロックだけ。対象が無い／重複なら 409 で全文保存へ落とす。版は作らない |
| `Sanitize` | `sanitize.go`（実体は `htmldoc`） | `append_page.go`（`rewritePageBody`）・`handler_version.go`（2）・`handler_view.go`（2）・`intake.go`（2）・`template.go`・`version.go` | **二層目の防壁**。保存経路を通っていない本文に効く |
| `SanitizeReport` | 〃 | `handler_save.go` | 変化したかも返す（エコーバックの判定） |
| `RenderComputedViews` | `view_render.go` | `handler_view.go`（2）・`handler_version.go`（2） | 計算ビューの中身を埋める（鏡型の段の入口）。本文を返す4経路すべてで掛かる |
| `RenderReferenceLinks` | `ref_render.go` | `handler_view.go` のみ | 参照タグ→リンク・宙ぶらりんへ `ref-missing`。**描画時だけ** |
| `RenderAnchors` | `anchor.go` | `handler_view.go` のみ | **描画時だけ**。`/api/load` へ通すと合成 id が保存される |
| `RenderPageShell` | `shell.go` | `handler_view.go` | 編集用の殻。届くのは認証済みだけ |
| `RenderPublicShell` | `shell_public.go` | `handler_view.go`（`servePublicPage`） | 公開専用の殻。SEOメタを組み立てる |
| `ReplaceBlock` | `block.go` | `handler_save.go` | `data-id` で本文の1ブロックを差し替え |
| `PageTitle` | `parser.go` | `sync.go`・`template.go`（`pageTitleFromDisk`） | 本文からタイトル（最初の `h1`）だけを取る |

> ⚠ **サニタイズの後にHTMLを足す関数が5つ**あります（`RenderComputedViews`・
> `RenderReferenceLinks`・`RenderAnchors`・`RenderPageShell`・`RenderPublicShell`）。
> それぞれ自前でエスケープ責任を負っており、規律が破れると保存型XSSになります。
> **`RenderComputedViews` の先にぶら下がる鏡型も同じ責任を負います**——各ビューの
> `…ViewHTML` と、`file_view.go` の `fileViewInnerHTML` が該当します（後者は
> `html.EscapeString` でタグ名・ID・URLを包んでから組み立てます）。

---

## 2. 回覧機構（本文の走査）
| 関数 | 定義 | 呼び出し元 | 段 |
|---|---|---|---|
| `walkers.walkObserve` | `walk.go` | `sync.go` | 保存時（観察係） |
| `walkers.walkMirror` | `walk.go` | `view_render.go` | 表示時（鏡型） |
| `walkers.walkSeed` | `walk.go` | `template_new.go` | 新規作成時（種まき） |
| `triggerOf` | `walk.go` | `walk.go` のみ（4箇所） | 実体は `vocabTypeOf` に委譲 |
| `vocabTypeOf` | `walk.go` | `walk.go`・`plugin.go`（`TagValue`）・`template_new.go`・`view_render.go`・`vocab_index.go`（2）・`vocab_notify.go`（2） | **形式の解決はここ1箇所**——`data-type` が正、無ければ section の機能見出し（`functionHeading` → `VocabDefByHeading`） |
| `SyncIndex` | `sync.go` | `handler_save.go`（2）・`handler_tree.go`（2）・`handler_view.go`（トップ自動生成）・`intake.go`（2）・`sync.go`（2）・`version.go` | コア同期＋観察係の段 |
| `RebuildDatabase` | `sync.go` | `handler_meta.go`・`sync.go`（`RebuildIfNeeded` 2箇所）・`cmd/w-cms/main.go` | **先頭で `LoadSettings` を読み直してから**全テーブルを DROP して作り直す |
| `RebuildIfNeeded` | `sync.go` | `cmd/w-cms/main.go` | 起動時。中断検出（`rebuild_state`）が**空判定より先** |
| `PurgePageIndex` | `sync.go` | `handler_delete.go` | 空の本文で `SyncIndex` → `page_perms` → `pages` の順に消す |
| `resyncSubtree` | `handler_tree.go` | `handler_tree.go`（`SetParentAPIHandler`） | 親の付け替え後、動かしたページと配下を本文から同期し直す |

登録の口は4つです——観察係は `Register`（`plugin.go`。`Observer` を実装していれば
`Triggers()` の宣言を `observerAdapter` が配線する）、鏡型は `RegisterMirror`、
種まきは `RegisterSeeder`、取り込み係は `RegisterIntake`（`intake.go`）。

登録されている担当（実測）:
| 段 | 引き金 | 担当 | 登録元 |
|---|---|---|---|
| 観察係 | `*`（全マーカー） | `vocabIndexPlugin`（②汎用索引）——**唯一の観察係** | `vocab_index.go` の `init` |
| 鏡型 | `*` | 計算ビュー（担当かはレジストリの `View: true` で判定）。**中身を全部消して描き直す** | `view_render.go` の `init` |
| 鏡型 | `file-view` | `renderFileView`——`data-ref` の指すファイルを開く枠を足す。**人が書いた中身は消さない**（消すのは前回のクロームだけ） | `file_view.go` の `init`（2026-09-14。配線を属性へ移したのは 2026-09-15） |
| 鏡型 | `drawing` | `markSupersededDrawing`——前に別の図面ブロックがあれば `drawing-superseded` を付ける | `ext/subcon/drawing_mirror.go` の `init` |
| 鏡型 | `part-materials` / `part-outsourcing` / `part-purchased` / `part-supplied` | `markObsoleteRows`——`状態＝廃版` の行に印。**行は消さない**（外注に出した紙が社内コードで指している） | 〃（2つ目の `init`） |
| 種まき | `*` | テンプレート新規化（同じくレジストリで判定） | `template_new.go` の `init` |
| 取り込み係 | 拡張子 `.eml` | `emlIntake`（`SourceRefFinder` も実装） | `intake_eml.go` の `init` |

> **引き金ごとに1人**です（`RegisterMirror` は同じ引き金の二重登録を `panic` で断ります）。
> `*`（`TriggerAll`）で受けるのは計算ビューだけで、ほかは形式名で名指しします。

`file_view.go`（2026-09-14）の部品:

| 関数 | 役割 | 覚えておくこと |
|---|---|---|
| `renderFileView` | 鏡型の本体 | 落とすのは**前回のクロームだけ**（`isChrome`）。参照が無い／開けないときは**理由を画面に出す**（無言の空白にしない） |
| `FileRefAttr`（定数） | 配線の属性名 `data-ref` | **サニタイザの許可と対**（`section` 限定）。書き手（拡張）・読み手（この鏡）・エディタの3者が同じ綴りを見るための定数。**`firstRefInTags` は 2026-09-15 に削除**——中に参照タグを書いても開かない（`TestFileViewIgnoresInnerTags`） |
| `attachmentURLFor` | 参照 → 添付のURLと開き方 | `page.CanView` を通す（**読めないページのファイルは出ない**）。`files/` の中を**名前だけ**見る |
| `fileViewKinds` | 拡張子 → 開き方の表 | **ここに無い形式も受けます**（`kindOther`＝開く口を出す・2026-09-15）。この表に足す基準は「ブラウザが**素で**描けるか」だけ（外部ライブラリを連れてこない） |
| `fileViewInnerHTML` | 枠のHTMLを組む | サニタイズ後に足すので**自前でエスケープ**。PDFは `#navpanes=0&view=FitH`（サムネイル欄を閉じる。**ツールバーは残す**——印刷・ダウンロード・回転がそこにしかない） |
| `appendChrome` | `.vocab-chrome` に包んで末尾へ足す | シリアライザが保存しないので、正本にはマーカーだけが残る |

`Register` を通るのは4つ——`vocabIndexPlugin`（コア）と、拡張の `materialsPlugin`・
`mailPlugin`・`contactsPlugin`（2026-09-15）。後の3つは `Observer` ではなく
（`Schema`/`Tables` は nil）、`RouteProvider` としてルートを足すだけです
（板金5本・メール4本・アドレス帳2本＝**拡張が足すルートは11本**）。

> **`descend` は OR で畳まれます**（全員が降りないと言ったときだけ降りない）。
> 観察係が1人のいまは効いていませんが、2人目を足した瞬間に AND だと索引が欠けます
> （`walk_equiv_test.go` の `TestObserversSeeNestedItemsTable` が固定）。

---

## 3. 認可の関門

**すべてのハンドラはこのどれかを最初に通ります。** 通さないハンドラを足したら設計ミスです。

> **例外は「ページを対象にしないAPI」だけ**——メールの4本（`/api/mail/*`）は
> `auth.CurrentUser(r) == nil` の確認だけで、ページ権限を見ません（メールボックスは
> 利用者のもので、ページの持ち物ではないため）。**返信だけは返信元ページの
> `page.CanView` を見ます**——読めない記録の宛先や件名を引き写す口になるためです。
| 関数 | 定義 | 呼び出し元 | 未認証／権限なしの応答 |
|---|---|---|---|
| `page.RequirePageRead` | `page/perms.go` | `handler_version.go`（2）・`ext/subcon/materials.go`・`template.go`（`loadTemplateBody`） | 401 ／ 403 |
| `page.RequirePageReadOrPublic` | 〃 | `handler_meta.go`・`handler_tree.go`・`handler_view.go`（`LoadAPIHandler`）・`page/perms.go`（2つの配信口）・`zip_list.go` | **匿名は404**（読めないと存在しないを区別させない）／ 認証済みは403 |
| `page.RequirePageWrite` | 〃 | `ext/contacts/contacts_handler.go`（3）・`editlock/handler.go`（2）・`handler_delete.go`・`handler_memo.go`・`handler_reorder.go`・`handler_save.go`（2）・`handler_tree.go`（3）・`handler_version.go`・`pdf_handler.go`（2）・`upload_file.go`・`upload_image.go`・`ext/subcon/analyze_pdf.go` | 401 ／ 403 |
| `page.RequireAdmin` | 〃 | `editlock/handler.go`・`handler_meta.go`・`perms_admin.go` | 401 ／ 403 |
| `auth.RequireAdmin` | `auth/admin.go` | `auth/admin_handlers.go`（6） | 同上（**同名だがパッケージが違う**） |
| `cms.GateJSONPageRead` | `handler_gate.go`（2026-09-14） | `handler_replies.go`・`handler_thread.go`・`ext/subcon/analyzed.go`・`ext/subcon/filing.go`（`FilingProposalAPIHandler`） | **JSONで答える読み取り口の共通の関門**。`Content-Type` → GET 以外は405 → `NormalizeID`（不正は400）→ `CanView`（読めない・無いはどちらも **404**）。`RequirePageRead` と使い分けること——あちらは `text/plain` で断るので、受ける側が `res.json()` に失敗して理由を落とす |
| `editlock.RequireEditLock` | `editlock/handler.go` | `handler_delete.go`・`handler_tree.go`・`handler_version.go`・`pdf_handler.go`・`perms_admin.go`（2）・`upload_file.go`・`upload_image.go` | 他者が保持中なら **409**。**エディタから来た要求のための関門**（トークンが要る） |
| `editlock.RefuseWhileEditing` | 〃（2026-09-14） | `ext/contacts/contacts_handler.go`（2） | **機械の本文書き換えのための関門**。一覧画面のボタンは編集トークンを持てないので `RequireEditLock` を通すと必ず409になる。**誰かが開いていれば409**（自分が開いていても断る） |
| `editlock.Locks.EditorOpen` | `editlock/lock.go`（同上） | `editlock/handler.go`（`RefuseWhileEditing`）・`handler_handled.go`（1件ずつ飛ばす）・`ext/subcon/filing.go`（行ごとに `skipped`） | 真偽値。**保持者が居なくなったロックは「開いていない」**（判定は明け渡しと同じ `holderPresent`）——数えると機械の書き込みが永久に止まる |
| `editlock.Locks.Validate` | 〃 | `editlock/handler.go`（`RequireEditLock`）・`handler_save.go`（2） | 真偽値。保存の2口は JSON の `token` で直接検証 |
| `page.CanView` | `page/perms.go` | **17ファイル・22箇所**——コア9（`breadcrumb.go`・`file_view.go`・`handler_gate.go`・`handler_replies.go`・`handler_thread.go`・`handler_tree.go`（子一覧の絞り込み）・`qr_handler.go`・`upload_file.go`（重複時に既存ページIDを返すか）・`view_unhandled.go`）＋拡張8（`ext/contacts/contacts.go`（3）・`ext/contacts/contacts_unknown.go`（2）・`ext/mail/attach.go`・`ext/mail/reply.go`（返信元を読めるか）・`ext/subcon/analyzed.go`・`ext/subcon/filing.go`（3）・`ext/subcon/filing_order.go`・`ext/subcon/materials.go`（集計の見せ分け）） | 真偽値。HTTPは返さない |
| `canWritePage`（`ext/subcon/filing.go`） | 拡張の中の薄い包み（`page.GetPerms(...).CanWrite`） | `filing.go`（`fileOneDrawing`） | **整理の実行は対象ページごとに判定**。HTTPを返さないので、結果は行ごとの `outcome` に載る |
| `page.EffectivePublic` | 〃 | `handler_sitemap.go`（2）・`handler_view.go`・`page/perms.go`（2）・`perms_admin.go`（3） | 実効公開＝自分と全先祖が public |
| `requirePageViewable` | `handler_view.go` | `handler_view.go`（`RootHandler`） | 画面用。認証済み×不可は 403 画面、匿名×非公開はトップだけ `/login` へ、他は 404 画面 |

ミドルウェア（`cmd/w-cms/main.go` の実測）:

```
CSPProtect( CSRFProtect( root ) )
                          ├─ /assets/     … 認証不要（Cache-Control: no-cache・ディレクトリ一覧なし）
                          ├─ /login, /api/login, /api/tag-schema … 認証不要
                          ├─ /sitemap.xml, /robots.txt          … 認証不要
                          ├─ OptionalAuth … /api/load, /data/, /api/me, /api/children,
                          │                 /api/page-meta, /api/zip-list, /  (RootHandler)
                          └─ RequireAuth(protected) … 上記以外の /api/（プラグインの
                                                       /api/required-materials を含む）
```

- `CSRFProtect` は GET/HEAD/OPTIONS を検証しません。**状態を変える経路は
  ハンドラ側でメソッドを絞ること**。固定しているのは
  `internal/cms/method_guard_test.go`（2026-09-14 新設・16本）で、**ハンドラを直接呼んで
  405 を期待します**。
  ⚠ **`cmd/w-cms/route_guard_test.go` の `TestStateChangingRoutesRejectGET` では足りません**
  ——`buildHandler()` へ流すので `RequireAuth` が先に 401 を返し、**ハンドラ自身のメソッド
  確認が一度も走らない**（「401 か 405 なら合格」だったため、確認を丸ごと外しても緑でした）。
  この穴で `PageChownHandler` のメソッド関門の欠落を 2026-09-14 まで見逃しています。
- `RequireAuth` は入口で `Cache-Control: no-store` を一律に付けます
  （`TestAuthenticatedAPIsAreNotCacheable`）。
- CSP の配線は `TestCSPIsWired`（`route_guard_test.go`）、ポリシー文字列は
  `internal/auth/csp_test.go` の `TestCSPPolicyIsStrict` が固定します。

---

## 4. 属性（サイドカー）と保存先
| 関数 | 定義 | 呼び出し元 | 覚えておくこと |
|---|---|---|---|
| `page.ReadSidecar` | `page/perms.go` | `handler_reorder.go`・`handler_tree.go`（並び順キー）・`intake.go`・`page/perms.go`（4）・`perms_admin.go`（2）・`sync.go`（2）・`template.go`（2）・`webdav_write.go` | 読めなければ `ok=false`。**既定値で補完しない** |
| `page.WriteSidecar` | 〃 | `handler_reorder.go`（`sort_key`）・`handler_view.go`（トップページ自動生成）・`intake.go`・`page/perms.go`（3）・`perms_admin.go`（2） | 本文保存APIは権限に触れない |
| `page.EnsureSidecar` | 〃 | `handler_tree.go`（新規作成）・`intake.go`（`CreateChildPage`） | 無ければ作る。group/mode は親から継承、public は常に false |
| `page.BumpUpdatedAt` | 〃 | `append_page.go`（`rewritePageBody`）・`handler_save.go`（2）・`version.go`（リバート）・`webdav_write.go`（上書き） | `requireSidecar` 経由——**読めなければ保存が止まる** |
| `page.SetSidecarParent` | 〃 | `handler_tree.go` | 親の付け替え（〃） |
| `page.GetPerms` | 〃 | `handler_handled.go`・`handler_tree.go`（2）・`handler_view.go`・`intake.go`・`page/perms.go`（5）・`perms_admin.go`・`webdav.go`・`webdav_write.go`・`ext/contacts/contacts.go`（`ensureChildByTitle`）・`ext/subcon/filing.go`（`canWritePage`） | **読むのは cms.db（派生）** |
| `page.SyncPageMeta` / `page.RefreshPerms` | 〃 | `sync.go` ／ `perms_admin.go`（2） | サイドカー→`page_perms`（Tx内／Tx外） |
| `page.FormatID` | `page/storage.go`（2026-09-15） | **15ファイル・21箇所**——`handler_tree.go`（5）・`ext/contacts/contacts.go`（3）・ほか13ファイル各1（`breadcrumb.go`・`handler_meta.go`・`handler_replies.go`・`handler_sitemap.go`・`handler_thread.go`・`intake.go`・`page/storage.go`（`NormalizeID`）・`page_lookup.go`・`ref_scope.go`・`version.go`・`view_unhandled.go`・`ext/contacts/contacts_unknown.go`・`ext/subcon/filing.go`） | 数値のページID→ゼロ詰め6桁。**索引の `id` 列から読んだ値を文字列にする所は必ずここ**（`%06d` の直書き4箇所と `%0*d, IDLength` 15箇所を寄せた）。桁を変える日に取りこぼすと同じページに2つのIDが生まれる |
| `page.NormalizeID` | `page/storage.go` | 27ファイル・47箇所 | パスに使う前に必ず通す。揺れた表記で別ディレクトリが生まれる。**2026-09-14 に「畳まない例外」を無くしました**（`/api/page-meta`・`/api/validate-parent`・`/api/load`・`/api/lock-events`・`/api/lock/force`）——`Atoi` した数値しか使っていなくても、`GetPageDir` / `AttachmentDir` は**文字列を取る**ので、あとで1行足した人が `"1"` を渡すと `data/1/1.html` を探しに行く |
| `page.GetPageDir` | 〃 | 12ファイル・22箇所 | `data/master/<先頭2桁>/<id>/` |
| `page.WriteFileAtomic` | 〃 | 9ファイル・14箇所（本文・サイドカー・添付・版・メールのトークン。宣言元は除く） | 一時ファイル＋rename。**正本を書く経路はすべてこれ**（`os.WriteFile` の呼び出しは本体に残っていない） |
| `page.AttachmentDir` | 〃 | `attachment_save.go`・`intake.go`・`page/perms.go`・`page/storage.go`（2）・`webdav_fs.go`・`webdav_write.go`（2）・`ext/subcon/drawing_match.go` | `<ページ>/files/` |
| `page.AttachmentPath` | 〃 | `pdf_handler.go`・`webdav_fs.go`（3）・`webdav_write.go`（2）・`zip_list.go`・`ext/mail/attach.go`・`ext/subcon/analyze_pdf.go`・`ext/subcon/drawing_match.go`（2） | 読む側の共用口。`files/` → 直下（旧）の順 |
| `page.AttachmentURLFor` | 〃 | `intake.go`・`pdf_handler.go`・`upload_file.go`・`upload_image.go`・`ext/mail/reply.go` | `/<ページID>/<名前>`（きれいなURL） |
| `page.GeneratedAttachmentID` | 〃 | `attachment_save.go`（添付3経路の共通の芯・2026-09-14）・`intake.go` | **ブロックIDと同じ4桁の base36**（2026-09-04 に時刻ミリ秒の8桁から変更）。保存名＝URL＝`data-id` の3役。避ける先は `files/` の中（**拡張子を問わず**）と**本文の data-id** の2つ |
| `SafeAttachmentName` | `pdf_handler.go` | `pdf_handler.go`（`attachmentFileName`）・`upload_file.go`・`upload_image.go`・`zip_list.go`・`ext/mail/attach.go`・`ext/subcon/analyze_pdf.go` | **名前の守りは1箇所**。口を増やすときは必ず通す |
| `page.DataFileHandler` | `page/perms.go` | `cmd/w-cms/main.go`（`/data/`） | 旧形の配信口（互換）。本文・サイドカーは配らない |
| `page.ServeCleanAttachment` | 〃 | `handler_view.go`（`RootHandler`） | きれいなURLの配信口。`files/` だけを読む |
| `setAttachmentHeaders` | 〃 | `page/perms.go`（上の2口） | PDF＝インライン／SVG＝attachment＋sandbox CSP／ラスタ＝インライン／他＝octet-stream＋attachment |

---

## 5. 語彙・索引・読み取り
| 関数 | 定義 | 呼び出し元 |
|---|---|---|
| `VocabDefByType` | `vocab.go` | `ext/subcon/materials.go`・`template_new.go`（2）・`view_render.go`・`vocab_index.go`（3）・`vocab_notify.go`（3）・`vocab_query.go` |
| `VocabDefByHeading` | `vocab.go` | `walk.go`（`vocabTypeOf`）のみ |
| `VocabDefs` | `vocab.go` | `handler_meta.go`（`/api/tag-schema`）・`view_render.go`（`hasViewMarker`） |
| `InferColumnType` | `vocab.go` | `normalize_ingest.go`（2）・`ref_render.go`（2。`email` 型か・`ref` 型かの判定）・`template_new.go`（2）・`vocab.go`（`tagLookupBind`）・`vocab_index.go`（2） |
| `NormalizeValue` | `vocab.go` | `normalize_ingest.go`・`vocab.go`（`tagLookupBind`）・`vocab_index.go`（`insertVocabEntry`）・`vocab_query.go`（`VocabNumber`） |
| `NormalizeText` / `NormalizeCode` | `normalize_text.go` | `NormalizeText`: `normalize_ingest.go`・`normalize_text.go`（`NormalizeCode` の入口）・`vocab.go`（`NormalizeValue` の text・enum 分岐）／`NormalizeCode`: `vocab.go`（code 分岐）・`ext/subcon/drawing_match.go`（`normalizeDrawingNo` が委譲） |
| `tagNormBind` | `vocab.go`（2026-09-15） | `vocab.go`（`tagLookupBind`）・`vocab_index.go`（`insertVocabEntry`） | **`page_tags.norm_value` へ束ねる形はここ1か所**——number は数の格納クラス（整数は INTEGER）、それ以外は文字列。**書き手と読み手が同じ関数を通る**のが、宣言型を持たない列が壊れない条件 |
| `tagLookupBind` | 〃 | `vocab_query.go`（`PagesByTagLoose`）のみ | 名前から型を決め、その型で畳み、その型で束ねる、を1つに。旧 `NormalizeForLookup`（畳んだ**文字列**だけ返していた）の後継——文字列で返すと number のタグが静かに引けなくなるので分けて持たない |
| `CanonicalForIngest` | `normalize_ingest.go` | `ext/subcon/analyze_pdf.go`（3。ヘッダ dl と明細の単価・数量） |
| `VocabularyDict` | `settings.go` | `handler_meta.go` |
| `VocabTableRowsOf` | `vocab_query.go` | `ext/subcon/materials.go`（3） |
| `VocabBlocksOf` | 〃 | `ext/subcon/analyzed.go`（2）・`ext/subcon/filing.go`・`ext/subcon/filing_order.go`（2）・`ext/subcon/materials.go` |
| `PagesByTag` | 〃 | `intake.go`（`ExistingIntakePage`）・`handler_replies.go`・`handler_thread.go`（2。`メッセージID` ⇄ `返信元メッセージID`）・`ext/subcon/materials.go` |
| `PagesByTagLoose` | 〃 | （まだ呼び手なし。**畳んだ一致で引く口**——サイト内検索で使う想定。`PagesByTag` と2段にしないのは、打った通りに在るのに別のページも返るのを避けるため）。**`page_tags.norm_value` を述語に使うのはいまここだけ**——`tagLookupBind` を通す。範囲で引く口を足すときも同じ関数を通すこと |
| `RelatedPages` | `ref_scope.go` | `ext/subcon/materials.go`（`RequiredMaterials` の集計スコープ） |
| `eachDLPair` | `vocab_index.go` | `plugin.go`（`dlTagValue`）・`ref_render.go`・`template_new.go`（`freshenDL`）・`vocab_index.go`（`syncVocabDL`）・`vocab_notify.go`（`dlHeadingKeys`） |
| `eachPlainVocabChild` | 〃 | `template_new.go`・`vocab_index.go`（`syncVocabSection`）・`vocab_notify.go` |
| `walkSkippingNested` | 〃 | `vocab_index.go` のみ（5） |
| `FirstVocabChild` | 〃 | `template_new.go` のみ |
| `TagValue` | `plugin.go` | `sync.go`（`ctx.Tag` の実体）・`vocab_notify.go`（`RequiresTag` の確認）。`plugin.go` の `dlTagValue` は別の関数（前の版で呼び手に数えていたのは偽陽性） |
| `WalkElements` | `plugin.go` | `anchor.go`（2）・`parser.go`・`plugin.go`（`TagValue`）・`ref_render.go`・`sanitize.go`・`shell_public.go`・`vocab_notify.go`（3） |
| `UnknownVocabTypes` / `UnresolvedVocabFields` / `ShellPrefixedIDs` | `vocab_notify.go`・`sanitize.go` | `handler_save.go`（各2。全文保存とブロック保存） |

> **鍵は見出しの表示文字**（`th`／`dt`）ただ1つ。機械キー（`Field`）は本文に現れず、
> 索引の `field` にも表示文字が入ります。機械キーへの変換は `vocab_query.go` の
> `vocabRows`（`columnFor`）が③計算の直前に一度だけ行います。見出しを改名すると
> ③計算に読まれなくなり、保存時に `unresolved_fields` で告知されます（拒否はしません）。

> **③計算プラグインは索引を `vocab_query.go` 経由でしか読みません。** `RequiredMaterials`
> の読み方——**`RelatedPages` でスコープを決め**（自ページ＋参照でつながるページ・深さ1・
> `page.CanView` で絞る）→ 各ページの受注明細 `client-order-items`（表）→ 部品番号タグで
> `PagesByTag` → 定義元の `part-materials`（表・ここでも `CanView`）→ 同じスコープの
> `our-order`（ヘッダ dl）と `our-order-items`（表）を **`(ページ, block_no)` の対**で対応づけ。
>
> **`block_no` はページの中の文書順連番**なので、スコープが複数ページに広がったあとは
> **ページごとに閉じて突き合わせないと仕入先が入れ替わります**（2026-09-04）。

---

## 5a. ページを名前で引く（`page_lookup.go`・2026-09-15）
| 関数 | 定義 | 呼び出し元 | 覚えておくこと |
|---|---|---|---|
| `PageTitleByID` | `page_lookup.go` | `handler_meta.go`・`handler_replies.go`・`handler_thread.go`・`webdav_write.go`（読み取り専用の範囲か）・`ext/contacts/contacts.go`（`ContactPageForAddress`）・`ext/contacts/contacts_handler.go`・`ext/subcon/analyze_pdf.go`（`pageTitleOf`） | 索引から題。**行が無くても題が NULL でも空**——「引けなかった」の扱いを1つに揃えるための口。`breadcrumb.go` は**使わない**（行が無ければ道を打ち切る必要があるので生の SQL のまま） |
| `TopLevelPageByTitle` | 〃 | `intake.go`（`MailBoxPageID`・`EnsureTopLevelBox`）・`ext/contacts/contacts.go`（`PartnerBoxPageID`） | トップ直下の題一致。通信箱・テンプレート置き場・取引先が共有する「名前が機能」の引き方。もとは `intake.go` の非公開関数——アドレス帳を `ext/` へ出すときに公開した |

---

## 5b. 設定（`config/settings.json`）
| 関数 | 定義 | 呼び出し元 |
|---|---|---|
| `LoadSettings` | `settings.go` | `cmd/w-cms/main.go`（起動時・**DBより先**）・`sync.go`（`RebuildDatabase` の先頭） |
| `MaxUploadBytes` | `settings.go` | `ext/subcon/analyze_pdf.go`・`ext/subcon/drawing_match.go`（どちらもZIP内の取り出し上限）・`pdf_handler.go`（2）・`upload_file.go`・`upload_image.go`（2） |
| `GenericAttachmentExts` | `settings.go` | `upload_file.go`（`SafeAttachmentName` へ渡す）・`ext/mail/attach.go`（返信に付けられる添付の絞り込み） |
| `activeTypeInference` | `settings.go` | `vocab.go`（`InferColumnType`）のみ |
| `VocabularyDict` | `settings.go` | `handler_meta.go`（`/api/tag-schema`）のみ |
| `MachineStages` / `ValidMachineStage` | `settings.go` | `ext/subcon/filing.go`（4／1）。`MachineStages` は `ValidMachineStage` からも |
| `activeCharFolder` | `settings.go` | `normalize_text.go`（`NormalizeText` の入口）のみ |
| `WebDAVHidden` / `WebDAVReadOnly` | `settings.go` | いずれも `settings.go` 内の集合化（`hiddenWebDAVTitles`・`readOnlyWebDAVTitles`）から。それを使うのは `webdav_fs.go`・`webdav_write.go` |

> **7項目**（`Settings` 構造体のフィールド数）——`vocabulary`（見出し語の辞書。型と選択肢を
> 1語1件で持つ）・`max_upload_mib`・`attachment_extensions`（`.json` は書けない）・
> `machine_stages`・`char_folding`・`webdav_hidden`・`webdav_readonly`。
> **未知のキーは弾く**（`DisallowUnknownFields`）。
> **Goコードに既定値はありません**（2026-09-07。ファイルが唯一の正本。例外は添付上限の
> 32MiB だけで、これは語彙ではなく安全の柵）。**起動時は無くても壊れていても止まります**が、
> **`RebuildDatabase` からの読み直しで「無い」ときだけは止めず**、読み込み済みの設定のまま
> 続けます（運用中に語彙が黙って空になるほうが害が大きいため）。

---

## 6. 版管理
| 関数 | 定義 | 呼び出し元 |
|---|---|---|
| `RecordVersion` | `version.go` | `append_page.go`（`rewritePageBody`——機械の書き換えも版を切る・`force=false`）・`handler_save.go`（保存時・`force=false`）・`version.go`（3: `init` のロック解放フック＝`editlock.OnRelease`・リバートの前後） |
| `ListVersions` | 〃 | `handler_version.go`・`version.go`（2） |
| `ReadVersion` | 〃 | `handler_version.go`（2）・`version.go` |
| `RevertToVersion` | 〃 | `handler_version.go` |
| `PruneVersions` | 〃 | `version.go`（版を書いた直後） |

> **対象は本文HTMLだけ**。サイドカーは触らないので、リバートで権限・所有者・親は
> 巻き戻りません。コアレッシングの窓は10分（起点は**版ID＝作成時刻**）、年限は5年（366日換算）。
> ブロック保存（`/api/save-block`）は版を作らず、ロック解放時のチェックポイントが拾います。

---

## 7. 添付・画像・ZIP・Gemini
| 関数 | 定義 | 呼び出し元 |
|---|---|---|
| `SniffImageKind` / `ValidateSVG` | `image.go` | `upload_image.go` のみ |
| `StripImageMetadata` | 〃 | `upload_image.go`・`upload_file.go`（`checkIntakeContent`——**通信箱へ着いた画像**。専用口が持っていた守りを取り込みの口が引き受ける） |
| `listZipEntries` | `zip_list.go` | `zip_list.go` |
| `DecodeZipName` | `zip_list.go` | `zip_list.go`・`ext/subcon`（ZIP内PDF/DXFの照合） |
| `GeminiGenerate` | `gemini.go` | `ext/subcon/analyze_pdf.go`（`judgeOrderPDFWithGemini`）・`pdf_handler.go`（`ParsePDFHandler`） |
| `StripJSONFence` | `gemini.go` | 同上の2箇所 |
| `SaveAttachment` | `attachment_save.go`（2026-09-14） | `pdf_handler.go`・`upload_file.go`・`upload_image.go` |
| `JSONFail` | `handler_save.go` | **16ファイル**——コア9（`handler_gate.go`・`handler_handled.go`・`handler_memo.go`・`handler_reorder.go`・`handler_replies.go`・`handler_thread.go`・`pdf_handler.go`・`upload_file.go`・`upload_image.go`）＋拡張7（`ext/contacts/contacts_handler.go`・`ext/mail` 3・`ext/subcon` 3）。宣言元の `handler_save.go` 自身は呼ばない |
| `DecodeJSONBody` | `handler_save.go` | `handler_handled.go`・`handler_memo.go`・`handler_reorder.go`・`handler_save.go`（2）・`handler_version.go`・`pdf_handler.go`・`perms_admin.go`（2）・`ext/contacts/contacts_handler.go`（2）・`ext/mail/reply.go`・`ext/subcon/analyze_pdf.go`・`ext/subcon/filing.go` |

> **`SaveAttachment` が引き受けるのは保存の作法だけ**です（`AttachmentDir` → `MkdirAll` →
> `GeneratedAttachmentID` → `WriteFileAtomic` → 上書き判定 → `Audit`）。**中身の検査**
> （PDFの先頭が `%PDF-` か・画像のマジックナンバー・拡張子の許可リスト）**と応答の組み立ては
> 呼ぶ側に残っています**——3本とも返すJSONの鍵が違うためです（PDFは `src`＋`href`、
> 汎用は `href`、画像は `src`＋`kind`）。
> ⚠ `IntakeContext.SaveAttachment`（`intake.go`）は**別物**です（取り込み係が
> 自分の作ったページへ添付を置く口）。

> ⚠ **`JSONFail` は必ず状態行を持ちます**（2026-09-14）。`status == 0`＝「HTTP 200 のまま
> 失敗を返す」口は **13箇所へ広がっていた**ため、分岐そのものを畳みました（0 が来たら 500 へ倒す）。
> `res.ok` を見る受け手からは**失敗が成功に見える**ためです。使い分けは
> 409＝システムの状態が整っていない（通信箱が無い／メール未サインイン）・
> 503＝機能が構成されていない（`GEMINI_API_KEY` 未設定・メール設定が無い）・
> 502＝上流が失敗（Gemini・SMTP）・501＝プラグインが入っていない・400＝入力が不正。

> `GeminiGenerate` は `GEMINI_API_KEY` が無ければ `ErrNoGeminiKey` を返し、呼び手は
> 「設定を促す」応答（**状態行 503**・`success:false`）に変えます。モデル名は
> `gemini.go` の定数 `geminiModelName` の1箇所。タイムアウトは60秒。

---

## 8. 取り込み・解析（通信箱まわり）
| 関数 | 定義 | 呼び出し元 | 覚えておくこと |
|---|---|---|---|
| `intakeHandlerFor` | `intake.go` | `upload_file.go`（`serveIntake`） | 拡張子→担当の表引き |
| `ExistingIntakePage` | `intake.go` | `upload_file.go`（`serveIntake`） | 鍵タグの逆引き（`PagesByTag`）。**専用テーブルは無い** |
| `serveIntake` | `upload_file.go` | `upload_file.go`（`UploadFileHandler`） | 通信箱への到着だけを取り込み係へ。担当が居なければ通常の添付へ戻す |
| `emlIntake.SourceRef` / `OnFile` | `intake_eml.go` | `serveIntake`（インターフェース経由） | 鍵は `Message-ID`（タグ名 `メッセージID`）。無くても取り込みは止めない |
| `IntakeContext.SaveAttachment` / `UpdatePage` | `intake.go` | `intake_eml.go` | **この取り込みで作ったページに限る**（`isCreated` で検査）。**`CreatePage`（通信箱の直下へ作る口）は 2026-09-14 に撤去**——2026-09-05 に記録が `通信箱／年／月` へ入る形になり、`CreateDatedPage` に取って代わられて呼び手がゼロだった |
| `CreateChildPage` | `intake.go` | `intake.go`（4。`createUnder`・`CreateRecordPage`・`ensureFolderUnder`・`EnsureTopLevelBox`）・`ext/contacts/contacts.go`（2。取引先ページの器・担当者ページ）・`ext/contacts/contacts_handler.go`（相手ページ）・`ext/subcon/analyze_pdf.go`（2）・`ext/subcon/filing.go`・`ext/subcon/revision.go`（旧版ページ） | ページ作成の芯（採番・サニタイズ・サイドカー継承・索引）。監査は呼び手が録る |
| `reserveNewPageID` | `handler_tree.go` | `handler_tree.go`・`intake.go` | `pages` への INSERT で原子的に採番 |
| `IntakeFile` | `intake.go` | `upload_file.go`（`serveIntake`）・`ext/mail/import.go` | **取り込みの共通の芯**。人のドロップもメール取り込みも同じ道を通る |
| `IntakeContext.CreateDatedPage` | `intake.go` | `intake_eml.go` | 通信箱の下の年フォルダ／月フォルダ（`2006年`／`01月`）を要るときだけ作る |
| `CreateRecordPage` | `intake.go` | `handler_memo.go`（手で記録を作る）・`ext/mail/reply.go`（送信の控え） | **同じ通信箱の同じ年月フォルダ**へ（取り込みの外から呼べる形） |
| `MailBoxPageID` | `intake.go` | `handler_memo.go`・`pdf_handler.go`・`upload_file.go`・`view_unhandled.go`・`ext/mail`（2） | トップ直下の「通信箱」。**無ければ取り込みも返信の控えも作れません**（送信の成否はそのまま返り、記録の失敗は `record_error` で別に伝わる） |
| `MarkHandled` / `MarkHandledAPIHandler` | `handler_handled.go` | `main.go`（`/api/intake/handled`） | 未処理の一覧から**1クリックで「対応：不要」**を付ける。まとめて押せる。**1件の権限不足で全体を止めない**——**編集中のページも同じく1件ずつ飛ばす**（`EditorOpen`・2026-09-14。`MarkHandled` は本文を読んで・変えて・書く） |
| `AnalyzeAttachmentAPIHandler` | `ext/subcon/analyze_pdf.go` | プラグインルート（`/api/analyze-attachment`） | write 権限のみ（編集ロック不要）。`judgeOrderPDF` は変数（テストが差し替える） |
| `buildOrderPageHTML` | 〃 | 同ファイル | 機能見出し形（`<section><h2>顧客の発注書</h2>`）＋タグ `受信元: <ページID>-<添付ID>`。日付と数値は `cms.CanonicalForIngest` で正規形にして書く |
| `buildPartPageHTML` / `drawingSectionHTML` | 〃 | 同ファイル（`AnalyzeAttachmentAPIHandler` → `buildPartPageHTML` → `drawingSectionHTML`） | 図面の枝。`<h2>図面</h2>`＋ヘッダ dl＋参照タグ＋**`<section data-type="file-view" data-ref="<ページID>-<添付ID>">`**（＝図面をその場に開くマーカー・2026-09-14。**配線を属性へ移したのは 2026-09-15**）。**この拡張はPDFの出し方を知りません**——描くのはコアの `file_view.go` で、ここは「ここに開く」と書くだけ。人が消せば図面は出なくなり、参照タグ（出所の記録）は残る |
| `MatchDXFAttachments` | `ext/subcon/drawing_match.go` | `ext/subcon/analyze_pdf.go` | PDFの図面番号でDXF添付（ZIP内も）を突き合わせる。畳み方は `cms.NormalizeCode` |
| `FilingProposalAPIHandler` / `FileDrawingsAPIHandler` | `ext/subcon/filing.go` | プラグインルート | **提案は何も作らない**（GET。入口は `cms.GateJSONPageRead`）。作るのは実行（POST）だけ——「置き場所は解析では決めない」 |
| `fileOneDrawing` / `fileOneOrder` | `ext/subcon/filing.go` ／ `filing_order.go` | `FileDrawingsAPIHandler` | **1行＝1ページ**。権限も編集中かも**行ごとに**見て、駄目なら `outcome: "skipped"` に載せる（複数ページへ書くので、1枚で全体を止めない） |
| `mergeAsRevision` / `checkRevision` | `ext/subcon/revision.go`（2026-09-14 に `filing.go` から分離） | `fileOneDrawing` | 同名の部品ページが在れば**改定図面**として合流。図面ブロックを先頭へ差し込み（`cms.InsertAfterH1`）、旧版は子ページへ移す（`createOldVersionPage`）。**保存は1回**——外して・足して・履歴を直すのを別々に保存すると、途中で失敗したときに図面の無いページが残る |
| `MailImportAPIHandler` | `ext/mail/import.go` | プラグインルート | `Message-ID` の見出しだけ先に読み、**本体を落とす前に**重複を弾く。**古い順**に取り込む（スレッドの親が先に着く）。IMAPの接続は取り込み1回につき1本 |
| `MailSendAPIHandler` | `ext/mail/reply.go` | プラグインルート | **送ってから記録する**。記録に失敗しても `sent: true` ＋ `record_error`（送ったものは取り消せない） |
| `mailAccessToken` | `ext/mail/auth.go` | `imap.go`・`smtp.go` | **受信と送信で1つ**——IMAP も SMTP も同じ Exchange Online なので、リソースをまたぐ取り分け（`AADSTS70011`）が起きません |

---

## 8b. アドレス帳（`ext/contacts`・2026-09-15 に移設）

**コアはアドレス帳を知りません。** 差し込みは `contacts.go` の `init` からの3フックだけで、
どれも任意——積まなければ `email` 型のタグは素のテキストで出るだけです。

| 関数 | 定義 | 呼び出し元 | 覚えておくこと |
|---|---|---|---|
| `cms.RegisterContactResolver` | `internal/cms/ref_render.go`（2026-09-15） | `ext/contacts/contacts.go`（`init`）のみ | アドレス→（ページID・題・見つかったか）の解決係を預ける口。**重複登録は `panic`**。使うのは `linkEmailDD`（`email` 型の `dd` をリンクに）で、解決係が nil なら何もしない。描画は匿名でも通る経路なので `user` は渡さず、**認可は解決係の中**（`ContactPageForAddress(nil, …)`＝存在確認だけ・踏んだ先で関門が判定） |
| `cms.RegisterView("unknown-contacts", …)` / `cms.RegisterVocab(contactsVocab…)` | `view_render.go` ／ `vocab.go` | 〃 | 作業面の描画と語彙。**`view_render.go` の表と `vocab.go` のリテラルから外した**のが移設の下ごしらえ（`3577c6a`） |
| `PartnerBoxPageID` / `EnsurePartnerBox` | `ext/contacts/contacts.go` | `PartnerBoxPageID`: `contacts.go`（4）・`contacts_handler.go`・**`ext/subcon/filing.go`（3）**／`EnsurePartnerBox`: `contacts_handler.go`・**`ext/subcon/filing.go`** | トップ直下の `取引先`。**無ければ作る**（通信箱と違い、機能の入口ではなくただの置き場）。初期本文に作業面 `<section data-type="unknown-contacts">` を載せる（機械が作るページを行き止まりにしない） |
| `PartnerTitleForAddress` | 〃 | **`ext/subcon/filing.go`（`suggestCustomer`）** のみ | 差出人アドレス→**会社の題**（完全一致がドメインより先・`取引：自社` は返さない）。整理の顧客名の推奨値がこれ |
| `ContactPageForAddress` | 〃 | `contacts.go`（`init` の解決係） | アドレス→**この人のページ**（担当者ページがあればその人・完全一致だけ）。`user` が nil なら認可を見ない |
| `PartnerOfPage` | 〃 | `contacts.go`（2）・`contacts_handler.go`（unfile の範囲検査）・`contacts_unknown.go`（2） | 担当者ページやその下から**会社へ戻る**（祖先を100段まで辿る）。取引先の外なら `ok=false` |
| `AddContactAddresses` / `EnsureContactPerson` | 〃 | `contacts_handler.go`（既存の相手へ足す枝） | `AddContactAddresses` は `cms.RewriteBody` の中で `cms.EndOfFirstTagList`（無ければ `cms.InsertAfterH1`）へ足す。`EnsureContactPerson` は `取引先／社名／担当者／氏名`（`ensureChildByTitle`・完全一致だけ） |
| `UnknownContacts` | `ext/contacts/contacts_unknown.go` | `contacts_view.go`（`contactsViewHTML`） | 索引の `差出人`・`宛先`・`CC`・`返信先` から「まだページになっていないアドレス」を1アドレス1件で。ドメインから社名を**推薦する**だけ |
| `RegisterContactAPIHandler` / `UnfileContactAPIHandler` | `ext/contacts/contacts_handler.go` | プラグインルート（`plugin.go` の `Routes()`。**`main.go` の直書きは消えた**） | 両方 `editlock.RefuseWhileEditing` を通す（本文を読んで・変えて・書く） |

> **`ext/subcon` が `ext/contacts` を import するのは `filing.go` の1ファイル**
> （`PartnerBoxPageID`（3）・`PartnerTitleForAddress`・`EnsurePartnerBox`・定数 `PartnerBoxTitle`）。
> 逆向きはありません。

---

## 9. 数え方（再現手順）

```bash
# 呼び出し元（テスト・コメント・宣言行を除く）
grep -rn --include='*.go' -F 'SyncIndex(' internal cmd \
  | grep -v '_test.go' | grep -vE ':[0-9]+:\s*//' | grep -vE ':[0-9]+:func (\([^)]*\) )?SyncIndex\('

# 公開APIの一覧
go doc ./internal/cms
```

**偽陽性に注意**（このプロジェクトで実際に踏んだもの）:

- **メソッド名**——`Name` / `Schema` / `Tables` / `Triggers` / `OnElement` / `OnFile` は複数の型が
  実装するので、「最初に宣言したファイルへの依存」に見えます。
- **同名関数**——`RequireAdmin` は `auth` と `page` の両方にあります。`page.` の有無で分けること。
- **同一パッケージ内の呼び出し**——`auth.Audit(` で grep すると `auth` 自身の
  11箇所（ログイン・ログアウト・管理API）が漏れます（`Audit(` は本体で28箇所）。
- **括弧なしの参照**——`page.DataFileHandler` は `main.go` で `http.HandlerFunc(...)` に
  渡されるだけなので `DataFileHandler(` では当たりません。
- **コメント内の言及**——件数だけ見て内訳を見ないと外します。
