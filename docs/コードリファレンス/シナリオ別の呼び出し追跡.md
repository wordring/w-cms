# シナリオ別の呼び出し追跡

**対象コミット: `f29bc0b`（2026-09-15 測り直し）**

主な操作について、**実際に追える形**で呼び出しの連鎖を並べます。関数名は実在するものだけ、
分岐（403／404／409／423 になる条件）も併記します。個々の関数の説明は `go doc` が、
呼び出し元の一覧は [【一覧】関数リファレンス.md](【一覧】関数リファレンス.md) が持ちます。

すべてのリクエストは最外周の2つを通ります:

```
CSPProtect  … 全応答へ Content-Security-Policy（strict。'unsafe-inline' なし）
CSRFProtect … GET/HEAD/OPTIONS 以外は Origin（無ければ Referer）とホストの一致を要求
              → 別オリジンなら 403、どちらのヘッダも無ければ 403
```

---

## 0. 起動

`cmd/w-cms/main.go` の `main()`:

```
cms.LoadSettings()                 … config/settings.json を読む（**DBより先**。**無くても**
                                     読めなくても不正でも log.Fatal＝Goコードに既定値は無い）
database.InitDB()                  … data/cms.db を開き CoreTables＋coreMigrations
cms.DriftedSchemaTables(DB)        … プラグインのテーブル定義のずれを**先に**見る
  └ ずれがあれば cms.RebuildDatabase()   … 全テーブルDROP→data/master から再構築
cms.ApplySchema(DB)                … CREATE TABLE IF NOT EXISTS を流す（vocab_index・page_tags
                                     と、その索引）。**再構築より後**であること（下）
cms.RebuildIfNeeded()              … 中断した再構築のやり直し／空DB＋ファイルありの復旧
database.InitAuthDB()              … data/auth.db を開き AuthTables
auth.BootstrapAdmin()              … users が空のときだけ初期管理者
editlock.StartLockReaper()         … 猶予満了を定期評価する goroutine
buildHandler()                     … ルート表（テストのために切り出してある）
http.ListenAndServe(":8080", …)
```

> **ずれの検出は `ApplySchema` より前**でなければ意味がありません（流したあとでは
> 「宣言どおりのテーブルが在る」ように見えてしまう）。
>
> ⚠ **作り直しも `ApplySchema` より前**です（2026-09-13 に順序を入れ替えた）。逆だと
> **列を変えた表へ新しい索引を張ろうとして起動が落ちます**——`page_tags` の列を選び直した日に
> `プラグインスキーマ作成エラー: SQL logic error: no such column: name` で踏みました。
> `RebuildDatabase` が表を落として作り直すので、そのあとで `ApplySchema` を流せば索引は
> 新しい表に張られます。
>
> ⚠ **表を足しただけでは中身は入りません**——`ApplySchema` は `CREATE TABLE IF NOT EXISTS`
> を流すだけで、既に索引済みのページは本文を読み直さない限り入りません（`page_tags` を
> 足した日にタグ88行が古い表に取り残されました）。それを拾うのが `DriftedSchemaTables` の
> 「索引に中身があるのに表が無い」判定です。

> **設定はDBより先**です——この後の再構築が語→型の推論辞書を使うため。
> `RebuildDatabase` も先頭で `LoadSettings` を読み直すので、辞書へ語を
> 足したら **DB再構築**（11章）だけで反映されます（再起動は不要）。

登録は `init()` で済んでいます（`main()` より前）。**コアの分**——`vocabIndexPlugin`（観察係）・
鏡型と計算ビューの器（`view_render.go`）・ファイル表示の鏡型（`file_view.go`）・
種まき（`template_new.go`）・`emlIntake`（取り込み係・**これ1人**）・
`editlock.OnRelease`（`version.go`）。**拡張の分**——`cmd/w-cms/ext_*.go` のブランク
import が引き金で、`ext/subcon` が語彙14形式・計算プラグイン・計算ビュー・鏡型
（`drawing_mirror.go` の `init` 2つ＝引き金5つ）・ルート5本を、`ext/comm/mail` が `Mailer` の実装とルート4本を、**`ext/comm/contacts`**（2026-09-15）が
語彙1形式（`unknown-contacts`）・計算ビュー・**アドレスの解決係**（`RegisterContactResolver`）・
ルート2本を足します。`ext/comm/contacts` は `ext/subcon` が import しているので、
`ext_contacts.go` が無くても載ります（そのファイルは起動ログに名前を出すためだけ）。

> **何が入っているかは起動ログに出ます**（「拡張セット: …」の行）。ビルドタグの付け外しは
> 目に見えないので、**タグを忘れたビルドは受注ページ生成も部材計算も無いまま普通に起動します**
> ——気づくのは「発注書を解析しても何も起きない」ときです。
>
> ```
> 拡張セット: contacts（アドレス帳）, mail（メール送受信・IMAP／SMTP）, subcon（下請け業務）
> メール送受信: 有効（IMAP outlook.office365.com:993／SMTP smtp.office365.com:587）
> メール送受信: 設定待ち（… が未設定）    ← 無いとき（他は全部動く）
> ```
>
> （並びは `ext_*.go` のファイル名順＝`init` の実行順。**2026-09-15 の実際の起動ログで
> 確認済み**——`拡張セット: contacts（アドレス帳）, mail（メール送受信・IMAP／SMTP）,
> subcon（下請け業務）` とこの順に出た）

---

## 1. ページを開く（`GET /{id}`）

`RootHandler`（`handler_view.go`）。**体裁は相手で分かれます。**

```
RootHandler
├ id が "" / "index.html"        → 302 /000000（クエリは保つ）
├ id == "000000" かつ pages に無い → 既定HTMLとサイドカー（mode 302）を作って SyncIndex（初回起動）
├ id に "/" を含む（/<6桁>/<名前>） → page.ServeCleanAttachment（5章の[配信②]）
│                                   （先頭が6桁のIDでなければ pageNotFound）
├ id に英数字ハイフン以外        → pageNotFound
├ strconv.Atoi 失敗             → pageNotFound
├ pages に行が無い              → pageNotFound
├ requirePageViewable ─────────────────────────────────────────┐
│   認証済み × page.GetPerms(…).CanRead が偽 → forbiddenPage（403・体裁つきHTML）
│   匿名 × page.EffectivePublic が偽
│     └ トップページ(0) → 302 /login?next=…
│     └ それ以外        → notFoundWithLogin（404・戻り先つきログイン導線）
├ os.ReadFile 失敗              → pageNotFound
│
├ body = RenderAnchors( RenderReferenceLinks( RenderComputedViews( Sanitize(content) ) ) )
│         ↑ 描画時だけ    ↑ 描画時だけ            ↑ 鏡型の段（walkMirror）  ↑ 二層目の防壁
│
├ 匿名 → servePublicPage
│         RenderPublicShell(body, PageSEO{Title, ExtractDescription(body), CanonicalURL})
│         Cache-Control: public, max-age=600 ＋ Vary: Cookie ＋ ETag
│         If-None-Match 一致 → 304（本文を送らない）
└ 認証済み → RenderPageShell(body, title)
             Cache-Control: no-store
```

`RenderComputedViews` は `hasViewMarker`（レジストリの `View: true` の `data-type` か
表示名が本文に現れるか）で早道し、あれば `walkers.walkMirror` で section を配ります。
`RenderReferenceLinks` は `<dl` が無ければ早道、あれば `eachDLPair` で値を `parseRefValue`
（`^[0-9]{6}[-‐−－ーｰ][0-9A-Za-z]+$`）に掛け、`pageExists`（`pages` の件数）で
リンク（`/<ページID>#<ブロックID>`）か `ref-missing` かを決めます。**`email` 型の名前
（`差出人`・`宛先`・`CC`・`返信先`）は `linkEmailDD`** が、`名前 <アドレス>` のアドレス部分を
連絡先ページへのリンクにします——「誰のページか」は `RegisterContactResolver` で預かった
解決係（`ext/comm/contacts` の `ContactPageForAddress(nil, …)`）に尋ね、**解決係が無ければ
素のテキストのまま**（2026-09-15。`-tags minimal` でも壊れない）。

**匿名の404は「読めない」と「存在しない」を区別しません**（例外はトップページだけ）。
404画面は `/login?next=…` を持ち、`safeNextPath` が同一サイト内の絶対パスだけに絞ります。

`pageNotFound` は相手で分かれます——匿名は `notFoundWithLogin`、認証済みは素の `http.NotFound`。

**本文API（`GET /api/load?id=`）**は `LoadAPIHandler`——GET 以外は405 → `page.NormalizeID`
（**2026-09-14 から**。空も不正も「ページIDが不正です」の400へ寄せた。前は空だけ
`Missing id` で分けていたが、呼ぶ側にできることは同じ）→ `RequirePageReadOrPublic` →
`pages.file_path` → `RenderComputedViews(Sanitize)` を `text/plain`＋`nosniff` で返す。
**アンカー合成と参照リンクは通しません**（エディタが本文として保存してしまうため）。

---

## 2. 保存する（`POST /api/save`）

`SaveAPIHandler`（`handler_save.go`）。**順序に意味があります。**

```
SaveAPIHandler
├ メソッドが POST でない        → 405
├ DecodeJSONBody（8MiB上限）    → 413 / 400
├ page_id が空                 → 400（新規は /api/new-page を通す）
├ page.NormalizeID             → 400
├ page.RequirePageWrite        → 401 / 403
├ editlock.Locks.Validate      → 409（他者が保持中／トークン失効。ロックが無ければ許可）
├ page.BumpUpdatedAt           → 500（**サイドカーが読めなければここで止まる**）
├ SanitizeReport(req.HTML)     → safeHTML, sanitized
├ page.WriteFileAtomic(<id>.html) → 500
├ RecordVersion(id, user, safeHTML, force=false)   … 失敗しても**保存は止めない**
├ SyncIndex(id, safeHTML)      → 500
├ auth.Audit(user, "save", id)
└ 200 {success, page_id, updated_at, html, sanitized, unknown_types, unresolved_fields, stripped_ids}
```

`SyncIndex`（`sync.go`）の中身:

```
html.Parse → PageTitle（タイトル）
page.ReadSidecar（親・作成情報・更新日時。読めなければ parent_id は**現状維持**）
tx = DB.Begin()
  pages を upsert
  page.SyncPageMeta      … サイドカー → page_perms
  各 Observer.OnPageStart … vocab_index と page_tags の当該ページ分を DELETE（＝洗い替えの前半）
  IsTemplateArea(id) が偽なら walkers.walkObserve(ctx, root)
    └ 引き金（vocabTypeOf）に当たった要素だけを担当へ配る（.vocab-chrome の中は歩かない）
       vocabIndexPlugin.OnElement
         section → syncVocabSection … 素の dl（ヘッダ）と素の table（Items 宣言があれば明細）を索引
         table   → syncVocabTable   … 見出し行の th が鍵。型は th の data-type > レジストリ > 推論辞書 > text
         dl      → syncVocabDL      … 直前の dt が鍵
         （block_no は形式ごとの文書順連番＝ctx.Counter）
  PageFinisher を実装する Observer があれば OnPageEnd（いまは無い）
tx.Commit()
```

> **テンプレート領域は「要素イベントを1つも発火しない」だけ**で除外されます。
> `OnPageStart`（DELETE）は呼ぶので、普通のページをテンプレートフォルダへ移すと
> 古い行が消えます。判定 `IsTemplateArea` は**サイドカーと本文HTMLで先祖を辿ります**
> （DBを見ない——再構築中は先祖が未同期でありうるため）。

**ブロック保存（`POST /api/save-block`）は版を作りません。** `ReplaceBlock` で本文を
マージしてから書き、`RecordVersion` は呼びません（対象が無い／重複なら 409 でクライアントが
全文保存へ落ちる）。最終状態はロック解放時のチェックポイント（3章）が拾います。

---

## 3. 編集モードへ入る／抜ける

```
[入る] POST /api/lock?id=
  LockAPIHandler（editlock/handler.go）
  ├ page.NormalizeID → 400
  ├ page.RequirePageWrite → 401 / 403
  ├ Locks.TryAcquire
  │   取れない → 423 Locked {ok:false, holder, same_user, grace_remaining_sec}
  └ 200 {ok, token}   ※**本文は返さない**
  → フロント（acquireLock）が GET /api/load を読んで populateEditor

[購読] GET /api/lock-events?id=&role=holder|waiter&token=   … SSE。presence の実体はこの接続の有無

[抜ける] POST /api/unlock?id=&token=（タブ離脱時は navigator.sendBeacon からも）
  UnlockAPIHandler → Locks.Release
                      └ 保持者本人でトークン一致のときだけ editlock.OnRelease を呼ぶ
                         └ cms/version.go の init が RecordVersion(…, force=true)
                            （中身が最新の版と同じなら何もしない）

[強制] POST /api/lock/force?id=   … page.RequireAdmin → Locks.ForceRelease → Audit "lock.force"
```

**猶予は競合トリガー方式**——誰も待っていなければ無期限、待機者が現れてから2分で明け渡します
（`lockGraceDuration`）。取得直後に SSE が繋がるまでの猶予は10秒（`holderConnectGrace`）。

---

## 4. ページを作る（`POST /api/new-page`）

```
NewPageAPIHandler（handler_tree.go）
├ POST 限定（GET は 405）
├ parent が空            → 400（親なしにできるのはトップページだけ）
├ page.RequirePageWrite(parent) → 401 / 403
├ loadTemplateBody（template.go）… template= があれば**採番の前に**検証して読む
│    NormalizeID → RequirePageRead → IsUnderTemplateRoot → isLeafPage → Sanitize
├ reserveNewPageID       … pages への INSERT で原子的に採番
├ html = 既定文 or FreshenTemplateBody(templateBody, newID)
│                           └ 種まきの段（walkSeed）。空欄を列型の既定値で埋め、
│                             order-no 列は PO-<新ID>（2件目以降 -2, -3…）
├ page.WriteFileAtomic(<新ID>.html)
├ page.EnsureSidecar     … owner=作成者・group/mode は親の GetPerms から継承・public は常に false
├ SyncIndex
├ auth.Audit(user, "new-page", "<新ID> under <親>[ (template <id>)]")
└ 302 /{新ID}?edit=true
```

取り込みと解析が使う `CreateChildPage`（`intake.go`）は同じ芯を関数にしたもの——
`reserveNewPageID` → `Sanitize` → `WriteFileAtomic` → `GetPerms(親)` で継承して `EnsureSidecar`
→ `SyncIndex`。監査は呼び手が録ります（`intake.create`／`analyze-pdf`）。

---

## 5. 添付する（3つの口）と配る（2つの口）

```
[汎用]  POST /api/upload-file   UploadFileHandler（upload_file.go）  フォーム名 file
[PDF]   POST /api/upload-pdf    UploadPDFHandler（pdf_handler.go）   フォーム名 pdf_file
[画像]  POST /api/upload-image  UploadImageHandler（upload_image.go） フォーム名 image_file

共通の順序:
├ POST 限定（GET は 405）
├ http.MaxBytesReader（MaxUploadBytes＝設定 max_upload_mib・既定32MiB）
├ page.NormalizeID → 400
├ page.RequirePageWrite → 401 / 403
├ [汎用のみ] 対象が通信箱（MailBoxPageID）なら serveIntake へ（6章）。担当が居なければ続行
├ editlock.RequireEditLock → 409
├ 名前の検査 SafeAttachmentName（ページの本文・サイドカーと同名は拒否）
│   汎用 … 拡張子は GenericAttachmentExts（設定・既定14種）。.pdf と画像は 400（専用口へ）
│   PDF  … .pdf のみ（attachmentFileName）
│   画像 … png/jpg/jpeg/webp/gif/svg（allowedImageExts）。HEIC/AVIF/TIFF/BMP は**形式ごとの理由**で 400
├ 中身の検査
│   汎用 … **しない**（安全の本体は配信側のヘッダ）
│   PDF  … 先頭が %PDF-
│   画像 … SniffImageKind ＋ 拡張子との一致 ＋ SVG は ValidateSVG、その後 StripImageMetadata
├ cms.SaveAttachment（attachment_save.go・**3経路の共通の芯**・2026-09-14）
│   page.AttachmentDir（files/）→ os.MkdirAll
│   → page.GeneratedAttachmentID（<生成ID>.<拡張子>。元の名前はURLに出ない）
│   → os.Stat で上書きかを判定 → page.WriteFileAtomic
│   → auth.Audit(user, "attach" | "attach.overwrite", "<id>/<保存名>")
│   ⚠ **ここが引き受けるのは保存の作法だけ**——中身の検査（上）と応答の組み立て（下）は
│     呼ぶ側に残る（3本とも返すJSONの鍵が違うため）
└ 200 {file_name, id, href|src …}   … href/src は page.AttachmentURLFor ＝ /<ページID>/<保存名>
     汎用 … {success, file_name, id, href}
     PDF  … {success, file_name, src, id, href}
     画像 … {success, file_name, kind, src, id}

[配信①] GET /<ページID>/<名前>            RootHandler → page.ServeCleanAttachment（**新しい形**）
├ 名前に / \ を含む・空・ドット始まり → 404
├ page.RequirePageReadOrPublic → 匿名は404／認証済み×権限なしは403
├ files/<名前> が無い → 404（**直下は見ない**）
└ setAttachmentHeaders → http.ServeFile

[配信②] GET /data/master/<先頭2桁>/<id>/<名前>   page.DataFileHandler（**旧形・互換**）
├ path.Clean（.. の解決）
├ page.RequirePageReadOrPublic
├ <id>.html / <id>.meta.json は配らない（404）
└ setAttachmentHeaders → http.ServeFile

setAttachmentHeaders（両口で共有）: X-Content-Type-Options: nosniff ＋
    .pdf                     … application/pdf（インライン）
    .svg                     … image/svg+xml ＋ attachment ＋ この応答限定の sandbox CSP
    .png/.jpg/.jpeg/.webp/.gif … 実際の種別でインライン
    それ以外                 … application/octet-stream ＋ attachment
```

**ZIP の目録**（`GET /api/zip-list?page_id=&file=`・`OptionalAuth`）: `RequirePageReadOrPublic` →
`SafeAttachmentName`（.zip のみ）→ `page.AttachmentPath` → `listZipEntries`（セントラルディレクトリ
だけ・最大500件・`DecodeZipName` で Shift_JIS 名を復号）。**展開はしません。**

閲覧モードのフロント（`refreshAttachmentPreviews`）は `a[href]` のうち
`/<6桁>/<base36>.(pdf|zip)` の形だけに「▶ 表示／▶ 中身」と、PDF には「🤖 解析」
（`makeAnalyzeButton`）を挿します（クロームは `.vocab-chrome`）。

---

## 6. 通信箱へ `.eml` をドロップする（`POST /api/upload-file` → 取り込み係）

```
UploadFileHandler（5章の共通順序で RequirePageWrite まで）
└ MailBoxPageID() == page_id なら serveIntake（upload_file.go）
   ├ FormFile "file" が無い → false（通常の添付経路へ戻る）
   ├ intakeHandlerFor(拡張子) が nil → false（担当なし＝ただの添付）
   ├ 中身を読む → 500
   ├ **重複検知**（担当が SourceRefFinder を実装しているときだけ）
   │   emlIntake.SourceRef … mail.ReadMessage → Message-ID → (名前 "メッセージID", 値)
   │   ExistingIntakePage(名前, 値) … PagesByTag（page_tags を name/value で逆引き）
   │     既存あり → auth.Audit(uploader, "intake.duplicate", …)
   │                200 {success, intake, duplicate:true, title, page_id（読める相手にだけ）}
   ├ ctx = IntakeContext{InboxID, Uploader}
   ├ emlIntake.OnFile(ctx, 元名, 中身)      → 失敗は 400「取り込めませんでした: …」
   │   ├ mail.ReadMessage → 件名・差出人・宛先・受信日時（ローカル時刻の RFC3339）
   │   ├ collectParts … multipart を再帰展開、text/* は charset から UTF-8 へ（x/text）
   │   ├ ctx.CreateDatedPage(受信日時, "<h1>件名</h1>")
   │   │       … 通信箱／<年>年／<月>月 を要るときだけ作り、その下へ
   │   │         CreateChildPage(月フォルダ, uploader, …) ＋ Audit "intake.create"
   │   │         （先にIDが要る——添付の置き場が新ページのフォルダなので）
   │   ├ 本文を組む: h1 ＋ <dl data-type="tags">（差出人・宛先・受信日時・メッセージID）
   │   │             ＋ text/plain の段落 ＋ 添付ごとに ctx.SaveAttachment
   │   │               （GeneratedAttachmentID・files/・Audit "attach"）
   │   │               → <p data-id="<生成ID>">📎 <a href="/<ID>/<生成ID>.<ext>" download="元名">…</a></p>
   │   └ ctx.UpdatePage(pageID, 本文)      … Sanitize → WriteFileAtomic → SyncIndex
   └ 200 {success, intake:true, page_id, title}
```

- **編集ロックは要求しません**——通信箱の本文は変わらず、子ページが増えるだけ。
- `SaveAttachment` / `UpdatePage` は**この取り込みで作ったページ**にしか効きません
  （`isCreated`）。既存ページを改変する口にはなっていません。
- 通信箱の権限（group/mode）が子ページへ継承されます——「受信物を誰が読めるか」は
  通信箱の設定で決まります。
- ここで PDF の解釈は**しません**（人間ゲート型。次章）。

---

## 7. 🤖 解析（`POST /api/analyze-attachment`・`ext/subcon`）

```
AnalyzeAttachmentAPIHandler（ext/subcon/analyze_pdf.go）  入力 {page_id, file, entry?}
├ POST 限定 → 405（JSON の cms.JSONFail）
├ cms.DecodeJSONBody → 413 / 400
├ page.NormalizeID → 400
├ page.RequirePageWrite → 401 / 403（**編集ロックは要らない**——子ページを作るだけ）
├ cms.SafeAttachmentName（.pdf / .zip）→ 400
├ loadPDFForAnalysis
│   page.AttachmentPath で実体を探す → 無ければ 400
│   .pdf … そのまま読む
│   .zip … entry（目録の表示名・cms.DecodeZipName で照合）の1件だけを
│          cms.MaxUploadBytes 上限つきで取り出す（申告サイズも実読みも打ち切る）
├ judgeOrderPDF(pdf)   … 変数。既定は judgeOrderPDFWithGemini
│   └ cms.GeminiGenerate(プロンプト, PDF) → cms.StripJSONFence
│      → JSON {is_client_order, doc_type, drawing_no, drawing_name, machine_name,
│              order_no, customer, order_date, items}
│   GEMINI_API_KEY 無し → **503** {success:false, message:"…設定してから起動し直して…"}
│   その他の失敗       → **502** {success:false, message:"解析に失敗しました: …"}
│   （2026-09-14。どちらも前は 200 だった——`res.ok` を見る受け手からは成功に見える）
│
├─ **図面の枝**（doc_type=="drawing"）
│   ├ MatchDXFAttachments(pageID, 図面番号)  … 同じページのDXF添付（ZIP内も）と突き合わせ。
│   │                                          畳み方は cms.NormalizeCode（索引と同じ規則）
│   ├ cms.CreateChildPage(…, buildPartPageHTML(…)) → 500
│   │   本文: <h1>図面番号 図面名称</h1>   ← 図面名称は重複しうるので番号が先
│   │         <section><h2>図面</h2>
│   │             <dl>図面番号・図面名称・装置名称・客先</dl>   ← 空でも欄は出す（人が埋める）
│   │             <dl data-type="tags">受信元・[元ファイル]・[対応DXF]・[対応DXFファイル]</dl>
│   │             <section data-type="file-view"            ← **図面をここに開く**（2026-09-14）
│   │                      data-ref="<元ページID>-<添付ID>"></section>  ← 配線は属性1つ（2026-09-15）
│   │         </section>
│   │         <section><h2>改訂履歴</h2><table data-type="drawing-revision-items">（新しい版が先頭）
│   ├ Audit "analyze-drawing"
│   └ 200 {success, is_client_order:false, doc_type:"drawing", page_id, title, matched_dxf}
│
├─ **発注書ではない枝** → 200 {success:true, is_client_order:false}
│                          （**生成しないのも正常な結果**——押した問いに「違う」と答える）
│
└─ **発注書の枝**
    ├ cms.CreateChildPage(page_id, 操作者, buildOrderPageHTML(…)) → 500
    │   本文: <h1>受注 <番号></h1>
    │         <section><h2>顧客の発注書</h2><dl>発注書番号・発注元・発注日</dl>
    │                  <table>品番・品名・単価・数量・状態(未着手)</table></section>
    │         <dl data-type="tags">受信元: <元ページID>-<添付ID>[, 元ファイル: <ZIP内パス>]</dl>
    │   **日付と数値は cms.CanonicalForIngest で正規形にして書く**（D-3。読めなければ生のまま）
    ├ Audit "analyze-pdf"
    └ 200 {success, is_client_order:true, page_id, title}
```

生成されたページは**機能見出し形**なので、保存時の索引では `vocabTypeOf` が
`<h2>顧客の発注書</h2>` をレジストリの表示名で引き、`syncVocabSection` がヘッダ dl を
`client-order`、素の表を `client-order-items`（`Items` 宣言）として `vocab_index` へ載せます。
`受信元` の値は参照タグの文法に一致するので、表示時に `RenderReferenceLinks` が
`/<元ページID>#<添付ID>` へのリンクにします。

**図面そのものは `section[data-type="file-view" data-ref]` が開きます**（2026-09-14。
**2026-09-15 から配線は属性1つ**で、中に参照タグは書きません）。描くのは
コアの鏡型（`internal/cms/file_view.go` の `renderFileView`。§1の
`RenderComputedViews` の段）で、**`ext/subcon` はPDFの出し方を知りません**
——「ここに開く」と書くだけです。**`RenderReferenceLinks` とは別の経路**である点が肝で、
あちらはページ描画でしか走らないため、以前クライアント側で `a.ref-link` から
`<embed>` を組んでいたころは `/api/load`（編集モードの出入り・`reloadContent()`）で
**図面が黙って消えていました**。鏡型は本文を返す4経路すべてで走るので、
公開ページ（匿名・ゼロJS）でも出ます。

**置き場所はここでは決めません**（2026-09-03 ユーザー決定「人間が見てもなにを言っているのか
判断に困る場合も結構多いです。なぜなら顧客は適当だからです」）。作られた部品ページは
通信記録ページの子のままで、**通信箱がそのまま「まだ分からないものの置き場」**になります。
顧客名／装置名称／図面名称へ移すのは、次章の「整理」という**別の操作**です。

---

## 7b. 整理（`GET /api/filing-proposal` → `POST /api/file-drawings`・`ext/subcon`）

**提案は何も作りません。** ページが生まれるのは実行のときだけ——「実行ボタンを押してから
ページ作成です」（ユーザー）。

```
FilingProposalAPIHandler（GET・filing.go）  入力 ?page_id=
├ cms.GateJSONPageRead … GET 以外は405／IDが不正なら400／読めない・無いはどちらも404
├ drawingChildrenOf … 子のうち**図面ブロック**を持つもの（cms.VocabBlocksOf(…, "drawing")）
├ orderChildrenOf   … 子のうち**発注書ブロック**を持つもの（同 "client-order"・2026-09-06）
└ 200 {success, rows, orders, stages, partners, machines}
      rows     … 部品 {page_id, title, drawing_no, drawing_name, customer, machine_name, stage}
                 ↑ 索引から埋めた**推奨値**。人がこの欄を書き直す
                   customer は suggestCustomer（受信元→通信記録→差出人→取引先ページの題。
                   最後の1段は contacts.PartnerTitleForAddress（`ext/comm/contacts`）。
                   鎖が全部**完全一致**なのでエイリアス表もAIも要らない）
                   stage は suggestStage（既にその装置が在ればその段・無ければ先頭）
      orders   … 受注 {page_id, title, order_no, client_name, ordered_at, destination}
                 ↑ **直す欄は無い**（行き先は発注日で決まる）
      stages   … 段の選択肢（設定 machine_stages）
      partners … 既にある取引先ページの題（打ち写す元を人の目の前に出す）
      machines … 既にある装置名称を**顧客ごと**に（他社の装置名を候補に出さない）

FileDrawingsAPIHandler（POST・filing.go）  入力 {rows:[…], orders:[page_id, …]}
├ POST 限定 → 405（JSON の cms.JSONFail）
├ cms.DecodeJSONBody → 413 / 400 ／ 未ログイン → 403
│
├─ rows（部品）1行ずつ fileOneDrawing … **判定は全部「行ごと」**（HTTPは返さない）
│   ├ page.NormalizeID → skipped
│   ├ cms.NormalizeNameForIngest（顧客名・装置名称・図面名称。**題の完全一致が階層の同一性**）
│   ├ どれかが空 → skipped（空欄は「まだ決められない」の意思表示。通信箱に置いたまま）
│   ├ cms.ValidMachineStage → skipped（段は表引きで閉じる）
│   ├ canWritePage → skipped
│   ├ **editlock.Locks.EditorOpen → skipped**（2026-09-14。誰かが開いていたらその行だけ飛ばす
│   │   ——整理は**複数のページへ書く**ので、関門はハンドラではなく行ごと）
│   ├ syncDrawingFields … 人が直した値を**図面ブロックにも書き戻す**（索引のため・2026-09-11）
│   ├ contacts.EnsurePartnerBox（**`ext/comm/contacts`**・2026-09-15）→ ensureChildPage(取引先, 顧客名)
│   │                                                          → (顧客名, 段) → (段, 装置名称)
│   ├ findChildByTitle(装置名称, 図面名称)  … **完全一致だけ**
│   │   ├ 無い   → movePage（親の付け替え）                        … outcome "moved"
│   │   └ 在る   → checkRevision（revision.go）
│   │              ├ 偽の改定（同じ添付由来・図面番号が同じ）→ "skipped" / "needs_confirm"
│   │              └ mergeAsRevision（**順序に意味がある**。途中で失敗しても、
│   │                 図面がどこにも無い状態は生まれない）
│   │                 ① 合流先の図面ブロックを外し、旧版の子ページを先に作る
│   │                    （createOldVersionPage。由来の `受信元` はブロックの中なので一緒に行く）
│   │                 ② cms.RewriteBody **1回**で合流先を書き換える——見出しの直後へ差し込み
│   │                    （cms.InsertAfterH1）＋改訂履歴に1行（InsertRevisionRow）＋
│   │                    その行から旧版ページへのリンク（linkRevisionRow）。**別々に保存しない**
│   │                    ブロックIDが衝突したら振り直す（reassignBlockIDIfTaken）
│   │                 ③ 仮のページをゴミ箱へ（cms.DeletePageToTrash）        … "revision"
│   └ Audit "file-drawing.move" / "file-drawing.revision"
│
├─ orders（受注）1件ずつ fileOneOrder（filing_order.go）
│   ├ 発注書ブロックが無ければ skipped（**受注ページ以外を動かさない**）
│   ├ cms.EnsureTopLevelBox("受注") → cms.EnsureDateFolders(発注日)
│   │   ↑ 年月は**発注日**（読めなければページの更新時刻→現在時刻。止めない）
│   └ cms.SetPageParent → Audit "file-order.move"        … outcome "moved"
│
└ 200 {success, results:[{page_id, outcome, message, target_id?}]}
     outcome は moved / revision / skipped / needs_confirm
```

- **同じ部品のページが既にあれば、その図面は改訂図面**（2026-09-03 ユーザー決定）。
  顧客名／装置名称の下では図面名称が一意なので、**ページが在ること自体が改訂の合図**です。
- 古い図面の**赤枠は表示時に付けます**（`drawing_mirror.go` の鏡型・`markSupersededDrawing`）
  ——`class` はサニタイズで落ちるので保存できません（`ref-missing` の薄赤と同じ作り）。
  ただし **2026-09-06 に「旧版は最新版の子ページ」へ変わった**ので、整理が作るページには
  図面ブロックが1つしか載らず、この鏡型が発火するのは**古いデータと手書きの本文だけ**です。

---

## 7c. メールを取り込む（`POST /api/mail/import`・`ext/comm/mail`）

**受信は IMAP、送信は SMTP**（どちらも OAuth2 の XOAUTH2）。古くからある平の
プロトコルなので、メールサーバーがレンタルへ移っても骨格は変わりません。

```
MailImportAPIHandler（import.go）  入力 {folder?, max?, since?}
├ POST 限定 → 405 ／ 未ログイン → 403
├ ImportMessages（10分のタイムアウト）
│   ├ openIMAP … mailAccessToken → TLS 993 → AUTHENTICATE XOAUTH2 → EXAMINE INBOX
│   │   未サインイン → cms.ErrMailNotSignedIn → **409** {success:false}（2026-09-14。前は 200）
│   ├ ListMessages … **一覧の上限（5000）と取り込みの上限（50）は別**
│   │                （混ぜると「押しても進まない」になる。2026-09-03 に踏んだ）
│   ├ **古い順に並べ替える**（スレッドの親が先に着く）
│   └ 1通ずつ:
│       ├ cms.ExistingIntakePage("メッセージID", internetMessageId)
│       │     既存 → duplicate++（**ダウンロードの前に弾く**）
│       ├ FetchRawMIME … /messages/{id}/$value（生MIME）
│       └ cms.IntakeFile(通信箱, 利用者, "<件名>.eml", 生MIME)
│             → コアの取り込み係（6章とまったく同じ道）
└ 200 {success, summary:{listed, imported, duplicate, failed, titles}}
```

**取り込みの中身はコアの仕事**です（`emlIntake`）。拡張がするのは「取ってきて、
コアの口へ渡す」ところまで——だから人が `.eml` をドロップしたときと**同じページ**ができます。

---

## 7d. 返信する（`POST /api/mail/send`・`ext/comm/mail`）

```
MailSendAPIHandler（reply.go）  入力 {to, cc, subject, body, source_page_id?}
├ POST 限定 → 405 ／ 未ログイン → 403
├ cms.DecodeJSONBody → 413 / 400
├ 宛先が空 / 本文が空 → 400
├ 返信元が指定されていれば page.CanView → 404
│   **読めない記録には返信させません**（宛先や件名を引き写す口になるため）
│
├ 1. 送る … SendAndReturnID → sendViaSMTP
│      ├ mailAccessToken … **受信と同じトークン**（IMAP と SMTP は同じリソース）
│      ├ buildMIME … 件名は RFC 2047・本文と添付は base64 76桁折り
│      │   **In-Reply-To ＋ References に返信元のメッセージID**（SMTP にした理由）
│      └ smtp.office365.com:587 → STARTTLS → XOAUTH2 → DATA
│      失敗 → **502**（上流が失敗）／プラグインが無い **501**／未サインイン **409**
│              いずれも {success:false, message} で、**記録は作らない**（出ていないので）
│              （2026-09-14。前は3つとも 200 だった）
├ Audit "mail.send"
│
└ 2. 記録する … recordSentMail（通信箱／年／月へ CreateRecordPage・向き：送信）
       失敗しても **200 {success:true, sent:true, record_error:"…"}**
       ——送ったものは取り消せないので、出た事実を隠さない
```

- **SMTP で投函したメールは Outlook の「送信済みアイテム」に残りません**
  （Exchange Online の仕様）。控えは w-cms の**通信箱**が担います（`向き：送信`）——承知のうえの選択。
- 記録には `返信元` タグ（値は返信元のページID）が入るので、元の記録の側からは
  `/api/replies`（`PagesByTag`）で**逆引きして覗けます**。

---

## 8. 親を付け替える（`POST /api/set-parent?id=&parent=`）

```
SetParentAPIHandler（handler_tree.go）
├ POST 限定
├ page.NormalizeID → 400
├ page.RequirePageWrite → 401 / 403
├ editlock.RequireEditLock → 409
├ pages から現在の親を読み、parentChanged のときだけ validateParentChange
│   未認証 → 401／トップ以外を親なしに → 403／親IDが数値でない・実在しない・
│   自分自身・自分の子孫（parentCreatesCycle）→ 400／新しい親への write なし → 403
├ page.SetSidecarParent → 500（サイドカーが読めなければ止まる）
├ UPDATE pages SET parent_id, updated_at
├ 親が変わったなら resyncSubtree(id)
│   … 幅優先で自分と配下（DBの parent_id で辿る）を os.ReadFile → SyncIndex
│     （テンプレート領域への出入りで②索引に載るかが変わるため。失敗はログのみ）
├ auth.Audit(user, "set-parent", "<id>-><新親>")
└ 200 {ok, parent_id, updated_at}
```

`GET /api/validate-parent?id=&parent=` は同じ `validateParentChange` を即時検証用に
返すだけ（権威はこちら）。

---

## 8b. 並べ替える（`POST /api/reorder?parent=`）

```
ReorderAPIHandler（handler_reorder.go）  入力 {order:[ページID…]}
├ POST 限定
├ page.NormalizeID（親）→ 400
├ page.RequirePageWrite（**親に対して**）→ 401 / 403
├ 渡された id が本当にその親の子か検査 → 400（他人の木を並べ替えられない）
└ その親の子を**全部**振り直す … ReorderKey(n) = fmt.Sprintf("%010d", (n+1)*reorderSpacing)
   をサイドカーの sort_key へ書く（page.WriteSidecar）
```

**並びの正本はサイドカーの `sort_key`**、順序は `sortChildren`（`handler_tree.go`）の
**並び順キー → 題 → ID**。キーが空でも題で並ぶので、年フォルダ・月フォルダは
何もしなくても正しい順に出ます。

## 8c. 未処理の受信・返信の逆引き

| 経路 | 何をするか |
|---|---|
| 計算ビュー `unhandled-intake` | `view_unhandled.go`。**`対応` のタグが無い**通信記録を新しい順に100件。**値は見ません**（`済` でも `不要` でも外す）——**人の印だけが判定**で、子ページの有無も向きも見ません（2026-09-05 に2度改めた決定）。`page.CanView` と `isDescendantOf(通信箱)` で絞る |
| `GET /api/replies?page_id=` | `handler_replies.go`。`PagesByTag("返信元", ページID)` の逆引き。**元の記録から控えを覗く**ための口 |
| `GET /api/thread?page_id=` | `handler_thread.go`（2026-09-14）。`メッセージID` ⇄ `返信元メッセージID` の**逆引き2回**で `In-Reply-To` の鎖を辿る。**上の行とは別の鎖**——`/api/replies` は w-cms を通った返信だけ、こちらは**受信どうしの返り**も繋がる |

> **上の表の `/api/replies`・`/api/thread` と `/api/analyzed`・`/api/filing-proposal` は、
> 入口が `cms.GateJSONPageRead` で共通**
> です（`handler_gate.go`・2026-09-14）——`Content-Type: application/json` → GET 以外は**JSONで**405 →
> `NormalizeID`（不正は400）→ `CanView`（読めない・無いはどちらも**404**）。
> 同じ16行が7箇所に写されていて、**うち2箇所はメソッド確認が抜けていました**
> （`handler_replies`・`handler_thread`）——写した回数だけ抜ける機会があった、ということです。

> `view_unhandled.go` は**先に読み切ってからページごとに絞ります**——行を読みながら
> 中で別のクエリを投げると、`:memory:` DBでは空の別DBに当たって**絞り込みが静かに
> 全部落ちます**（2026-09-03 に本番コードで踏んだ）。

## 8d. 連絡先を登録する／未分類へ戻す（`POST /api/contacts/register` ／ `/unfile`・`ext/comm/contacts`）

**2026-09-15 から `ext/comm/contacts` の持ち物**です。ルート2本は `plugin.go` の `Routes()` が
持ち込み（`main.go` の直書きは消えた）、作業面は `取引先` ページの計算ビュー
`unknown-contacts`（`contacts_view.go`・`cms.RegisterView` で名乗る）。
索引にあってページになっていないアドレスを1行1件で並べ、**確定するのは人**です。

```
RegisterContactAPIHandler（ext/comm/contacts/contacts_handler.go）
├ POST 限定 → 405（JSON の cms.JSONFail）／ 未ログイン → 403
├ cms.DecodeJSONBody {name, relation, addresses[], page_id?, person_name?}
├ normalizeEmail … 1件も残らなければ 400
│
├─ **既存の相手へ足す枝**（page_id あり。同じ会社が2つ目のドメインから送ってきた場合）
│   ├ page.NormalizeID → 400 ／ isPartnerPage でなければ 400（**箱の外へは足さない**）
│   ├ page.RequirePageWrite → 401 / 403
│   ├ **editlock.RefuseWhileEditing → 409**（2026-09-14。本文を読んで・変えて・書くので、
│   │   エディタが開いているとオートセーブと上書きし合う。**自分が開いていても断る**）
│   ├ person_name があれば EnsureContactPerson … 取引先／社名／担当者／氏名
│   ├ AddContactAddresses … cms.RewriteBody の中で、最初の可変タグの並びの末尾へ
│   │   （cms.EndOfFirstTagList。無ければ cms.InsertAfterH1 で新しい `dl` を作る）。
│   │   既に載っているアドレスは足さない（**文字列で見る**——二度押しよけだけの判定）
│   └ Audit "contact.add-addresses" → 200 {success, page_id, title, added, merged:true}
│
└─ **新しい相手ページを作る枝**
    ├ cms.NormalizeNameForIngest（**部品階層の顧客名と同じページ**なので題を畳む）
    ├ validRelation（取引の値は表引きで閉じる）
    ├ page.RequirePageWrite（箱がまだ無ければ**トップへの write**）
    ├ EnsurePartnerBox → cms.CreateChildPage（h1＋`取引` タグ＋`メールアドレス` タグ）
    └ Audit "contact.register" → 200 {success, page_id, title}

UnfileContactAPIHandler（同ファイル）  入力 {page_id, address}
├ POST 限定 → 405 ／ 未ログイン → 403 ／ page.NormalizeID・normalizeEmail → 400
├ PartnerOfPage … 「取引先」の下でなければ 400（よそのページのタグを消せる道を増やさない）
├ page.RequirePageWrite → 401 / 403 ／ **editlock.RefuseWhileEditing → 409**
├ cms.RewriteBody → removeEmailTag（**畳んだ一致**で探す。空になった `dl` も畳む）
│   1件も外れなければ 404
└ Audit "contact.unfile" → 200 {success, page_id, address, remaining, children, empty}
```

- **コアが借りているものは無く、拡張がコアから借りるだけ**——`cms.RewriteBody`・
  `cms.EndOfFirstTagList`（2026-09-15 に公開）・`cms.InsertAfterH1`・`cms.CreateChildPage`・
  `cms.TopLevelPageByTitle`（`PartnerBoxPageID` の実体）・`cms.PageTitleByID`。
  逆向きの2本（計算ビューの表・アドレスの解決）はフックで裏返した（0章）。

- **登録の印はタグそのもの**です。未登録の一覧は「どこにも `メールアドレス` タグが無い
  アドレス」という**索引からの派生**なので、外せば自動的に作業面へ戻ります
  （解析済みの印と同じ形——状態を別に持たない）。
- **ページは消しません。** 空になったことは返り値（`empty`）で伝え、消すかは人が決めます。

---

## 9. 版を取り出す／戻す

```
[一覧] GET /api/versions?id=      VersionsAPIHandler
  NormalizeID → page.RequirePageRead → ListVersions（新しい順。本文の無い版は飛ばす）

[取得] GET /api/version?id=&v=    VersionAPIHandler
  NormalizeID → page.RequirePageRead → ReadVersion
    ├ safeVersionID … 時刻由来の形（20060102T150405Z）しか受け付けない（/ \ . は不可）→ 404
    └ gzip を 8MiB 上限で展開
  → RenderComputedViews(Sanitize(body)) を text/plain ＋ nosniff で返す

[戻す] POST /api/revert           RevertAPIHandler
├ POST 限定
├ DecodeJSONBody {page_id, version}
├ page.NormalizeID → 400
├ page.RequirePageWrite → 401 / 403
├ editlock.RequireEditLock → 409
├ RevertToVersion(id, version, author)          → 失敗は 400
│   ├ ReadVersion
│   ├ **いまの内容を RecordVersion(force=true)**（リバート自体を取り消せる）
│   ├ Sanitize（過去の版も**いまの許可リスト**で濾す）
│   ├ page.WriteFileAtomic → page.BumpUpdatedAt → SyncIndex
│   └ 戻した内容も RecordVersion(force=true)
├ auth.Audit(user, "revert", "<id> -> <版ID>")
└ 200 {success, page_id, version, html: RenderComputedViews(Sanitize(戻した本文))}
```

**サイドカーは触りません**——権限・所有者・親は現状のままです。

`RecordVersion` の規則: 中身のハッシュが最新の版と同じなら何もしない／`force=false` で
同じ編集者かつ最新の版IDから10分以内なら**その版を置き換える**／それ以外は新しい版を切り、
`PruneVersions` で5年（366日×5）を超えた版を落とす。

---

## 10. 削除する（`POST /api/delete-page?id=`）

```
DeletePageAPIHandler（handler_delete.go）
├ POST 限定
├ page.NormalizeID → 400
├ id == "000000"          → 400（トップページは削除できない）
├ page.RequirePageWrite   → 401 / 403
├ editlock.RequireEditLock → 409
├ 子ページの件数を数える  → 409（件数つき。「先に移動するか削除して」）
├ moveToTrash(id)         … data/trash へ**移動**（同IDが既にあれば -2, -3… を付ける）
├ PurgePageIndex(id)      … 空の本文で SyncIndex → page_perms → pages を削除
├ editlock.Locks.ForceRelease(id)
├ auth.Audit(user, "delete-page", "<id> -> <移動先>")
└ 200 {success, page_id, trash_path}
```

**復元UIはありません。** 戻すには手で `data/master` へ移してDB再構築します。

---

## 11. DB再構築（`POST /api/rebuild-db`）

```
RebuildDBAPIHandler（handler_meta.go）
├ POST 限定
├ page.RequireAdmin → 401 / 403
├ RebuildDatabase（sync.go）                          → 500
│   ├ LoadSettings                … 語→型の推論辞書を読み直す（再起動なしで効く）
│   ├ markRebuildStarted          … rebuild_state に「始めた印」（finished_at = NULL）
│   ├ dropAllTables               … sqlite_master の全テーブル（rebuild_state を除く）を DROP
│   ├ database.CreateCoreTables ＋ ApplySchema
│   ├ resyncAllPages              … data/master を歩き、versions/ を飛ばして .html ごとに SyncIndex
│   │                               （個々の失敗は握り潰して続行・件数を数える）
│   └ markRebuildFinished(件数, ミリ秒)
├ lastRebuildResult → auth.Audit(user, "rebuild-db", "<件数>ページ / <ms>ミリ秒")
└ 200 {success, pages, duration_ms}
```

起動時の `RebuildIfNeeded` は、`data/master` に .html があるとき、**印が残っている
（前回が途中で止まった）なら pages が空でなくてもやり直し**、そうでなければ pages が
空のときだけ再構築します。スキーマのずれ（`DriftedSchemaTables`）は0章で先に処理されます。

---

## 12. ログイン・ログアウト

```
[画面]   GET /login          auth.LoginPageHandler（認証不要）
[認証]   POST /api/login     auth.LoginAPIHandler（認証不要・CSRFProtect は通る）
         ├ loginSem（argon2id の同時実行を4件に制限）
         ├ Authenticate
         │   ├ isLockedOut … 5回連続失敗で15分（login_attempts）
         │   ├ 不在・無効化・ハッシュ無しは decoyHash で時間を均してから失敗
         │   └ VerifyPassword 失敗 → recordFailure
         ├ 失敗 → auth.Audit(username, "login.fail", clientIP)
         │         302 /login?error=1&next=…
         ├ CreateSession → setSessionCookie（HttpOnly・SameSite=Lax・Secure は WCMS_SECURE_COOKIES）
         ├ auth.Audit(user, "login", clientIP)
         └ 302 next（safeNextPath が同一サイト内の絶対パスだけに絞る）
[破棄]   POST /api/logout    auth.LogoutAPIHandler（POST 限定）
         ├ ResolveSession でユーザー名を得てから auth.Audit(user, "logout", clientIP)
         └ DeleteSession → clearSessionCookie → 302 /login
[状態]   GET /api/me         auth.MeAPIHandler（OptionalAuth。未認証は 200 {authenticated:false}）
```

**接続元アドレス**（`clientIP`）は、前段がループバック／私設アドレスのときだけ
`X-Forwarded-For` を採ります（`IsFromTrustedProxy`。`requestBaseURL` も同じ判定を使う）。

---

## 13. クローラ（`GET /sitemap.xml` / `GET /robots.txt`）

```
SitemapHandler … publicPages()（全ページを回して page.EffectivePublic で絞る）
                 → <urlset> ＋ public, max-age=600 ＋ Vary: Cookie

RobotsHandler  … database.DB == nil または page.EffectivePublic(0) が偽
                 → "Disallow: /"（サイトが閉じている＝業務インスタンス）
                 → それ以外は /api/ と /login を閉じ、Sitemap: 行を出す
```

絶対URLの基底は `WCMS_BASE_URL` を最優先し、無ければリクエストから組み立てます
（`X-Forwarded-Proto`/`-Host` は前段がプロキシのときだけ採用）。

---

## 14. フロント側の起動順

```
index.html
├ <head>  boot.js（11行目）… **同期**スクリプト。レールの開閉を描画前に確定（FOUC防止）
├ <head>  app.css
├ <body>  …本文はサーバーが合成済み（#w-editor-content に埋まった状態で届く）
└ </body> 直前 app.js（274行目）… defer なし。DOMが揃ってから同期実行
             bindChromeActions()  … クロームのボタン配線（addEventListener。on*= は無い）
             window.onload:
             ├ await Promise.all([loadMe(), loadTagSchema()])（語彙は保存が走る前に必要）
             ├ initBlocks / buildToc / setupEditorEvents / setupAutoSave
             ├ 匿名なら applyMode / loadPageMeta / loadChildNav して終了
             └ onModeToggle（?edit ならロック取得→/api/load）→ loadPageMeta / loadPerms /
               loadChildNav / loadVersions
```

`applyMode` は閲覧モードで `refreshAttachmentPreviews` を呼び、編集モードでは
添付クロームを外します。コンテキストツールバーは `pointer: coarse` の端末で
`is-docked`（画面下のドック）になり、`positionDockedToolbar` が `visualViewport` の
下端へ合わせます。スラッシュメニューの挿入骨格は `buildVocabSkeleton`——
`usesHeadingForm(def)` なら `<section><h2>表示名</h2>…素の中身…</section>`
（`data-type` を書かない）を生成します。

> `boot.js` も `app.js` も `defer` を持ちません。`<head>` の同期スクリプトが先、
> `</body>` 直前が後、という**書いてある順**どおりに実行されます。

---

## 15. 追い方（この文書を疑うとき）

```bash
grep -nE 'Handle(Func)?\("' cmd/w-cms/main.go     # コアのルート表
grep -rn 'Pattern:' --include='*.go' ext/          # 拡張が足すルート
go doc ./internal/cms RootHandler                  # 個々の説明
go doc -src ./internal/cms SaveAPIHandler          # 実装をそのまま見る
go doc ./internal/cms IntakeContext                # 取り込み係の権能
go doc ./internal/cms Mailer                       # メールの口（実装は ext/comm/mail）
```

E2E（`e2e/verify-*.js` 15本＋`e2e/probe-*.js` 10本・計3,382行・**git 管理下**。2026-09-15 時点）は、
ここに書いた連鎖の多くを実際に走らせています——挙動が疑わしいときは該当スクリプトを読むのが
早道です。`verify-preview.js` は汎用添付（`/api/upload-file`）と閲覧モードの添付クローム
（▶ 表示・🤖 解析ボタンの**表示**）まで見ます。2026-09-14 に2本足しました:
`probe-thread.js`（`🧵 やりとりの前後` が出る・押して移れる・前後の無い記録には出さない）と
`probe-analyze-refresh.js`（**Gemini を呼ばずに** `/api/analyze-attachment` の応答を横取りし、
子ページが生まれたあとの画面の取り直し——左レールと本文の鏡——の配線だけを見る。
子ページは本物を作って後始末する）。
それでも**取り込み・解析・整理・メールの実行そのものは E2E に無く**、Go のテストが固定しています:

| 経路 | 固定しているテスト |
|---|---|
| `.eml` → 通信記録ページ・重複検知 | `internal/cms/intake_eml_test.go`（11本） |
| 🤖 解析（判定器は差し替え） | `ext/subcon/analyze_pdf_test.go`（9本） |
| 整理（移動・改訂の併合） | `ext/subcon/filing_test.go`（7本） |
| アドレス帳（登録・担当者・逆引き・`email` タグのリンク） | `ext/comm/contacts/*_test.go`（12本＋`TestMain`。**ファイルDB**の支度は `setup_test.go`——`page.CanView` を通るので `:memory:` では動かない） |
| アドレスの解決係（無ければ素のまま・登録済みなら使う・空振りは素のまま） | `internal/cms/contact_resolver_test.go`（3本） |
| `page_tags.norm_value` の格納クラス（型を取り違えると0件） | `internal/cms/page_tags_norm_type_test.go`（4本） |
| PDFとDXFの突き合わせ・表題欄 | `ext/subcon/drawing_match_test.go`・`dxf_test.go` |
| 参照追従集計・見せ分け | `ext/subcon/aggregate_test.go`・`materials_perms_test.go` |
| 送信のMIME（`In-Reply-To`・XOAUTH2） | `ext/comm/mail/smtp_test.go` |
| **実物への送信** | `ext/comm/mail/live_test.go`——**既定では走りません**（`WCMS_MAIL_LIVE_TEST=1` のときだけ。本当にメールが飛びます） |
