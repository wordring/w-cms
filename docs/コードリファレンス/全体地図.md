# 全体地図

対象コミット `ef4bef0`（2026-08-19。監査未達3件の修正まで反映）。数値は実測。

## 30秒で掴む

- **単一バイナリのGoサーバー**（`cmd/w-cms`）が、ポート 8080 で HTTP を受ける。外部サービスは
  PDF解析の Gemini のみ（任意）。
- **ページの正本はファイル**。本文は `data/master/<先頭2桁>/<id>/<id>.html`、属性（所有者・権限・
  親・作成/更新）は同じフォルダの `<id>.meta.json`（サイドカー）。
- **SQLite は使い捨ての索引**。`data/cms.db` はファイルから再生成できる（`RebuildDatabase`）。
  再生成できない情報（ユーザー・セッション・監査ログ）だけが `data/auth.db` にある。
- **画面はサーバー合成**。`GET /{id}` が `assets/index.html`（殻）に本文を埋めた完成HTMLを返す。
- **フロントはバニラJS＋Web Components**。ビルド工程なし。`assets/app.js` がエディタ本体。
  カスタム要素10種は**全廃が決定済み**（2026-08-17。マーカー付き標準HTMLへ段階移行。
  新規カスタム要素は作らない——正本は [【考察】語彙モデル.md](../【考察】語彙モデル.md) §9）。
- **語彙モデルの縦切り第1段が動いている**。`<table data-type>`／`<dl data-type>` を
  ①コード内レジストリ（`vocab.go`）が定義し、②汎用索引（`vocab_index.go`）が
  縦持ちテーブル `vocab_index` へ同期する。サンプル語彙は「検査記録」。
- **同時編集はページ単位の悲観ロック**。プロセス内メモリ＋SSE。

## レイヤと依存（Goパッケージ）

```
cmd/w-cms/main.go            ルーティングと起動
        │
        ▼
internal/cms                 HTTPハンドラ層＋プラグイン実装
   │   handler_save/view/tree/meta・perms_admin・pdf_handler
   │   shell・sync・parser・sanitize（語彙の合成）・block
   │   vocab（語彙レジストリ）・vocab_index（汎用索引）
   │   plugin.go（レジストリ）＋ plugin_*.go（実装7本）
   │
   ├──▶ internal/cms/htmldoc   サニタイズの実装（純粋なHTML部品・語彙は注入）
   │
   ├──▶ internal/cms/editlock   編集ロック（状態＋HTTP/SSE）
   │        │
   │        ▼
   └──────▶ internal/cms/page   保存パス・サイドカー・認可・添付配信
                 │
                 ▼
            internal/auth        セッション・CSRF・CSP・ユーザー/グループ
                 │
                 ▼
            internal/database    SQLite接続とコアテーブル定義
```

**逆流はありません**（`page` はハンドラを知らない、`editlock` は `cms` を知らない、
`htmldoc` はプラグインを知らない）。

## ファイルごとの役割

### `cmd/w-cms/main.go`（159行）

全ルーティングの一覧。読むならここが入口。ミドルウェアの入れ子は
`auth.CSPProtect(auth.CSRFProtect(root))` で、**CSP が最外周**。
`/api/` は原則 `auth.RequireAuth` 配下だが、`/api/login`・`/api/tag-schema` は公開、
`/api/load`・`/data/`・`/api/me`・`/api/children`・`/api/page-meta` は `auth.OptionalAuth`（匿名可・
認可は各ハンドラが実効公開で判定）。

### `internal/cms`（ハンドラ層＋プラグイン）

| ファイル | 行 | 役割 |
|---|---:|---|
| `handler_save.go` | 250 | `POST /api/save`（全文）と `POST /api/save-block`（1ブロック）。応答に `unknown_types`（未定義 `data-type` の告知）。`decodeJSONBody`（8MiB上限・413）もここ |
| `handler_view.go` | 170 | `GET /{id}`（画面）と `GET /api/load`（本文） |
| `handler_tree.go` | 346 | 新規作成・子一覧・親の付け替えと検証（循環・権限・実在） |
| `handler_meta.go` | 118 | `GET /api/page-meta`・`GET /api/tag-schema`（`vocab`・`type_inference` を含む）・`POST /api/rebuild-db` |
| `perms_admin.go` | 208 | `POST /api/page-perms`（chmod/chgrp/publish）・`POST /api/page-chown` |
| `pdf_handler.go` | 276 | 添付アップロード（`.pdf` のみ）と Gemini によるPDF明細抽出 |
| `sanitize.go` | 60 | サニタイザの合成ラッパ（`PluginTags()` を `htmldoc.New` へ注入。実装は `htmldoc`） |
| `block.go` | 111 | `data-id` を目印にした1ブロック差し替え（`ReplaceBlock`） |
| `sync.go` | 211 | `SyncIndex`（ファイル→DB）と `RebuildDatabase`（全再構築） |
| `parser.go` | 42 | 本文から**タイトルだけ**抽出（属性はサイドカーが正本） |
| `shell.go` | 81 | `assets/index.html` へ本文とタイトルを埋め込む（mtimeキャッシュ付き） |
| `vocab.go` | 261 | **語彙レジストリ（3層モデルの①）**。形式定義の宣言テーブル（`VocabDef`）・語→型推論辞書（`TypeInferenceDict` でエディタへ配布）・正規化（数値・日付）・`UnknownVocabTypes` |
| `vocab_index.go` | 256 | **汎用索引（3層モデルの②）**。全 `<table data-type>`／`<dl data-type>` を縦持ち `vocab_index` へ同期（プラグイン機構に相乗り・カスタム要素は所有しない） |
| `plugin.go` | 231 | プラグイン機構（`Plugin` IF・レジストリ・DOMヘルパー） |
| `plugin_*.go` | 38〜234 | 1ファイル＝1ユースケース。7本（後述） |

### `internal/cms/htmldoc`（純粋なHTML部品・2026-08-17 切り出し）

| ファイル | 行 | 役割 |
|---|---:|---|
| `sanitize.go` | 414 | 許可リスト方式サニタイズの実装・構造HTMLの語彙・URL規則・`BlockIDAttr`・`ParseFragment`。カスタム要素の語彙は `New([]TagSpec)` で注入され、プラグインを知らない。語彙マーカーは**属性名だけを要素限定で許可**（`data-type`→`table`・`dl`・`th`／`data-field`→`th`・`dd`。値は不活性） |

### `internal/cms/page`（ページの実体）

| ファイル | 行 | 役割 |
|---|---:|---|
| `storage.go` | 36 | `GetPageDir`（`data/master/<先頭2桁>/<id>`）・`NormalizeID`（ゼロ詰め6桁）・定数 `IDLength`(6)・`MasterDir` |
| `perms.go` | 421 | サイドカー読み書き・`GetPerms`・`EffectivePublic`・`Require*` 認可ゲート・`DataFileHandler` |

### `internal/cms/editlock`（編集ロック）

| ファイル | 行 | 役割 |
|---|---:|---|
| `lock.go` | 309 | ロック状態（`Locks`）。取得・検証・解放・SSE購読・猶予の評価 |
| `handler.go` | 207 | `/api/lock`・`/api/lock-events`・`/api/unlock`・`/api/lock/force`・`RequireEditLock` |

### `internal/auth`

`middleware.go`（`RequireAuth`／`OptionalAuth`／`CSRFProtect`／`CSPProtect`／Cookie 属性）、
`session.go`（トークンはSHA-256で保存。絶対8時間・アイドル30分）、
`users.go`（`Authenticate`・ロックアウト5回/15分）、`password.go`（argon2id）、
`handlers.go`（ログイン画面とAPI・`/api/me`）、`context.go`（`CurrentUser`／`WithUser`）、
`admin.go`／`admin_handlers.go`（ユーザー・グループ・監査ログ）。

### `assets`（フロント）

| ファイル | 行 | 役割 |
|---|---:|---|
| `index.html` | 260 | 殻の markup。`<!--WCMS_CONTENT-->` にサーバーが本文を差す。インラインは `<head>` のFOUC防止スクリプト1本だけが残る。表の行・列操作 `#table-toolbar`・dl の `#dl-toolbar`・列設定 `#col-popover`・`#enum-menu` は本文DOM外のクローム |
| `app.js` | 2822 | エディタ本体（後述の [【一覧】関数リファレンス.md](【一覧】関数リファレンス.md) に一覧） |
| `app.css` | 936 | 殻のスタイル（本文の表・編集クローム・機械属性/型不一致ハイライトを含む） |
| `web-components.js` | 660 | カスタム要素。`MElement` を基底に7クラス（うち `MBusinessBlock` は業務要素4種の中間基底）＝`define` される要素は10種 |
| `components.css` | 186 | カスタム要素の外観 |
| `templates/*.html` | 12本 | カスタム要素の閲覧用／編集用テンプレート（内訳は下表） |
| `admin.html` / `admin.css` / `admin.js` | 72/40/144 | 管理コンソール |

テンプレート12本の内訳（`fetchTemplate` が要素と編集モードで選ぶ）:

| 要素 | テンプレート | 本数 |
|---|---|---:|
| `<m-tag>` | `m-tag-{view,edit}` | 2 |
| `<m-item>` | `m-item-{view,edit}-{client,our}`（親が `<m-client-order>` か仕入側かで分岐） | 4 |
| `<m-file>` | `m-file-{view,edit}` | 2 |
| `<m-material>` | `m-material-{view,edit}` | 2 |
| `<m-required-materials>` | `m-required-materials-{view,edit}` | 2 |
| 業務要素4種・`<m-child-list>` | テンプレート無し（JSが直接組み立てる） | 0 |

## プラグイン（`plugin_*.go` 7本）

`Plugin` インターフェース（`Name` / `Schema` / `Tables` / `Tags` / `Sync`）を実装し、
`init()` で `Register()` する。**コアはカスタム要素を1つも知らない。**

| ファイル | 所有するカスタム要素 | 所有テーブル |
|---|---|---|
| `plugin_page_tags.go` | `<m-tag>` | `page_tags` |
| `plugin_file.go` | `<m-file>` | なし（容器のみ） |
| `plugin_child_list.go` | `<m-child-list>` | なし（動的一覧） |
| `plugin_client_order.go` | `<m-client-order>`・`<m-item>` | `client_orders` / `client_order_items` |
| `plugin_our_order.go` | `<m-supplier-order>`・`<m-item>` | `our_orders` / `our_order_items` |
| `plugin_estimates.go` | `<m-our-estimate>`・`<m-supplier-estimate>` | `our_estimates` / `supplier_estimates` |
| `plugin_materials.go` | `<m-material>`・`<m-required-materials>` | `part_materials`（＋集計API） |

`plugin_materials.go` だけが `RouteProvider` も実装し、`GET /api/required-materials` を提供する。

このほか **`vocab_index.go` の `vocabIndexPlugin` も同じ機構へ `Register` される**（登録は計8つ）。
ただしこれはドメインのユースケースプラグイン（③計算プラグイン）ではなく、3層モデルの
**②汎用索引**——カスタム要素を所有せず（`Tags()` は nil）、マーカー付き標準HTMLだけを読んで
`vocab_index` テーブルへ洗い替える。

## データの正本性（この設計の背骨）

| データ | 正本 | 派生 |
|---|---|---|
| ページ本文 | `data/master/.../<id>.html` | `pages.title`（見出しから抽出） |
| ページ属性（所有者・権限・親・作成/更新） | `<id>.meta.json`（サイドカー） | `pages.parent_id` ほか・`page_perms` |
| カスタム要素由来の業務データ | 本文HTML | 各プラグインのテーブル |
| マーカー付き標準HTML（`table`/`dl` の `data-type`） | 本文HTML | `vocab_index`（縦持ち・洗い替えのみ） |
| 語彙の形式定義（列・型・表示名） | `vocab.go` の `vocabRegistry`（Goコード内宣言） | `/api/tag-schema` の `vocab` |
| ユーザー・セッション・監査ログ | `data/auth.db` | **なし（再生成不可・要バックアップ）** |
| 本文で使える語彙 | `htmldoc/sanitize.go` の `structuralElements` ＋ 各プラグインの `Tags()` | `GET /api/tag-schema` |

`data/cms.db` は消しても `data/master` から再生成できる（起動時に空を検出すれば自動再構築）。

## 覚えておくべき不変条件

1. **サニタイズは二層**。保存時（`SanitizeReport`）と描画時（`Sanitize`）。片方だけにしない。
2. **語彙は1箇所**。エディタのシリアライザは `/api/tag-schema` に従う。手書きの要素リストを
   フロントに置かない（置くと「画面に出るのに保存で消える」が再発する）。
3. **添付は `.pdf` のみ**。ページのフォルダには本文とサイドカーが同居しているため、
   任意の名前・種類を書けると権限昇格と保存型XSSになる（2026-08-05 に実際に塞いだ）。
4. **フロントでテンプレートに値を差すときは `escapeAttr`**。サニタイザでもCSPでも止まらない。
5. **カスタム要素は Light DOM**。Shadow DOM に描くと保存時のDOM走査から外れる。
6. **`page` は上位（ハンドラ・ロック）を知らない**。逆向きの import を足さない。
7. **語彙レジストリは安全性の門ではない**。未知の `data-type` も保存を通し（告知のみ）、
   サニタイズは属性名の許可（要素限定）だけを見る。レジストリを検査に使い始めると
   「自由な記述をシステムが認識する」というコア・コンセプトが壊れる。
8. **新規カスタム要素（`<m-*>`）は作らない**（2026-08-17 決定・即時発効）。新しい形式は
   `vocabRegistry` への宣言1件（マーカー付き標準HTML）で足す。

## テストの地図（16ファイル・97テスト）

「どの不変条件がテストで固定されているか」の一覧。変更する前に、対応するテストを見れば
**壊してはいけない振る舞い**が分かる（テスト名は `grep "^func Test"` の実測）。

| ファイル | 固定している振る舞い |
|---|---|
| `auth/auth_test.go` | argon2id の往復・セッションの発行/失効（絶対・アイドル）・ロックアウト（5回）・未知ユーザーでも一様に失敗・初期adminの作成 |
| `auth/admin_test.go` | ユーザー管理（作成・パスワード・無効化）・グループ管理・監査ログ |
| `cms/sanitize_test.go` | 危険要素の削除・カスタム要素と宣言属性の維持・未知属性の除去・未知要素のアンラップ・構造HTMLの維持・URL規則（リンク外部可/埋め込み相対のみ/プロトコル相対拒否）・**冪等性**・レポート・**エディタ出力が無変更で通ること** |
| `cms/plugin_tags_test.go` | 語彙の合成（和集合・整列）・**プラグインが読む属性は必ず宣言されていること**・許可リストがプラグイン由来であること・コア（htmldoc）がカスタム語彙を持たないこと |
| `cms/save_sanitize_test.go` | 保存時サニタイズ＋エコーバック・見積属性の維持・通常内容の不変 |
| `cms/block_test.go` | `ReplaceBlock` が対象ブロックだけ差し替える・入れ子維持・空行を蓄積しない・不在/重複の検出・`data-id` の維持と `id` 属性の除去 |
| `cms/shell_test.go` | サーバー合成（本文・タイトルの埋め込み）・**描画時サニタイズ**・画面の認可（匿名×非公開=302 `/login`・匿名×公開=200・権限なし=403。**404 のテストは無い**） |
| `cms/storage_test.go` | `GetPageDir`・IDの原子的採番・入れ子受発注の同期・サイドカー同期・**部材手配計算**・プラグイン`Tables()`とスキーマの整合・DB再構築・起動時自動再構築 |
| `cms/perms_test.go`・`perms_admin_test.go` | mode のビット判定・サイドカー→`page_perms` 同期・親変更の検証・`GetPerms` のフェイルクローズ・**実効公開（パスゲート）**・匿名read・chmod/chown・編集ロック検証 |
| `cms/page_tags_test.go` | **同名タグの複数許容**・旧「親ページID」タグの非取込・洗い替え |
| `cms/upload_test.go` | サイドカー上書きの拒否・非PDF/偽PDFの拒否・パス要素の除去・正常受理・**配信側が実行可能な型を返さないこと** |
| `cms/vocab_test.go` | レジストリの整形（kebab-case・一意・enum整合）・`VocabDefs` の整列・型推論・正規化（数値・日付）・**語彙マーカーがサニタイズを通ること／他要素では落ちること**・未知種別の検出・`/api/tag-schema` に `vocab`・`type_inference` が載ること（辞書がサーバー実装と一致すること） |
| `cms/vocab_index_test.go` | 表・dl の索引（鍵と型の解決・`data-field`／`th data-type` の優先）・**素の table/dl を索引しないこと**・見出しのみの表・同一形式の複数ブロック・**保存応答の `unknown_types`** |
| `cms/audit_fixes_test.go` | `NormalizeID` の規則・保存/アップロードが**正規ディレクトリへ書き揺れた表記のディレクトリを作らない**こと・8MiB超のJSONボディが413 |
| `cms/editlock/lock_test.go` | 取得・検証・解放のガード・待機者で猶予が張られる・満了で明け渡し・保持者/待機者の切断検知・**presence の無い保持者は次の取得で奪えること** |

## 既知のねじれ

- ~~**`cms` パッケージ内の循環**~~ **解消済み（2026-08-17）**: サニタイザを
  [internal/cms/htmldoc](../../internal/cms/htmldoc/sanitize.go) へ切り出し、カスタム要素の
  語彙は `htmldoc.New([]TagSpec)` の引数として**注入**する形に反転した。`htmldoc` の import は
  `x/net/html` と標準ライブラリのみで、`PluginTags()` を呼ぶのは cms 側の合成ラッパ
  （[sanitize.go:34](../../internal/cms/sanitize.go)）1箇所。
- **暗黙の初期化順序**: 語彙は `sync.Once` で初回利用時にキャッシュされる。「全プラグインの
  `init()` 登録 → 最初のサニタイズ」という順序が型で表現されていない。
- ~~**ページIDの正規化漏れ**~~ **解消済み（2026-08-19）**: `page.NormalizeID`（ゼロ詰め6桁）を
  新設し、パスやサイドカーに id を使う入口（保存2本・添付2本・`/api/set-parent`・
  `/api/page-perms`・`/api/page-chown`・`/api/lock`）で正規化するようにした。
