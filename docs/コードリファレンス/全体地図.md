# 全体地図

**対象コミット: `e259968`（2026-08-30 更新）**

`docs/コードリファレンス/` は**実装の現状を写した文書群**です。設計の「なぜ」は各設計書が持ち、
ここは「いま何がどうなっているか」だけを扱います。

> **`go doc` が第一の窓口です。** 各パッケージには冒頭コメントを置いてあるので、
> まずそちらを読んでください——本ファイルはパッケージを**横断する**話（依存の向き・
> 正本性・ねじれ）を担当します。
>
> ```bash
> go doc ./internal/cms          # パッケージの概説＋公開APIの一覧
> go doc ./internal/cms Observer # 特定の型・関数の詳説
> go doc -all ./internal/cms/page  # そのパッケージの全公開APIをコメントごと
> ```

> ## ⚠ この文書群は一度畳まれています（2026-08-21）
>
> 手書きの「現状の写像」は実装と同じ速度で陳腐化し、**行数主張30件中16件が誤り・
> 一部は内容が逆**という状態になりました——読んだ人が正しい実装をバグと誤認しかねない、
> という理由で全部消しています。2026-08-27 に作り直し、2026-08-30 に測り直したのがこの版です。
>
> 同じ轍を踏まないための決め事:
>
> - **説明の本体は `go doc`（＝コードの中）へ置く。** パッケージの冒頭コメントは
>   コードと同じファイルにあるので、実装を変える人の目に入ります。この文書群が担うのは
>   **横断的な話だけ**——依存の向き・正本性・シナリオの連鎖です。
> - **数字は「対象コミットの実測」であって仕様ではない。** 行数・件数は上の対象コミット
>   時点の値で、各文書に**数え直すコマンド**を添えてあります。合わないときは
>   コードが正しく、文書が古いのです。
> - **古くなったら直すのではなく作り直す**（スキル `/code-reference`）。
>   直し続けるより安い、というのが 2026-08-21 の判断でした。

---

## 1. 規模（`wc -l` の実測・テストを除く）

| 層 | 行数 | 内訳 |
|---|---:|---|
| Go（本体） | 10,610 | `internal/cms` が最大。下表参照 |
| Go（テスト） | 8,234 | 本体の 78%。`internal/cms` に集中 |
| フロント | 5,555 | `app.js` 3,568／`app.css` 1,245／`index.html` 259 ほか |
| E2E（Playwright） | 1,647 | `e2e/` の9本 |

Go の大きいファイル（上位11）:

| 行数 | ファイル | 役割 |
|---:|---|---|
| 600 | `internal/cms/vocab.go` | ①語彙レジストリ（形式の宣言・型推論・正規化） |
| 490 | `internal/cms/page/perms.go` | サイドカー・認可の関門・添付の配信 |
| 471 | `internal/cms/htmldoc/sanitize.go` | 本文の語彙（許可リスト）の唯一の正本 |
| 470 | `internal/cms/walk.go` | 配送係（回覧機構）と段別コンテキスト |
| 422 | `internal/cms/sync.go` | `SyncIndex`（コア同期＋回覧の入口）と全再構築 |
| 388 | `internal/cms/image.go` | 画像の種別判定・EXIF除去・SVG検査 |
| 378 | `internal/cms/handler_tree.go` | ページ新規作成・親の付け替え・子一覧 |
| 358 | `internal/cms/vocab_index.go` | ②汎用索引・dt/dd 走査の反復子・③向けの読み取りヘルパ |
| 356 | `internal/cms/version.go` | 版管理（記録・一覧・読み出し・リバート） |
| 346 | `internal/cms/editlock/lock.go` | 悲観ロック（SSE presence・競合トリガー方式） |
| 329 | `internal/cms/handler_view.go` | ページ描画（殻の合成）と本文API |

---

## 2. パッケージと依存の向き

`go list -f '{{join .Imports "\n"}}'` の実測。**循環はありません。**

```
cmd/w-cms
  └→ internal/auth, internal/cms, internal/cms/editlock, internal/cms/page, internal/database

internal/cms
  └→ internal/auth, internal/cms/editlock, internal/cms/htmldoc, internal/cms/page, internal/database

internal/cms/editlock  →  internal/auth, internal/cms/page
internal/cms/page      →  internal/auth, internal/database
internal/auth          →  internal/database
internal/cms/htmldoc   →  （w-cms の依存なし）
internal/database      →  （w-cms の依存なし）
```

**守るべき向きが2つ**あります。

- **`htmldoc` は誰も import しない。** 本文の語彙（許可要素・属性）の唯一の正本で、
  アプリのドメイン（プラグイン・レジストリ）を知りません。かつては各プラグインが
  宣言したカスタム要素を `New()` へ注入していましたが、語彙モデル移行（2026-08-20）で
  カスタム要素がゼロになり、注入ごと消えました。**`htmldoc` から `cms` を import しないこと。**
- **`editlock` は `cms` を import しない。** 版のチェックポイントを打つために
  「ロックを手放した」という出来事だけが必要なので、関数変数 `editlock.OnRelease` で
  受けます（設定するのは `cms` 側の `init`）。これで一方向のままになります。

---

## 3. 何が正本か（不変条件）

**この節がこの文書群のいちばんの価値です。** 破ると静かに壊れる約束が並びます。

| 対象 | 正本 | 派生 | 破ったときに起きること |
|---|---|---|---|
| ページの**内容** | `data/master/<先頭2桁>/<id>/<id>.html` | `pages.title`・`vocab_index`・③計算テーブル | — |
| ページの**属性**（所有者・グループ・mode・公開・親・作成/更新） | サイドカー `<id>.meta.json` | `pages.parent_id` 等・`page_perms` | 派生から正本へ書き戻すと、欠損が派生の内容で埋まって「見た目は健全なのに中身が違う」状態になる |
| **本人確認**（利用者・グループ・セッション） | `data/auth.db` | — | **ファイルから再生成できない**。`data/master` とは別に必ずバックアップ |
| 本文の**語彙**（許可要素・属性） | `htmldoc` の `structuralElements` | `/api/tag-schema` の応答 | エディタとサーバーで許可がずれ、保存のたびに値が消える |
| **形式**の宣言（`data-type` の意味） | `vocab.go` の `vocabRegistry` | スラッシュメニュー・挿入骨格・型推論 | — |
| **設定**（語→型の推論辞書） | `data/settings.json` の `type_inference`（settings.go・2026-08-30 新設） | `/api/tag-schema` の `type_inference`・②索引の型解決 | 再生成できない正本（消すと既定値へ戻るだけ）。在るのに読めない・不正なら**起動を止める**——既定値で黙って上書きすると運用者が足した語が消えるため |
| 版 | `versions/<版ID>.html.gz` ＋ `.json` | — | — |

派生に関する規律:

- **`cms.db` は使い捨て**。`data/master` から丸ごと作り直せます（`RebuildDatabase`）。
  ファイルから再生成できない値をここへ持たせてはいけません。
- **⚠ 認可の読み口は正本ではなく派生**——`page.GetPerms` も `page.EffectivePublic` も
  `cms.db` を読みます。**ずれたら認可は索引に従う**ので、`parent_id` の喪失は
  そのまま公開範囲の問題になります。
- **派生（DB）から正本（サイドカー）へは書き戻さない**（2026-08-21 決定）。
  サイドカーが読めなければ `requireSidecar` がエラーを返し、本文保存・親の付け替え・
  権限変更が**止まります**（運用者が手で直す）。

---

## 4. ファイルの役割

### `internal/cms`（コア＋プラグイン）

**本文の入口・出口**

| ファイル | 役割 |
|---|---|
| `handler_view.go` | ページ描画（`RootHandler`）と本文API（`LoadAPIHandler`）。殻の合成・体裁の出し分け・404/403 |
| `handler_save.go` | 全文保存・ブロック保存。サニタイズ→ファイル→索引→版 |
| `shell.go` / `shell_public.go` | 編集用の殻／公開専用の殻の合成（＋SEOメタ・ETag・基底URL） |
| `sanitize.go` | サニタイズの薄いラッパ。実体は `htmldoc` |
| `anchor.go` | ページ内アンカーの合成（**描画時だけ**。`/api/load` へは通さない） |
| `block.go` | ブロック単位の差し替え（`data-id` で識別。無ければ全文保存へ落ちる） |
| `parser.go` | 本文からタイトルだけを取る（`PageTitle` の1関数。属性はサイドカーが正本。旧 `ParseCore`＋`CorePage` は 2026-08-30 に畳んだ） |

**回覧機構（走査はここに1つだけ）**

| ファイル | 役割 |
|---|---|
| `walk.go` | 配送係・引き金の解決（`triggerOf`）・段別コンテキスト・登録の口 |
| `sync.go` | `SyncIndex`（コア同期＋観察係の段）・全再構築・中断検出 |
| `view_render.go` | 計算ビュー（鏡型の段）。子ページ一覧・手配集計 |
| `template_new.go` | テンプレート新規化（種まきの段）。空欄を列型の既定値で埋める |

**語彙と索引**

| ファイル | 役割 |
|---|---|
| `vocab.go` | ①語彙レジストリ。形式の宣言・列型・語→型推論・値の正規化・保存時の告知 |
| `vocab_index.go` | ②汎用索引（観察係・引き金 `*`）・dt/dd 走査の反復子 `eachDLPair`・③向けの読み取りヘルパ |
| `plugin.go` | `Plugin` / `Observer` の定義・登録・スキーマ適用・ずれ検出・DOMヘルパ |
| `plugin_page_tags.go` | 可変タグ → `page_tags` |
| `plugin_client_order.go` / `plugin_our_order.go` | 受注／発注（ヘッダ＋明細） |
| `plugin_estimates.go` | 販売見積・購入見積（1プラグインで2形式・2テーブル） |
| `plugin_materials.go` | 部材（BOM）＋ 部材手配の集計API |

**その他**

| ファイル | 役割 |
|---|---|
| `version.go` / `handler_version.go` | 版管理（記録・一覧・読み出し・リバート）とそのAPI |
| `image.go` / `upload_image.go` | 画像の種別判定・EXIF除去・SVG検査／アップロード |
| `pdf_handler.go` | PDF添付と、Gemini による明細のAI抽出 |
| `template.go` / `handler_template.go` | テンプレート領域の判定／テンプレート一覧 |
| `handler_tree.go` | ページ新規作成・親の付け替え・子ページ一覧 |
| `handler_delete.go` | 削除（ゴミ箱への移動） |
| `handler_meta.go` | 語彙の配布・ページ属性・DB再構築 |
| `handler_sitemap.go` | `sitemap.xml` / `robots.txt` とキャッシュ秒数の定数 |
| `settings.go` | 設定ファイル `data/settings.json` の読み書き（語→型の推論辞書の正本。2026-08-30 新設）。在るのに読めなければ起動を止める |
| `perms_admin.go` | 権限変更・所有者変更（**サイドカーを書くのはここと新規作成だけ**） |

### そのほかのパッケージ

| パッケージ | 役割 |
|---|---|
| `internal/auth` | パスワード（argon2id）・セッション・利用者/グループ・監査記録・ミドルウェア |
| `internal/cms/page` | サイドカー・認可の関門・添付の配信・ID正規化・置き場所 |
| `internal/cms/editlock` | ページ単位の悲観ロック（SSE presence・競合トリガー方式） |
| `internal/cms/htmldoc` | 本文サニタイズ（許可リスト）。**本文の語彙の唯一の正本** |
| `internal/database` | SQLite 接続とコアテーブル。cms.db（派生）と auth.db（正本）の2本 |
| `cmd/w-cms` | 起動とルート表（`buildHandler`） |
| `cmd/list-models` | Gemini のモデル名を一覧する診断ツール（本体とは無関係） |

### フロント（`assets/`）

| ファイル | 行数 | 役割 |
|---|---:|---|
| `app.js` | 3,568 | エディタ本体。シリアライザ・保存・ロック・表エディタ・スラッシュメニュー・版履歴 |
| `app.css` | 1,245 | 編集画面のスタイル |
| `index.html` | 259 | 編集用の殻。`<!--WCMS_CONTENT-->` へ本文が合成される |
| `public.html` / `public.css` | 27 / 150 | **公開専用ビュー**。スクリプトを読み込まない |
| `boot.js` | 21 | `<head>` で同期実行。レールの開閉を描画前に確定（FOUC防止） |
| `admin.html` / `admin.js` / `admin.css` | 72 / 152 / 40 | 管理コンソール |
| `login.css` | 21 | ログイン画面・404/403 画面 |

> **読み込み順と実行順**: `index.html` の `boot.js` は `<head>` の**同期**スクリプト
> （`defer` なし）なので、`</body>` 直前の `app.js` より先に実行されます。
> どちらにも `defer` は付いていません。

---

## 5. 既知のねじれ・注意点

- **`RequireAdmin` が2つある**——`auth.RequireAdmin`（`admin_handlers.go` から）と
  `page.RequireAdmin`（`editlock/handler.go`・`handler_meta.go`・`perms_admin.go` から）。
  同名で同じ意味ですが、パッケージが違うだけです。
- **`Sanitize` は薄い委譲**——`cms.Sanitize` は `htmldoc` の `Sanitizer` を呼ぶだけ。
  許可リストを直すときは `htmldoc/sanitize.go` を見ます。
- **プラグインテーブルにマイグレーションが無い**——`ApplySchema` は
  `CREATE TABLE IF NOT EXISTS` を流すだけなので、列を足しても既存DBには反映されません。
  起動時のずれ検出（`DriftedSchemaTables`）＋自動再構築が入っているので、
  **スキーマを変えたら起動時に1度だけ再構築が走ります**。
- **コアテーブルには手書きのマイグレーションがある**——`database/sqlite.go` の
  `coreMigrations`（`ALTER TABLE ... ADD COLUMN`。重複列のエラーは無視）。
  プラグイン側とは対処が違うので混同しないこと。
- **`WalkElements` を回すファイルは6つだけ**（grep の実測）——`anchor.go`（アンカー合成）・
  `parser.go`（タイトル抽出）・`plugin.go`（`TagValue`）・`sanitize.go`（`ShellPrefixedIDs`）・
  `shell_public.go`（`ExtractDescription`）・`vocab.go`（保存時の告知）。いずれも索引・計算の
  ための走査ではありません。本文を走査する新しいコードを書いたら、たいてい
  配送係（`walk.go`）を使うべき場面です。
- **dt/dd の対を読む処理は `eachDLPair`（`vocab_index.go`）の1つだけ**——2026-08-30 に
  6箇所の状態機械を併合しました。新しく `dl` を読むコードが「直前の dt を覚えて dd で使う」
  ループを書き始めたら、この反復子を使うべき場面です。入れ子 `section` へ降りるかは
  引数 `skipSection` で分かれます（形式の読み取りは降りない・タグと②索引の書き込みは降りる）。

---

## 6. 関連文書

| 知りたいこと | 見る文書 |
|---|---|
| どの関数が誰から呼ばれるか | [【一覧】関数リファレンス.md](【一覧】関数リファレンス.md) |
| 操作ごとの呼び出しの連鎖 | [シナリオ別の呼び出し追跡.md](シナリオ別の呼び出し追跡.md) |
| なぜそう作ったか（設計の理由） | [../アーキテクチャとDBスキーマ.md](../アーキテクチャとDBスキーマ.md)・[../認証認可設計.md](../認証認可設計.md)・[../パーサとプラグイン.md](../パーサとプラグイン.md) ほか |
| HTTPエンドポイントの仕様 | [../【一覧】API.md](../【一覧】API.md) |
| プラグインの書き方 | [../【ガイド】プラグイン開発.md](../【ガイド】プラグイン開発.md) |

---

## 7. 数え直す（この文書を疑うとき）

```bash
# 規模
find internal cmd -name '*.go' -not -name '*_test.go' | xargs wc -l | sort -rn | head -12
find internal cmd -name '*_test.go' | xargs wc -l | tail -1
wc -l assets/*.js assets/*.css assets/*.html
wc -l e2e/*.js | tail -1

# パッケージ依存の向き
for p in ./cmd/w-cms ./internal/auth ./internal/cms ./internal/cms/page \
         ./internal/cms/editlock ./internal/cms/htmldoc ./internal/database; do
  printf "%-28s " "$p"
  go list -f '{{join .Imports "\n"}}' $p | grep 'w-cms/' | sed 's|w-cms/||' | tr '\n' ' '; echo
done

# ルート表
grep -nE 'Handle(Func)?\("' cmd/w-cms/main.go
```

**`gofmt -l` は判断材料にしないこと**——このリポジトリでは CRLF のため全ファイルが挙がります。
整形の確認は `go build` / `go vet` / `go test` で行います。
