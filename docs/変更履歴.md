# 変更履歴

「いつ・何を・**なぜ**変えたか」の記録です。**現行の仕様はここには書きません**——正本は
[コンセプト.md](コンセプト.md)・[アーキテクチャとDBスキーマ.md](アーキテクチャとDBスキーマ.md)・
[エディタ仕様.md](エディタ仕様.md)・[本文サニタイズ設計.md](本文サニタイズ設計.md) 等の各設計書で、
領域と正本の対応は [要件定義書.md](要件定義書.md) §6 にあります。

---

## ワンノートの実物を読み取り調査し、移行の条件を洗い出す（2026-08-25）

運用中の「板金部」ノート（現役766ページ）を COM 経由・**読み取り専用**で棚卸しし、
w-cms 側に必要な受け皿を [【考察】ワンノート移行.md](【考察】ワンノート移行.md) へまとめた。
主要な発見: ①画像4,825枚（86%のページ）で**画像アップロードが移行の絶対前提**
②ワンノートの検索は画像のOCRテキスト（32.5万トークン）で機能しており**受け皿が無いと検索が後退する**
③2番目に多い定型（工程表497件）は**縦持ち表（第1列が鍵）**で現行の語彙では索引に載らない
④添付は15拡張子・約950件が現行の許可（`.pdf` のみ）に入らない
⑤`単価（ロット20）倉橋` のように**列名がデータを運ぶ**——硬い列スキーマでは受からず、
汎用索引（名前：値）一本化の検討を実データが裏付けた。
あわせて [コンセプト.md](コンセプト.md) §1② を「役割は階層とタイトルでだいたい決まり、
`data-type` は数えるための副」へ改めた（2026-08-25 ユーザー方針）。
同日追記: **部品ページから引き出したいのは「購入部品・購入材料・外注依頼品」の3種**という
要件が示され、実データでの現れ方（購入材料は表で揃い、購入部品はヘッダ無し表、
外注依頼品は様式が定着せず自由文）と、既存の手動テンプレート「コピペ ノート」2枚の
節構成を同書 §2.5 へ追記した。

## 機能見出し提案——data-type の言葉を見える場所へ（2026-08-25）

ユーザー提案の連鎖（caption に機能を書く → **表を含むセクションの最初の見出しが機能を
表し、日付や企業名は名前：値のタグで表す**）を
[【考察】語彙モデル.md](【考察】語彙モデル.md) §11 へ「提案（未決定）」として起こした。
コンセプト §1②（役割は置き場所と名前で決まる）のブロックへの再帰であり、ワンノートの
実データ（■見出し＋表）と1:1で写る。あわせてユーザー指摘の**二種類の計算**——
最初の一度だけ生成して以後は人間の持ち物（種まき型）と、見るたび最新（鏡型）——が、
それぞれテンプレート新規化 `FreshenTemplateBody` と計算ビュー `RenderComputedViews` として
**実装済みの先例を持つ**ことを確認して記録した。「ここまでの考察は私の使い方であって、
ほかのユーザーはそうとは限らない。プラグインのような機構が必要」（ユーザー）——
**コアは機構だけを持ち、言葉と結びつきはレジストリ／プラグインへ**という分担を提案の柱に
据えた。決定はスキーマ設計し直し（作業引き継ぎ）と一緒に行う。
同日追記（§11.4）: 実行モデルもユーザー提案——**コアが木を1回歩き、ノードごとに
プラグインへ回覧し、担当なら書き換えて返す**。各プラグインの自前走査が消えるため、
「走査コードはクローム除外を忘れるな」の罠が構造ごと消える。手すり3本
（ノード受け渡し［`html.Render` の自動エスケープを検証済み］・サニタイザは鎖に入れない・
書き換え係は言葉ごとに一意）を提案の一部とした。
続くやり取りで2点を確定した: ①APIは**イベントハンドラ型**（プッシュ型）で、パーサーは
既存の `x/net/html` のまま（SAX over DOM。除外は「イベントが来ない」で表現）
②コンテキストは**サーバー資源の運び手**でもあり、段ごとに型を分けて最小権限にする
（観察係=書き込みTx・鏡型=読み取り専用＋閲覧者の身元・種まき型=読み取り＋時計。
鏡型に書く力を渡さないことで、派生→正本の逆流事故がコンパイル時に不可能になる。
サイドカーの読み出しはA-7の「自動で治さない」をAPIの意味論として固定する）。
同日、委嘱により実装可能な粒度の設計へ詳細化し
[【考察】パーサとプラグイン.md](【考察】パーサとプラグイン.md) として独立させた。

## 語彙とプラグインの運用者追加を要件に含める（2026-08-26）

ユーザー指示:「プラグインを様々な企業やサイト運営者が自分で設置することは要件に
含まれていますか？含まれていない場合、含めてください。**利用者は私のみではありません**」。
[要件定義書.md](要件定義書.md) §1.1 へ「設置者は開発元に限らない」を、§4.5 へ
「語彙とプラグインは運用者のもの」を追加した。開発元の業務語彙（受発注・部材など）は
**同梱の既定セット**であってコアの一部ではない。現行の追加手段（Goコード＋再ビルド）では
要件を完全には満たせず、レジストリの育成路線（コード→管理DB→語彙定義ページ）の後段と、
定義がデータになった段階の信頼境界の再設計（パーサとプラグイン §5.5）がこの要件に接続される。
[【考察】語彙モデル.md](【考察】語彙モデル.md) §11.3 も「提案の根拠」から「要件」へ格上げした。

## 集計プラグインが動く時機をワンノートの時刻で実測する（2026-08-25）

「集計に関わるプラグインがいつ動くか。現在のワンノート運用を見てわかることは」
（ユーザー）に対し、板金部766ページの作成・最終更新時刻を分析した。
編集は作成の約10倍・42%のページが1年後にも編集される・手集計の「まとめ」は
67セクション中34件でまとめより後に他ページが更新されていた（遅れ中央値35日・最大495日）。
帰結——①設計した3段（保存時・表示時・新規作成時）で実運用の全瞬間を覆い、バッチは不要
②鏡型の価値は速さでなく「腐らないこと」③ページ単位の洗い替えが編集パターンに合う
④ロット実績の日付が自由文に埋まっており時期絞り込み集計にはデータモデル側の宿題が残る。
[【考察】パーサとプラグイン.md](【考察】パーサとプラグイン.md) §2.5 へ記録した。

## data-type 依存文書の言い回しを副の位置づけへ揃える（2026-08-25）

ユーザー指示「**data-typeに依存する文書を修正してください。data-typeは以前ほど力を
持ちません**」。決定ログ・履歴・実装記述（要件定義書・アーキテクチャ・用語集）は記録なので
触らず、**物語を語っている箇所**だけを §1② の序列（役割は階層と名前が主・`data-type` は
機械が数えるための副）へ揃えた——[コンセプト.md](コンセプト.md) §1 導入文（旧「タグを付与する
ことで意味を認識させる」は親ページまでタグ扱いの最古の物語だった）と §2 導入・図のラベル、
[【一覧】語彙.md](【一覧】語彙.md) の冒頭注記、
[【ガイド】プラグイン開発.md](【ガイド】プラグイン開発.md) の冒頭注記（回覧型への設計し直しの
予告を含む）。
続くユーザー指示により [コンセプト.md](コンセプト.md) §2 を**節ごと差し替えた**——旧
「標準HTML＋`data-*` マーカーによるブロック構造」（マーカーが主役の章立て）を廃し、
「**見える文字がデータの手掛かり**」として再構成（何の手掛かりがどの文字かの対応表・
マークアップ例は「見えている文字がそのまま鍵」の実例として存置）。冒頭の主題文も
「タグ（名前：値）による意味付け」から「見える文字を手掛かりとした双方向データ連携」へ。

さらに「**見える文字を手掛かりにする前提ですべての文書を見直す**」（ユーザー指示）として
全文書を掃引し、残っていた旧い物語を7箇所直した——コンセプト §1 の節題
（「タグ」によるデータの意味付け→見える文字による手掛かり）と「意味マーカー」の残り、
README の紹介文（マーカーによる構造化→見える文字を手掛かりに読み取る）、
[要件定義書.md](要件定義書.md) §1.1 の2項（「タグから自律的に意味を認識」
「配置したブロックによって役割が決まる」→階層とページ名・見える文字の手掛かりへ）と
§1.2 の語彙増加の記述（`data-type` 値として増える→レジストリの宣言として増える）、
[【考察】語彙モデル.md](【考察】語彙モデル.md) §2 の属性表への前方注記
（役割マーカーも §11 で見出しの言葉へ移す提案）、
[【一覧】用語集.md](【一覧】用語集.md) の「マーカー付き標準HTML」の定義
（意味づけを担う→手掛かりを見える文字が運ぶ）。
決定ログ・履歴・実装の事実記述（アーキテクチャ・エディタ仕様・サニタイズ設計等）は
記録なので据え置いた。

## 「黙って壊れる」3件を塞ぐ（2026-08-22）

判断待ちで決まっていた優先3件を順に実装した。いずれも「修正前に落ちるテスト」を
先に書いてから直している。

**1. 途中で止まった索引再構築を次の起動でやり直す**（`9bcbb4e`）。
中断すると（強制終了・停電）次の起動は何事もなく成功し、取り込めなかったページは
開いても「ありません」のままだった——起動時のフックが見ていたのは `pages` が空かどうか
だけで、途中まで入ったDBは再構築済みと見分けが付かない。決定どおり3点を入れた:
①1行テーブル `rebuild_state` に開始時刻を残す（このテーブルだけ `dropAllTables` の対象外）
②`RebuildIfEmpty` を `RebuildIfNeeded` へ改め、「pages が空」より先に中断を見る
③終わったら件数と所要時間を起動ログ・`rebuild_state`・管理コンソールの完了表示まで通す
（「完了しました」だけだと取りこぼしても気づけないため）。方式（ファイルから作り直す）は維持。

**2. サイドカーが読めないときは自動で治さない**（`a15da5f`）。
前日の `bd7ee98` は「親ページIDを失う」バグを、索引から親を拾い直して書き戻す形で塞いだ。
欠損の緩和としては働くが、**派生から正本を書くと、正本の欠損が派生の内容で上書きされ
「見た目は健全なのに中身が変わっている」状態**を作る。決定は「運用者が手作業で修正する。
自動で治さない」なので方向を戻した——`requireSidecar` が読めなければエラーを返し、
本文保存・親の付け替え・権限変更・所有者変更が止まる（修復対象のパスを添えて知らせる。
内容 `<id>.html` は無事）。`SyncIndex` が索引側の `parent_id` を保つのはDB内で完結する
保護なので維持。`sidecar_loss_test.go` は決定ログの指示どおり期待を反転させた。

**3. 匿名応答を404へ統一し、ログイン後に元のページへ戻す**（`9abf32a`）。
匿名に対して「読めない」（画面302／API401）と「存在しない」（404）の応答が分かれていた。
狙いは列挙対策ではなく**権限の無い書類を開いた社員が迷わないこと**なので、画面とAPIの
匿名×非公開を404へ揃えた（`/api/load`・`/api/page-meta`・`/api/children`・`/data/` は
同じ関門を通るので一度に揃う）。例外はトップページだけ——ここまで404にすると、サイトを
閉じたとき誰も入口へ辿り着けない。認証済み×read不可は403のまま。
404だけを先に入れると社員どうしのアドレス共有が行き止まりになるため、**戻り先の復元と
同時に**入れた: 404画面が `/login?next=<元のパス>` を持ち、ログインフォームが `next` を
運び、成功時にそこへ戻す（失敗しても戻り先は保つ）。`safeNextPath` が同一サイト内の
絶対パスだけを通す（`//host`・`/\host`・スキーム付き・制御文字は "/" へ倒す）。
不存在ページも同じ404画面なので、これで存在が分かることはない。

---


## この文書の読み方

- **載っているもの**: **2026-08-16 以降**——本文を「標準HTML＋`data-*` マーカー」へ転換して
  からの記録です。現行アーキテクチャの判断根拠がそのまま入っているので、厚いまま残しています。
- **載っていないもの**:
  - **2026-08-06 以前** → [変更履歴アーカイブ.md](変更履歴アーカイブ.md) へ**丸ごと移しました**
    （要約も削除もしていません）。下の索引から当たりを付けて引いてください。
  - **いま何をしていて次に何をするか** → [作業引き継ぎ.md](作業引き継ぎ.md)。
  - **未決の問い** → [判断待ち.md](判断待ち.md)。
  - **関数名や行番号** → コードそのもの。ここに書くと実装と同じ速度で陳腐化します
    （手書きのコードリファレンスを畳んだのがまさにその理由——2026-08-21 の節）。
- 日付降順（新しいものが上）。1つの節が1回の作業セッションにおおむね対応します。

---

## 索引

### この文書に載っている期間（2026-08-16〜）

| 日付 | 節 |
|---|---|
| 2026-08-21 | 入力が黙って消える編集画面のバグを直し、文書と調査残骸を片付ける ／ 設計総点検の補完調査を完走し、11件を修正する |
| 2026-08-20 | ページテンプレートを実装する ／ ページテンプレートを設計する ／ 「w-cms が見る要素」の視覚ヒントを広げる ／ バグ探しで3件見つけて直す ／ ページ削除（ゴミ箱への移動）を実装する ／ ページ内アンカーを描画時に合成する ／ 本文に `id` を書けるようにする（殻の id を `w-` で隔離） ／ 受注・進捗・添付の設計を詰める ／ `data-type` を「型名」と定め `data-field` を撤去する ／ 編集モードで計算ビューの中身が消える退行を直す ／ 旧カスタム要素の最終掃除 |
| 2026-08-19 | 語彙モデルの縦切り第4段（計算ビューSSR・Web Components全廃・CSP strict） ／ 旧形式読み取りの短期保険を除去する ／ 縦切り第3段（受発注・見積・容器の移行） ／ スマホ幅の表示崩れを修正する ／ 縦切り第2段（`m-tag`・`m-material` の移行） ／ 文書の総点検 ／ E2E環境を自宅にも再現し、dl項目追加のバグを修正する ／ 監査で残っていた3点を塞ぐ ／ 汎用表エディタへ列操作・dl項目操作・型検証・enum入力補助を追加する |
| 2026-08-17 | 語彙モデルの縦切り第1段を実装する ／ **Web Components の廃止を決定する** ／ サニタイザを htmldoc パッケージへ切り出し、循環参照を解消する |
| 2026-08-16 | 文書を刷新し、タグ設計を語彙モデルへ統合する ／ 語彙モデルの決定を各正本へ反映し、CSPの残作業を再計画する ／ **【考察】語彙モデル.md を新設し、根本判断を決定する** ／ 要件定義書を語彙モデル（3層）で書き直す ／ **タグ設計を「標準HTML＋`data-*` マーカー」路線へ転換する** ／ 設定の渡し方を「環境変数のみ」と決定する ／ 要件の問題点を洗い出し、5つの決定を反映する ／ 前提の更新：カスタムタグ100種規模・一般公開ウェブサイト用途 ／ 要件定義書を新設する |

### アーカイブへ移した期間（→ [変更履歴アーカイブ.md](変更履歴アーカイブ.md)）

引くときの当たり。**判断の根拠が本文にしか無いもの**は行を厚くしてあります。

**2026-08-02〜08-06（Web Components 期の後半・現行体制の直前）**

| 日付 | 節と、覚えておく価値のある判断 |
|---|---|
| 08-06 | **サーバー側の構造を分ける（`handler.go` 分割・`page` / `editlock` パッケージ）**。依存の向きを規約でなく**型**で守る形へ。サニタイザの切り出しは `cms → htmldoc → cms` の循環参照になるため見送り、(a) レジストリごと `plugin` パッケージへ出す／(b) 依存を反転させる の2案を残した（→ (b) を 2026-08-17 に実施） |
| 08-06 | **殻からインラインを外へ出す**（CSP strict の前提。`index.html` 3015行→215行）。`on*=` 全廃の途中で、`onclick="resetPw('${esc(u.username)}')"` と**文字列でJSを組み立てて**いたため属性注入が成立していたことが判明——現在の勘所「文字列を組み立てて `innerHTML` へ入れない」の出所の1つ |
| 08-05 | **添付アップロードの脆弱性を修正（権限昇格・保存型XSS）**。`/api/upload-pdf` が任意の名前・種類を受けていた。`<id>.meta.json` を上書きすれば**権限の正本を差し替え**られ（所有者の奪取・匿名公開の有効化）、`evil.html` を置けば `DataFileHandler` が**同一オリジンのHTMLとして**返した。入口（名前正規化・`.pdf` 限定・中身が `%PDF-`・32MiB上限）と出口（本文とサイドカーは配信しない・`nosniff`・PDF以外は `attachment`）の二重で塞いだ。**画像アップロードを実装するときの前提になる節** |
| 08-05 | カスタム要素を Light DOM へ統一（Shadow DOM の残骸除去）。`getRootNode().host` が Light DOM では `undefined` になり、タグ値・明細・削除ボタンの編集が全滅していた。機構ごと撤去済み（2026-08-19〜20） |
| 08-04 | **テンプレート展開の保存型XSS修正**。`escapeAttr` が `<m-file>` にしか当たっておらず、実ブラウザでスクリプトが**発火した**。教訓は「**サニタイズとCSPの二層があっても、描画側のエスケープは別途必要**」——のちにこの `escapeAttr` 頼みの機構ごと廃し、現在は `createElement`＋`textContent`。同節にドキュメント整理（24本→22本）も含む |
| 08-04 | **許可タグ・属性の拡張と、エディタ語彙のサーバー一元化**。「タグは寛容・属性は厳格」。**URLの規則を2種類に分けた**（利用者がクリックする `href`/`cite` は外部可、閲覧するだけで取得される `src`/`srcset`/`poster` は**相対のみ**＝トラッキングビーコン防止）、プロトコル相対URL の穴を塞ぎ、`document.execCommand` を廃止。決定の正本は [本文サニタイズ設計.md](本文サニタイズ設計.md) §5 |
| 08-02 | **編集中のアンドゥ・リドゥ**。標準 undo は `execCommand` とネイティブ入力しか捕捉せず、プログラム的DOM操作を戻せないため独自スタック。粒度は保存単位、キャレットは `data-id`＋文字オフセットで復元、**編集権を失ったら履歴を破棄**（戻せない状態へアンドゥできてしまうため） |
| 08-02 | **ブロック単位保存**。`data-id` を**任意**の属性としフォールバックを残した（「正本HTMLは人が読めて手でも書ける」を保つため）。`id` にしなかったのは、本文から殻の要素を乗っ取れてしまうから |
| 08-02 | カスタムタグをプラグイン所有にし（`Tags()`）、`<m-file>` を純粋なファイル容器にした。前者は語彙モデル移行で撤去済み、**後者（容器と中身の分離）は現在も生きている** |
| 08-02 | **本文のサーバー合成と二層サニタイズ、`/upload` 廃止**。`GET /{id}` を完成HTMLの返却へ変えたことで**サニタイズが不可分**になった（従来は `DOMParser` 経由で「たまたま」安全だった）。エコーバック方式と「**閲覧＝サニタイズ済み／編集＝生**」の非対称はここが出所 |
| 08-02 | `page_tags` の主キーを廃止し同名タグを許可（多値属性が正当な使い方なのに、2つ置くと保存が500になっていた） |

**2026-06-20〜06-25（初期構築期）**

| 日付 | 節と、覚えておく価値のある判断 |
|---|---|
| 06-20 | **プラグイン機構の導入**。**「なぜ」がアーカイブ本文にしか無い唯一の節**——ユースケース追加が6ファイル13箇所の散弾銃手術で、実際に `tablesToDelete` の二重管理が不整合を起こしていた／Go の `plugin`（`.so`）は Windows 非対応で不採用、`database/sql` 流のレジストリ＋インターフェース方式を採った／**集計ロジックだけは宣言化できない**ので2階層に分けた。必要になったら原文を読むこと |
| 06-20 | 認証基盤（フェーズ1）・認可（フェーズ2）・管理機能（フェーズ3）。argon2id／サーバ側セッション／Unix風 owner-group-other＋mode／監査ログ。設計と現行仕様の正本は [認証認可設計.md](認証認可設計.md) |
| 06-20 | DB全再構築を全テーブルDROP方式へ＋起動時の自動リビルド。「`data/master` が正本、`cms.db` は使い捨ての派生インデックス」を実装に反映した節 |
| 06-20 | ページ情報欄の統合（`<m-page-info>` 新設）→ **翌日に撤回**（下の 06-21） |
| 06-21 | **ページ属性のサイドカー化と独立サイドパネル**。UNIX に倣い「内容＝HTML本体／属性＝サイドカー `<id>.meta.json`」へ分離。現行の `PageMeta` の出自で、保存APIのHTML属性注入・改竄復元が丸ごと不要になった |
| 06-21 | 同時編集の競合対策 Phase 1（ページ単位の悲観ロック・純HTTP）。合意の経緯は [【考察】同時編集の競合対策.md](【考察】同時編集の競合対策.md) |
| 06-22 | 同時編集ロックを SSE プッシュへ移行（Phase 2）。**presence を SSE 接続の有無で判定**しポーリングを廃止 |
| 06-22 | 編集ロックで変更操作（権限・所有者・親付け替え）を直列化 ／ 権限カードの編集を編集モード限定に ／ busyトーストを「再試行」から「待機をやめる」へ |
| 06-21〜25 | 左サイドパネルと左右レールの開閉 ／ ページ操作バーの廃止とレールの役割整理（左＝階層への操作／右＝このページへの操作） ／ UI設定を単一ルート `wcms.ui` へ階層化 ／ 狭幅時のドロワー化 ／ モバイルのヘッダーオーバーフロー修正 ／ ヘッダーの headroom 化とモード移行の緑トースト |
| 06-25 | 親なし（トップレベル）ページの作成・付け替えを**トップページのみ**に限定（トップレベル化は admin でも不可） |
| （日付なし） | 旧 `実装計画.md`／`実装完了報告.md` から取り込んだ3節（ドキュメント構成の整理・発注書への部品ネスト・Web Components テンプレートの外部ファイル化）。対象コードはいずれも撤去済み |

---

## 入力が黙って消える編集画面のバグを直し、文書と調査残骸を片付ける（2026-08-21）

同日の11件修正（次の節）に続けて、[判断待ち.md](判断待ち.md) への回答を実装へ落とした。

**黙って入力が消える編集画面のバグ2件**（`b31276b`）。どちらも画面には「編集モード」
「✅保存済」が出たまま**実際には入力が保存されない**、利用者が気づけない種類の欠陥。
E2E（`e2e/verify-editor-loss.js`）を先に書き、修正前に落ちること（編集できるブロック 3→0・
計算ビューの中身 1→0・状態表示は「✅保存済」のまま）を確認してから直した。

- **安全化された応答が返るたびにページ全体が編集不能になる**。`applySanitizedHtml` が
  `outerHTML` を直接比較していた。編集モードの `contenteditable` とサーバー事前描画のクローム
  （`.vocab-chrome`）は**現在のDOMにしか無い**ので、サーバーが返す保存形とは必ず不一致になり、
  安全化のたびに全ブロックが差し替えられていた。差し替え後の要素は保存形なので
  `contenteditable` を持たず、その場で打てなくなる（再読込か閲覧⇄編集の往復で復帰するため
  原因が分かりにくい）。比較を**両辺 `serializeBlock` に通す**形へ変えた——「保存される意味」で
  比べるので、編集用の属性もクロームも差に出ない。本当に差し替えたときは `applyMode` で
  モードを当て直し、計算ビューは**中身をサーバーが所有している**ので据え置く
  （差し替えると再読込までSSRの内容が戻らない）。
- **語彙を取り損ねると1文字も保存されないのに「保存済」と出る**。`loadTagSchema` が失敗時に
  `tagSchema = {}` を代入していたため、ガード `if (!tagSchema)` が素通りし、語彙が空＝
  全要素が保存対象外になっていた。`null` のままにしてガードを効かせ、見送りを
  `console.warn` で黙らせず「⚠ 保存できません」と消えない通知で知らせる（書きかけは失われるので、
  先にコピーするよう促す）。

**E2E を `/api/new-page` の POST 化へ追随させた**（`02e18ef`）。**E2E 5本が全滅していた**。
原因は保存型CSRF修正（`beb98a1`）で GET を落としたのに E2E 側が GET のままだったことで、
気づかれなかったのは誰も回していなかったため——つまり**あの11件の修正は E2E の裏づけを
得ていなかった**。ブラウザ遷移する4本には `gotoNewPage` ヘルパ（POST してから `Location` へ）を足し、
裏で作るだけの箇所は `request.post` ＋ `Origin` へ。修正後は6本すべて通過
（stage2 48・stage3 20・stage4 25・delete 9・template 15・editor-loss 11）。

**コードリファレンスを畳んだ**（`eda9b52`）。手書きの「現状の写像」は**実装と同じ速度で陳腐化する**。
`docs/コードリファレンス/` は行数主張30件中16件が誤りで、一部は内容が逆（修正済みの退行を
「現在の仕様」として説明）——**読んだ人が正しい実装をバグと誤認しかねない**状態だった。
必要になれば `/code-reference` でその時点のコードから作り直せる。**直し続けるより作り直すほうが安い**。
あわせて調査用の残骸（`_probe/`・`_bench/`・`zz_probe*_test.go`・`.claude/worktrees/` の移行前コード）を
消した。これが残っていると grep 調査が「カスタム要素は0種」「インライン style ゼロ」を偽と誤判定するので、
**AIの調査精度に直接効く**。`.vscode/` は追跡下へ置いた。

**位置づけの正本を [コンセプト.md](コンセプト.md) §0 に据えた**（`afcab41`）。ユーザーの一文
「そもそもがワンノートに検索や集計機能を付けたようなものです」から、文書が主でDBが副・
見える文字がすべて・未知の形式も保存を通す・`data-type` の改名を許す・名前で機能が変わる、
という既決がすべて導けることを表にした。**「ワンノートに無い制約は持ち込まない」という物差し**として
今後の判断に使える。あわせて2件を決定——**Gemini は有料枠で使う**（無料枠は入力が製品改善に
使われるため。コードからは判定できないので [【ガイド】デプロイ・運用.md](【ガイド】デプロイ・運用.md) へ
警告つきで記録した。書いていないと、いつか誰かが無料枠のキーを挿した時点で取引先の書類が学習に使われる）と、
**DB再構築はファイルから作り直す方式のまま**（100万ページで約1時間半の楽観値。1日以内で終わるなら
差分更新は当面不要）。

---

## 設計総点検の補完調査を完走し、11件を修正する（2026-08-21）

2026-08-20 の [【考察】設計総点検.md](【考察】設計総点検.md) は §4 に自分で限界を書いていた
——6領域のうち3領域がAPIエラーで取得できず、31件中5件しか反証検証していない、補完調査は
結果未回収。これを完走した。3領域（データモデル・安全性・エディタ）の地図を取り直し、
未検証項目を**16件の予定に対し全23件**、反証側に立って検証した。

**原文のまま通った指摘は1件も無かった**。検証済み5件がすべて「成立するが範囲の訂正が要る」
だった傾向が、残り全部でも再現している。判定が覆ったものが6件、範囲を絞るべきものが11件、
**新規に見つかったものが8件**。詳細は同文書 §5。

**修正した11件**（すべて「修正前に落ちるテスト」を先に書いてから）:

- **手配集計の認可越境**（`af5c032`）。`RequiredMaterials` が `part_materials` を `part_id`
  だけで引き、定義元ページの read を見ていなかった。`page_id` 列は**あるのに使っていなかった**。
  匿名漏洩より重かったのは指摘に無い方で、write を1つ持つ一般ユーザーが自分のページに任意の
  品番を1行置けば、読めない部品定義ページの**原価まで API から列挙できた**。判定を
  `page.CanView` へ集約し、`visibleChildren` も寄せた。
- **`section` の未知 `data-type` を告知**（`15495dc`）。業務文書ブロックの担い手が告知から
  漏れており、`<section data-type="cliet-order">` のような綴り違いが完全に無症状だった
  （③計算から受注が丸ごと消えるのに画面も告知も変わらない）。論点A に告知側だけが
  追随していなかった形。
- **添付アップロードの編集ロック**（`1324807`）。同名を無条件で上書きし、リビジョンも
  ゴミ箱も無いため、発注書PDFの証跡が復元不能に失われる経路だった。`parse-pdf` は
  永続状態を変えないので対象外が正しい。
- **保存型CSRF**（`beb98a1`）。サニタイザは相対URLの `<img src>` を通すので、本文へ
  `<img src="/api/logout">` を1つ保存するだけで、そのページを開いた全員を無音で
  ログアウトさせられた。`/api/new-page`・`/api/logout` を POST 限定にし、フロントは
  `window.open` の GET から form POST ＋ `target=_blank` へ。
- **部品番号タグの改名告知**（`3068d11`）。鍵をレジストリ宣言 `RequiresTag` へ移し、
  `plugin_materials.go` の日本語魔法文字列を消した。**総点検の私案「`tags` に列を1行足す」は
  採らなかった**——自由語なので担当者しか書いていないページでも鳴り、全ページで誤検知になる。
- **`parse-pdf` の名前検証**（`f6ca63c`）。本文と権限サイドカーを外部LLMへ送れた。
  置く側と同じ関門（`attachmentFileName`）を通して非対称を解消。
- **`parent_id` の喪失**（`bd7ee98`）。2段の経路で、サイドカーが読めないと1回の保存で
  ページがツリーから消え、DB再構築でも戻らなかった。単純な COALESCE にしないのは、
  admin によるトップレベルへの付け替えが黙って戻ってしまうため。
- **E2E をリポジトリへ**（`7312850`）。`e2e/` へ移設。`serializeChildren` の
  `.vocab-chrome` 除外は Go テストが無く E2E だけが守っているので、git の外にあるべきでは
  なかった。
- **リポジトリ衛生**（`ad7c7b3`）。追跡された18MBの実行ファイル（Linux ではなく Windows の
  PE32+ だった）と、ルート直下の2つ目の `package main` を片付けた。
- **ルート保護レベルの固定テスト**（`2580632`）。`main()` へ埋め込まれていたルート表を
  `buildHandler()` として切り出し、保護レベル・CSRF・メソッド・**CSPの配線**を固定した。
  `csp_test.go` はポリシー文字列しか見ておらず、`CSPProtect` を外しても通ってしまう。
  変異テストで、両方の退行が新テストだけに捕まることを確認済み。
- **`order_no` のページ内スコープ化**（`f2458a9`）。UNIQUE がページ横断で、同じ番号を使った
  後勝ちのページが先のページの受注を奪っていた（番号が空だと `''` 同士が衝突して全域で1件）。
  明細テーブルにも `page_id` を持たせ、洗い替えと集計をすべてページで閉じた。あわせて
  **起動時のスキーマずれ検出＋自動再構築**（`DriftedSchemaTables`）を入れた——`ApplySchema` は
  `CREATE TABLE IF NOT EXISTS` を流すだけで既存テーブルの定義変更を反映しないため、
  これが無いと「起動は成功して保存だけが 500」になる。

**残り**: ②テンプレート判定の h1 依存は、対処方針（サイドカー属性への昇格＋右レールの
トグル）をユーザーと決めたところで中断。未対処項目は同文書 §6 末尾。

> **2026-08-21 追記（この「残り」は決着した）**: ②テンプレート判定の h1 依存は、
> 上の対処方針を**着手前に取り消した**。ユーザーの判断「機能が名前で変わるのは目論見通りです」
> ——**表示されている言葉が機能を表す**のが本システムの方針であり、h1（ページ名）で
> テンプレート置き場が決まるのは仕様であって欠陥ではない、という結論。
> サイドカー属性 `template_root` への昇格も右レールのトグル追加も行わない。
> 決定と、受け入れたリスク（改名すると配下が②索引・③計算から出入りするが告知は出ない）の正本は
> [【考察】ページテンプレート.md](【考察】ページテンプレート.md) §9・§3.3。

---

## ページテンプレートを実装する（2026-08-20）

同日に設計した [【考察】ページテンプレート.md](【考察】ページテンプレート.md) を3段に分けて実装した。
設計時にコードを読んで見つけていた穴2つも、実装しながら塞いだ。

**第1段: テンプレート領域を②索引・③計算から除外**（[template.go](../internal/cms/template.go)）。
テンプレートに書いた仮の発注書が `client_orders` に入り、手配集計・利益計算へ出てくるのを防ぐ。
除外は**「飛ばす」ではなく「空の本文を渡す」**——全プラグインの `Sync` は冒頭で当該ページの行を
`DELETE` する洗い替えなので、空を渡せば「古い行は消える・新しい行は入らない」となり、
**普通のページをテンプレートへ移したときに古い行が残らない**（飛ばすと残る）。
`PurgePageIndex` と同じ流儀に揃った。

**穴①: サイドカーにタイトルが無かった。** 設計では「先祖辿りはサイドカーで」と書いたが、
`PageMeta` は owner/group/mode/public/parent_id/日時だけで、「その先祖が『テンプレート』という
名前か」が分からない。**先に親を見て「トップの直下」で弾く**ことにした——その条件を満たす先祖は
鎖の中にただ1つなので、本文HTMLをディスクから読むのは1回の辿りで高々1ページ分で済み、
順序非依存という肝心の性質も保たれる。

**穴②: 親の付け替えが索引を張り直していなかった。** `SetParentAPIHandler` は
`UPDATE pages SET parent_id` とサイドカー更新だけで `SyncIndex` を呼ばない。上記の
「空の本文を渡す」方式のおかげで、**張り直しさえすれば古い行は自動で消える**。
テストで往復（テンプレートへ入れる→出す）を固定した。

**第2段: コピーと新規化パス**（[template_new.go](../internal/cms/template_new.go)）。
`/api/new-page` に `template=` を足した。規則は「空欄は列型の既定値で埋める・書いてある値は
そのまま保つ」ただ1つ。`order_no` は UNIQUE 制約があるので再採番が必須で、番号は
`PO-<新ページID>` とした（エディタの時刻下6桁より衝突しにくく、由来ページも辿れる）。
検証は**採番より前**に行う——後に失敗するとファイルの無いページ行が `pages` に残るため。

**第3段: 一覧の読み口とメニュー**（[handler_template.go](../internal/cms/handler_template.go)・
`createSubpage`）。`GET /api/templates` がツリーを返し、枝は見出し・葉は選択肢として描く。
テンプレートが1つも無ければ従来どおり即作成。

### テストで見つけた `:memory:` の落とし穴

一覧のテストだけ**ファイルDB**を使う。`visibleChildren` は行を回しながら1件ずつ
`page.GetPerms` を引くが、入れ子のクエリはプールの**別接続**で走り、`:memory:` では
接続ごとに別の空DBになるため権限がフェイルクローズして子が1件も見えなくなる。
本番（`data/cms.db`）はファイルなので同じ問題は起きない。

検証: go test 全パス（除外を外すと第1段の3件が落ちることを確認）。
E2E は既存の stage2（48）／stage3（20）／stage4（25）／delete（9）に加え、
**新設の verify-template（15）** が実機で全通過。

---

## ページテンプレートを設計する（2026-08-20・設計のみ）

新規ページ作成時に、形式の決まった文書を雛形から起こしたい（ユーザー提起）。
考察を [【考察】ページテンプレート.md](【考察】ページテンプレート.md) へ新設した。**実装は未着手**。

到達点は「**テンプレートも普通のWikiページ**」で、置き場所と見分け方が主な論点だった。
当初はタイトル接頭辞 `【テンプレート】` を提案したが、**ユーザー案「テンプレート用フォルダの
子孫」へ差し替えた**——接頭辞はコピー先のタイトルへ漏れる（`【テンプレート】受注ページ` を
コピーすると同名になり、剥がす魔法か毎回の改名が要る）のに対し、フォルダ方式なら
**テンプレートのタイトルがそのままコピー先の正しいタイトル**になる。加えて分類が階層でタダで手に入り、
group/mode の継承（setgid 相当）がテンプレート共有をそのまま解く。

深さの扱いはさらにユーザーが絞り込み、**葉だけがテンプレート・枝は分類**に決まった。
「子孫はすべてテンプレート」だと分類の階層とテンプレートの階層が混ざるが、葉に限れば
**ページは必ずどちらか一方**になる。副産物として分類ページは誤ってコピーされ得なくなり、
その本文が「このカテゴリの使い方」の説明場所として空く（分類ページはコピーされないため）。

ルート自身は**トップの子でタイトルが「テンプレート」**という約束で見分ける。名前を持つのが
1ページだけなので接頭辞案の欠点が当てはまらず、スキーマもUIも増えない。

コード側で見つけた実装上の必須事項が2つ:

- **テンプレート配下はプラグイン同期から除外する。** `SyncIndex` は保存のたびに全プラグインの
  `Sync` を回すので、`client-order` を含むテンプレートを保存すると仮の発注書が `client_orders` に
  入り、手配集計・利益計算に出てくる。
- **その先祖辿りはサイドカーで行う。** `EffectivePublic` と同じく `pages` テーブルから
  `parent_id` を読むと**DB再構築で壊れる**——空のDBへ1ページずつ同期する過程で、先祖がまだ
  `pages` に無い順序だと「テンプレートではない」と誤判定し、静かに索引が汚れる。
  親の正本はサイドカーなので `ReadSidecar` なら順序に依存しない。
  なお**葉の判定はメニュー表示時（DBが揃っている場面）にしか要らない**ので、この罠は及ばない。

書き込み（日付・一意番号）は既存の `defaultFieldValue` と同じ「**空欄を列型の既定値で埋める**」
規則をサーバー側へ移す。`order_no` には UNIQUE 制約があるため再採番は必須で、
**テンプレート機能を作るなら新規化パスは省けない**。プレースホルダ（`{{今日}}`）と
対話的な入力フォームは v1 では作らない（新規ページは編集モードで開くので、ページ自体が入力フォーム）。

---

## 「w-cms が見る要素」の視覚ヒントを広げる（2026-08-20）

**DBに入るもの／入らないものが見た目で区別できる必要がある**（ユーザー指摘）。
既に `table`/`dl[data-type]` には編集モードの破線枠があったが、**`section[data-type]` が抜けていた**
——`section[data-type="client-order"]` は型付きテーブルへ直接入る最も重要な業務ブロックなのに
印が出ていなかった。ファイル容器・計算ビューも同様。

**印の規則は `data-type` の有無ただ1つ**とした。「w-cms がその要素を見て何かするなら `data-type` を
付ける」という決定（[【考察】語彙モデル.md](【考察】語彙モデル.md) §9）の当然の帰結で、
形式ブロック（DBへ入る）・ファイル容器（配線）・計算ビュー（表示専用）はどれも `data-type` を持ち、
素の文書は持たない。**基準を1つ決めておくと派生が自動的に決まる**、という例になった。

### ページタイトルの h1 に `data-type` を付ける案は見送った

DBに入る（`pages.title`）のに `data-type` を持たない唯一の例外なので、付ける案を検討したが**見送り**。

- タイトルは「**文書順で最初の h1**」という**位置**で決まる（[parser.go](../internal/cms/parser.go) の
  `ParseCore`）。属性にすると上に見出しを足した瞬間ずれ、並べ替え・挿入のたびに同期させる機構が要る
  ——**位置が既に持つ情報を属性が二重に持つ形で、撤去したばかりの `data-field` と同じ構造**になる。
- 値の意味も決まらない（`data-type="title"` は列も同期プラグインも骨格も持たず、
  レジストリの形式とは性質が違う）。既存ページには属性が無いので抽出規則は「最初の h1」のまま残り、
  **正本が2つ**になる。

かわりに**実行時クラス**（`is-page-title`）で印だけ付け、位置から毎回計算し直す。
class はサニタイザもシリアライザも通さないので保存に漏れない（`.cell-invalid` と同じ流儀）。
計算し直す契機は目次（`buildToc`）とまったく同じなのでそこへ相乗りさせた。

なお「タイトルを書き手が明示的に選べるべきか」も検討したが、**最初の h1 でよい**と決まった
（選べるようにするなら属性が正しい機構になるが、それは印の話ではなく仕様変更）。

検証: go test 全パス。E2E stage2（48・タイトルの印3件を追加。**上に見出しを足すと印が移る**＝
属性ならずれる箇所を固定）／stage3（20・`section` の枠2件を追加）／stage4（25）／delete（9）全通過。

---

## バグ探しで3件見つけて直す（2026-08-20）

### ① `GET /api/load` がサニタイズを通っていなかった（安全性）

この応答は `populateEditor` が**属性を濾さずDOMへ入れる**（トップレベルの `<script>` を飛ばすだけ）。
そのため、保存経路を通っていない本文——手動配置・バックアップ復元・**将来の取り込みAPIが直接
書いたページ**——に仕込まれた `id="w-…"` が**殻の要素を乗っ取れた**。`getElementById` は文書順で
最初を返し、本文の挿入点より後ろに権限UIの入力欄（`pp-mode`・`pp-owner-input` 等）が並ぶので、
管理者がそのページを開いて保存すると意図しない権限が送信され得る。

**実機で再現して確認した**——描画経路（`RootHandler`）は接頭辞を剥がして `id="html-preview"` に
していたが、`/api/load` は `id="w-html-preview"` のまま返し、編集モードでの
`getElementById('w-html-preview')` が殻の `<textarea>` ではなく本文の `<p>` を返した。

`id` を全面拒否していた時代からある穴（当時は `id="html-preview"` で同じことが起きた）。
描画時と同じく `Sanitize` を通して塞ぎ、**「正本をそのまま返す」というコメントの方を改めた**。
ページ内アンカーの合成は引き続きこの経路では行わない（合成 id が本文として保存されるため）。

### ② 計算ビューのクローム見出しが目次に載る（表示）

`view_render.go` がサーバー側で埋める `<h3>📊 部材手配・発注進捗状況</h3>` は `.vocab-chrome` の
中身＝本文ではないのに、`buildToc` が除外せず拾って目次に並べていた（移行第4段からの既存バグ）。
あわせて `RenderAnchors` もクロームへアンカーを合成していた——保存はされないが、
**本文の見出しが使いたいアンカー名を先に消費する**。両方ともクロームを除外した。
本文の `class` はサニタイザが必ず落とすので、`vocab-chrome` の判定は誤爆しない。

### ③ フォルダが失われたページを削除できない（軽微）

`moveToTrash` が stat 失敗で 500 を返し、DB行が残って画面から永久に消せなかった。
フォルダが無いなら索引の掃除だけ進めるようにした。

検証: go test 全パス（再現テスト3件を先に追加）。E2E stage2（45）／stage3（18）／
stage4（25・目次とアンカーの除外2件を新規追加）／delete（9）全通過。

---

## ページ削除（ゴミ箱への移動）を実装する（2026-08-20）

[【考察】通信記録処理.md](【考察】通信記録処理.md) §2.7④「自動判定してよい。ただし常に
取り消せること」の**前提**として必要だったが、**登録ルートに削除に相当するものが1つも無かった**。
`data/master/<prefix>/<id>/` を手で消して「DB再構築」する以外に手段がなく、管理者権限と
サーバーのファイルシステムへのアクセスが要るため通常運用では取り消しができない状態だった。

- **物理削除ではなく `data/trash` への移動**。削除自体も取り返しがつくようにする（「常に柔軟性」）。
  DB再構築は `data/master` だけを走査するので、移すだけで索引からも消える。
  同じIDが既にゴミ箱にあるとき（削除→復元→再削除）は連番を付けて上書きを避ける。
- 守りは3つ——**トップページは不可**（親を持てない唯一のページで、消すと復旧導線が無くなる）／
  **子ページを持つページは不可**（再帰削除は「取り消せる範囲」を一気に広げるので採らず、
  409＋件数で先に移すか消すよう促す）／**編集ロックが要る**（chmod・chown・set-parent と同じ直列化）。
- `PurgePageIndex`——プラグインの `Sync` はどれも「`page_id` の行を削除してから入れ直す」形なので、
  **空の本文で同期を走らせる**と型付きテーブルが洗い流される。そのあとコアテーブルを消す
  （`SyncIndex` が `pages` を upsert するため、順序はこの通りでなければならない）。
- UIは左レール最下部。**編集モードでトップページ以外のときだけ**表示し、色を分けて confirm を挟む。
  削除後は親（無ければトップ）へ遷移する。

**やらなかったこと**: ゴミ箱からの**復元UI**（戻すには手でフォルダを `data/master` へ移して
DB再構築する）。ゴミ箱の自動整理（保持期間）。§4.4 の「N件から引用されています」警告は
`page_refs` の実装後。

検証: go test 全パス（ゴミ箱への移動・トップページ拒否・子あり409・他者ロック中409）。
E2E: `verify-delete.js` を新設（9項目）。stage2（45）／stage3（18）／stage4（23）も全通過。

---

## ページ内アンカーを描画時に合成する（2026-08-20）

本文に `id` を書けるようにしたのに続き、**書き手が何も書かなくてもリンクできる**ようにした。
既存ページも遡ってアンカーを持つ。設計の正本は [本文サニタイズ設計.md](本文サニタイズ設計.md) 5.3。

- `RenderAnchors`（[anchor.go](../internal/cms/anchor.go)）——見出しは表示文字から、
  それ以外のブロックはブロックID（`data-id`）から `id` を合成する。
  **書き手が付けた `id` は上書きしない**（先に既存 id を集め、ぶつかる合成は避ける。
  重複は `-2`・`-3` の連番）。空白は `-` へ寄せ、リンクを壊す文字を落とし、殻の接頭辞は剥がす。
  日本語はそのまま残す。**冪等**（既に id がある要素は素通り）。
- **呼ぶのは `RootHandler`（ページ描画）だけ**で `GET /api/load` は通さない。
  通すと合成した id がエディタのDOMへ入り、シリアライザが本文として保存してしまう
  ——`id` は語彙に含まれる属性なので、計算ビューの `.vocab-chrome` のようには飛ばされない。
  編集モードへ入るときは `/api/load` で載せ替えるので、保存形式は合成前のまま保たれる。

**やらなかったこと**: 目次（`buildToc`）は編集モードでも動く必要があるため `scrollIntoView` の
まま。書き手向けの「この見出しへのリンクをコピー」UIも未実装。

検証: go test 全パス（合成規則9件＋冪等性）。E2E stage4（23・合成と保存への非漏洩4件を新規追加）／
stage2（45）／stage3（18）全通過。

---

## 本文に `id` を書けるようにする——殻の id を接頭辞 `w-` で隔離する（2026-08-20）

**規律の向きを反転させた。** 「本文の書き手が接頭辞を守る」のではなく、**殻の側が接頭辞を独占する**。
守るべき対象が無数の書き手から1つの殻へ移り、本文でページ内リンクが普通に書けるようになった。
設計の正本は [本文サニタイズ設計.md](本文サニタイズ設計.md) 5.3。

### なぜ `id` を拒否していたのか（2026-08-02 の判断）

本文は殻 `assets/index.html` と同じDOMへ合成され、id の名前空間を共有する。
`getElementById` は文書順で最初の要素を返し、**本文の挿入点より後ろに権限UIの入力欄が並ぶ**
（`pi-parent-input`・`pp-owner-input`・`pp-group`・`pp-mode`・`pp-public`）。したがって本文に
`<input id="pp-mode" value="777">` を仕込むと、**そのページを開いた人の権限パネルが
攻撃者の値を読む**——管理者が開いて保存を押せば意図しない権限が送信される。

### 何をしたか

- 殻の**62個の id を `w-` つきへ一括改名**（`index.html`／`app.js`／`app.css`／E2E 3本）。
  本文が合成されるのは `index.html` だけなので `admin.html` は対象外。
- **ガードテスト** `TestShellIDsAreNamespaced`——接頭辞なしの id を殻へ足すと落ちる。
  規律をコードで固定した（CSPの後戻りを `csp_test.go` が検出しているのと同じ流儀）。
- サニタイザは `id` を全要素で許可し、**殻の接頭辞だけを剥がして**通す（拒否ではなく告知）。
  剥がすのは**無くなるまで繰り返す**——1回だけだと `w-w-x` で冪等性が崩れ、
  保存時エコーバックが収束しない。剥がした結果が空なら属性ごと落とす。
- 剥がしたことは保存応答の **`stripped_ids`** で告知し、エディタが警告を出す。
- `AllowedVocabulary` へ全要素共通の属性（`data-id`・`id`）を含めた。含めないと
  「サーバーは通すのにエディタのシリアライザが落とす」非対称ができ、保存のたびに id が消える。
  あわせて `serializeBlock` の `data-id` 二重出力を解消した。

### 副産物：保存状態の退行を1件発見・修正

サーバーが本文を書き換えて返す（`sanitized`）と、`applySanitizedHtml` のDOM変更を
MutationObserver が拾って「未保存」を立て、その後のデバウンス保存が `decideSaveStrategy` の
'none' で早期returnするため**表示が「未保存」のまま戻らなかった**。データは保存されているのに
未保存に見える。通常の保存で `sanitized` が真になる経路が実質無かったため表面化していなかったが、
今回「接頭辞を剥がす」で日常的に踏むようになり、E2E が検出した。

### `data-id` は廃止しない（検討して見送り）

「`id` が使えるなら `data-id` を廃止できないか」を検討し、**残す**と決めた。2つは
**別の情報**（保存の単位／リンクの宛先）を運ぶので、統合は冗長性の除去でなく概念の混同になる。
統合するとアンカー名の変更でブロック同一性が壊れ、`id` の一意制約により人が付けた名前を
`ensureBlockIds` が振り直し、貼り付けた `id` がブロック識別子として解釈される。
理由の詳細は [本文サニタイズ設計.md](本文サニタイズ設計.md) 5.3 の注記、
決定ログは [【考察】語彙モデル.md](【考察】語彙モデル.md) §9。

### 残る課題

書き手が `id` を付けていないページにはアンカーが無く、目次のページ内移動も
JSの `scrollIntoView` のままなので**公開ページではアンカーが効かない**。
次の一手は「**保存形式を変えず、描画時にサーバーが `id` を合成する**」
（見出しとブロックへ自動付与。既存の全ページが遡って恩恵を受ける）。

検証: go test 全パス。E2E stage2（45・id の挙動4件を新規追加）／stage3（18）／stage4（19）全通過。

---

## 受注・進捗・添付の設計を詰める（2026-08-20・設計のみ）

「1つの受注が複数の発注書から出来る」というユーザーの指摘を起点に、通信記録の取り込み周りの
設計を詰めた。**コードの変更は無く、文書のみ**。決定の正本は
[【考察】通信記録処理.md](【考察】通信記録処理.md) §2.7（新設）。

### 決定5件

1. **添付の実体はすべて通信記録ページのフォルダ**——発注書PDF・図面・ZIP・`.eml` 原本を問わない。
   文書ページは実体を持たずアドレスで指す。§9.1 の「自己完結性＝文書ページが実ファイルを所有」を改定。
2. **変更発注書は「追記」**——既存の明細を書き換えず、受注ブロックをページに足す。
   **人が付けた進捗が失われない**のが理由で、マージUI・差分適用UIは不要。
   **帰結: ファイル容器の入れ子は維持**（1受注ブロック＝1発注書PDF が保たれる）。
   検討途中で「入れ子をやめる」と判断したが誤読によるもので撤回した。
3. **進捗マークの正本は受注ページの `status`**——計算ビューの中身は保存されない（`.vocab-chrome`）ので
   進捗ページには書き込めない。進捗ページは表示に徹する。
4. **自動判定してよい。ただし常に取り消せること**——要件は精度ではなく可逆性。
   通信記録が不変アーカイブだから何度でもやり直せる、というのが根拠。
5. **`data-src` は汎用の「出所アドレス」**——`000200` ／ `000200#<ブロックID>` ／ `000200/<ファイル名>`。
   解釈は `data-type`（型）が決める。**進捗ページのソースはファイルでなく受注ページにしかなりえない**
   ことが一般化の動機。サニタイザは変更不要。決定ログは
   [【考察】語彙モデル.md](【考察】語彙モデル.md) §9 にも記載。

### 判明した欠落

- **ページ削除の機能が無い**——登録ルートを確認したところ、削除に相当するものが1つも無い。
  いまは `data/master/<prefix>/<id>/` を手で消して「DB再構築」する以外に手段がなく、
  通常運用では取り消しができない。決定4の前提なので、独立した作業項目として立てた。
- **保存済み文書のリバートが要件へ格上げ**——決定2のブロック追記を取り消す手段が
  ページ削除では代替できない（消すと元の受注と進捗まで失われる）。

### ページ内アンカーの再考察

`id` を落としているため `#fragment` が効かず、目次すらJSの `scrollIntoView` に頼っている
（公開ページでは機能しない）。`id` を許可へ戻す案は採らない——本文の挿入点
（`assets/index.html` 151行目）より**後ろ**に権限UIの入力欄があり
（`pi-parent-input` 173／`pp-owner-input` 197／`pp-group` 202／`pp-mode` 203／`pp-public` 215）、
`getElementById` が文書順で最初を返すため、本文に仕込んだ偽の入力欄が**必ず勝つ**。
管理者がそのページを開いて保存すると意図しない権限が送信される。

採る案は「**保存形式に `id` を入れず、描画時にサーバーが合成する**」
（`id="h-<見出し>"`／`id="b-<ブロックID>"`、`href="#発注書A"` も同時に変換）。
接頭辞の名前空間をサーバーが独占するので衝突が原理的に起きず、拒否リストは不変のまま。
既存の全ページが遡ってアンカーを持ち、目次も標準リンクへ乗り換えられる。
詳細は [本文サニタイズ設計.md](本文サニタイズ設計.md) 5.3。

---

## `data-type` を「型名」と定め、`data-field`（機械キー）を撤去する（2026-08-20）

ユーザー提案「w-cms が機能を提供するタグ全般に `data-type` を付けてはどうか（プログラミングの
型名のようなもの）」を受けた設計判断と、その延長で見つかった `data-field` の冗長性の解消。
決定の正本は [【考察】語彙モデル.md](【考察】語彙モデル.md) §9（新規2行）。

### 決定1: `data-type` は型名

**w-cms がその要素を見て何かする（DB保存・描画・配線・入力補助）なら `data-type` を付ける。
何もしないなら付けない。** `data-type` が型名で、他の `data-*`（`data-src`）はその型のフィールド。
`th[data-type]`（列型 text/number/date/enum/image）は**プリミティブ型**で、レジストリの
形式名（複合型）と同じ型宇宙に属する——という読み方で既存のマーカー体系が全部説明できる。

サニタイザの許可は**要素限定のまま**とし、機能を足すときにその要素へ都度広げる
（規約と実装のギャップがそのまま未実装の可視化になる）。この決定は、移行で唯一残った
**参照マーカー**（`受信元`・`前版`）に `<a data-type="…" href>` という素直な置き場を与える
——`dl` は「見える名前:値」の形式なので配線を移せず行き詰まっていた。

### 決定2: 項目の鍵は見出しの表示文字ただ1つ（`data-field` の撤去）

鍵の解決は元々 `columnFor`（`Field` でも `Label` でも引ける）を通っており、`data-field` は
**すでに冗長**だった（`inspection-record` は1つも持たずに動いていた）。唯一の仕事は
「見出しの改名耐性」だけだったので、それを告知へ移して属性ごと撤去した。

- **告知を先に追加**（`UnresolvedVocabFields`）——保存応答の `unresolved_fields` と
  エディタの警告トースト。報告するのは**機械キーを持つ列だけ**で、かつ**改名の徴候**
  （解決されなかった宣言列がある **かつ** どの宣言列にも当たらない見出しがある）が
  あるときに限る。列の削除だけ・独自列の追加だけでは黙る。仕様は
  [【一覧】API.md](【一覧】API.md) 2章の注記。
- **撤去**——サニタイザの許可リストから外し（`dd` の許可属性は**空**になった）、
  ②汎用索引・`VocabDLFields`／`VocabTableRows`・部材表・可変タグの読み取りからも
  分岐を削除。**属性が残っていても読まない**ので、サニタイズを通った本文と通っていない
  本文で `Sync` の結果が変わらなくなった（保存経路の有無に結果が依存しない）。
- **エディタ**——骨格生成が書き出すのをやめ、AI明細の列対応と列型の解決はレジストリ経由の
  共通ヘルパ `findVocabColumn`（サーバーの `columnFor` と同じ規則）へ。編集モードの
  機械属性ハイライトは `th[data-type]`（型の明示）だけになった。
- `VocabDef.Field` は**残す**——③計算プラグインが使う内部の機械キーで、本文には現れない。

**本文のマーカーは `data-type`（型名）と `data-src`（配線）だけ**になり、
「見える文字がすべて」（語彙モデル §2）が鍵について例外なく成立した。

**副作用**: ②汎用索引の鍵が機械キーから表示文字へ変わる（`order-no`→`発注書番号`）。
`vocab_index` を読むコードはまだ無く、形式ごとにばらついていた鍵（`inspection-record` は
元から表示文字）が揃うため、今のうちに変えた。

**検証**: go test 全パス。**実データ68ページを `UnresolvedVocabFields` で走査して未解決ゼロ**
（撤去で壊れるページが無いことを確認）。E2E は stage2（41）／stage3（18・「見出しの改名を
保存時に告知する」を新規追加）／stage4（19）全通過。

**文書**: 語彙モデル §2・§4・§5.1・§5.3・§8.2・§9・§10、本文サニタイズ設計 §5、
【一覧】語彙 §1.1・§2.2・§2.3、【一覧】API 2章、要件定義書 §2.5、アーキテクチャとDBスキーマ、
エディタ仕様、コンセプト、ユースケース2種、プラグイン開発ガイド、通信記録処理、用語集、
コードリファレンス3種、README を追随。

---

## 編集モードで計算ビューの中身が消える退行を直す（2026-08-20）

移行第4段で計算ビューをサーバー事前描画へ移したとき、`/api/load` には
`RenderComputedViews` を通したが、**`POST /api/lock` の応答に同梱していた生HTML**を
見落としていた。編集モードへ入るとフロントはそちらで本文を載せ替えるため、
子ページ一覧・手配集計の中身が消えていた（旧 `<m-child-list>` は編集モードでも
自分で描いていたので**退行**にあたる）。文書整備の過程で発見。

- **ロック応答から本文の同梱をやめた**。フロントはロック取得後に `GET /api/load`
  （＝SSR を通る経路）を読む。ロック保持中は他者が保存できないので、2手に分けても
  内容は変わらない。
- 副産物として `editlock` パッケージが**ページ本文をファイルから読む責務を手放した**
  （`readPageHTML` 削除）。依存の向き `cms → editlock` のままでは editlock から
  SSR を呼べないという制約も、本文の取得口を `/api/load` の1つへ寄せることで解消。
- E2E に「編集モードでもSSRの中身が残る」検査を追加（退行の固定）。

**あわせて最終掃除の消し残しを整理**（いずれも文書整備で検出）:

- `Tags()` 撤去後の古いコメント7箇所（`main.go`・`handler_meta.go`・`vocab_index.go`・
  `plugin.go`・`database/sqlite.go`・`app.js`）。`vocab_index.go` は**削除した
  メソッドの説明が文の途中で切れたまま `Sync` の doc コメントとして残っていた**。
- `app.js` のデッドコード——カスタム要素のシリアライズ経路
  （`serializeCustomElement`・`customSelector`・`CUSTOM_ATTR_DEFAULTS`）と、発注書要素に
  「＋ 部品を追加」を出す到達不能な分岐。未知のハイフン要素は汎用シリアライザが
  アンラップする（サーバーのサニタイザと同じ扱い）ので、経路は1本に統合できる。
  `isCustomTag` は貼り付け対策の防御として残した。
- `htmldoc` のマーカー許可範囲のコメント（`section`・`data-src` の追加漏れ）。

---

## 旧カスタム要素の最終掃除——語彙宣言・変換ツールを撤去する（2026-08-20）

両環境の一括変換完了（職場 2026-08-19・自宅 2026-08-20）を確認し、移行のために
残していた足場をすべて撤去した。**本文の語彙は「htmldoc の構造HTML＋`data-*`
マーカー」の1箇所だけ**になり、語彙モデルへの移行（§8.4）は後始末まで完了した。

- **語彙宣言の機構ごと撤去**——Plugin の必須メソッド `Tags()`・`PluginTags()`・
  `TagSpec`・`htmldoc.New(custom)` の注入引数。全プラグインが nil を返すだけになり、
  受け皿が「常に空」になったため機構ごと外した。新しい形式は①語彙レジストリ
  （[vocab.go](../internal/cms/vocab.go)）へ `data-type` として足す
  （**カスタム要素は作らない**・2026-08-17 決定）。
- **空になったプラグイン2本を削除**——`plugin_file.go`・`plugin_child_list.go`
  （語彙宣言だけが実体だった）。プラグインは「計算ユースケース」だけになった。
- **一括変換ツールを撤去**——`migrate_vocab.go`・`POST /api/migrate-vocab`・
  管理コンソールのボタン。**語彙宣言を外すと旧要素はサニタイズ時点でアンラップ
  される**ため、変換ツールだけ残しても機能しない（同時撤去が必然）。
  未変換のデータが後から出てきた場合は、この版より前のバイナリで変換するか、
  手作業で書き換える（中身のテキストはアンラップで残るので文章は失われない）。
- **副産物: 初期化順序のねじれが解消**——語彙注入が消え、[sanitize.go](../internal/cms/sanitize.go)
  の遅延初期化（`sync.Once` ＋「全プラグインの `init()` 登録を待つ」暗黙の順序依存。
  2026-08-17 の htmldoc 切り出し時に残した課題）が不要になり、パッケージ変数1本になった。
- **テスト**——機構のテスト（`plugin_tags_test.go`）は**到達状態のテスト**へ差し替え
  （`vocabulary_test.go`＝カスタム要素ゼロ・マーカー属性の維持を固定）。旧要素を
  題材にしていた5本は新形式の本文へ書き換え、旧要素がアンラップされること
  （中身のテキストは残る）を `sanitize_test.go` で固定。
- **E2E**——`verify-migration2.js` を廃棄、stage3/stage4 から変換の節を削除。
  README に現行3本と落とし穴（`settleSaved` 等）を記載。

**検証**: `go build`／`go vet`／`go test ./...` 全パス。E2E は stage2(41)・
stage3(16)・stage4(18) 全通過。

---

## 語彙モデルの縦切り第4段（計算ビューSSR・Web Components全廃・CSP strict）を実装する（2026-08-19）

移行の最終段（語彙モデル §8.4-4）。最後のカスタム要素2種を標準HTMLの
ビューマーカーへ移行し、**Web Components 機構をファイルごと撤去**、
**CSP の strict 化（`'unsafe-inline'` 除去）を完了**した。
計算ビューの中身は**サーバー事前描画**で埋める（ユーザー決定。持ち越しの
再質問7に回答を得た。閲覧ゼロJS・公開ページのSEO要件と整合）。

- **ビューマーカー**: `<m-child-list>`→`<section data-type="child-list">`・
  `<m-required-materials>`→`<section data-type="required-materials">`。
  本文に保存されるのは**空のマーカーだけ**。
- **SSR**: [view_render.go](../internal/cms/view_render.go) 新設。RootHandler と
  `/api/load` がマーカーへ `<div class="vocab-chrome" contenteditable="false">` と
  して中身を埋める。シリアライザが `.vocab-chrome` を保存しない既存規則
  （第3段のエンハンサと同じ）に相乗りし、保存往復でマーカーだけが残る。
  子ページ一覧は `/api/children` と同じ閲覧者絞り込み（`visibleChildren` へ切り出し・
  匿名は実効公開のみ）、手配集計は `/api/required-materials` と同じ集計
  （`RequiredMaterials` へ切り出し・部材名順で安定化）を共用。
  編集中の変化（子ページ作成など）の表示反映は**再読み込み**（描画ロジックを
  GoとJSの2箇所に持たない割り切り）。挿入直後の空マーカーには CSS `::before` の案内。
- **撤去**: `web-components.js`（660行）・`assets/templates/` 12ファイル
  （インライン style= 75個＋生成8行の発生源ごと消滅）・`components.css`。
  エディタからカスタム要素の再描画ループ・`window.currentPageId`・旧2項目の
  手書きメニューを削除。
- **CSP strict 化**: `script-src 'self'; style-src 'self'`（`'unsafe-inline'` 撤去）。
  FOUC 防止スクリプトは **`/assets/boot.js` へ外部化**（従来方針の per-request
  nonce から変更——head の同期外部スクリプトで同じ順序が保て、nonce 機構が不要で
  静的配信とも両立するため）。ログイン画面の `<style>` も `/assets/login.css` へ外部化。
- **一括変換**: 旧2要素→空マーカー（`data-id` 引き継ぎ・`page-id` 属性は破棄）。
- **検証**: go test 全パス（SSR・変換・CSP strict 固定の新テスト6本）。
  E2E は新設 verify-stage4.js（20項目）＋回帰 41+16+19 全通過。
  **E2Eが実バグ1件を検出→修正**——挿入直後のビューマーカーが contenteditable に
  なる（applyMode 側だけ除外し挿入経路の除外が漏れていた）。
  ほかに **verify-stage3.js のテスト側レース2件を修正**——①再読込後の検査が
  populateEditor の再構築を待たない ②直前に飛んでいた保存の応答が「保存済」を
  上書きし、注入分のデバウンス保存（1.5秒）の完了前に遷移する（`settleSaved`
  ＝2周目を掴む待ち方に統一）。

**残る後始末**（機能上は第4段で移行完了）: 旧 `m-*` の語彙宣言 `Tags()`・
変換ツール・旧要素E2E（migration2 等）の撤去は、**自宅環境の一括変換完了後**に
最終掃除としてまとめて行う。参照系（受信元・前版）は通信記録処理の設計後。

---

## 旧形式読み取りの短期保険を除去する（2026-08-19）

移行第2段・第3段で「変換ツールが安定するまでの短期の保険」として残していた
旧カスタム要素の読み取りを除去した（語彙モデル §8.3——恒久両対応は語彙の
単一正本に反する）。除去に先立ち、職場ローカルの実データを一括変換で確認
（変換対象0＝変換済み。残存は意図的除外の旧「親ページID」タグと
第4段対象の表示専用要素のみ）。

- **除去した読み取り**: Sync の旧要素分岐（`<m-tag>`・`<m-material>`・
  `<m-client-order>`/`<m-item>`・`<m-supplier-order>`/`<m-item>`・
  `<m-our-estimate>`・`<m-supplier-estimate>`）、`TagValue` の `<m-tag>`、
  `ClosestFileSrc` の `<m-file src>`。旧分岐専用の補助関数
  `ClosestAttr`・`Quantity` も削除。
- **残したもの**: 語彙宣言 `Tags()`（サニタイザが旧要素を剥がさないため）・
  エディタの旧要素表示・一括変換ツール（`POST /api/migrate-vocab`）。
  いずれも第4段（web-components.js／templates/ 撤去）まで。
  **未変換の環境（自宅・実運用）は、この版へ更新したらまず管理コンソールの
  「語彙モデルへの一括変換」を実行すること**（実行までの間、旧要素ページは
  表示・編集はできるが索引・集計に乗らない）。
- **テスト**: 旧要素が索引に乗らないことを固定するテストへ差し替え
  （TestLegacyElementsNoLongerSynced・TagValue の旧形式拒否）。変換の同値性
  テスト2本は「変換で初めて索引に乗り、旧 Sync と同じ抽出結果（quantity
  省略→1 など）になる」形へ再構成。実物系テスト3本（NestedOrders・
  RebuildDatabase・EstimateAttributes）は本文を新形式へ書き換え。
- **検証**: `go test ./...` 全パス。E2E は verify-stage2（41）＋
  verify-migration2（16）＋verify-stage3（19）全通過。stage3 に**検査レース**
  （再読込後、populateEditor がブロック構造を組み直す前に変換後の容器を検査
  していた——連続実行時のみ顕在化）があり、`waitFor` を足して修正
  （テスト側のバグ。アプリの退行ではない）。

---

## 語彙モデルの縦切り第3段（受発注・見積・容器の移行）を実装する（2026-08-19）

論点A（業務文書ブロックの外形）を**案1＝`<section data-type>` で包む**に決定し
（ユーザー決定。ヘッダ `<dl>` は data-type 無し・`dd` に `data-field` 自動付与、
見積2種は明細が無いため dl のみ）、`<m-client-order>`／`<m-supplier-order>`／
見積2種／`<m-item>`／**`<m-file>`（容器）**を標準HTMLへ移行した。
参照系（受信元・前版・参照追従JOIN）は通信記録処理の設計待ちでスコープ外。

- **新形式**: 受注＝`<section data-type="client-order">`（ヘッダdl＋
  `<table data-type="client-order-items">`）、自社発注＝`our-order`／`our-order-items`、
  見積＝`<dl data-type="our-estimate"|"supplier-estimate">`、容器＝
  `<section data-type="file" data-src>`＋**可視のファイル名リンク**（閲覧ゼロJSでも
  PDFを開ける。プレビューはエンハンサ＝§10 の未決を「エンハンサ方式」で決着。
  SSRは第4段の計算ビューと合わせ再評価）。`ext` 属性は廃止。
- **サニタイザ**: `section` へ `data-type`/`data-src` を許可（論点A採用時の予定どおり）。
  `data-src` は埋め込みと同じ**相対URL限定**（多層防御）。
- **レジストリ**: 7形式を追加（file/client-order/client-order-items/our-order/
  our-order-items/our-estimate/supplier-estimate）。VocabDef へ `Items`（明細表）・
  `Container`（挿入時の容器）・`Hidden`（メニュー非表示）を追加。
- **同期の両対応**: 3プラグイン＋汎用読み取りヘルパ（`VocabDLFields`・
  `VocabTableRows`・`FirstVocabChild`・`ClosestFileSrc`）。数値は正規化
  （`¥8,000`→8000）、quantity 空セルは旧既定の 1。
- **変換ツール拡張**: 受発注（子 m-item→明細行）・見積（**1要素＝1dl・連結しない**）・
  容器（リンク生成に pageID が要るため `ConvertVocabHTML(pageID, html)` へ変更）。
- **エディタ**: 旧4項目を除去（手書き項目は h1/p/手配状況/子ページ一覧の4つに）。
  骨格生成は section＋容器ラップ対応（日付=今日・order-no=仮番号の既定値、
  空 dd の `<br>` 足場）。**初のエンハンサ実装**＝`enhanceFileSections`
  （PDFプレビュー・ドロップゾーン・AI明細解析ボタン。すべて `.vocab-chrome`＝
  シリアライザが保存しない編集クローム。呼び出しは applyMode／populateEditor／挿入直後）。
  PDFアップロード・Gemini解析は新形式の表の行へ挿すよう再実装。
- **検証**: go test 全パス（新形式同期・**4テーブルの変換前後同値性**など3本追加）。
  E2E: verify-stage3.js（19項目・新設）＋stage2回帰41＋migration2回帰16 全通過。

**やらなかったこと**: 旧形式読み取りの除去（変換完了後）・`m-child-list`／
`m-required-materials`／`web-components.js`／`templates/` の撤去（第4段）・
参照マーカー（通信記録設計後）。カスタムタグ一覧・API一覧・エディタ仕様の細部追随は
未実施（次セッションで doc-audit／code-reference と合わせて）。

---

## スマホ幅の表示崩れ（表による横スクロール）を修正する（2026-08-19）

375px 等の狭い画面で、本文に幅の広い表が1つあるだけでページ全体が横スクロールに
なっていた（ユーザー報告）。原因は2段:

1. 縦積みレイアウト（`@media (max-width: 760px)` の `.work-area { flex-direction: column }`）は
   `align-items: flex-start` のため、flex アイテムの横幅が fit-content になる。
   **fit-content の下限は min-content** なので、min-content 以下に縮められない表が
   あると文書列（`.doc-column`）ごと押し広がる。
2. 表はセルの内容幅（min-content）以下に縮まない。

対処（[app.css](../assets/app.css) の 760px メディアクエリ内）:

- `.doc-column { align-self: stretch }` — 文書列は常に親幅いっぱいに固定
- `#editor-content table { display: block; max-width: 100%; overflow-x: auto }` —
  幅の広い表は**表自身の中で横スクロール**させる（`display: block` は行内容を
  無名テーブルボックスが包む標準的なレスポンシブ対処。セルの罫線・contenteditable・
  行操作ツールバーには影響しない）。`pre` にも同じ横スクロールを付与
- デスクトップ幅（>760px）は無変更

検証: Playwright で 375px の実測（`scrollWidth` 552→375、管理コンソールも 375 で
横はみ出しなし）と、デスクトップ幅の E2E 回帰（verify-stage2.js 41項目＋
verify-migration2.js 16項目）全通過。
なお画面端のレール開閉タブ（ページ階層・目次・ページ情報のつまみ）が本文に重なって
見えるのは狭幅用の設計どおりのクローム（[【一覧】用語集.md](【一覧】用語集.md)）で、
本修正の対象外。

---

## 語彙モデルの縦切り第2段（m-tag・m-material の移行）を実装する（2026-08-19）

[【考察】語彙モデル.md](【考察】語彙モデル.md) §8.4-2「計算に絡まないものから移行」を実装した。
`<m-tag>`→`<dl data-type="tags">`（`page_tags` の同期元切り替え）と
`<m-material>`→`<table data-type="part-materials">`（部材手配計算の読み先切り替え）。

### サーバー側

- **レジストリへ後継2形式を追加**（[vocab.go](../internal/cms/vocab.go)）:
  `tags`（可変タグ・dl。名前は自由語なので骨格は1項目のみ）と `part-materials`
  （部材定義・③計算形式の表。列は `data-field` item-name/cost/supplier-name/quantity を
  骨格生成時に自動付与）。③の語彙宣言をプラグイン側へ移す（①と③の合成）のは将来課題。
- **同期の両対応**（旧形式の読み取りは §8.3 の「変換ツールが安定するまでの短期の保険」）:
  - [plugin_page_tags.go](../internal/cms/plugin_page_tags.go): `dl[data-type="tags"]` の
    dt/dd（多値は dt 繰り返し／複数 dd・trim・`dd data-field` 優先）と旧 `<m-tag>` の
    両方を `page_tags` へ。「親ページID」の除外は新形式にも適用。
  - [plugin_materials.go](../internal/cms/plugin_materials.go): `table[data-type="part-materials"]`
    のデータ行と旧 `<m-material>` の両方を `part_materials` へ。列は文書の見出し行から
    解決（`data-field` 優先→見出しテキスト→レジストリの Label 経由で機械キーへ）。
    数値は語彙の正規化（`¥8,000`→8000）を通し、quantity の空セルは旧既定と同じ 1。
  - `TagValue`（[plugin.go](../internal/cms/plugin.go)）を両対応へ——部材手配の
    `部品番号` 注入がどちらの形式でも効く。
- **一度きり変換ツールを新設**（`migrate_vocab.go`（2026-08-20 に撤去）・§8.3）:
  `POST /api/migrate-vocab`（admin）＋管理コンソールの「語彙モデルへの一括変換」ボタン。
  全ページを走査し、**連続する** `<m-tag>` は1つの dl へ、連続する `<m-material>` は
  1つの表へまとめる（`data-id` は先頭ブロックから引き継ぎ、ブロックの同一性を保つ）。
  実行前に `data/master` を `data/vocab-migrate-backup-<時刻>/` へ自動バックアップし、
  変換ページを `SyncIndex` で再同期する。処理は冪等。
  **変換しないもの**: 配線の `<m-tag>`（`受信元`・`前版`＝第3段の参照マーカー行き）・
  旧「親ページID」（遺物。dl 化すると可視タグとして復活するため）・名前が空のもの。
- 検証方針は §8.3 どおり「**変換前後で Sync() の抽出結果が一致する**」ことをテストで固定
  （`migrate_vocab_test.go`（同日に撤去）。同値性・バックアップ・
  冪等性・除外・両対応・新形式同期の9本）。

### エディタ側

- スラッシュメニューの手書き項目から `m-tag`（🏷️ 可変タグ）と `m-material`（🔩 部材定義）を
  **除去**（手書き10項目→8項目）。後継はレジストリ由来の動的項目（可変タグ＝dl・
  部材定義＝表）で、新規挿入は必ず新形式になる。`createComponentElement` の該当分岐も削除。
- 既存ページの旧要素の**表示・編集**（web-components.js）は変換完了まで従来どおり。

### 検証

- `go build`／`go vet`／`go test ./...` 全パス（新テスト9本）。
- E2E: 既存の verify-stage2.js（41項目・回帰）全通過＋新設の
  **verify-migration2.js（16項目）**全通過——旧項目の除去・後継項目・dl/表の挿入と
  保存往復・旧要素注入→管理コンソールの一括変換→変換後ページの確認・JSエラーなし。
  スクリプトの正本は `%OneDrive%\tools\wcms-e2e\`。

### やらなかったこと（スコープ外）

- 旧形式読み取り（短期保険）の除去——実環境での変換完了後に行う
- `web-components.js`・`assets/templates/` からの m-tag/m-material 撤去——移行第4段で一括
- `<m-item>` 統合・参照マーカー・参照追従JOIN——第3段（論点A確定後）
- 【一覧】カスタムタグ.md の §3「既知の名前」の dl 前提への書き直し（注記のみ更新）

---

## 文書の総点検（2026-08-19）

doc-audit の未検証リスト（アーキテクチャとDBスキーマ・エディタ仕様・【考察】各種・
【ガイド】2本・【ユースケース】2本・用語集・画面構成と用語）を全消化し、あわせて
直近の変更（縦切り第1段＋第2段・Web Components 全廃決定・監査3件・htmldoc 切り出し）への
追随漏れを全文書から一掃した。主な修正:

- **実装済みなのに「未実装」だった記述**: プラグイン開発ガイドの「レジストリと汎用索引は
  未実装」（→2026-08-17 実装済み・宣言1件で完結する旨へ）、本文サニタイズ設計 §5.3 の
  「語彙モデル（未実装）では…広がります」（→現在形へ）、要件定義書の汎用表エディタ
  「未実装」（→v1＋拡張を実装済み・残りは date/image）とスラッシュメニュー「未達」
  （→レジストリ駆動の生成は実装済み・手書き10項目の置換と絞り込みが未達）、
  カスタムタグ一覧の「将来は語彙レジストリが合成される」（→合成済み）、
  アンドゥ・リドゥ §2「現状」（→設計時点の現状と明記）、
  デプロイ運用 §6「フェーズ1では全機能利用可」（→ページ単位認可・匿名公開まで実装済み）。
- **実装済みなのに「未実装」（続き）**: 要件定義書 §2.2 の「tag-schema をレジストリの
  形式定義も返す形へ拡張する（未実装）」と §2.5 挿入経路表の「レジストリ駆動化は未実装」
  （→どちらも実装済み）、認証認可設計 §3.4 の「子ページ作成時の group/mode 継承は
  設計合意・未実装」（→ `NewPageAPIHandler` で実装済み）。
- **廃止決定との矛盾**: 原価と利益の「実装時はカスタム要素 `<m-profit-calculator>` を追加する
  想定」→ 新規カスタム要素は作らない決定（2026-08-17）に合わせ**計算ビュー**へ。
  通信記録処理 §4.5 の転換先 `<m-field>`/`<m-table>` 案 → マーカー付き標準HTMLへ差し替え済みと注記。
- **コードとの食い違い**: アーキテクチャの `part_id` 注入元（`parser.go` → 実際は
  `plugin_materials.go` の `Sync`＋`TagValue`）、新規ページの初期HTML（`<m-child-list>` は
  含まれない）、保存APIの本文例（DOCTYPEではなく断片）とレスポンス（`unknown_types` 追加）、
  テンプレートエンジン不使用の理由（インラインJSは外部化済み→サニタイズ済みHTMLのため）、
  コンセプト §1③ の親子関係（タグ→サイドカーが正本）、
  本文サニタイズ設計 §7 の tag-schema 応答（3キー→5キー）。
- **数値・一覧の補正**: 殻の外部化の行数誤記を本修正（2768→198／196→76 は誤りで、
  実測 **3015→215／195→72**。作業引き継ぎ2箇所）、エディタ仕様冒頭の殻の行数（198→260）、
  README のツリーへ `vocab.go`・`vocab_index.go` を追加、
  画面構成と用語へ新クローム4種（表ツールバー・dlツールバー・列設定ポップオーバ・
  選択肢メニュー）、用語集へ語彙モデルの中核語5件（マーカー付き標準HTML・語彙レジストリ・
  汎用索引・汎用表エディタ・③計算プラグイン）。

検証して**正しかった**主な記述: 開発方針 §1 の依存一覧は `go.mod` と一致（`x/text` の
indirect 注記も正確）、CSP強化の残数（templates の `style=` 75個・web-components の生成
`style=` 8行・`on*=` ゼロ・インラインscript 1本）はすべて実測一致、アーキテクチャの
スキーマ定義（受発注・見積・部材・vocab_index の全列）と部材手配計算のロジック、
ブロックIDの「4桁 base36」、Gemini のモデル名 `gemini-3.5-flash`、デプロイ運用の
バックアップ対象、同時編集・アンドゥ・許可タグと属性の決定ログ（履歴として整合）。

---

## E2E環境を自宅にも再現し、dl項目追加のバグを修正する（2026-08-19）

職場と同じ Playwright 環境（`winget install OpenJS.NodeJS.LTS`＋`~\tools\wcms-e2e\`・
リポジトリ外）を自宅にも構築した。職場の `verify-stage1.js`（31項目・第1段の範囲）は
職場マシンにしか無いため、**第1段の回帰＋第2段の新機能まで**を検証する
**`verify-stage2.js`（41項目）** を新規に書いた——骨格挿入・セル編集・行操作・
列操作と列設定・型検証（明示が推論に優先・通貨/日付の解釈）・enum 補助・
dl 項目操作・未知種別トースト・**保存往復で編集クロームと実行時の印が漏れないこと**・
閲覧モード・JSエラーなし。

> **2026-08-19 追記**: スクリプトの正本は **`%OneDrive%\tools\wcms-e2e\`** へ移し、
> 両マシンで共有する形にした（README 同梱）。`node_modules` は同期せず各マシンの
> `~\tools\wcms-e2e\` に置き、スクリプトは playwright をカレントディレクトリから解決する
> （`createRequire`）。実行はサーバー起動後
> `cd ~\tools\wcms-e2e && node "$env:OneDrive\tools\wcms-e2e\verify-stage2.js"`。

E2E が実バグを1件検出したので修正した:

- **空の dt/dd への入力が隣の項目へ混入する**。contenteditable では空の dt/dd に
  キャレットが実際には入れず、「＋項目」直後に打った名前が**次の項目の先頭**へ
  挿入されていた（Chrome の挙動。表の td/th はキャレット容器として特別扱いされる
  ため無事）。定石どおり `<br>` の足場を置く `emptyEditable()` を新設し、
  `dlItemAdd`・`dlValueAdd`・`buildVocabSkeleton` の dl 分岐で使う。`<br>` は
  入力を始めるとブラウザが取り除き、未入力のまま保存されても無害（許可要素）。

---

## 監査で残っていた3点を塞ぐ（2026-08-19）

2026-08-05 の監査で「脆弱性ではないが直す価値がある」とされていた3点（いずれも
設計判断不要の小粒な修正）を実装した。

- **JSONボディのサイズ上限**: `/api/save` 等は `json.NewDecoder(r.Body)` を直接使って
  おり、認証済み利用者が巨大なボディでメモリを圧迫できた。cms 側は共通ヘルパ
  `decodeJSONBody`（**8MiB**・超過は **413**）を新設して保存2本・権限2本・PDF解析へ、
  管理API（auth の `decodeJSON`）は **1MiB** を適用。添付（32MiB・実装済み）とは別系統。
- **ページIDの正規化**: 権限判定は `strconv.Atoi` を通すため `"0012"` や `"+12"` でも
  12 として通るのに、ファイルパスとサイドカーには受け取った文字列をそのまま使って
  いたため、**正本が `data/master/00/0012/` のような別ディレクトリへ書かれて名前が
  揺れる**。`page.NormalizeID`（ゼロ詰め6桁）を新設し、パスに id を使う入口
  （保存2本・添付2本・`/api/set-parent`・`/api/page-perms`・`/api/page-chown`・
  `/api/lock`）で適用した。トラバーサルにはならない（`.` や `/` は Atoi を通らない）
  ため優先度を下げていたもの。
- **`/assets/` のキャッシュ制御**: 無指定だとブラウザのヒューリスティックキャッシュが
  効き、デプロイ後も古いフロント（`web-components.js` 等）が残り得た。
  **`Cache-Control: no-cache`**（使う前に毎回再検証）を付与。`http.FileServer` が
  `Last-Modified` を返すため、変化が無ければ 304 で済む。ハッシュ付きURLは
  ビルド工程ゼロの方針と相性が悪いため採らなかった。

検証: 新テスト4本（`NormalizeID` の規則・保存/アップロードが正規ディレクトリへ書き
`0012` ディレクトリを作らないこと・8MiB超のボディが413）を含め `go test` 全パス。
実サーバーで `/assets/app.js` の応答に `no-cache` と `Last-Modified` を確認。

---

## 汎用表エディタへ列操作・dl項目操作・型検証・enum入力補助を追加する（2026-08-19）

縦切り第1段（2026-08-17）でスコープ外とした項目のうち3つを実装した
（[【考察】語彙モデル.md](【考察】語彙モデル.md) §5.2「編集の2面」・§7 の3原則に従う）。
仕様の正本は [エディタ仕様.md](エディタ仕様.md) §3.2。

- **列操作**: 追加（現在列の右）・削除・左右移動を `#table-toolbar` へ追加。全行に同時に
  効き、**最後の1列は削除しない**（行側の「見出し行は消さない」と同じ骨格保護）。
- **列設定ポップオーバ**（属性側の編集口）: 列型の明示（`th` の `data-type`）を編集できる。
  空にすると推論へ戻る。`data-field` は骨格生成の自動付与のみ（手編集の口は設けない——
  壊すと `Sync()` が読めなくなるため）。属性変更は `MutationObserver` の `attributeFilter` に
  載らないため、保存は明示的に蹴る。
- **dl の項目操作**: `#dl-toolbar` を新設。項目（dt＋後続dd）の追加・削除・上下移動と
  多値（`＋値` で dd を追加。§5.3）。**最後の1項目は削除しない**（空の dl はキャレットの
  置き場が無く項目を足せなくなる）。
- **型不一致の通知（拒否しない）**: `number`/`date` 列の解釈できない値に `.cell-invalid`
  （赤印）。class はシリアライザが書き出さないため**実行時だけの印**で保存されない。
  列型の決定順序はサーバーの `resolveColumnType` と同一。**語→型の推論辞書は
  `/api/tag-schema` の `type_inference` として配布**し（`TypeInferenceDict` 新設）、
  エディタに手書きの語彙を置かない（原則1）。検証と索引の型判定が食い違わない。
- **enum の入力補助**: レジストリ宣言の選択肢をセル直下のメニューで提示（宣言が無い
  enum 列には出さない＝原則3「レジストリがあれば向上・無くても編集可能」）。

### 検証

`go test` 全パス（`type_inference` の応答テストを追加）。実ブラウザは
`/assets/` が認証不要である流儀の一時ハーネスで **37項目全通過**——列の追加/移動/削除と
データ行の同期・型明示が推論に優先・赤印の付け外し・enum メニュー・dl の項目操作と
骨格保護・**シリアライズへ `.cell-invalid` が漏れず `data-type`（表・`th` とも）は残る**こと・
コンソールエラーなし。ハーネスは削除済み。

### やらなかったこと（スコープ外）

- `date` のカレンダー等の入力補助・`image` のセル内挿入（**画像アップロード自体が未実装**
  ——[要件定義書.md](要件定義書.md) §2.6 の実装が先）
- 既存9要素の移行（第2段以降）・論点A（業務文書ブロックの外形＝ユーザー判断待ち）

---

## 語彙モデルの縦切り第1段を実装する（2026-08-17）

[【考察】語彙モデル.md](【考察】語彙モデル.md) §8.4 の実装順・第1段
「**新語彙だけで新機構を稼働**」を実装した。①コード内レジストリ＋②汎用索引＋
汎用表エディタv1（セル編集・行操作）が、サンプル語彙「検査記録」で end-to-end に動く。
**既存のカスタム要素9種（10要素）には一切触れていない**（移行は第2段以降）。

### サーバー側

- **サニタイザ**（[htmldoc/sanitize.go](../internal/cms/htmldoc/sanitize.go)）:
  語彙マーカーの属性名を**要素限定**で許可した——`data-type`→`table`・`dl`・`th`、
  `data-field`→`th`・`dd`（決定ログどおり。`section` は論点A未決のため見送り）。
  値は不活性な文字列として検査しない。
- **①語彙レジストリ**（[vocab.go](../internal/cms/vocab.go)・新設）: Goコード内の宣言テーブル
  （v1。決定ログどおり）。形式定義（`data-type`・表示名・分類・アイコン・要素種別
  table/dl・列＝ラベル/型/enum選択肢/機械キー `data-field`）と、語→型の推論辞書
  （`単価`→number 等）・正規化（`¥8,000`→`8000`、`2026/8/1`→`2026-08-01`）を持つ。
  列型は要件定義書 §2.5 の5種（text/number/date/enum/image）に揃え、**通貨は number に
  含めた**（推論辞書と正規化が ¥・桁区切り・円を吸収する）。サンプル語彙は
  「検査記録」（品番/判定/検査写真/検査日）の1件。
- **②汎用索引**（[vocab_index.go](../internal/cms/vocab_index.go)・新設）: 全
  `table[data-type]`/`dl[data-type]` を縦持ちの `vocab_index` テーブル
  （page_id・data_type・block_no・block_id・row_no・field・value・norm_value）へ
  洗い替え同期する。鍵は `data-field` 優先・無ければ見出しテキスト、型は
  `th data-type` 明示 > レジストリ宣言 > 推論辞書の順。正規化値は解釈できた値だけ
  **併記**し生テキストが正本。**未知の `data-type` もそのまま索引に載る**（オプトインの
  規約は data-type の有無だけ）。同期の駆動は既存プラグイン機構（`Register`）へ
  相乗りするが、3層モデルの②＝コアの1実装であり、ドメイン語彙は知らない。
- **API**: `GET /api/tag-schema` に `vocab`（レジストリの形式定義）を追加。
  `/api/save`・`/api/save-block` の応答に `unknown_types`（レジストリ未定義の
  data-type の**告知**。拒否ではない——決定ログ「通す＋保存時通知」）を追加。

### エディタ側（[app.js](../assets/app.js)）

- **スラッシュメニュー**: レジストリ由来の項目（アイコン＋表示名）を `vocab` から
  動的生成して静的項目の後ろへ追加。挿入骨格（見出し行＋空のデータ行1行の
  `<table data-type>`、または `<dt>/<dd>` の `<dl data-type>`）は列定義から機械的に
  生成する。DOM生成は `createElement`＋`textContent` 代入のみ（Web Components 廃止
  決定の作法。`innerHTML` の文字列組み立ては使わない）。メニュー全体の再設計
  （絞り込み・分類）は引き続き別課題（エディタ仕様 §3.1）。
- **汎用表エディタv1**: セルの値は既存の contenteditable がそのまま担い（値＝中身＝
  表示される文字）、**行操作ツールバー**（追加・削除・上下移動）を新設した。
  ツールバーは本文DOMの**外**（シェル直下の `#table-toolbar`）に置いてキャレットの
  行へ追従させる——ボタンを表の中へ挿すとシリアライザが中身を本文として拾うため。
  見出し行（＝列の鍵と型を運ぶ）は削除・移動の対象外。カスタム要素が描画する表では
  出さない。列型に応じた入力補助（enum ドロップダウン等）は段階的に足す（§5.2 の予定どおり）。
- **機械属性のハイライト**（決定ログ）: `th[data-field]`・`th[data-type]`・`dd[data-field]` を
  編集モードで背景ハイライトし、見えている文字が鍵・型として使われていないことを示す
  （CSS属性セレクタ。[app.css](../assets/app.css)）。あわせて本文の表に最小限の枠線
  スタイルを与えた（ブラウザ既定では枠が無く、挿入直後の空セルが見えないため）。
- 挿入直後は構造HTML全般（表・定義リスト含む）を contenteditable にし、最初のセルへ
  キャレットを置く（従来は h1〜h3・p の名指しだった）。

### 検証

- `go build` / `go vet` / `go test ./...` 全パス。新規テストは
  [vocab_test.go](../internal/cms/vocab_test.go)（レジストリの規約・推論・正規化・
  マーカー属性のサニタイズ・未知種別の検出・tag-schema 応答）と
  [vocab_index_test.go](../internal/cms/vocab_index_test.go)（表/dl の索引・鍵と型の
  決定順序・素の表の除外・洗い替えの冪等性・複数ブロック・保存応答の告知）。
- 実サーバーでも確認: ログイン→ロック取得→検査記録＋未知種別の保存で
  `sanitized: false`（往復無変化）・`unknown_types: ["mystery-x"]`、`vocab_index` に
  鍵・値・正規化値（`¥8,000`→`8000`・`2026/8/10`→`2026-08-10`）が期待どおり入った。

### やらなかったこと（スコープ外）

- dl の項目追加・削除のUI（行操作ツールバーは表のみ。dl はセル編集のみ可能）
- 列操作（列の追加・削除・並べ替え）と列設定UI（ポップオーバ）——§5.2 の「編集の2面」の
  属性側は未着手
- 型不一致の入力検証（通貨列に通貨以外→赤ハイライト）と enum/date/image の入力補助
- 既存9要素の移行（第2段以降）・論点A（業務文書ブロックの外形）

> **2026-08-19 追記**: 上の1〜3行目（dl 項目UI・列操作/列設定・型検証と enum 補助）は
> 実施した（本履歴の 2026-08-19 の節）。残りは date/image の入力補助と移行第2段以降。

---

## Web Components の廃止を決定する（2026-08-17）

「Web Components の採用が妥当か」をユーザー指示で再検討し、**全廃を決定**した
（**素のHTMLへ移行し、カスタム要素 `<m-*>` を使わない**。決定の正本は
[【考察】語彙モデル.md](【考察】語彙モデル.md) §9）。

### 再検討の要点

- **採用理由の再帰属**: コンセプト §3 の3本柱（保存形式＝DOM・バックエンド連携の単純さ・
  contenteditable共存）は**バニラJS＋素のDOMの性質**であり、`customElements` 固有ではない。
  「フレームワークを入れない」理由ではあっても「カスタム要素を使う」理由ではなかった。
- **語彙モデル後に残る価値は自動ライフサイクルだけ**（`connectedCallback`）。既存の
  `MutationObserver`・初期化パスで代替できる。
- **廃止で構造的に消えるもの**: `escapeAttr` の規律に頼るテンプレートXSS経路（2026-08-04 に
  実際に発火）・テンプレートの `style=`（CSP strict 化の最後の障害）・グローバル結合と
  全体再描画（コンセプト既知の課題）・`lit` の再評価問題。
- **計算ビューのSEO**（検討項目）は、マーカー容器へ**Goがサーバー合成時に中身を埋める**
  事前描画で自然に解ける（カスタム要素のSSRより素直）。
- **追加ライブラリは不要**と確認: エンハンスは `querySelectorAll`＋`addEventListener`＋既存
  `MutationObserver`、DOM生成は殻の `<template>`＋`cloneNode`＋`textContent` 代入
  （DOMが自動エスケープ）。htmx/Alpine/morphdom/DOMPurify も不要（解く問題が無い）。
  将来の唯一の例外候補はバーコード読取の Safari 対応（`BarcodeDetector` 非対応）のみ。

### 決定の内容と反映

- 語彙は標準HTML＋`data-*` に**完全統一**。動的3系統（容器・参照・計算ビュー）も
  マーカー付き標準HTMLへ置き換え、移行第4段で `web-components.js`・`assets/templates/` を
  **撤去**する（語彙モデル §8.1・§8.4 を更新。**CSP strict 化はこの撤去と同時に完了**）。
- **新しいカスタム要素は作らない（即時発効）**。現行10種は移行完了まで従来の作法
  （エディタ仕様 §0.1）で保守する。`lit` の再評価は打ち切り。
- 反映した文書: 語彙モデル（§3・§7・§8・§9・§10・§11）／コンセプト §3（節名を
  「バニラJS＋標準HTML」へ書き換え）・既知の課題／エディタ仕様 §0・§0.1／要件定義書
  （§2.2・§2.5・§4.4・§4.5・§5）／CSP強化 §4(4)(5)（`style=` の個別除去を全面中止・
  (5) は撤去に置換）／プラグイン開発ガイド／本文サニタイズ設計 §5.3。
- 未決として残る設計: 動的3系統のマーカー設計（`data-*` 許可範囲の拡張含む）・
  エンハンスパスの規律（全挿入経路で）・PDFプレビューの方式（サーバー合成か
  エンハンサか）・撤去の段取り（語彙モデル §10）。

---

## サニタイザを htmldoc パッケージへ切り出し、循環参照を解消する（2026-08-17）

2026-08-06 の構造分割で「やらなかったこと」として残していたサニタイザの切り出しを実施した。
当時整理した2案のうち **(b) 依存を反転させる**を採り、振る舞いは変えていない。

- **`internal/cms/htmldoc` を新設**。許可リスト（`dangerousElements`・`structuralElements` 等）・
  URL規則・`Sanitize`/`SanitizeReport`・`AllowedVocabulary`・`BlockIDAttr`・`ParseFragment` を
  移し、**純粋なHTML部品**にした。カスタム要素の語彙は `New([]TagSpec)` の引数として
  **注入**され、`htmldoc` はプラグイン・レジストリを知らない（import は `x/net/html` と
  標準ライブラリのみ）。依存は `cms → htmldoc` の一方向。
- 見送り時に未決だった2点はこう解いた:
  - **`TagSpec` の置き場所** — `htmldoc` に置き、`cms` 側は
    `type TagSpec = htmldoc.TagSpec` の**エイリアス**。全プラグイン（約50箇所の修飾変更が
    必要と見積もっていた）は**無変更**で済んだ。
  - **初期化順序** — 従来と同じ「初回利用時に `sync.Once` で構築」を `cms` 側に残した
    （`sanitizer = htmldoc.New(PluginTags())`）。「全プラグインの `init()` 登録 → 最初の
    サニタイズ」という順序への依存は増えても減ってもいない（全体地図の「既知のねじれ」
    に引き続き記載）。
- **`cms/sanitize.go` は語彙の合成だけを担う60行のラッパ**になり、公開名
  （`Sanitize`・`SanitizeReport`・`AllowedVocabulary`・`VoidElementNames`・`BlockIDAttr`）は
  従来どおり。ハンドラ・既存テストの呼び出し側は無変更。
- 付随の移動: `block.go` が使っていた `parseFragment` は `htmldoc.ParseFragment` として公開、
  `attrValue` は唯一の利用元である `block.go` へ移した。テスト2箇所
  （`plugin_tags_test.go`）は内部識別子（`allowedElements()`・`structuralElements`）から
  公開API（`AllowedVocabulary()`・`htmldoc.New(nil)`) 経由へ変えた。
- 検証: `go build ./...`・`go vet ./...`・`go test -count=1 ./...` 全パス。
  `htmldoc` の import に `w-cms` 内パッケージが無いことを `go list -f` で確認。

---

## 文書を刷新し、タグ設計を語彙モデルへ統合する（2026-08-16）

日付つきの上書き注記を積層させる書き方をやめ、各文書を**現在の設計を直接語る形**へ
書き換えた（ユーザー指示）。あわせて文書の統合・削除を実施。

- **`【考察】タグ設計.md` を [【考察】語彙モデル.md](【考察】語彙モデル.md) へ統合し、削除**。
  マークアップの原則（役割＝属性・見える値＝中身）は語彙モデル §2 が引き継ぎ、実現手段が
  `<m-field>`/`<m-table>` から標準HTML＋`data-*` へ差し替わったことで独立文書の意味が
  無くなったため。語彙モデルは全面再構成した——§2 マークアップの原則／§3 語彙の3分類／
  §4 3層モデル／§5 表と定義リストの設計／§6 挿入経路／§7 エディタの形式知識／
  §8 標準HTMLへの移行／§9 決定ログ／§10 未決事項。
- **刷新した文書**: コンセプト §3（規模前提の文言を現在形へ）／エディタ仕様 §0・§3
  （`lit` 再評価の位置づけを本文へ、スラッシュメニュー再設計を §3.1 として節に昇格）／
  本文サニタイズ設計 §5.3・§5.4／アーキテクチャ §2／認証認可設計 §10.5（404統一・
  公開専用ビュー・キャッシュ前提を本文へ統合）／プラグイン開発ガイド冒頭（振り分けを
  本文化）／カスタムタグ一覧の冒頭・末尾／CSP強化 §4(4)／許可タグと属性
  （基準Eを現行の役割分担へ改め、`dl`「不採用」の記録を実装どおり「許可」へ是正、
  `<m-table>` 記述を `data-type` 方式へ差し替え）。
- 旧タグ設計へのリンクは全文書で語彙モデルへ付け替えた（変更履歴の過去節も**リンク先のみ**
  更新し、本文の記述は履歴として維持。過去節に現れる §番号は当時の文書構成のもの）。

---

## 語彙モデルの決定を各正本へ反映し、CSPの残作業を再計画する（2026-08-16）

実装（縦切り第1段）に入る前に、語彙モデルと矛盾する既存の方針記述を洗い出して
（指摘リストはチャットで提示・A/B/C に分類）、**いま直すべき A・B を反映**した。
C（ユースケース2文書・通信記録処理の m-field/m-table 前提・カスタムタグ一覧の生成化）は
移行時に直す（いま変えると二度手間）。

- **本文サニタイズ設計 §5.4**: 語彙マーカーの例外を追記——`data-type`（`table`・`dl`・`th`）と
  `data-field`（`th`・`dd`）を**要素限定**で許可（ユーザー決定。`globalAttributes` に入れない。
  値は不活性で検査しない。`section` は論点A採用時に追加）。§5.2 の表の備考も
  旧 `<m-table>` 案から差し替え。
- **本文サニタイズ設計 §5.3・【一覧】カスタムタグ.md 冒頭**: 「`Tags()` が唯一の正本」の
  適用範囲を明確化——**カスタム要素については**そのまま真。語彙全体は
  「構造HTML ∪ `Tags()` ∪ レジストリ」の合成へ広がり、**サニタイズ許可リストへの寄与は
  従来どおりコード側だけ**（レジストリは安全性の門ではない）。
- **アーキテクチャとDBスキーマ §2**: 「コアテーブルは2つだけ」に予定を追記——汎用索引が
  コアテーブルとして加わるが、**「コアはドメイン語彙を知らない」原則は不変**
  （`data-type` の値はコアにとって不透明な文字列）。
- **【ガイド】プラグイン開発**: 冒頭に**振り分け**を追加——計算が要るなら本ガイド（③）、
  記録・表示だけならレジストリ宣言1件（実装まではガイド手順が唯一の方法である旨も明記）。
- **【考察】CSP強化 §4(4)**: 残る `style=` 75個の大半は**移行で消滅する要素のテンプレート**に
  あるため、そこには手を入れない段取りへ再計画。先行除去は残存3要素
  （`m-file`・`m-child-list`・`m-required-materials`）と `web-components.js` の8行だけとし、
  strict 格上げの完了は語彙モデル移行第2段に相乗りさせる。

---

## 【考察】語彙モデル.md を新設し、根本判断を決定する（2026-08-16）

3層モデルの検討が要件定義書 §2.5 に収まらない分量になったため、**設計の正本として
[【考察】語彙モデル.md](【考察】語彙モデル.md) を新設**した（3層モデル・挿入経路・
エディタの形式知識・決定ログ・標準HTMLへの移行の検討）。要件定義書 §2.5 は要約に格下げし、
正本ポインタを差し替えた。README の文書一覧にも追加。

**ユーザーが決定した根本判断**（正本は同書 §5 決定ログ）:

| 判断 | 決定 |
|---|---|
| レジストリの置き場（v1） | **Goコード内の宣言テーブル**から始める（管理UIのDB→語彙定義ページへ段階的に育てる。v1の間は語彙追加に再ビルドが要る） |
| 未知の `data-type` | **通す**＋保存時に「未定義の種別」と通知（レジストリを安全性の門にしない） |
| 移行の段取り | **まず標準HTMLへの移行の検討から始める**（同書 §6 に初版を起こした） |
| レジストリの定義スキーマ | **サンプルスキーマで実装を先行**し、動いてから実運用スキーマを確定 |
| 識別子の文字種 | `data-type`・`data-field` の値は **kebab-case** |

**§6「標準HTMLへの移行の検討（初版）」の要点**:

- 現行9要素の移行マップ: `m-tag`（可視）→`<dl data-type="tags">`／`m-tag`（配線）→参照要素へ
  分離／`m-item`・`m-material`→表の行／受発注4種→**論点A**（`<section data-type>` で
  dl＋table を包む案1に傾き。案1なら `<m-field>` は導入不要になる可能性）／
  `m-file`・`m-child-list`・`m-required-materials` は**容器・参照・計算ビュー**として残す。
- 移行の機構: 一度きり変換ツール＋「変換前後で `Sync()` 抽出結果が一致」のテスト。
  読み取り両対応は短期の保険に留める。
- 実装順（縦切り）: (1) 新語彙（例: 検査記録）だけで①②＋汎用表エディタを稼働（既存9要素に
  触れない）→ (2) 計算に絡まない `m-tag`・`m-material` から移行 → (3) 受発注4種＋参照追従を一括。

---

## 要件定義書を語彙モデル（3層）で書き直す（2026-08-16）

「標準HTML＋`data-*` マーカー」路線（下の節）を起点に検討を進め、**語彙と拡張の全体像**が
見えたため、[要件定義書.md](要件定義書.md) を書き直した。**章番号は他文書からの参照を壊さない
よう維持**し、§2.5 を「語彙モデルと業務機能」として全面改訂、§2.2・§4.5 等を追随させた。
3層モデル・挿入経路の設計記述は、【考察】文書への展開までは要件定義書 §2.5 が記録の置き場。

### 検討で得た構造（チャットでの検討を文書化）

- **挿入経路の列挙**: 構造化ブロックが本文へ入る経路は、明示挿入／貼り付け変換／
  ファイル取り込み変換（PDF→表は実装済み）／デバイス起点（カメラ・バーコード）／
  他ページ参照／計算ビュー（保存されない派生）／雛形・複製／エディタ外生成（fan-out・
  外部投入・移行）に分類できる。**不変条件は「どの経路でも出口は同じ」**——同じ語彙・
  同じ保存時サニタイズ/検証・同じ編集ロックを通り、語彙の単一正本を迂回する経路を作らない。
- **3層モデル**: ①語彙レジストリ（宣言データ・再デプロイ不要が目標。`data-type` の定義＝
  表示名・分類・列・雛形）②汎用索引（コアに1実装。全 `table[data-type]`/`dl[data-type]` を
  汎用テーブルへ同期＝`page_tags` の一般化）③計算プラグイン（Goコード・少数。型付き
  テーブル＋計算・取り込み変換・集計API）。**100種の大半が①のデータ1件になり、開発者が
  ボトルネックにならない**。プラグインは「1計算ユースケース＝1プラグイン」へ意味が狭まる。
- **エディタの形式知識は3場面で異なる**: 挿入時＝レジストリから受信（骨格はヘッダ定義から
  機械生成）／編集時＝**自己記述ヘッダだけで自立**（レジストリ無しでも既存文書は編集可能）／
  入力補助・検証＝レジストリがあれば向上。エディタ実装は汎用表エディタ1つ
  （列型 `text`/`number`/`date`/`enum`/`image`）で、**型の数に対して不変**。
- **カメラ写真はモデルの検証例**: 素の写真＝コア＋標準HTML（`<img>`）で完結し、どの層にも
  載らない。意味を持つ場面で合流する——表の写真列＝①の列型 `image`（データ1行）、
  撮影画像→明細表＝③の取り込み変換、写真への注記＝将来のSVG編集アプリ。
  EXIF一律除去の帰結として**撮影日時を業務データにするならEXIFに頼れない**
  （受付時刻のサーバー記録 or `date` 列へ入力）ことも §2.6 に明記。
- **サニタイザとレジストリの分離**: 許可リスト（要素・属性名）は従来どおりコードで固定し、
  `data-type`/`data-field` は属性名だけ許可して**値は不活性**。レジストリを安全性の門に
  しないことで、将来レジストリを実行時編集可能（管理UI・語彙定義ページ）にしても危険な
  要素は増やせない。
- **`lit` 再評価の位置づけを更新**: 語彙モデルで表・定義リストが標準HTML化し、カスタム要素は
  少数（計算ビュー系）に戻るため、再評価の緊急度は下がった（§4.5）。
- **未決点3つ**を §2.5 (5) に記録: ①未知 `data-type` の扱い（通す＋通知を推奨・未確定）
  ②レジストリの置き場（コード内→管理UI→語彙定義ページの段階論）③既存プラグインの移行の段取り。

---

## タグ設計を「標準HTML＋data-* マーカー」路線へ転換する（2026-08-16）

ユーザーから「受注表などは HTML の `table` を使い、class 指定で用途や挙動を決めては」との
提案があり、検討の結果**方向として採用**した（マーカーは `class` でなく `data-*` に修正）。
続けて「`<m-tag>` のような名前:値のタグは定義リストに変えられないか」も**検討対象に追加**。
正本は [【考察】タグ設計.md](【考察】語彙モデル.md) §7 冒頭の注記と §9。

- **繰り返し明細**: `<m-table>` で包む従来案（2026-08-02 決定）を再々検討し、
  **生の `<table data-type="…">`（ヘッダ `data-field` で列役割を自己記述）を第一候補**へ。
  決め手は公開ウェブサイト用途の追加——カスタム要素は表示に `web-components.js` が要るが、
  生テーブルは**閲覧ゼロJS**で、SEO検討項目（カスタム要素のサーバー側展開）が表形式ブロック
  については問題ごと消える。§7 の設計（自己記述ヘッダ・列設定UI・`Sync()`）は要素名が
  変わるだけでそのまま生きる。
- **マーカーを `class` にしない理由**: ①サニタイザの `class` 全面拒否（本文はシェルと同一DOM）に
  例外を作らない——`data-*` には `data-id` の前例と `globalAttributes` の仕組みがある
  ②見た目のフックと機械可読の役割の分離（改名がデータ破壊にならない）③シェルとの衝突ゼロ。
  装飾は `table[data-type=…]` の属性セレクタで当てる。
- **名前:値 → 定義リスト**: `<dl data-type="tags"><dt>名前</dt><dd>値</dd></dl>` 方式。
  名前も値も表示される実テキストになり原則に最も忠実。多値は `dt` 繰り返し／複数 `dd`。
  論点4つ（マーカー必須・対応規則・**配線タグは dl へ移せず参照要素へ分離**・
  プラグイン語彙宣言の `data-*` マーカー対応拡張）を §9 に記録。
- 要件定義書 §2.5 にも「標準HTML＋`data-*` マーカーを第一候補、カスタム要素は標準HTMLで
  表せないものに限る」を追記。**カスタム要素の総数が減るため100種問題の緩和にも効く**。

---

## 設定の渡し方を「環境変数のみ」と決定する（2026-08-16）

2インスタンス運用（業務用／サイト用）の決定を受けて、設定の受け渡し方式を検討した。
候補は ①環境変数のみ（現行の延長）②設定ファイル（JSON）＋環境変数上書き ③フラグ併用の
3案で、**①環境変数のみで確定**（ユーザー決定）。

- 決め手: アプリ実装が最小・秘密情報がリポジトリやバックアップへ紛れる事故が構造的に
  起きにくい・「1インスタンス＝1環境ファイル」という運用（systemd の `EnvironmentFile=`）と
  素直に噛み合う。一覧性はアプリではなく運用側のファイルが担う。
- [要件定義書.md](要件定義書.md) §4.3 に**設定項目の一覧表（正本）**を置いた。命名は
  `WCMS_` 接頭辞（`GEMINI_API_KEY` のみ業界慣行名のため例外）。新設予定は
  `WCMS_ADDR`（待受 `host:port`。プロキシ同居時のローカル束縛用）と `WCMS_DATA_DIR`
  （`data/` の場所）の2つで、どちらも未実装（§5）。
- あわせて「起動時に有効な設定を1行ログへ出す（秘密は値を出さず有無のみ）」を要件化。
  環境変数方式の「いま何が効いているか見えない」弱点への補い。
- [【ガイド】デプロイ・運用.md](【ガイド】デプロイ・運用.md) §2 に `EnvironmentFile=` の
  運用パターン（0600 保護・2インスタンスはユニット＋環境ファイルを1組ずつ）を追記。

---

## 要件の問題点を洗い出し、5つの決定を反映する（2026-08-16）

新設した [要件定義書.md](要件定義書.md) を「要件同士の整合／実装との突き合わせ／欠落」の
3観点で洗い出し、重大5件・中程度6件・軽微5件を挙げた。うち5件についてユーザーが決定し、
要件定義書と各正本へ反映した。

### 決定した5件

| 問題 | 決定 | 反映先 |
|---|---|---|
| 公開サイトなのに画像を置く手段が無い（添付は `.pdf` のみ、埋め込みURLは相対のみ→経路ゼロ） | **画像アップロードを付ける**。png/jpeg/webp/gif・マジックナンバー検証・検証済み種別のみインライン配信。**同日追記①: モバイルのカメラ撮影から直接挿入できること**（OS標準UIでよい）。付随して **EXIF（GPS位置情報）のサーバー側一律除去**と HEIC の理由つき拒否も要件化。**同日追記②: SVGは当初「拒否」としたが、将来SVG編集アプリを作る構想が示されたため「不活性な画像として許可」へ変更**——`<img>` 経由のみ（画像コンテキストはスクリプト不実行）＋配信ヘッダ三点セット（`image/svg+xml`・`attachment`・レスポンス限定 CSP `sandbox; default-src 'none'`）で無害化し、中身の完全サニタイズはしない。編集アプリ化の際は「編集器の語彙への許可リストサニタイズ」へ拡張する（本文HTMLと同じ流儀） | 要件定義書 §2.6（サニタイザは `img`+相対URLを許可済みで変更不要と確認） |
| 業務データと公開サイトが同一ツリー・1フラグで誤公開ガードが無い | **別サーバー・別ドメインの2インスタンス運用**。データ・認証は完全独立、業務データは構成そのもので公開面から分離。ソフトウェアは同一バイナリ | 要件定義書 §1.1・§1.4・§4.3、認証認可設計 §10.5 |
| 匿名への応答が 302（非公開）/404（不存在）で分かれ、連番IDと合わせて非公開ページの存在・総数が列挙できる | **匿名には404で統一**（読めない＝存在しない）。画面・API・`/data/` の全匿名経路。例外はトップ経由の `/login` 誘導のみ | 要件定義書 §2.1、認証認可設計 §10.5（未実装の決定として注記） |
| 「JS無しクローラでも本文が読める」とカスタム要素のクライアント描画が矛盾（クローラには属性しか見えない） | **検討項目**とする（公開ページのカスタム要素をサーバー側で展開するか） | 要件定義書 §2.1 注記・§5 |
| `class`/`style` 全面拒否と公開サイトの表現力が両立しない | **検討項目**とする。候補は ①表示専用カスタムタグ ②`class` の許可リスト ③テーマCSS | 要件定義書 §4.1 注記・§5 |

### 洗い出しで挙がったが未決のもの（要件定義書 §5 と本節が記録）

- 語彙追加＝Goコード＋再デプロイの是非（100種を誰が増やすか。宣言的定義を許すか）
- 100種の命名・分類ガバナンス（`<m-item>` は既に2プラグインの属性和集合）
- テンプレート資産のスケール（1タグ2テンプレート→約200ファイル、`/assets/` キャッシュ制御なし）
- キャッシュ「即座に反映」の許容遅延（パスゲートは先祖1つの変更が全子孫に波及する）
- 2026-08-05 監査の未達3件（JSONボディ上限なし・`page_id` 正規化漏れ・`/assets/` キャッシュ制御）が
  §5 に未掲載のまま
- セッション絶対8時間と長時間編集の衝突時の挙動、公開サイトの運用要件（ログ・可用性）、
  アクセシビリティ、要件ID・受け入れ基準の欠如

### 記述の修正

- 要件定義書 §2.5 の「進捗ボードで俯瞰できること」は**実装が見当たらない**
  （実装済みは部材手配集計 `<m-required-materials>` のみ）ため「構想・未実装」へ訂正し、
  §5 に載せた。ユースケース文書の構想を実装済みとして引き写していた本書側の誤り。
- 待受ポートの設定化（現状 `:8080` 固定）を、2インスタンス同居に備えて §5 に追加。

---

## 前提の更新：カスタムタグ100種規模・一般公開ウェブサイト用途（2026-08-16）

ユーザーから2つの前提変更が示された。**どちらも複数の設計文書が根拠として引いていた前提**
なので、影響箇所を洗い出して同時に直した。決定を新たに下したわけではなく、
**「前提が変わったので再評価が要る」ことを各文書に明示**した状態にしてある。

| 変更前の前提 | 変更後 |
|---|---|
| カスタム要素6〜9種程度 | **最終的に100種規模を予定** |
| 社内CMS（イントラ想定） | **社内業務システム＋一般公開のウェブサイト** |

### 影響と対応

- **[コンセプト.md](コンセプト.md) §3**: Web Components 採用理由の一つが「カスタム要素6種類程度の
  社内CMS」という**規模の議論**だった。この根拠は失効。ただし決定打である「保存形式とDOMの一致」
  「バックエンド連携の単純さ」は**語彙数に依存しない**ため、採用判断そのものは維持と明記した。
- **[エディタ仕様.md](エディタ仕様.md) §0**: 「ブロック種別が大幅に増えたら `lit` の費用対効果を
  再評価する」という**再検討の契機に到達した**（未評価）。React/Vue が選択肢外である理由は
  語彙数と無関係なので変わらない。
- **[エディタ仕様.md](エディタ仕様.md) §3**: 語彙数の増加で**最初に壊れるのはスラッシュメニュー**。
  現状は [assets/index.html](../assets/index.html) に `.slash-menu-item` を10項目手書きし、
  `showSlashMenu` がそのDOMを読むだけである。100種では ①絞り込み・分類による到達性 と
  ②`/api/tag-schema` からの生成（殻の markup を手で足さない）の2点が要る。
  ②を怠ると、§6.1.1 で解消した「エディタ側に手書きの語彙を持つ」問題の再来になる。
- **【一覧】カスタムタグ.md**（現 [【一覧】語彙.md](【一覧】語彙.md)）: 「現行実装の鏡」を手書きで維持する運用は
  二桁を超えると破綻する（載せ忘れた属性は保存のたびに黙って除去される）。`Tags()` からの
  生成へ移す方針を注記。
- **[認証認可設計.md](認証認可設計.md) §10.5**: 公開専用ビューの位置づけを「案がある（未着手）」から
  **ウェブサイト用途の必須要件**へ格上げ。あわせて、いま画面応答が一律 `Cache-Control: no-store`
  であることを踏まえ、公開ページをキャッシュ可能にする場合は
  **非公開ページ・認証済み応答を絶対にキャッシュさせない切り分けが前提**だと明記した。
- **[要件定義書.md](要件定義書.md)**: 用途2つの表（§1.1）、100種前提（§1.2）、ブロック挿入UIの
  スケール要件（§2.2）、語彙追加コスト一定・一覧の生成化・専用テーブルと汎用索引の切り分け
  （§2.5）を追記。新設した §4.4「公開ウェブサイトとしての要件」（体裁・SEO/OGP・キャッシュ・
  公開面の負荷・エラー画面）と §4.5「拡張性（100種規模）」に非機能要件をまとめ、
  §5 の未実装表へ3件（公開専用レンダリング・ブロック挿入UI再設計・タグ一覧の生成化）と
  フロント実装方式の再評価を追加した。

### 検討したが今回は決めなかったこと

- **`lit` を入れるか**: 契機に到達しただけで、費用対効果の評価は未実施。判断は
  「Light DOM 統一との衝突（`static styles` を失う）」「CDN不可で自前配置が要る」を
  受け入れるかとセット（[エディタ仕様.md](エディタ仕様.md) §0）。
- **100種を全部プラグイン＋専用テーブルにするか**: タグごとに専用テーブルを作ると100テーブル
  規模になる。「型付き既知列」と「汎用 name/value 索引」の切り分けが要るが、その線引きは
  [【考察】通信記録処理.md](【考察】通信記録処理.md) §6.1 の主題と重なるため、そちらと
  合わせて決める。

---

## 要件定義書を新設する（2026-08-16）

アプリケーション要件を一覧できる文書が無かった（旧 `要件定義書.md` は記入例のままの
空テンプレートで、2026-08-04 のドキュメント整理で削除済み）。現行の実装・設計文書から
要件を書き起こし、[要件定義書.md](要件定義書.md) を新設した。

- 位置づけは**要件の「目次・逆引き」**。各要件の詳細仕様の正本は既存の設計文書のままとし、
  要件定義書は正本へリンクする（正本の二重化を避ける）。構成は、目的と対象／機能要件
  （ページ管理・エディタ・認証認可・同時編集・業務機能・添付・管理機能）／データ要件／
  非機能要件（セキュリティ・依存最小化・デプロイ）／未実装・検討中の要件／領域別の正本一覧。
- 実装済みと未実装を明確に区別し、未実装（通信記録取り込み・版管理・利益集計UI・
  CSP strict 化・公開専用ビュー等）は第5章へ隔離した。
- `README.md` の文書一覧（中核）へ追加。
- 作成過程で見つけた食い違いを1件修正: [【ガイド】デプロイ・運用.md](【ガイド】デプロイ・運用.md)
  §5.1 がコアテーブルを「`pages` / `page_tags`」と記載していたが、`page_tags` は
  プラグイン所有へ移設済み（正は「`pages` / `page_perms`」。
  [アーキテクチャとDBスキーマ.md](アーキテクチャとDBスキーマ.md) §2 と整合させた）。

---

**これより前（2026-08-06 以前）の記録は [変更履歴アーカイブ.md](変更履歴アーカイブ.md) にあります。**
