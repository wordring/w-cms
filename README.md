# w-cms (Content Management System)

Go言語で構築された、簡易的なファイル統合型のコンテンツ管理システム（CMS）です。

アップロードされたHTMLファイルを、ファイルシステム（物理ストレージ）へ効率的に分散配置して保存し、同時にHTML内のメタデータやコンテンツ構造を解析してSQLiteデータベースにインデックス情報として登録・同期します。

---

## 🚀 特徴

1. **物理ストレージと仮想階層の分離**
   - 物理ファイルは、自動生成される連番の数字IDをベースに自動でディレクトリ分散（例: `/data/master/00/00001/00001.html`）して格納されます。これにより、同一フォルダへのファイル集中を防ぎます。
   - 一方で、ユーザーがドキュメント同士を整理するための「仮想階層（親子関係）」は、ページ内に配置した親ページ指定タグ（例: `<m-tag name="親ページ" value="親ページID"></m-tag>`）から読み取られ、データベース側で柔軟に管理されます。

2. **CGO不要のPure Go SQLite**
   - Windows環境などでもCコンパイラなしで動作するPure Go実装のSQLiteドライバ (`modernc.org/sqlite`) を採用しています。

3. **Web Componentsを活用した自律型HTML解析**
   - HTML内のカスタムタグ（`<m-tag>`, `<m-file>`）から可変メタデータや取引情報（見積・発注単価、原本PDFパス）を抽出し、データベースへインデックス情報として自動で同期します。
   - HTML内にWeb Componentsの描画ロジックが組み込まれているため、ブラウザでファイルを開くだけできれいな表示・編集（自動保存）が自律的に動作します。

---

## 📂 プロジェクト構成

```text
w-cms/
├── cmd/
│   └── w-cms/
│       └── main.go         # アプリケーション起動とルーティングの設定
├── docs/
│   ├── requirements.md     # 新機能・改修の要件定義書
│   ├── concept.md          # ソフトウェアコンセプト・設計仕様書
│   └── deploy.md           # 本番デプロイ・運用マニュアル
├── internal/
│   ├── database/
│   │   └── sqlite.go       # データベース（SQLite）の初期化と接続管理
│   └── cms/
│       ├── handler.go      # HTTPハンドラ（アップロード処理・一覧描画）
│       ├── parser.go       # HTML解析とメタデータ抽出
│       ├── storage.go      # IDの自動採番（Base-36）と保存ディレクトリの決定
│       ├── sync.go         # データベースへのメタデータ同期（UPSERT）
│       └── storage_test.go # ID採番やファイルパス決定ロジックの単体テスト
├── data/                   # 起動時に自動生成されるデータ保存先（.gitignore対象）
│   ├── cms.db              # SQLiteデータベースファイル
│   └── master/             # HTMLの物理ファイルが階層化されて保存されるディレクトリ
└── go.mod / go.sum         # go.mod依存関係
```

---

## 🛠 動作方法

### 1. 依存関係のセットアップ
Goがインストールされている環境で、プロジェクトルートにて依存パッケージをダウンロードします。

```bash
go mod tidy
```

### 2. アプリケーションの起動
以下のコマンドでサーバーを起動します。

```bash
go run cmd/w-cms/main.go
```
起動すると、`http://localhost:8080` でWebサーバーが立ち上がります。

### 3. テストの実行
ID採番ロジックや保存パス生成ロジックのテストを実行します。

```bash
go test ./internal/cms -v
```

---

## 🚀 デプロイ・運用について

本番環境（Linuxサーバーなど）で運用する際のディレクトリ設定や `systemd` によるデーモン化手順は、[deploy.md](file:///C:/Users/kouic/source/repos/w-cms/docs/deploy.md) にまとめてあります。
会社や自宅など、異なる環境で同じように動かすための仕様も記載していますので、デプロイ時にはそちらを参照してください。
```