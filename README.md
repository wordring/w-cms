# w-cms (Content Management System)

Go言語で構築された、ファイル統合型のコンテンツ管理システム（CMS）です。

アップロードされたHTMLファイルを、ファイルシステム（物理ストレージ）へ効率的に分散配置して保存し、同時にHTML内のメタデータやコンテンツ構造を解析してSQLiteデータベースにインデックス情報として登録・同期します。

最新のアップデートにより、**Notionライクなブロックベースのインタラクティブエディタ**と、**Web Componentsによるカスタムタグ機能**を備えており、高度なドキュメント作成と構造化データの同期を両立しています。

---

## 🚀 特徴

1. **Notion風ブロックエディタ**
   - 直感的なドラッグ＆ドロップによるブロック単位の並び替え。
   - `/` キーによるスラッシュコマンドメニューからの見出し・段落・発注書などの即座な挿入。
   - ブロックごとにポップアップするコンテキストツールバーによる、スマートなインライン操作。

2. **物理ストレージと仮想階層の分離**
   - 物理ファイルは、自動生成される連番の数字IDをベースに自動でディレクトリ分散（例: `/data/master/00/00001/00001.html`）して格納されます。
   - ユーザーがドキュメント同士を整理するための「仮想階層（親子関係）」は、ページ内に配置したタグから読み取られ、データベース側で柔軟に管理されます。

3. **CGO不要のPure Go SQLite**
   - Windows環境などでもCコンパイラなしで動作するPure Go実装のSQLiteドライバ (`modernc.org/sqlite`) を採用しています。

4. **Web Componentsを活用した自律型HTML解析**
   - HTML内のカスタムタグ（`<m-tag>`, `<m-file>`, `<m-material>` など）から可変メタデータや取引情報（見積・発注単価、部品構成）を抽出し、データベースへインデックス情報として自動で同期します。
   - ブラウザでファイルを開くだけできれいな表示・編集（自動保存）が自律的に動作します。

---

## 📂 プロジェクト構成

```text
w-cms/
├── cmd/
│   └── w-cms/
│       └── main.go         # アプリケーション起動とルーティングの設定
├── docs/
│   ├── コンセプト.md                          # ソフトウェアコンセプト・コア思想
│   ├── アーキテクチャとDBスキーマ.md           # アーキテクチャ・DBスキーマ・API仕様
│   ├── エディタ仕様.md                        # Notion風ブロックエディタの技術仕様
│   ├── ユースケース_受注と進捗管理.md          # 受発注・進捗管理のユースケース
│   ├── ユースケース_原価と利益.md              # 原価・利益管理のユースケース
│   ├── 開発方針.md                            # 設計・開発・命名規則の方針
│   ├── デプロイ・運用マニュアル.md             # 本番デプロイ・運用マニュアル
│   ├── 要件定義書.md                          # 新機能・改修の要件定義書
│   ├── プラグイン開発ガイド.md                  # 新ユースケース（タグ＋テーブル＋集計）の追加手順
│   ├── 拡張性とプラグイン化の考察.md            # ユースケース拡張・プラグイン化の設計検討メモ
│   └── 変更履歴.md                            # 過去の大きな改修・整理の記録
├── internal/
│   ├── database/
│   │   └── sqlite.go       # DB初期化・接続管理とコアテーブル(pages/page_tags)定義
│   └── cms/
│       ├── plugin.go              # プラグイン機構（レジストリ・IF・DOMヘルパー）
│       ├── plugin_client_order.go # ユースケース: 顧客の発注書
│       ├── plugin_our_order.go    # ユースケース: 弊社の発注書
│       ├── plugin_estimates.go    # ユースケース: 見積もり（弊社／業者）
│       ├── plugin_materials.go    # ユースケース: 部材BOM＋部材手配計算API
│       ├── handler.go      # コアHTTPハンドラ（保存・読込・子ページ作成など）
│       ├── pdf_handler.go  # PDFアップロード・Gemini解析API
│       ├── parser.go       # コア情報（タイトル・親ID・タグ）の抽出
│       ├── storage.go      # IDの自動採番と保存ディレクトリの決定
│       ├── sync.go         # コア同期＋プラグイン走査（SyncIndex / RebuildDatabase）
│       └── storage_test.go # 単体テスト
├── assets/                 # フロントエンド用静的ファイル
│   ├── index.html          # エディタのメインUIと操作ロジック
│   ├── web-components.js   # カスタムWebコンポーネント定義
│   └── templates/          # Web Componentsの閲覧用/編集用HTMLテンプレート
├── data/                   # 起動時に自動生成されるデータ保存先（.gitignore対象）
│   ├── cms.db              # SQLiteデータベースファイル
│   └── master/             # HTMLの物理ファイル保存先
└── go.mod / go.sum         # 依存関係
```

---

## 🛠 動作方法

### 1. 依存関係のセットアップ
Goがインストールされている環境で、プロジェクトルートにて依存パッケージをダウンロードします。

```bash
go mod tidy
```

### 2. アプリケーションの起動
以下のコマンドでサーバーを起動します。

```bash
go run cmd/w-cms/main.go
```
起動すると、`http://localhost:8080` でWebサーバーが立ち上がります。
エディタ機能の検証は `http://localhost:8080/assets/index.html` にアクセスしてください。

### 3. テストの実行

```bash
go test ./internal/cms -v
```

---

## 🚀 ドキュメント・仕様詳細

より詳しい技術仕様や概念については、`docs/` フォルダ内の以下のドキュメントを参照してください。
* [コンセプト.md](docs/コンセプト.md): 設計思想とコンセプト
* [アーキテクチャとDBスキーマ.md](docs/アーキテクチャとDBスキーマ.md): データベーススキーマと同期アーキテクチャ
* [エディタ仕様.md](docs/エディタ仕様.md): ブロックエディタのUI/UX実装仕様
* [ユースケース_受注と進捗管理.md](docs/ユースケース_受注と進捗管理.md): 受発注・部品進捗管理のユースケース
* [ユースケース_原価と利益.md](docs/ユースケース_原価と利益.md): 原価・利益管理のユースケース
* [開発方針.md](docs/開発方針.md): 設計・開発・命名規則の方針
* [デプロイ・運用マニュアル.md](docs/デプロイ・運用マニュアル.md): 本番デプロイ・運用手順
* [要件定義書.md](docs/要件定義書.md): 新機能・改修の要件記入用テンプレート
* [プラグイン開発ガイド.md](docs/プラグイン開発ガイド.md): 新しいユースケース（カスタムタグ＋DBテーブル＋集計API）の追加手順
* [拡張性とプラグイン化の考察.md](docs/拡張性とプラグイン化の考察.md): ユースケース拡張時の影響範囲とプラグイン化の設計検討
* [変更履歴.md](docs/変更履歴.md): 過去の大きな改修・ドキュメント整理の記録