# w-cms (Content Management System)

Go言語で構築された、ファイル統合型のコンテンツ管理システム（CMS）です。

エディタで編集したHTMLを、ファイルシステム（物理ストレージ）へ効率的に分散配置して保存し、同時にHTML内のメタデータやコンテンツ構造を解析してSQLiteデータベースにインデックス情報として登録・同期します。

最新のアップデートにより、**Notionライクなブロックベースのインタラクティブエディタ**と、**Web Componentsによるカスタムタグ機能**を備えており、高度なドキュメント作成と構造化データの同期を両立しています。

---

## 🚀 特徴

1. **Notion風ブロックエディタ**
   - 直感的なドラッグ＆ドロップによるブロック単位の並び替え。
   - `/` キーによるスラッシュコマンドメニューからの見出し・段落・発注書などの即座な挿入。
   - ブロックごとにポップアップするコンテキストツールバーによる、スマートなインライン操作。

2. **物理ストレージと仮想階層の分離**
   - 物理ファイルは、自動生成される連番の数字IDをベースに自動でディレクトリ分散（例: `/data/master/00/00001/00001.html`）して格納されます。
   - ユーザーがドキュメント同士を整理するための「仮想階層（親子関係）」は、ページ属性のサイドカー（`<id>.meta.json`）が正本で、データベース側に検索用インデックスとして同期されます。

5. **認証・認可とページ単位の権限**
   - argon2id によるパスワード認証、セッション、CSRF/CSPの多層防御。
   - ページごとに Unix風の read/write を owner・group・other で制御し、明示的に公開したページだけを匿名へ開けます。

6. **同時編集の直列化**
   - ページ単位の悲観ロック＋SSEで、複数人が同時に開いても上書き事故（lost update）が起きません。

3. **CGO不要のPure Go SQLite**
   - Windows環境などでもCコンパイラなしで動作するPure Go実装のSQLiteドライバ (`modernc.org/sqlite`) を採用しています。

4. **Web Componentsを活用した自律型HTML解析**
   - HTML内のカスタムタグ（`<m-tag>`, `<m-file>`, `<m-material>` など）から可変メタデータや取引情報（見積・発注単価、部品構成）を抽出し、データベースへインデックス情報として自動で同期します。
   - ブラウザでファイルを開くだけできれいな表示・編集（自動保存）が自律的に動作します。

---

## 📂 プロジェクト構成

```text
w-cms/
├── cmd/
│   └── w-cms/
│       └── main.go         # アプリケーション起動とルーティングの設定
├── docs/                   # 設計・仕様・ガイド（一覧は下の「ドキュメント」節）
├── internal/
│   ├── database/
│   │   ├── sqlite.go       # cms.db の初期化とコアテーブル(pages)定義
│   │   └── auth.go         # auth.db（ユーザー・グループ・セッション）の初期化
│   ├── auth/               # 認証・認可の基盤
│   │   ├── password.go     # argon2id ハッシュ（+ ユーザー列挙対策の decoyHash）
│   │   ├── session.go      # セッション（絶対8時間／アイドル30分）
│   │   ├── middleware.go   # RequireAuth / OptionalAuth / CSRFProtect / CSPProtect
│   │   ├── users.go        # ユーザー・グループ・ログイン試行の記録
│   │   ├── handlers.go     # ログイン・ログアウト・/api/me
│   │   └── admin*.go       # 管理API（ユーザー/グループ/監査ログ）
│   └── cms/
│       ├── plugin.go              # プラグイン機構（レジストリ・IF・DOMヘルパー）
│       ├── plugin_page_tags.go    # <m-tag>（ページ横断メタ）
│       ├── plugin_file.go         # <m-file>（ファイル容器）
│       ├── plugin_child_list.go   # <m-child-list>（子ページ一覧）
│       ├── plugin_client_order.go # ユースケース: 顧客の発注書
│       ├── plugin_our_order.go    # ユースケース: 弊社の発注書
│       ├── plugin_estimates.go    # ユースケース: 見積もり（弊社／業者）
│       ├── plugin_materials.go    # ユースケース: 部材BOM＋部材手配計算API
│       ├── handler_save.go  # 保存API（全文・ブロック単位）
│       ├── handler_view.go  # 画面（サーバー合成）とページ本文API
│       ├── handler_tree.go  # ページの木構造（新規作成・子一覧・親の付け替え）
│       ├── handler_meta.go  # ページ属性・本文の語彙・DB再構築
│       ├── shell.go         # ページ本文とindex.htmlのサーバー合成
│       ├── sanitize.go      # 本文HTMLの許可リスト方式サニタイズ＋語彙の正本
│       ├── block.go         # ブロック単位保存（data-id での差し替え）
│       ├── perms_admin.go   # 権限・所有者・公開の変更API（chmod/chown/publish）
│       ├── pdf_handler.go   # PDFアップロード・Gemini解析API
│       ├── parser.go        # コア情報（タイトル）の抽出
│       ├── sync.go          # コア同期＋プラグイン走査（SyncIndex / RebuildDatabase）
│       ├── page/            # ページの実体（保存パス・サイドカー・認可・添付配信）
│       │   ├── storage.go   # 保存ディレクトリの決定（GetPageDir）
│       │   └── perms.go     # Unix風 read/write ＋ 匿名公開、/data/ の配信ゲート
│       ├── editlock/        # 編集ロック（悲観ロック＋SSE）
│       │   ├── lock.go      # ロック状態（プロセス内 mutex map・競合トリガー方式）
│       │   └── handler.go   # /api/lock・/api/lock-events・/api/unlock・/api/lock/force
│       └── *_test.go        # 単体テスト
├── assets/                 # フロントエンド用静的ファイル
│   ├── index.html          # エディタの殻（markup のみ。本文はサーバーが合成）
│   ├── app.css             # 殻のスタイル
│   ├── app.js              # 殻の操作ロジック（エディタ本体）
│   ├── admin.html          # 管理コンソール（ユーザー・グループ・監査ログ）
│   ├── admin.css           # 管理コンソールのスタイル
│   ├── admin.js            # 管理コンソールの操作ロジック
│   ├── components.css      # カスタム要素の共通スタイル
│   ├── web-components.js   # カスタムWebコンポーネント定義
│   └── templates/          # Web Componentsの閲覧用/編集用HTMLテンプレート
├── data/                   # 起動時に自動生成されるデータ保存先（.gitignore対象）
│   ├── cms.db              # 派生インデックス（master から再生成可能）
│   ├── auth.db             # 認証情報（再生成不可・要バックアップ）
│   └── master/             # HTMLの物理ファイル保存先（＝正本）
└── go.mod / go.sum         # 依存関係
```

---

## 🛠 動作方法

### 1. 依存関係のセットアップ
Goがインストールされている環境で、プロジェクトルートにて依存パッケージをダウンロードします。

```bash
go mod tidy
```

### 2. アプリケーションの起動
**初回のみ**、初期管理者を環境変数で指定して起動します（認証が有効です）。

```bash
# ローカル検証（HTTP）では Secure Cookie を無効化する
WCMS_ADMIN_USER=admin WCMS_ADMIN_PASSWORD='強いパスワード' WCMS_SECURE_COOKIES=0 \
  go run cmd/w-cms/main.go
```
2回目以降は環境変数なしで `go run cmd/w-cms/main.go` で起動できます（管理者は `data/auth.db` に保存済み）。

起動すると `http://localhost:8080` で立ち上がります。ブラウザでアクセスするとログイン画面（`/login`）にリダイレクトされるので、上で設定した管理者でログインしてください。

> 本番（インターネット公開）では TLS/HTTPS が必須です。リバースプロキシ（Caddy/nginx）でTLS終端し、`WCMS_SECURE_COOKIES` は設定しない（既定の Secure 有効）でください。詳細は [認証認可設計.md](docs/認証認可設計.md) と [【ガイド】デプロイ・運用.md](docs/【ガイド】デプロイ・運用.md)。

### 3. テストの実行

```bash
go test ./...
```

---

## 🚀 ドキュメント・仕様詳細

`docs/` 配下の文書は、**種別を `【 】` の接頭辞で表す**命名規則に従います（[開発方針.md](docs/開発方針.md) §2）。
`【考察】` は「検討中・未実装」の状態マーカーで、実装が固まると外れます。

### 中核（現行仕様の正本）
* [要件定義書.md](docs/要件定義書.md): アプリケーション要件の一覧・逆引き（詳細は各正本へリンク）
* [コンセプト.md](docs/コンセプト.md): 設計思想とコンセプト（HTMLが正本・単一バイナリ）
* [アーキテクチャとDBスキーマ.md](docs/アーキテクチャとDBスキーマ.md): DBスキーマ・同期・保存/読込の詳細
* [エディタ仕様.md](docs/エディタ仕様.md): ブロックエディタのUI/UX実装仕様
* [認証認可設計.md](docs/認証認可設計.md): 認証(argon2id)・Unix風のページ単位認可・匿名公開
* [本文サニタイズ設計.md](docs/本文サニタイズ設計.md): 本文HTMLの許可リストと二層サニタイズ
* [開発方針.md](docs/開発方針.md): 設計・開発・命名規則の方針（**AIエージェントの行動規範**）

### 一覧・リファレンス
* [【一覧】API.md](docs/【一覧】API.md): 全HTTPエンドポイント（メソッド・認可・ロック要否）
* [【一覧】カスタムタグ.md](docs/【一覧】カスタムタグ.md): 実装済みカスタム要素と所有プラグイン
* [【一覧】用語集.md](docs/【一覧】用語集.md): 受注／発注・販売見積／購入見積などの業務用語

### ガイド（手順）
* [【ガイド】デプロイ・運用.md](docs/【ガイド】デプロイ・運用.md): 本番デプロイ・バックアップ・TLS構成
* [【ガイド】プラグイン開発.md](docs/【ガイド】プラグイン開発.md): 新ユースケース（タグ＋テーブル＋集計API）の追加手順

### ユースケース（業務仕様）
* [【ユースケース】受注と進捗管理.md](docs/【ユースケース】受注と進捗管理.md): 受発注・部品進捗管理
* [【ユースケース】原価と利益.md](docs/【ユースケース】原価と利益.md): 原価・利益管理

### 考察（設計判断の記録・検討中の設計）
* [【考察】語彙モデル.md](docs/【考察】語彙モデル.md): 3層モデル（レジストリ/汎用索引/計算プラグイン）と標準HTMLへの移行（**検討中・方向確定**）
* [【考察】通信記録処理.md](docs/【考察】通信記録処理.md): メール/FAX取り込みと参照ハブ（**検討中**）
* [【考察】タグ設計.md](docs/【考察】タグ設計.md): 値を属性から中身へ移す設計原則（**検討中**）
* [【考察】CSP強化.md](docs/【考察】CSP強化.md): CSPのstrict化（中間版は適用済み）
* [【考察】同時編集の競合対策.md](docs/【考察】同時編集の競合対策.md): 悲観ロック＋SSEに至る決定ログ
* [【考察】アンドゥ・リドゥ.md](docs/【考察】アンドゥ・リドゥ.md): 編集中のundo（実装済み）と版管理（未実装）
* [【考察】許可タグと属性.md](docs/【考察】許可タグと属性.md): 許可リストを決めた経緯（実装済み）

### 運用メタ
* [作業引き継ぎ.md](docs/作業引き継ぎ.md): 直近の状態と次の作業候補（**別環境で再開するときはここから**）
* [変更履歴.md](docs/変更履歴.md): 過去の大きな改修・ドキュメント整理の記録