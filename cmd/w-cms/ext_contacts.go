//go:build !minimal

package main

// ─────────────────────────────────────────────────────────────────────────
// アドレス帳（コンパイル時に選ぶ拡張・2026-09-15）
//
// `ext/subcon` が import しているので**これが無くても載ります**。それでも
// 1枚置くのは、**既定の組にアドレス帳が入っていることを取り付け口で明示するため**です
// ——いつか下請けが import しなくなっても、黙って消えないように。
// （2026-09-15 まではここが起動ログに名前を足していましたが、いまは拡張が
// `cms.RegisterExtension` で自分で名乗ります。）
//
// **`ext/comm/contacts` の側にビルドタグはありません。** タグを付けると
// 「板金は入れたがアドレス帳は外した」という組み合わせが作れてしまい、整理が
// 取引先ページを作れずに黙って壊れます。依存は import で表し、ここは表示だけを
// 足す形にしています——だから**このファイルを消しても機能は消えません**
// （消えるのはログの1語だけ）。
// ─────────────────────────────────────────────────────────────────────────

import _ "w-cms/ext/comm/contacts"
